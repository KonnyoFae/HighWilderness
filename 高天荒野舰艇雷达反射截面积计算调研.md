# 《高天荒野》舰艇雷达反射截面积计算调研

## 一、研究目标与结论

本调研用于把自由绘制的多层船壳、船体涂料和外部模块转换为战略模式侦察系统可直接读取的方向性雷达反射截面积（Radar Cross Section，RCS）。第一版结论如下：

- RCS 是目标自身的被动特征，以平方米线性保存，界面可换算为 `dBsm=10log10(σ/1m²)`；
- 第一版只保存 `0°～359°` 水平方位表，`0°` 表示雷达位于舰艏方向，`90°` 表示雷达位于右舷方向；
- 第一版统一以 `λ_ref=0.10m`、约 `3GHz` 的参考波长生成目标特征，不为每个雷达频段保存一套船体表；
- 船壳使用“规范化可见表面组的展宽镜面项＋非相干投影底项＋凹角项”计算，再由全舰统一的船体涂料倍率修正；
- 外部模块使用模块原型自带的基准 RCS 和方向修正，在线性平方米口径下与船壳相加；
- 雷达功率、天线、接收机噪声、实际工作波长、天气、高度层传播、电子干扰和目标跟踪状态全部属于侦察系统，不混入目标 RCS；
- 第一版公式结构和原型测试已经通过；隐身涂料倍率、外挂模块基准特征以及全部全局系数仍需随教程雷达与教程舰联合标定。

本方案不是全波电磁仿真。它保留 RCS 的量纲、方向性、面积、波长、表面朝向和涂料影响，同时主动舍弃相位、极化、厘米级表面细节和逐频点谐振，以获得可解释、可缓存且不会被绘制方式操纵的游戏规则。

当前规范实现已经落地为《高天荒野舰艇RCS缓存.py》并接入《高天荒野舰艇无界面船壳编译器.py》。缓存模型标识为 `gaotian.rcs.hull/v1alpha1`，参数集为 `gtw.rcs.prototype.v1`，平衡状态为 `prototype_unbalanced`：公式结构、规范输入和相对关系已经可执行，但绝对数值仍不是教程舰正式平衡值。

## 二、为什么第一版只保存水平角

战略地图和战术地图都使用二维平面位置与离散高度层，舰艇没有连续高度坐标，也没有连续俯仰姿态。若直接增加“雷达在目标上方、同高、下方”三档 RCS，必须人为指定三组代表俯仰角；同一相对层级在十公里和一百公里水平距离上却会被错误视为相同俯仰角。

因此第一版只计算水平方位：

- 船壳竖直侧面和逐层外形直接参与；
- 水平露天甲板不作为船壳表面项参与；
- 顶挂雷达、炮塔等设备仍通过外挂模块自身的 RCS 参与；
- 舰队位于不同高度层造成的探测差异，由雷达传播、遮蔽和天气系统处理。

缓存结构仍建议使用 `rcs_table[elevation_band][azimuth]`，第一版仅有 `elevation_band=LEVEL`。将来若加入连续高度或证明高度差 RCS 具有足够玩法价值，可以增加 `ABOVE` 与 `BELOW`，而不需要修改蓝图的外部接口。

## 三、物理依据与必须进行的简化

### 3.1 RCS 与雷达方程

RCS 表示目标把入射能量向雷达方向散射的等效面积，并不等于几何投影面积。单站雷达接收功率的基础关系为：

`P_r = P_t G_t G_r λ² σ / [(4π)³ R⁴ L]`

在雷达其他条件不变时：

`R_max ∝ σ^(1/4)`

所以四倍 RCS 只使最大探测距离变为约 `1.414` 倍，一百倍 RCS 使其变为约 `3.162` 倍。游戏必须在线性平方米口径下累加船壳和模块，再在显示阶段转换为 dBsm，不能把多个 dBsm 直接相加。

### 3.2 理想平板公式不能直接用于完整舰体

大尺寸理想导电平板在法向单站照射时具有以下物理光学数量级：

`σ_plate = 4πA²/λ²`

但真实平板离开镜面方向后会出现非常狭窄且带有旁瓣的方向图；掠射方向还会由边缘绕射主导。实际舰体又存在曲率、接缝、表面粗糙、遮挡、多次反射、材料损耗、极化和相位干涉。NASA 对矩形平板的理论与实验比较也表明，近法向物理光学结果较好，而近掠射与边缘区域需要衍射或等效电流方法补足。

如果把每条五米船壳边都当成完全相干理想平板，结果会出现极端窄峰，且任何细小拆边都会改变相位和峰值。第一版只保留 `A²/λ²` 的物理骨架，再加入有效相干面积软上限、展宽方向瓣和线性底项。

## 四、从多层船壳生成规范化表面组

### 4.1 每个五米高度带独立建立外边界

每层甲板被视为五米高的棱柱。对该层全部合法多边形先求平面并集，只保留并集的外部边界：

- 同层多个船壳区域彼此遮挡的内部边不参与；
- 凹形轮廓保留，但从特定方向被前方船壳遮挡的凹面不重复计算；
- 不完整内部格是否可安装模块与 RCS 无关，RCS 读取精确船壳边；
- 上下层外形不同时，退台处仍形成各自高度带的竖直表面。

### 4.2 规范化合并规则

RCS 计算前合并满足以下条件的相邻表面：

1. 位于同一直线上并具有相同外法线；
2. 空间上连续。

同一五米高度带内按以上规则合并；上下甲板之间的结构接缝视为表面组边界，不把相邻高度带合成一块完全相干平板。基础装甲的材质与厚度都不作为 RCS 分组条件，也不直接进入 RCS。外轮廓相同的平直舷侧无论内部使用钢、铝、钛或复合装甲，也无论不同区段厚度如何变化，都会形成同一个几何表面组；雷达表面性能改由全舰统一船体涂料决定。

### 4.3 方向可见性

对每个整数方位角建立正交投影视图。令从目标指向雷达的水平单位向量为 `s`，垂直于视线的屏幕轴为 `u`。在同一高度带内：

1. 只保留外法线 `n_i` 满足 `n_i·s>0` 的朝向雷达表面；
2. 把候选边投影到 `u` 轴；
3. 在重叠投影区间内只保留沿 `s` 方向距离雷达最近的表面；
4. 将可见投影宽度除以 `μ_i=n_i·s`，恢复该表面的实际可见长度；
5. 乘以五米或合并后的外露高度，得到可见表面组面积 `A_i`。

这一离线正交深度缓冲能够处理凹多边形和同层多个船壳区域，不需要战斗中进行射线求交。

## 五、第一版船壳 RCS 公式

### 5.1 有效相干面积软限制

定义：

`Q(A_i) = A_i² / (1 + A_i/A_coh)`

其中 `A_coh` 是统一有效相干面积。它具有以下性质：

- 当 `A_i << A_coh` 时，`Q≈A_i²`，保留小平板的面积平方关系；
- 当 `A_i >> A_coh` 时，`Q≈A_i A_coh`，大表面不再按完全相干面积平方无限增长；
- 规范化后再应用本式，因此拆分共线边不会改变结果。

第一轮原型暂取 `A_coh=25m²`，即一个五米乘五米表面块的量级。它不是现实材料固定常数，而是把大型舰体的形变、接缝、粗糙度和非完全相干折算为游戏代理参数。

### 5.2 展宽镜面项

令全舰船体涂料的 RCS 倍率为 `K_coat`，普通涂料基准为 `1.000`；令表面法线朝向雷达的余弦为：

`μ_i = max(0,n_i·s)`

单个规范化可见表面组的镜面项为：

`σ_spec,i = K_coat K_spec [4π/λ_ref²] Q(A_i) μ_i^p`

其中：

- `K_spec` 把理想平板数量级压缩为大型粗糙舰体的有效回波；
- `p` 控制方向瓣宽度，数值越大，只有越接近正对雷达的表面才产生强回波；
- 第一轮原型暂取 `K_spec=0.001`、`p=4`。

这不是声称真实方向图等于余弦四次方，而是把极窄且带旁瓣的真实镜面峰替换为可供玩家理解和利用的连续方向瓣。

### 5.3 非相干投影底项

边缘绕射、表面粗糙、细小结构和无法表达的非相干散射不能在倾斜时全部归零。第一版使用：

`σ_diff,i = K_coat K_diff A_i μ_i`

其中 `A_i μ_i` 就是该表面对雷达的可见投影面积。第一轮暂取 `K_diff=0.50`。这个底项不会产生与舰体尺寸无关的固定隐身下限；小艇的底项仍然小于大型战舰。

### 5.4 凹角项

凹形船壳中两块同时朝向雷达的表面可能形成强多次反射。第一版不求解真实二面角反射器，而对规范化后的凹顶点增加：

`σ_corner,j = K_coat K_corner A_corner C_geometry C_view`

`A_corner = H_visible min(L_a,L_b,L_corner_cap)`

其中：

- `C_geometry` 由凹转角决定，直角凹槽最大，接近平直或折回时降低；
- `C_view=min(1,4μ_aμ_b)`，只有相邻两面同时朝向雷达时才生效；
- `L_corner_cap` 防止两条极长舷侧仅因共享一个顶点而形成无限大的角项；
- 第一轮暂取 `K_corner=8`、`L_corner_cap=5m`。

凹角项只在规范化顶点上计算。人为增加真实锯齿或凹槽会提高特征，这是几何后果而不是拆边漏洞。

### 5.5 船壳与整舰总式

船壳方向特征为：

`σ_hull(β) = Σ_i[σ_spec,i(β)+σ_diff,i(β)] + Σ_jσ_corner,j(β)`

整舰方向特征为：

`σ_total(β) = σ_hull(β) + Σ_kσ_module,k(β)`

全部项目始终以平方米线性相加，最后才转换为 dBsm。

## 六、船体涂料接口

每条舰艇必须选择一种覆盖全部船壳外表面的统一涂料，不进行逐边涂装管理。涂料保存以下字段：

| 字段 | 含义 | 第一版边界 |
| --- | --- | --- |
| `K_coat` | 对全部船壳镜面项、投影底项和凹角项的线性倍率 | 普通涂料固定为 `1.000`；隐身涂料小于 `1.000` |
| 单位面积材料消耗 | 新造、替换涂料所需资源 | 按船壳外表面积计算，待经济阶段标定 |
| 单位面积施工工时 | 涂装所需锚地时间 | 待经济阶段标定 |
| 设施与蓝图条件 | 是否能够生产和施工该涂料 | 普通涂料无特殊门槛，隐身涂料属于高阶内容 |

涂料选择属于舾装/维护状态，不绑定精确船壳几何蓝图：船壳蓝图只保存普通涂料基准下的几何 RCS 表，建造时默认使用普通涂料；玩家在合适锚地可以消耗材料与工时更换为隐身涂料。更换涂料只需将基准船壳方向表乘以新的 `K_coat`，无需重新进行几何可见性计算。

`K_coat` 直接线性缩放船壳平方米特征。若测试用隐身涂料取 `0.5`，同一方向的船壳 RCS 减半，但同一雷达的理论最大探测距离只变为 `0.5^(1/4)=0.8409` 倍。正式隐身涂料倍率必须与教程搜索雷达发现距离、舰艏朝向收益、施工成本和取得门槛联合标定。

机器目录《舰艇数据/涂料/船体涂料.v1.json》当前只允许普通涂料投入运行，倍率锁定为 `1.0`。隐身涂料倍率保存为 `null` 且不可装备；上述 `0.5` 仅是公式关系例，不是内容数值。

涂料不改变装甲穿深、局部耐久、结构承载或维修难度。第一版也不单独模拟涂层在战斗中的逐区剥落和耐久衰减；更换或恢复涂料统一在锚地维护中处理。

## 七、外部模块接口

只有实际外露部分参与。纯内部模块没有 RCS；具有内部和顶挂/侧挂部分的复合模块，只为外露部分提供一份模块特征。每个外部模块原型至少保存：

| 字段 | 含义 |
| --- | --- |
| `base_rcs_m2` | 参考波长下的完整外露模块基准 RCS |
| `direction_table[360]` 或简化参数 | 相对模块安装朝向的方向倍率；无明显方向性时全为 `1` |
| `mount_heading` | 模块局部朝向相对舰艏的角度 |
| `geometry_state` | 展开、收拢、脱落等确实改变外形的状态 |
| `state_multiplier` | 对应几何状态的 RCS 倍率 |

模块实际贡献为：

`σ_module(β)=base_rcs_m2 K_direction(β-mount_heading) K_state`

模块不重新参与船壳表面遮挡或求交，始终以非负平方米贡献相加。船体涂料倍率不自动作用于外部模块：搜索雷达、火控雷达、炮塔和侧挂发动机若要降低特征，应使用自身的低特征型号、整流罩或专用模块设计。模块断电不会使物理外形消失，因此开关和供电状态不改变被动 RCS；主动雷达开机暴露属于电子支援侦察，不是 RCS。模块仅仅“功能损毁但残骸仍在”时默认保持原几何状态，只有收拢、折断、脱落或清除残骸时才换用其他 `K_state`。

第一版不允许外挂模块以负数 RCS 抵消船壳。船体隐身涂料直接使用独立的正倍率接口，不能用负平方米相减。

## 八、缓存、插值与战损

### 8.1 蓝图和舾装缓存

船壳蓝图保存时以普通涂料 `K_coat=1.000` 生成：

- `hull_rcs_level[360]`：完整船壳水平方位表；
- 可选的逐表面组中间数据，用于编辑器解释某个方向的主要贡献；
- `min_rcs`、`mean_rcs`、`max_rcs` 及其对应方向，用于设计器显示。

舾装方案或舰艇实例保存当前船体涂料，并生成：

- `module_rcs_level[360]`；
- `coated_hull_rcs_level[360]=K_coat×hull_rcs_level`；
- `total_rcs_level[360]=coated_hull_rcs_level+module_rcs_level`。

运行时根据目标舰艏与“目标指向雷达”的相对方位读取相邻两个整数角并线性插值。船壳强制轴对称，所以纯船壳表应满足左右镜像；舾装不强制对称，整舰表可以左右不同。

### 8.2 战损更新

RCS 主要在战略模式使用，战术模式又会暂停战略时间，因此没有必要在每次命中后逐帧重算三百六十个方向：

- 普通局部装甲耐久下降不改变外轮廓，不重算；
- 局部装甲耐久归零本身仍只表示防护失效，第一版不自动认定整块外皮消失；
- 模块脱落、残骸清除或几何状态切换时，增量更新模块方向表；
- 第一版不按命中位置削减涂料效果；只有在锚地更换涂料时更新 `K_coat`。

## 九、与侦察系统的严格边界

目标 RCS 不读取以下项目：

- 雷达发射功率、天线增益、孔径、接收机噪声与积累时间；
- 传感器实际工作频率、扫描模式与火控/搜索用途；
- 上层、云层、雨层的传播损失和地形遮蔽；
- 降雨、云雾、电子干扰、诱饵和杂波；
- 目标是否已经被发现、精确跟踪或火控锁定；
- 目标自身雷达是否开机。

第一版把 `σ_total` 视为统一参考频段目标特征。传感器实际波长仍进入雷达方程、天线增益和传播模型，但不再自动把目标表按 `(λ_ref/λ)²` 缩放；真实目标的频率响应并不等于所有散射面统一按理想平板公式缩放。若以后需要让低频预警雷达和高频火控雷达面对隐身涂料时表现不同，应把缓存扩展为有限频段表，并为涂料与模块分别标定频段参数。

## 十、第一轮原型参数与结果

### 10.1 当前参数状态

| 参数 | 第一轮值 | 状态 |
| --- | ---: | --- |
| 水平方位采样 | `1°`、共 `360` 项 | 第一版接口确定 |
| `λ_ref` | `0.10m` | 第一版接口确定 |
| `A_coh` | `25m²` | 原型暂定，待教程舰联合标定 |
| `K_spec` | `0.001` | 原型暂定 |
| `p` | `4` | 原型暂定 |
| `K_diff` | `0.50` | 原型暂定 |
| `K_corner` | `8` | 原型暂定 |
| `L_corner_cap` | `5m` | 原型暂定 |

### 10.2 标准船壳结果

原型以 `155×20m`、单层五米高、普通涂料 `K_coat=1`、无外挂模块的标准船壳测试。数值仅用于检验数量级和相对关系：

现行规范回归已经改由 `HullBlueprint` 输入，经同一船壳编译器生成三百六十方向缓存，见《高天荒野舰艇RCS缓存测试.py》。测试覆盖同层共线边合并、多区域正交遮挡、多层累加、凹角方向峰值、结构/装甲材质与装甲厚度不影响几何 RCS、`359°→0°` 插值连续以及未标定隐身涂料不可装备；旧公式原型仍只作为调研过程记录保留。

| 船壳 | 正前方 | 右舷 | 全向线性均值 | 最小～最大 |
| --- | ---: | ---: | ---: | ---: |
| 平艏平艉 | `2563m² / 34.1dBsm` | `23974m² / 43.8dBsm` | `10066m² / 40.0dBsm` | `2435～23974m²` |
| 尖艏平艉 | `157m² / 22.0dBsm` | `22722m² / 43.6dBsm` | `9436m² / 39.7dBsm` | `157～22839m²` |
| 尖艏尖艉 | `56m² / 17.5dBsm` | `22711m² / 43.6dBsm` | `8901m² / 39.5dBsm` | `56～22711m²` |

MIT Lincoln Laboratory 的教学材料给出的数量级示例中，约五千吨级船舶在 L 波段可取约 `10000m²`。该数值不是本项目的精确标定目标，但说明第一轮全向均值没有偏离现实舰船数量级数个数量级。尖艏主要降低狭窄的正前方特征，宽大舷侧仍然显著，符合朝向管理玩法。

### 10.3 已通过的自动检查

- 同一平直边拆成多段后，三百六十个方向的结果完全一致；
- 轴对称船壳左右方向结果一致；
- 尖艏正前方低于平艏，尖艏尖艉正前方低于尖艏平艉；
- 长舷侧正对雷达时高于平艏正对雷达；
- 船体涂料倍率减半时，船壳平方米 RCS 也精确减半；
- 外部模块按平方米精确线性增加；
- 隐身涂料不自动缩减外部模块贡献；
- 同形双层在独立五米高度带口径下为单层船壳的两倍，收窄上层介于单层与同形双层之间；
- 平面长度和宽度同时放大两倍后，三百六十个方向的特征均单调增加；
- 方向表在 `359°/0°` 环绕处能够正常线性插值；
- 四倍和一百倍 RCS 的雷达方程距离倍率分别为 `1.4142` 与 `3.1623`。

可复现代码见《高天荒野舰艇雷达反射截面积公式原型测试.py》。

## 十一、仍需标定但不改变公式结构的内容

以下参数统一推迟到规范蓝图集成、玩家编辑器往返和教程舰建立后的共同标定阶段；当前不因原型方向表已经通过便提前定值。

1. 标定隐身涂料的 `K_coat`、单位面积消耗、施工工时和取得条件；
2. 建立顶挂搜索雷达、火控雷达、常见炮塔、侧挂发动机、反应装甲和主动防御系统的模块基准 RCS；
3. 用教程舰与至少一条小艇、一条大型舰测试 `A_coh`、`K_spec`、`p`、`K_diff` 和 `K_corner`；
4. 建立首轮搜索雷达，验证正前方、斜向与侧向发现距离是否符合战略玩法；
5. 决定设计器是否只显示极坐标图与三项摘要，还是同时显示贡献最大的表面与模块；
6. 只有在多频段雷达形成明确玩法差异后，才扩展有限频段 RCS 表。

## 十二、资料来源

- [MIT Lincoln Laboratory：Introduction to Radar Systems，Lecture 4—Target Radar Cross Section](https://www.ll.mit.edu/sites/default/files/outreach/doc/2018-07/lecture%204.pdf)：RCS 定义、方向/尺寸/频率影响、典型目标数量级与主要散射机制。
- [MIT Lincoln Laboratory：Introduction to Radar Systems 课程主页](https://www.ll.mit.edu/outreach/web-based-course-radar-introduction-radar-systems)：雷达方程、传播、RCS、检测与跟踪的系统边界。
- [NASA CR-140756：Radar Cross Section of Rectangular Flat Plates as a Function of Aspect Angle](https://ntrs.nasa.gov/api/citations/19750002834/downloads/19750002834.pdf)：矩形理想导电平板随观察角变化的物理光学、几何绕射与实验结果。
- [NASA CR-188685：High-frequency Techniques for RCS Prediction of Plate Geometries](https://ntrs.nasa.gov/archive/nasa/casi.ntrs.nasa.gov/19910018126.pdf)：物理光学、边缘等效电流及其在近法向和掠射区域的适用差异。
- [NASA CR-195140：A Physical Optics/Equivalent Currents Model for the RCS of Trihedral Corner Reflectors](https://ntrs.nasa.gov/api/citations/19940020143/downloads/19940020143.pdf)：多次反射、物理光学与边缘等效电流对角反射器 RCS 的共同贡献。
