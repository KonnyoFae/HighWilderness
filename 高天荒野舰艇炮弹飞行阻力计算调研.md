# 《高天荒野》舰艇炮弹飞行阻力计算调研

## 一、目标与边界

本公式连接已经确定的火炮内容锚点与甲弹命中公式，负责在战术模式中逐步更新普通炮弹的位置和世界速度。发生碰撞时，再由碰撞适配层用炮弹世界速度减去目标命中点局部表面速度，向甲弹结算提供相对命中速度与二维方向。

第一版只处理：

- 无动力普通炮弹，包括半穿高爆弹、高爆弹、燃烧弹和尾翼稳定脱壳穿甲弹；
- 制导炮弹在没有进行横向修正的飞行阶段也可以复用同一阻力骨架；
- 俯视平面内的位置、世界速度、相对介质速度、飞行时间和累计航程；
- 与能见度、云雨特效相互独立的弹道介质参数；
- 发射舰世界速度对炮弹初始速度的继承；
- 超过最大航程、寿命或战场边界后的自毁。

第一版不处理：

- 逐发求解三维抛物线、炮口真实仰角、科里奥利力、自转漂移和马格努斯力；
- 炮弹为了抛射而在上层、云层和雨层之间自动切换；
- 用空气阻力公式重新计算穿深；
- 用一条现实制式弹的完整阻力表冒充所有弹种；
- 火控散布、提前量误差和制导修正，这些属于后续命中前火控层。

现有战术场景和装甲命中均以俯视二维空间为核心。第一版将炮口俯仰和重力下坠视为火控系统已经完成的纵向解算，屏幕上的炮弹位置是弹道在当前高度层平面上的投影；弹丸不会仅因显示用抛物线而离开当前高度层。普通同层射击使用本文公式，跨层攻击另行受到现有跨层攻击规则限制，而不是在本公式内偷偷生成第四个连续高度轴。

## 二、选择点质量阻力模型

标准阻力方程为：

`D = 0.5 × ρ × C_D × A × |u|²`

NASA 对带阻力弹道的说明同样从弹丸质量、横截面积、介质密度、阻力系数和速度平方建立运动方程；真实阻力系数还会随形状、雷诺数和马赫数变化。[NASA Glenn：Flight Equations with Drag](https://www1.grc.nasa.gov/beginners-guide-to-aeronautics/flight-equations-with-drag/)、[NASA Glenn：Modern Drag Equation](https://www1.grc.nasa.gov/beginners-guide-to-aeronautics/modern-drag-equation/)

现实炮弹的阻力系数不是常数。历史弹丸与火箭弹研究明确把 `C_D` 作为马赫数函数；炮弹在跨音速区间的气动力还会出现窄峰值。因此第一版保留马赫数曲线，而不使用“每飞一公里固定损失若干速度”的线性衰减。[NASA/NACA：无翼弹体阻力计算资料](https://ntrs.nasa.gov/api/citations/19960014812/downloads/19960014812.pdf)、[美国陆军/NASA：典型炮弹跨音速气动研究](https://ntrs.nasa.gov/citations/19780057966)

该模型比完整六自由度弹道简单得多，但能保留本项目真正需要的关系：

- 只有弹道介质密度确实改变时阻力才会改变，云雨与低能见度本身不产生额外炮弹减速；
- 初速越高，初始阻力按速度平方快速增加；
- 大质量、小迎风面积的高截面密度弹丸保速更好；
- 弹形和弹种可以通过形状系数区分；
- 穿越跨音速区时出现额外减速；
- 发射舰航向与速度改变炮弹相对空气的初始速度；
- 碰撞适配层能够从炮弹世界速度和目标局部表面速度得到实际相对命中速度。

## 三、数据字段

### 3.1 高度层介质

| 字段 | 符号 | 说明 |
| --- | --- | --- |
| 等效介质密度 | `ρ_h` | 单位 `kg/m³`，决定动压和减速强度 |
| 声速 | `a_h` | 单位 `m/s`，用于计算马赫数 |
| 介质流速 | `v_air,h` | 第一版为零；以后加入风场时直接复用 |

原型暂用：

| 层级 | 首版炮弹 `ρ_h` | 首版炮弹 `a_h` |
| --- | ---: | ---: |
| 上层 | `0.55 kg/m³` | `320 m/s` |
| 云层 | `0.55 kg/m³` | `320 m/s` |
| 雨层 | `0.55 kg/m³` | `320 m/s` |

首版三个高度层共用同一套炮弹阻力参数。雨层的暴雨和低能见度影响光电、雷达、火控锁定、制导可靠性和画面表现，不自动提高普通炮弹的空气阻力；云层同理。现实标准海平面空气的 `ρ≈1.225 kg/m³`、声速约 `340.3 m/s` 只作为数量级参照，不能因为某层有雨就直接套用。以后若确实需要表现实际海拔造成的空气密度差异，应显式修改独立的 `ρ_ballistic`，而不是读取能见度或天气标签。[NASA：1976美国标准大气海平面参数](https://ntrs.nasa.gov/api/citations/20060053240/downloads/20060053240.pdf)

舰艇船壳气动仍使用既有的舰艇介质参数与玩法规则。炮弹介质、舰艇气动介质和传感器环境是三个独立数据通道，不能因为共用“高度层”名称就强制绑定为同一数值。

### 3.2 弹药原型

| 字段 | 符号 | 说明 |
| --- | --- | --- |
| 弹丸质量 | `m` | 单位 `kg` |
| 弹径 | `d` | 单位 `m`，由毫米口径换算 |
| 参考迎风面积 | `A` | `πd²/4` |
| 炮口初速 | `v_muzzle` | 相对于炮管和发射舰的速度 |
| 形状系数 | `i_form` | 同一基础马赫曲线上的弹形修正 |
| 阻力曲线 | `drag_curve_id` | 普通旋稳弹、脱壳长杆弹等可以使用不同曲线 |
| 最大航程 | `R_max` | 超过后自毁；不是战术场景半径 |
| 最大寿命 | `T_max` | 防止低速失的弹丸长期存续 |
| 最低存续速度 | `v_min` | 可选；低于该值时自毁或转为无伤害背景 |

弹丸的截面密度为：

`σ_section = m / A`

它只用于解释保速能力和内容标定，不直接替代完整阻力方程，也不直接生成穿深。

### 3.3 炮弹实例

- 当前二维世界位置 `x_p`；
- 当前二维世界速度 `v_p`；
- 当前高度层；
- 已飞时间与累计航程；
- 发射者、弹药原型和稳定随机种子；
- 是否仍具备制导、近炸或穿透结算资格。

## 四、初始速度

炮口初速是相对炮管的速度。炮弹生成时的世界速度必须继承发射舰速度：

`v_p,0 = v_ship + v_muzzle × d_hat_fire`

其中 `d_hat_fire` 是炮管在俯视平面内的单位射击方向。不能只把炮弹世界速度写成固定炮口初速，否则一艘以三百米每秒航行的战舰顺航和逆航开火会得到完全相同的弹道。

发射舰的横向速度同样会进入炮弹世界速度。自动火控应在求提前量时补偿这个继承速度；手动射击则允许玩家直接感受到舰体运动造成的弹着偏差。

原型中，一艘以 `300 m/s` 航行的战舰发射炮口初速 `1015 m/s` 的120毫米炮时：

| 发射方向 | 初始世界速度大小 |
| --- | ---: |
| 顺航 | `1315.0 m/s` |
| 横向 | `1058.4 m/s` |
| 逆航 | `715.0 m/s` |

这是速度向量相加的直接结果，不是额外的方向伤害倍率。这里得到的仍是炮弹世界速度；最终穿深还要在碰撞时扣除目标命中点局部表面速度。

## 五、实时阻力公式

炮弹相对于介质的速度为：

`u = v_p - v_air,h`

马赫数为：

`M = |u| / a_h`

实际阻力系数为：

`C_D = i_form × C_base(M)`

第一版基础曲线使用分段线性控制点：

| `M` | 0.00 | 0.80 | 0.95 | 1.05 | 1.20 | 1.50 | 2.00 | 3.00 | 5.00 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `C_base` | 0.160 | 0.170 | 0.250 | 0.340 | 0.300 | 0.250 | 0.220 | 0.200 | 0.180 |

这是一条明确标记为“游戏首轮标定”的普通炮弹曲线，不宣称是任何现实制式弹的实测表。其首要用途是保留亚音速、跨音速峰值和超音速三个阶段；以后取得具体弹形或需要区分脱壳弹时，可以新增曲线而不改运动方程。

阻力向量为：

`F_D = -0.5 × ρ_h × C_D × A × |u| × u`

炮弹加速度为：

`a_p = F_D / m`

这一向量写法保证阻力始终与相对介质速度相反。第一版没有风场时，`v_air,h=0`，炮弹方向在未制导和未碰撞前保持不变，只衰减速度大小。

## 六、积分与碰撞

### 6.1 离线距离表

在固定层级、无风、直线飞行的离线标定中：

`dv/dx = -k(v) × v`

`k(v) = ρ_h × C_D(M) × A / (2m)`

将短距离步长内的 `C_D` 视为常数，可以用解析形式更新：

`v_1 = v_0 × exp(-kΔx)`

`Δt = [exp(kΔx)-1] / (kv_0)`

原型使用 `10 m` 距离步长生成测试表。该步长只用于离线标定，不代表运行时每一发炮弹都必须按十米切成大量子步。

### 6.2 战斗运行时

在一个固定物理时间步内，若暂时把 `C_D` 视为常数，二次阻力方程可以更新相对速度大小：

`u_1 = u_0 / (1 + k|u_0|Δt)`

相对介质位移长度为：

`s_rel = ln(1 + k|u_0|Δt) / k`

再叠加介质流速产生的位移。每个物理步重新计算马赫数和曲线插值即可；未来存在强风或制导转向时，可以改用中点法而不更换数据口径。

高速炮弹绝不能只检测离散终点。一发 `1780 m/s` 的炮弹在六十赫兹画面帧中会前进约三十米，远大于五米网格。每个物理步必须从旧位置到新位置进行连续线段扫掠，并按最早命中时间处理外部防御、模块轮廓或船壳边；运动积分频率和碰撞扫掠频率不必等于渲染帧率。

## 七、射程与自毁

火炮射程不是由“速度降为零”决定。尖头弹丸在纯阻力模型中只会渐近减速，因此弹药原型仍保存最大航程和最大寿命。炮弹在以下任一条件满足时结束：

1. 命中有效碰撞体并完成相应结算；
2. 累计航程达到 `R_max`；
3. 飞行时间达到 `T_max`；
4. 速度低于该弹种的 `v_min`；
5. 离开当前战术场景的有效模拟区域；
6. 制导弹失去目标并触发已有自毁规则。

首轮普通弹最大射程继续使用30/76/120/155/255毫米的 `3/16/8/30/35 km`。这些是内容上限，不要求弹丸在该处仍有理想命中率。推荐交战距离由火控、目标运动与预测命中速度共同决定；雨层可以因火控与制导可靠性下降而缩短推荐交战距离，但不能把这种命中率惩罚伪装成炮弹速度衰减。

## 八、原型结果

各标准弹在自己的普通弹最大射程处得到：

| 弹种 | 射程 | 三层共用末速 | 三层共用飞行时间 |
| --- | ---: | ---: | ---: |
| 30毫米高爆燃烧弹 | `3 km` | `772 m/s` | `3.31 s` |
| 76毫米半穿高爆弹 | `16 km` | `462 m/s` | `24.91 s` |
| 120毫米半穿高爆弹 | `8 km` | `787 m/s` | `8.96 s` |
| 155毫米半穿高爆弹 | `30 km` | `471 m/s` | `45.43 s` |
| 255毫米半穿高爆弹 | `35 km` | `426 m/s` | `64.14 s` |

相对于炮口速度，最大射程保速比例依次约为 `0.721/0.510/0.776/0.502/0.608`。这说明此前设定的普通弹射程没有要求所有火炮覆盖五十公里，也没有让弹丸在最大射程前就失去全部动能。

三个层级在首版具有相同末速与飞行时间。雨层远射仍可能因为能见度、火控和制导可靠性较差而不实用，但这是命中链问题，不是外弹道问题。

完整逐距离结果和可复现代码见《高天荒野舰艇炮弹飞行阻力公式原型测试.py》。

## 九、三十毫米高射速优化接口

`4200 发/分` 等于每秒七十发。多舰、多座近防炮全部生成独立完整炮弹实例会让碰撞扫掠成本迅速超过主炮。

第一版数据结构应预留“弹群包”接口：

- 弹药消耗、枪口火焰和声音仍按真实射速累计；
- 一个弹群包保存若干发同一短点射中的炮弹数量、散布种子和时间跨度；
- 飞行阶段只更新一条中心弹道与包络；
- 接近目标或发生碰撞时，再按稳定随机种子展开各发命中点和伤害；
- 主炮、脱壳穿甲弹和需要精确穿透路径的低射速弹药仍逐发模拟。

每包具体合并几发必须通过性能测试确定，不在公式阶段写死。这个接口只改变高射速弹丸的运行时表示，不改变总弹药消耗、理论射速、每发伤害或局部装甲命中次数。

## 十、下一步

以下顺序属于规范蓝图集成、玩家编辑器往返和教程舰建立后的共同标定阶段，不是当前紧接着实施的工作；当前优先级统一服从《高天荒野舰艇建造与派生性能剩余实施计划》。

1. 用本文输出的静止目标相对命中速度，把五档口径的半穿高爆弹 `P_ref` 放在各自代表性参考速度上；
2. 建立教程阶段常见装甲钢厚度组；
3. 以推荐距离与极限距离分别测试击穿、未击穿和局部装甲耗损；
4. 为30毫米和120毫米脱壳穿甲弹建立独立质量、阻力曲线与高速穿深锚点；
5. 随后再做火控提前量、散布、目标机动和制导炮弹修正。

在完成这一步以前，不应仅凭炮口动能直接给所有射程填写同一穿深，也不应把最大射程当作舰艇自动开火距离。
