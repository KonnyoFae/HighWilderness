# T0b.2d4 完整安全场景接线实施细则

建立日期：2026 年 9 月 5 日。进入本轮时的提交基线为 `5c25564`。

## 1. 切片与接续顺序

| 切片 | 内容 | 状态 |
| --- | --- | --- |
| d4.1 | 硬故障跳闸、锁存与显式复位 | 完成 |
| d4.2 | 精确运行时硬故障事实投影 | 完成 |
| d4.3 | 硬故障开边界、复位与紧急断推 | 完成 |
| d4.4 | 对向通道方向切换互锁 | 完成 |
| d4.5 | 无场景硬故障→互锁→时间→交付→软安全组合 | 完成 |
| d4.6 | 新场景、存档、诊断与迁移合同 | 完成 |
| d4.7 | 统一场景接线、资源重建与存档重载重放 | 完成；验收见 d4g 阶段报告 |
| d4.8 | 独立 d4 黄金、长序列与全回归收口 | 实现完成；退出门验收见 d4h 阶段报告 |

## 2. 独立版本与执行门

场景使用 `gaotian.tactical-scene-timeline/v7alpha1`，单步结果使用 `gaotian.tactical-scene-step-resolution/v6alpha1`，存档使用 `gaotian.governed-propulsion-scene-save/v2alpha1`，诊断使用 `gaotian.actual-propulsion-step-diagnostics/v3alpha1`，治理标记使用 `gaotian.governed-propulsion-execution/v2alpha1`。

新治理标记明确软安全、硬故障和互锁全部参与所选择的权威语义，固定事实采样为 `opening_runtime`。d4.6 只允许 v7 解析和保存；d4.7 已将完整适配器接入 `advance_tactical_scene_step`，要求精确资源上下文，缺失时返回 `full_scene.context_required`。旧结果对象不能把 v7 输出序列化为 d3 的 v5 结果。

逐舰推进状态继续复用 v3、发动机时间状态继续复用 v2；已验证的 d4.5 开收记录原样嵌入新逐舰审计记录。旧 d3 版本、诊断、存档和三套既有黄金不隐式升级。旧统一场景导入时不加载 d4 合同和算法。

## 3. 命令所有权与保存边界

`SceneHardFaultCommandBatch` 保存源场景 SHA-256、开边界固定步号及按舰艇 id 唯一排序的非空硬故障命令。未列出的舰艇表示没有本步硬故障命令。复位只能引用本舰已经跳闸的执行器；故障是否已经清除，仍由 d4.3 使用实际开边界 runtime 判断。

复位与紧急断推不得在同舰同一步混用。命令批次绑定精确 `S_n`，不可重复套用到下一步或另一个场景。d4.7 在步进前验证完整批次；复位再依据真实 opening 硬事实判断。失败不会提交新场景，原场景及其推进状态保持不变。

玩家持续方向/车钟由逐舰 `propulsion_control` 与 governor 保存。复位和紧急断推属于一次性步进输入，不能作为持久命令自动重放；其证据保存在单步开边界记录中。显式复位和紧急断推的同边界禁止重启语义沿用 d4.3/d4.5，下一开边界是否启动重新按持久控制及硬事实决定。

存档只接受 `boundary_phase = committed` 的完整场景。它不保存待复位队列、开边界中间状态、有效控制影子、缓存或待交付物理请求。每个活动舰六通道 governor 已提交到场景边界；退出舰保留冻结的推进状态与控制、不得接受硬命令，机动步号仍遵守旧场景的同步规则。

## 4. 事件与诊断证据链

单步输入的命令批次必须逐项对应完整开边界中的硬命令。每个非退出舰必须恰有一份开边界和收边界记录；收边界引用同一完整开边界，并绑定最终 runtime、运动和推进状态。所有执行器包括跳闸成员必须保留在物理聚合记录中。

新诊断绑定完整开边界、运行时硬事实投影、方向互锁、源推进状态和六通道 governor 的指纹。它的积分请求必须对应聚合请求经过生命周期抑制后的交付。逐执行器贡献、汇总物理请求及诊断来源必须一致；基于真实资源重算这些物理值属于 d4.7 的加载/结果验证层。

推进边界事件统一按以下顺序发布：固定步号→opening/closing→hard/time/soft→舰艇 id→局部序号。局部序号保留底层适配器产生的因果次序，不按事件类型重新排序。开边界只发布硬故障及时间事件，收边界发布时间与软安全事件。方向互锁决定完整保存在 opening 中，不伪造执行器事件。紧急断推沿用 d4.3 的独立结果记录，不伪装为资源跳闸。

## 5. d3→d4 具名迁移

迁移 id 为 `gaotian.migration/d3-save-v1-to-d4-save-v2-committed-unambiguous/v1`。调用者提供原始存档指纹与精确 d3 资源上下文，迁移先严格解析原版本、验证资源血缘，再建立新治理身份。

第一条迁移只接受 `off/ready/running`、实际输出已到达目标且没有待转换步的明确边界。启动中、停止中、未完成响应、跳闸、未知的待复位字段及方向切换字段均拒绝；同轴对向残留或对向请求也拒绝。这样不会凭空补出历史故障、排程锚点或互锁状态。其他历史运行状态若未来需要迁移，必须另建具名规则。

迁移不推进时间、不接令、不计算硬事实、不改发动机阶段、不重算 governor，不改变舰艇、弹丸、燃料、运动或资源身份。迁移凭证保存源/结果存档指纹及去除三项治理身份字段后的场景内容指纹，并可根据原始来源重验。迁移后的存档仍必须通过 d4.7 加载入口重建资源，再继续推进。

## 6. d4.7 开工边界

1. 从具名资源构建 v7 初态；严格重建资源、runtime 和模型，校验保存状态的执行器集合、排程及可用性来源。
2. 在统一场景开边界消费一次性硬命令和持久控制，先硬故障、再方向互锁、再时间提交；只使用已提交实际输出积分。
3. 延续现有武器、命中、毁伤、人员和生命周期时序；收边界在最终 runtime/运动上唯一执行软安全。区间内新资源故障在下一 opening 采样，不提前改变 d4.5 语义。
4. 以本轮合同输出 v6 单步结果和诊断 v3，验证源/结果场景与逐舰完整证据。正确处理同一步退出与已经退出舰的冻结、自动制动和无指挥能力抑制。
5. 开放执行门之前完成跳闸、复位、紧急断推、对向零输出放行、故障中的 overg 以及存档重载重放。独立黄金、正式性能、弹丸优化、Web/编辑器默认切换仍留在后续阶段。

## 7. d4.7 接线与重载验收记录

`高天荒野舰艇完整受控推进场景.py` 提供具名 v7 构建、保存、严格加载与资源级单步验证。加载重新编译目录、舾装、快照及出航资源，并创建新的 runtime/模型缓存；保存状态的资源身份、执行器集合、时钟、精确响应排程和持久控制须逐一通过校验。方向选择保留原始请求，由 d4.4 决定对向降推与零输出放行。

统一场景依序完成硬事实投影与一次性命令、方向互锁、opening 时间提交、实际输出积分及 closing 时间/软安全提交。单步 v6 保留完整开收记录与诊断 v3；物理验证使用真实开边界运行时见证重算推进聚合与交付，并用最终 runtime/运动重算收边界。见证仅在内存中保留，不进入存档或结果 JSON。

区间实际交付与收边界生命周期采样各有明确来源：若舰艇在区间末被摧毁，单步交付状态和燃耗保留已经发生的积分，closing 使用 `suppressed_falling` 采样，下一 opening 才投影新硬故障。d4.6 合同相应解除两处状态必须相等的错误约束，并以最终生命周期验证 closing 状态；字段和版本不变，d3 行为不变。

十二个具名场景覆盖 224 艘舰、1224 个执行器和 1344 个 governor，均通过首步结果重算、边界 1 保存加载和第二步连续/重载一致验证。专项覆盖故障中的 overg、资源恢复后的锁存、失败复位原状态不变、显式复位与紧急断推不重复消费、52 步反向切换及三个边界重载、退出冻结、自动制动、无指挥能力抑制和真实弹丸命中后的 runtime 更新。阶段报告：`舰艇数据/报告/阶段T0b2d4g统一场景完整安全与存档接口.v1.json`。

下一步 d4.8 建立独立 d4 黄金，执行十二场景三次重放、较长故障/复位/反向组合序列及完整回归。三套既有黄金继续逐字不变；本轮正式性能运行数为 0，正常玩法与 Web/编辑器默认路径继续保持原状态。

## 8. d4.8 独立黄金与退出门

独立文件 `contracts/web_bridge/t0-fully-governed-propulsion-step-golden.v1.json` 保存十二个具名 v7 场景各 12 步的输入流、资源、初末场景、逐步结果、最终运动与事件指纹。黄金还覆盖混合长序列和 d3 迁移后续跑，不覆盖或改写三套既有黄金。先生成单次可审查基线，再在完整回归中独立执行三次并逐项比对；基线生成不算三次重放验收通过。

十二场景每次覆盖 2688 个舰步及等量开/收边界。比较直接使用冻结的 d3 黄金：默认场景下运动、燃料、资源包、控制输入与原生事件应保持一致；新治理身份、完整硬事实/互锁记录和诊断造成的新场景、结果指纹另行冻结。每次重放重新建立绑定缓存，首末步另做资源级结果重算。

长序列先通过真实退出指令建立一艘活动舰和五艘冻结舰，保留完整六舰资源集合，再从边界 1 执行 401 步至边界 402。活动舰包含主机冷启动、软上限释放、主机与双向转向喷口联合控制、两次断油/补油/显式复位、运行中紧急断推、停车与再次启动。技术夹具没有反向主机通道，场景级对向互锁由双向转向喷口覆盖；全通道纯边界覆盖继续由 d4.4 验收承担。

三次长序列分别连续运行、在 `[92,112,118,212,272,277]` 重载、在 `[111,117,211,267,276,337]` 重载。重载点覆盖反向降推、故障前后、锁存、复位前后、紧急断推前后及启动/响应边界。输入序号从 0 计，首次反向请求在 90，精确零输出放行在 105；两次跳闸在 110/270、复位在 116/275、紧急断推在 210。每条轨迹比较逐步完整结果、场景、事件、燃料及采样，冻结舰推进状态与控制全程不变。

d3 已提交存档通过具名迁移、v2 加载入口和资源重建后继续推进，首步须与具名 v7 初态直接推进完全一致。长序列中的燃料清零与补回是显式记录的测试输入，不代表新增正常玩法补给接口。

退出门以 `舰艇数据/报告/阶段T0b2d4h完整安全黄金与全回归接口.v1.json` 为准：十二场景及长序列三次一致、三套旧黄金逐字不变、完整 86 项测试和测试入口 3 项检查均通过后，d4.1—d4.8 收口。重型 d4h 已加入 full 档，quick 保留 19 项。下一切片为 `T0b.2e1` Python 弹丸批处理的边界复核与实施；正式性能、跨步调度、多速率、Rust、T0c 和 Web/编辑器默认切换各自依原门禁推进，不计入本轮 d4 完成范围。
