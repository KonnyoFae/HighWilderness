# 《高天荒野》舰艇规范数据契约

## 一、定位与当前版本

本文件定义船壳建造、舾装、测试、编辑器和战术运行之间唯一的数据口径。当前首个可执行切片为：

`gaotian.ship/v1alpha1`

本切片已经实现六类玩家/内容源资源：

- `MaterialCatalog`：结构材质或逐边基础装甲材质目录；
- `HullCoatingCatalog`：船体涂料目录与运行时可用状态；
- `ModulePrototypeCatalog`：模块原型、安装几何、物理与功能目录；
- `HullBlueprint`：船壳蓝图源数据；
- `OutfitPlan`：绑定精确船壳版本的模块实例布局与船体涂料选择；
- `SortieConfiguration`：绑定精确舾装版本的离港初始高度层、控制方式、燃料、人员、逐货仓大宗货物及可选弹药初态；离港后作为不可变来源记录。

`DerivedShipSnapshot` 已作为编译器生成、禁止手填覆盖的设计态输出落地，接口为 `gaotian.derived-ship-snapshot/v1alpha1`。`ShipInstanceSnapshot` 也已作为可保存的当前舰艇实例状态落地；它由设计态和出航配置初始化，保存当前高度层、燃料、船员、大宗货物、弹药库库存、武器待发弹、当前全舰船壳耐久、逐模块当前耐久/工作模式、全舰供电策略，以及可选的建造时船壳/当前舾装自持快照，不是另一套玩家手填整舰性能。`RuntimeShipParameters` 再由当前派生设计、冻结的首次出航来源与实例状态共同派生。

机器可读模式为《舰艇数据/模式/高天荒野舰艇数据契约.v1alpha1.schema.json》。Python 读取、规范化和编译实现为《高天荒野舰艇数据契约.py》、《高天荒野舰艇无界面船壳编译器.py》、《高天荒野舰艇无界面舾装编译器.py》、《高天荒野舰艇出航配置编译器.py》、《高天荒野舰艇运行时参数编译器.py》、《高天荒野舰艇实例设计状态.py》与《高天荒野舰艇武器时间与射击队列.py》。阶段 I2/I3 的船坞设施、库存、战略工时与舾装变更订单使用独立扩展模式 `gaotian.ship-logistics/v1alpha1`，见《舰艇数据/模式/高天荒野舰艇后勤数据契约.v1alpha1.schema.json》与《高天荒野舰艇船坞后勤与战略工时.py》；它引用本契约的精确材质、模块、设计和实例身份，不复制舰艇派生性能。

## 二、共同资源信封

每个规范资源都必须具有：

| 字段 | 口径 |
| --- | --- |
| `schema` | 固定为 `gaotian.ship/v1alpha1` |
| `kind` | 资源类别 |
| `id` | 稳定、全局唯一、与显示名称分离的标识 |
| `version` | 正整数资源版本；精确引用必须同时包含 `id` 与 `version` |
| `name` | 面向用户或开发者的显示名称，不参与引用 |

规范 JSON 使用 UTF-8、对象键排序、两空格缩进和文件末尾换行。同一语义资源经过读取—规范化—保存后得到确定性文本。

第一版所有物理字段直接在字段名中标出单位，例如 `_m`、`_kg_m3`、`_mpa`。角度若进入后续资源，运行时统一使用弧度，内容表和编辑输入字段必须在名称中明确 `_deg` 或 `_rad`，不得依赖隐含单位。

## 三、基础目录资源

### 3.1 结构材质

`MaterialCatalog.catalog_type=structure`。每项保存：

- `id`、`version`、`name`；
- `density_kg_m3`；
- `strength_coefficient`；
- `durability_coefficient`；
- `cost_coefficient`；
- `work_difficulty`。

### 3.2 基础装甲材质

`MaterialCatalog.catalog_type=base_armor`。每项在共同字段之外保存：

- `protection_coefficient`；
- `local_durability_coefficient`；
- `shell_strength_coefficient`；
- `cost_coefficient`；
- `work_difficulty`。

材质密度和各系数读取现有正式材质表。目录本身也有版本；船壳边仍精确引用单项材质 `id+version`，不能只引用显示名称或目录中的数组序号。

### 3.3 船体涂料目录

`HullCoatingCatalog` 每项保存稳定引用、平衡状态、船壳 RCS 线性倍率、运行时是否可用以及是否为新造舰默认涂料。目录必须恰有一个默认项；默认项必须可用且倍率固定为 `1.0`。

当前普通涂料 `gtw.coating.hull.ordinary@1` 是可用默认项，倍率为 `1.0`。隐身涂料 `gtw.coating.hull.stealth@1` 处于 `awaiting_joint_calibration`，倍率为 `null` 且不可装备，直到与教程雷达、教程舰和经济成本联合标定后才启用。不得为使测试通过而填入临时正式倍率。

涂料属于舾装/维护状态，不是精确船壳几何的一部分，因此 `HullBlueprint` 不保存涂料引用。船壳编译器始终生成普通涂料基准 RCS，未来由舾装方案或舰艇实例精确引用涂料并在线性平方米口径下应用倍率。

### 3.4 模块原型目录

`ModulePrototypeCatalog` 当前实现十三类最小原型。基础九类为货仓、CIC、灵烷贮槽、主发动机、侧挂转向发动机、发电设备、损管设备、人员舱和遥控核心舱；E5 扩展四类为武器、弹药库、火控和传感器。每项保存：

- 精确引用、类别和 `contract_fixture/prototype_unbalanced/balance_reference` 状态；
- 内部、顶挂、侧挂、跨层、净空、尾焰净空、允许旋转与甲板规则；
- 质量、模块耐久和待标定的外部模块 RCS；
- 默认工作模式、自动激活事件、发电量、待机/工作负载和稳定供电类别；
- 分船员类型的最低工作人数、标准人数和自动化子功能；
- 无人改进版精确引用及工程微机团块成本接口；
- 类别专属的升力、燃料容量、推力、损管能力、人员容量或大宗货物质量容量。

E5 战斗模块接口为 `gaotian.combat-system-modules/v1alpha1`。武器保存类别、目标域、兼容弹药标识、待发容量、射程和火控需求；弹药库保存全舰共享池容量与兼容弹药；火控保存解算/持续制导能力、并行通道和锁定距离；传感器保存雷达/红外/光电通道、工作模式和仪表距离。这些字段只冻结模块依赖，不在本契约层计算弹道、制导、命中或毁伤。

模块轮廓的相对格心用 `2.5m` 为一单位的半格整数坐标保存。奇数格轮廓可用 `[0,0]` 作为中心格；二格宽轮廓可用 `[-1,0]` 与 `[1,0]`，因此模块中心不必被错误强迫到某个五米格心。侧挂外形使用船壳边局部坐标，不与内部/顶挂轮廓共享全局奇偶性。

遥控核心舱不是与 CIC 重叠的普通内部模块：CIC 原型提供 `cic_internal` 槽位，遥控核心舱嵌入该槽位并在舾装编译时继承宿主锚点。完整边界见《高天荒野舰艇模块原型契约》。

## 四、船壳蓝图

### 4.1 固定坐标约定

`HullBlueprint.grid` 首切片固定为：

- `cell_size_m=5.0`；
- `deck_height_m=5.0`；
- `forward_axis=+Y`；
- `symmetry_axis=Y`；
- `cic_origin_cell=[0,0]`。

`cic_origin_cell` 使用整数格坐标，表示 CIC 模块必须占据的固定原点格；当前船壳编译器同时验证该格中心 `(0m,0m)` 位于基底船壳内部，并验证整个五米 CIC 原点格被船壳完整覆盖。

船壳端点使用米制坐标。为兼容五米格和关于 CIC 中心轴的对称外形，`v1alpha1` 允许端点落在 `2.5m` 半格步长上。闭合多边形不重复保存首端点。

### 4.2 甲板与区域

每层甲板保存：

- 稳定 `id`；
- 从零开始的整数 `level`；
- 是否为唯一基底层的 `is_base`；
- 精确引用的 `structure_material`；
- 一个或多个 `regions`。

每个区域保存：

- 稳定 `id`；
- 按边界顺序排列的 `vertices_m`；
- 与顶点等长的 `edge_armor` 数组。

`edge_armor[i]` 作用于 `vertices_m[i] → vertices_m[(i+1) mod n]`，保存基础装甲材质精确引用和实际 `thickness_m`。厚度允许为零，以支持已经存在的无基础装甲结构公式基准体；这不代表装备与启航校验会允许未安装 CIC 或完全无防护的舾装方案。

### 4.3 舾装方案

`OutfitPlan` 必须精确引用一个 `HullBlueprint` 版本和一个可用的船体涂料版本，并以稳定且唯一的实例 `id` 引用模块原型。三种放置方式为：

- `grid`：保存甲板 `id`、以 `2.5m` 为单位的全舰半格锚点和顺时针直角旋转；
- `side`：保存甲板、区域、规范边序号、边内起始五米槽位和局部直角旋转；
- `hosted`：保存宿主模块实例与宿主提供的嵌入槽位，不另外占据船壳格。

规范化时模块实例按 `id` 排序，数组原始顺序不参与布局语义。栅格模块旋转后，其内部或顶挂本体格心必须精确落在船壳编译器输出的完整五米格心上；侧挂模块使用规范边切向与朝外法向组成的局部坐标系。当前不猜测同时具有内部与侧挂本体的复合模块锚点关系，此类原型由稳定的延期错误拒绝，待专门冻结连接语义后再开放。

`OutfitPlan` 本身不保存或强制“必须轴对称”的合法性标记。未来舾装编辑器的轴对称模式只是可开关的批量编辑操作：负责生成、联动或解除镜像实例，不改变关闭该功能后各模块实例完全独立的规范语义。

## 五、当前编译能力

### 5.1 船壳编译器

无界面船壳编译器当前接受：

- 从 `level=0` 开始连续排列、每个层级至多一层的甲板；
- 唯一基底层，且基底层恰好一个无孔洞简单多边形；
- 非基底层包含一个或多个彼此分离的无孔洞简单多边形；
- 每个上层区域被相邻下层的某一连续区域完整支撑，不允许任何悬挑；
- 每层全部区域的几何与逐边装甲整体关于 `Y` 轴强制对称；
- CIC 原点和完整 CIC 原点格位于基底船壳内部；
- 合法材质精确引用。

它已经派生：

- 规范化多边形与确定性蓝图文本；
- 舰长、舰宽、面积和周长；
- 被船壳完整覆盖的五米内部格；
- 根据相邻上层实际投影生成的各层局部露天顶挂格；
- 每条绘制边上独立生成、不跨越拐角的五米侧挂步长；
- 逐层、逐结构材质的内部骨架体积、基础装甲体积和质量；
- 多层船壳部分绕 CIC 的转动惯量；
- 多层混合材质的纵向、横向结构安全加速度；
- 安全偏航角加速度与角速度；
- 每条边的局部装甲耐久代理量；
- `0°～359°` 每个整数侧滑角的气动几何方向缓存；
- 气动包络总湿表面积；
- `0°～359°` 每个整数方位角的普通涂料基准船壳 RCS 缓存；
- 源蓝图规范文本的 SHA-256 指纹。

船壳编译器不处理模块占格、舾装运行状态或出航载荷；这些能力分别由舾装编译器和后续运行时适配承担。

气动缓存模型标识为 `gaotian.aero.geometry/v1alpha1`。每个方向样本保存：

- `projected_area_m2`：同一高度带先合并遮挡后的迎风投影面积；
- `front_bluntness_area_m2`：可见迎风表面的三次方向权重面积；
- `rear_bluntness_area_m2`：可见背风表面的三次方向权重面积；
- `flow_length_m`：沿来流方向的全船投影长度；
- `wave_area_change_m2`：以不超过五米的等长区间计算的截面积变化指标。

气动缓存只读取船壳多层几何和固定五米层高，不读取结构材质、基础装甲材质、装甲
厚度或任何舾装模块。同一精确船壳蓝图的不同舾装方案因此共享相同缓存。

运行时函数按实际侧滑角在相邻整数方向之间线性插值，再由外部注入的形状系数、
粗糙度系数、雷诺数下限、高度层密度/动力黏度/音速和马赫响应计算
`f_form`、`f_skin`、`f_wave` 与 `D=0.5ρV²f_D`。当前没有为这些待共同标定参数
写入默认正式值。

船壳 RCS 缓存模型标识为 `gaotian.rcs.hull/v1alpha1`，当前参数集为
`gtw.rcs.prototype.v1`，平衡状态明确标记为 `prototype_unbalanced`。每个方向样本保存
展宽镜面项、非相干投影底项、凹角项及其普通涂料基准总和；缓存同时保存三百六十方向的
最小值、线性均值和最大值。计算前会合并同层共线船壳边，并对同一高度带的多个区域进行
正交深度遮挡，避免不可见表面重复计入。

RCS 几何缓存只读取多层船壳外形与固定五米层高，不读取结构材质、基础装甲材质或厚度。
船体涂料在线性平方米口径下缩放船壳基准表；舾装编译器会识别外露模块，但首批模块的
独立 RCS 仍为待标定 `null`，因此输出明确的未解析警告，也不会把船体涂料倍率错误套用到外部模块。

每层内部格坐标 `[x_index,y_index]` 的格心为
`[x_index×5m,y_index×5m]`，其中 `[0,0]` 即 CIC 原点格。格子只有在格心和
四角均处于船壳内或边界上，且没有船壳边穿过格子内部时才可用；任何未完整覆盖的
格子都不输出。数组规范顺序为先由舰艉向舰艏，再由左舷向右舷。

侧挂位在每层、每个规范化多边形边内独立计算。边长能够容纳的完整五米段居中排列，首尾
不足五米的余量均分；输出同时保存规范边序号、边内槽位序号、起止偏移和米制端点。
该规则保证强制轴对称船壳的镜像边得到镜像槽位，也明确禁止槽位跨越拐角。

一层的内部格只有在完全不受相邻上层任何正面积投影覆盖时，才成为本层露天顶挂格。
因此，上层边缘即使只覆盖下层某格的一部分、还不足以在上层形成完整内部格，该下层格
也不能安装需要完整五米净空的顶挂设备。仅有边或角相接、没有正面积覆盖时不阻挡。

### 5.2 舾装编译器

无界面舾装编译接口为 `gaotian.outfit-compiler/v1alpha1`。当前已经实现：

- 船壳、模块和涂料的精确版本引用；
- 内部、顶挂、跨层、侧挂和宿主槽位放置；
- 本体重叠、外部净空、尾焰净空、甲板规则和 CIC 固定原点校验；
- 船壳与模块设计质量、模块位置惯量、升力余量；
- 总发电量、四个稳定供电类别的工作负载、最低/标准船员与人员舱容量汇总；
- 每台主发动机和转向发动机的实际作用点、机体坐标推力方向、推力、响应、耗油与绕 CIC 力矩；
- 四个平动方向的主机配平能力，以及顺/逆时针转向能力；
- 保留真实主机残余偏航与转向机残余平动力的执行器聚合；
- 带船壳、舾装、模块目录和涂料目录精确来源指纹的 `DerivedShipSnapshot`；
- 确定性布局结果、稳定错误码、警告和源方案指纹。

设计合法性只使用船壳与模块质量；`SortieConfiguration` 保存离港大宗货物初态，离港后的当前货物则由 `ShipInstanceSnapshot.operational_state` 按实际货仓实例位置加入当前质量与惯量。战损、实时供电与模块人员效率同样由实例状态和运行时编译器处理。燃料、人员、武器已装弹药和已装导弹不计质量。主发动机以推力方向穿过 CIC 的作用线划分两侧：离轴两侧各使用较小侧的总推力，中轴主机独立全额输出；配平后仍按各实例真实力臂保留残余偏航。转向发动机按力矩符号分别形成顺/逆时针能力，未抵消合力保留为真实平动力。运行时必须从现存可用实例重新聚合，不能直接沿用未受损设计上限。

编译结果必须明确列出延期能力，不能因为静态质量和执行器结果已经生成就把当前切片称为完整运行时舰艇。

船壳编译接口已经冻结为 `gaotian.hull-compiler/v1alpha1`。综合回归入口为《高天荒野舰艇阶段C综合回归.py》，保存报告为《舰艇数据/报告/阶段C船壳编译接口.v1.json》；脚本会逐字段比较实时结果与保存报告，报告过期时失败。

### 5.3 出航配置与初始载荷编译器

首版接口为 `gaotian.sortie-compiler/v1alpha1`。`SortieConfiguration` 必须精确绑定舾装方案版本，保存离港初始高度层、有人/遥控核心控制模式、实际启用的遥控核心舱实例、初始燃料单位、分类型人员数、逐项绑定货仓实例的大宗货物质量，以及可选的完整弹药初态。它不允许手填整舰质量、惯量、升力或执行器能力，离港后也不作为持续变化的资源容器。

当前编译器已经实现：

- 人员舱容量校验，以及有人出航的最低船员硬限制；
- 遥控核心模式必须引用本舰实际安装的遥控核心舱；
- 燃料容量校验，同时明确燃料与人员不增加舰艇质量；
- 大宗货物只能绑定本舰货仓实例，逐仓校验容量；
- 一旦声明 `ammunition_loadout`，必须恰好列出舾装中的全部弹药库与武器实例；逐库校验兼容弹药和容量，逐武器校验待发弹种和待发容量；
- 以货仓实际锚点按 `I=m(x²+y²)` 增加当前惯量；
- 当前质量、当前惯量和当前升力余量派生，超出升力时禁止出航；
- 有人员时启用 `12G` 乘员安全锁，完全无人时解除该锁；
- 规范化文本、来源指纹、低于标准船员警告与稳定错误码。

当前载荷切片的回归入口为《高天荒野舰艇阶段E载荷回归.py》，保存报告为《舰艇数据/报告/阶段E出航载荷接口.v1.json》。它负责验证出航初态合法性；离港后持续变化的资源由 `ShipInstanceSnapshot.operational_state` 保存并由同一载荷规则重新派生。

### 5.4 舰艇实例状态与运行时参数编译器

`ShipInstanceSnapshot` 精确绑定舾装方案、`DerivedShipSnapshot` 内容指纹和 `SortieConfiguration` 精确版本/内容指纹。每个舾装实例必须恰好具有一条状态，保存：

- `operational_state`：当前高度层、燃料、分类型船员和逐货仓大宗货物；耗油、伤亡、卸货与换层只修改本字段，不修改或重绑初始出航配置；
- `current_hull_integrity_fraction`：当前全舰船壳耐久比例，范围为 `0～1`；OverG 等结构损伤修改本字段，归零时运行时报告 `hull_structure_collapsed`；
- `current_durability_points`：不得超过原型耐久；归零时模块成为仍占质量、惯量和安装位置的损毁残骸；
- `operating_mode`：`off/standby/active`，描述设备当前状态，不构成供电界面的逐设备电源管理授权；
- 全舰 `power_policy`：四个稳定供电类别的当前顺序、关闭类别，以及普通整类跳闸或安全供电模式。
- 可选 `ammunition_state`：逐弹药库的当前物理库存，以及逐武器的待发弹种与待发发数；新实弹实例由出航配置复制初态，开火和装填后只修改本字段。旧版实例可省略以保持规范文本和内容指纹兼容，但在补建完整初态前不能执行武器动作。
- 可选 `design_state`：建造时完整规范船壳快照及内容指纹、当前规范舾装快照及内容指纹、当前 `DerivedShipSnapshot` 内容指纹和独立修订号。旧实例省略时继续按原有舾装引用/派生指纹校验；新造舰和首次改装的旧舰写入后，外部蓝图删除也不影响实例读取，并可从舰艇重建配套船壳与当前舾装。
- 可选 `weapon_timeline_state`：精确武器时间配置目录引用及内容指纹、当前战术时刻、逐武器下次可开火时刻，以及每件武器至多一条活动射击序列。序列保存弹种、剩余发数、目标域/距离、火控实例、当前阶段和下一事件时刻；旧实例省略时规范文本与既有内容指纹不变。

原 `outfit_plan`、`derived_ship_snapshot_sha256`、`sortie_configuration` 和其指纹在 I3 后明确作为建成/首次出航历史，不随改装覆盖。运行时若存在 `design_state`，以其中的当前舾装和当前派生指纹校验传入设计，同时仍用冻结的出航配置检查历史来源；因此玩家可在战略层切换到改装后舰艇，而不会伪造一份新的“首次出航”。

模块原型使用 `default_operating_mode` 和 `automatic_activation_events` 声明初始状态与按需唤醒来源。运行时编译器接收当前瞬时事件集合：命中事件的 `standby` 模块临时按 `active` 参与人员、供电与功能结算，但不会改写实例快照；实例中显式设为 `off` 的模块不会被自动事件唤醒。当前损管响应 `ship.damage_control_required`，遥控核心舱响应 `ship.remote_control_selected`；E5 夹具中的武器、火控和传感器分别响应 `ship.weapon_fire_requested`、`ship.fire_control_required` 和 `ship.sensor_scan_required`。

运行时接口为 `gaotian.runtime-ship-parameters/v1alpha1`，供电策略为 `gaotian.power-allocation/categories-and-nearest/v1`，逐功能战损策略为 `gaotian.module-damage-response/per-function-curves/v1`。稳定类别和默认等级为：损管 `P0`、武器与主动防御 `P1`、火控 `P2`、传感器 `P3`；P0～P3只是默认顺序的显示口径，不再作为模块类别字段。当前实现：

- 普通模式按玩家当前排列的稳定类别顺序供电；某类总需求无法全额满足时，该类全部跳闸，剩余电力继续尝试后续类别；
- 安全供电模式仍先遵守类别顺序，但在供不满某类时按模块锚点到 CIC 的二维直线距离由近到远逐台供电，同距离以稳定实例 `id` 决定顺序；
- 玩家关闭一个供电类别时全类断电，不提供单台设备电源选择；安全供电模式的核心舱升级解锁资格仍由成长系统检查；
- 可用发电量只来自未毁、处于工作状态且能够执行 `generator.regulation` 的发电设备；
- 船员先依次保障 CIC、主发动机、转向发动机、发电和损管的最低岗位，再保障其他工作模块的最低岗位；剩余人员按所有工作模块的标准岗位缺口成比例分配。未自动化功能按实际到岗/标准人数比例运行；模块原型已经声明的自动化子功能不受缺员比例削减；
- 当前船员可以因战损低于有人出航最低值；最低船员只在离港校验时构成硬限制，战斗中的缺员由运行效率结算；
- 主发动机和转向发动机按当前损毁、工作、供电、人员/自动化和燃料状态重新生成逐实例输出，再调用同一物理配平策略重新聚合；
- 每个模块原型按子功能保存 `damage_responses`；运行时先由当前耐久求得战损倍率，再与人员/自动化倍率相乘。推进、发电和升力不共享一条隐藏的全局线性曲线；
- CIC 损毁会使嵌入其中的遥控核心舱连带不可用；灵烷贮槽损毁会重新计算当前升力及坠毁条件；
- 当前质量与惯量继续包含损毁模块残骸，不因模块功能失效而凭空减轻；
- `RuntimeShipParameters` 保存设计态、出航配置、舰艇实例、气动缓存和 RCS 缓存指纹，禁止绕过来源手填聚合参数。

大于零但低于满值的模块记录为 `damaged`，但具体输出完全由该子功能声明的 `binary_until_destroyed`、`piecewise_linear` 或 `stepwise` 响应决定。当前契约夹具以推进的非线性分段曲线、发电的线性曲线和升力/CIC 的归零失效阈值验证差异化路径；曲线节点尚不构成正式平衡参数。

### 5.5 弹药状态与武器动作

接口为 `gaotian.ammunition-actions/v1alpha1`。弹药库仍是独立的物理存储模块，状态按库记录，以便模块损毁能够阻断其中弹药；但所有仍可工作的兼容弹药库在装填时构成全舰共享访问池，不计算管线、通道或距离损耗。武器待发弹独立于弹药库库存，因此弹药库为空或损毁后，武器仍可使用已经装入待发位置的弹药。

首版提供两项原子动作：开火验证武器兼容弹药、目标域、射程、瞄准/击发效率、火控需求与待发弹，然后只扣减待发弹；装填验证武器待发容量和装填效率，再按弹药库实例 id 的确定性顺序，从仍可执行 `ammunition.feed` 与 `ammunition.inventory` 的兼容弹药库扣减库存。两项动作均读取同一个运行时编译结果，因此模块战损、人员、自动化、P1/P2 供电和 CIC 控制失效会自然阻断相应动作。

当前一发待发弹按一单位具体弹药库存计数。通常弹药/特殊弹药战略资源与具体弹药之间的换算、重新武装工时和补给成本属于后续经济合同。阶段 I 测试使用阶段 F 常规有人舰的精确舾装作为 `technical_surrogate`，其中弹量只属契约夹具，不是正式平衡值。

### 5.5.1 武器时间状态与射击队列

接口为 `gaotian.weapon-action-timeline/v1alpha1`，事件排序策略为 `gaotian.weapon-timeline/event-ordered-atomic-actions/v1`。时间配置使用独立 `gaotian.weapon-timing/v1alpha1` 目录，机器模式见《舰艇数据/模式/高天荒野舰艇武器时间数据契约.v1alpha1.schema.json》；目录逐武器原型提供循环射速和逐发装填基准时长，舰艇时间状态冻结其精确引用与内容指纹，配置变化时拒绝继续推进旧队列。

连续射击序列一次只执行一发：已经有正确待发弹时等待武器冷却并调用原子开火；待发位置为空时等待装填时长，再调用原子装填。开火成功后的间隔为 `60 / cyclic_rate_rpm / weapon.fire效率`；装填时长为 `reload_seconds_per_round / min(weapon.reload, ammunition.feed, ammunition.inventory效率)`。装填开始不预扣库存，只有完成事件到期并通过原子校验后才扣减弹药库、增加待发弹。

齐射至少包含两件不同武器，每件武器首版恰好发射一发；所有武器必须已经装入指定待发弹且在当前战术时刻完成冷却，随后在同一时刻按稳定序列 id 逐项调用原子开火。队列时间只由显式战术时钟推进，与战略世界绝对时间和暂停界面无关。

活动序列可以取消，取消不扣弹药。事件到期时重新编译当前运行时；战损、断电、缺员、CIC 或火控失效造成原子动作失败时，记录稳定错误码、保持弹药状态不变并终止该武器序列。改装涉及被移除或替换的武器时必须先取消其活动序列；未变化武器保留冷却和队列，新增武器从当前战术时刻建立时钟。

《阶段I武器时间技术替身配置.v1.json》的 `60rpm` 与 `0.25s` 只验证冷却、装填和事件排序，不构成正式射速或装填时间。当前仍不计算火控通道并发、炮塔多炮管发射节奏、正式散布或甲板层概率；成功原子开火之后的非制导弹丸生成、阻力飞行、船壳边命中和甲弹/损伤回写由 I5 接口处理。

### 5.5.2 弹丸世界、局部装甲与损伤回写

弹丸性能目录使用 `gaotian.projectile-profile/v1alpha1`，机器模式见《舰艇数据/模式/高天荒野舰艇弹丸性能数据契约.v1alpha1.schema.json》。每种具体弹药 id 分别声明口径、质量、初速、形状系数、最大寿命、参考穿深/速度、速度和斜面指数、归一化/跳弹参数、后效类型，以及把公式输出换算为局部装甲耐久、模块耐久与船壳完整度的适配参数。《阶段I弹丸与损伤技术替身配置.v1.json》仅为 `contract_fixture`，不构成正式 76 毫米弹药平衡值。

`gaotian.tactical-projectile-world/v1alpha1` 将弹丸世界时刻、性能目录精确引用/指纹与全部活动弹丸持久化。每发弹丸记录来源舰、来源武器实例、弹药、目标舰、所选甲板层、生成时刻、当前位置/速度、存活时长与已飞行路程。只有 I4 成功的单发原子开火事件能生成弹丸；发射原点为精确舀装中武器模块中心，初始世界速度叠加炮口速度与发射点局部舰体速度。

固定时间步内冻结当前 `Cd`，对二次阻力解析积分；每步重新计算跨音速阻力系数。同一步内将弹丸起终点分别转换到目标舰当时位姿的局部坐标，再与显式选定甲板层的规范多边形边求最早交点。目标命中点局部表面速度包含舰体平动和回转，甲弹公式使用弹丸对该表面的相对速度。

`ShipCombatState` 将舰艇实例与每条规范船壳边的当前/最大局部装甲耐久一同保存。局部耐久大于零时仍按原材质和原厚度计算防护，耐久归零后所需穿深才变为零。跳弹/未击穿只回写局部装甲和同边侧挂/附近顶挂外部模块；击穿则仅检索所选甲板层的内部占格，再回写被后效命中的模块耐久与全舰船壳完整度。

散布、火控瞄准、命中甲板层概率和跳弹随机滚点均由上层显式注入；I5 不在底层伪造命中率。I5 单独调用时仍支持常速平动与定角速度外推；I6 可以额外提供同一场景固定步的实际步首、步末位姿，由弹丸层在该步内确定性插值。制导、火灾蔓延、爆炸空间传播、人员伤亡、殉爆和损管队状态仍未实现。

### 5.5.3 统一战术场景时间线

统一编排接口为 `gaotian.tactical-scene-timeline/v1alpha1`，I7 后的顺序策略为 `gaotian.tactical-scene/boundary-lifecycle-fire-motion-impact/v2`，活动状态机器模式见《舰艇数据/模式/高天荒野舰艇统一战术场景状态契约.v1alpha1.schema.json》。`TacticalSceneState` 保存唯一的 `60Hz` 场景步号/战术时刻、弹丸世界，以及逐舰的机动状态、实例/局部装甲战损状态、生命周期、派生快照内容指纹和出航配置内容指纹；不可变船壳、舾装与出航对象继续作为外部精确绑定，不在战斗存档中复制第二份规则数据。

每个固定步按以下顺序原子编排：先结算步首边界到期的 I4 武器事件并由成功开火生成弹丸；再用步首运行时同时预计算各舰本步机动及 OverG/燃料变化；随后以实际步首—步末舰体轨迹推进 I5 弹丸并按事件时刻回写命中；最后结算步末边界武器事件。步末新生弹丸年龄为零，不会提前运动一个固定步。命中所在步已经预计算的舰艇运动不回滚，战损与 OverG 结果合并进同一实例，下一固定步开始前由既有 `RuntimeShipParameters` 链重新计算推力配平、功率、人员、升力和终止故障。

I4 武器事件若会生成战术弹丸，必须精确落在场景固定步边界；I6 对非网格时刻直接报错，不作四舍五入。每个成功单发开火事件必须匹配唯一的显式发射指令，指令继续注入目标舰、甲板层和局部发射方向；缺失、多余、重复或跨步指令都会使整步不提交。各舰武器时钟、机动步号、弹丸世界时刻与场景时刻必须同步，完整状态保存重载后应产生相同的后续事件与内容指纹。

I7 为每舰增加互相正交的物理状态与指挥权状态。物理状态为 `operational / falling / exited`；指挥权为 `scene_command / local_only / uncommanded`。船壳完整度归零、CIC模块耐久归零或当前升力不足会触发不可逆的 `falling`，立即撤销指挥权并清空未完成武器序列。CIC仅因关闭、断电或缺员暂时不可用时，舰体仍保持 `operational`，只转为 `uncommanded`；不得把“CIC失去工作能力”误报为“CIC已被摧毁”。遥控核心失联时，有实体人员且CIC仍可工作的舰转为 `local_only`，完全无人舰转为 `uncommanded`，两者都不会仅因失联而物理坠毁。

当前二维求解器不伪造垂直坠落秒数。`falling` 舰不再接受操纵输入，水平方向只保留既有速度、角速度和气动演化，直到上层在固定步边界以 `fell_below_scene` 明确确认已经坠出；单舰脱离使用 `distance_disengaged`，剧情或控制对象迁移使用 `scripted_transfer`。离场舰继续保存在战斗结果状态中但不再运动、开火或作为新目标；已经在途且以其为目标的弹丸以 `target_left_scene` 终止。

I8 在逐舰状态中加入稳定 `side_id` 与 `fleet_id`，并由 `TacticalEngagementState` 保存主动接战方、被动应战方、边界配置精确引用/指纹、当前 `active / disengaged / resolved` 状态、最近敌对舰距离、仍在边界内的敌对舰对数量和终止原因。首版自动边界只接受两个阵营，但同一阵营可有多支舰队；阵营或舰队绑定在活动场景中不得静默变化。

交战距离策略为 `gaotian.tactical-engagement/responding-ship-layer-pairwise-distance/v1`。对每一组“主动方可战舰—被动方可战舰”计算二维距离，并按该组被动方舰艇当前高度层查询边界；不使用主动方层级，也不把全场压成单一高度。只要任意一组敌对舰距离小于等于对应边界，场景保持 `active`；双方仍有可战舰但所有组合均超距时转为 `disengaged`；任一方已无 `operational` 舰时转为 `resolved`。`falling` 与 `exited` 舰不计入可战舰，但失去场景指挥权而物理上仍正常的舰仍留在交战空间。

当前《阶段I战术交战边界技术替身配置.v1.json》保存上层 `50000m`、云层/雨层 `25000m`，标记为 `prototype_unbalanced`；机器模式为 `gaotian.tactical-engagement-boundary/v1alpha1`。创建自动边界场景时必须已经有至少一组敌对舰在边界内；每个固定步完成机动、命中、生命周期与离场处理后重新评估。整体 `disengaged/resolved` 后冻结战术时间线并交回上层处理，不把正常脱离舰改写为逐舰坠毁或消失。

### 5.5.4 战术舰队角色与命令仲裁

I9 指挥状态接口为 `gaotian.tactical-fleet-command/v1alpha1`，策略为 `gaotian.tactical-fleet-command/one-direct-ship-and-rts-orders/v1`，机器模式见《舰艇数据/模式/高天荒野舰艇战术舰队指挥状态契约.v1alpha1.schema.json》。它独立绑定当前精确 `TacticalSceneState` 的内容指纹和步号，并保存玩家阵营、逐舰 `main_flagship / branch_flagship / ordinary_ship` 角色、编队锚点与相对位置、唯一冻结的直控舰、每舰至多一条当前命令，以及直控失效后的撤离状态。角色绑定必须恰好覆盖场景中的全部玩家舰艇；敌方单位仍由其独立 AI 输入，但不得借此向玩家舰艇注入第二条直接操纵通道。

常规战术模式必须选择且只能选择一艘主旗舰或分旗舰作为直控对象，普通舰不能直控；进入场景后没有改换直控对象的状态迁移。最终动员模式恰好相反：只接受普通舰角色，不保存直控对象，所有玩家舰艇均由命令仲裁。直控舰没有本帧输入时由既有底层零舵/零推力语义稳定航行；可以另存一条预设动作作为自动驾驶输入。玩家直接输入仍可请求 OverG，并继续受现有结构与有人舰 `12G` 安全锁约束；命令自动驾驶始终不主动启用 OverG。

首版 `TacticalShipOrder` 覆盖多段移动、对空巡逻、反舰巡逻、前往区域自由开火、指定目标攻击、原地稳定及搜救撤离。通用字段保存航点序列与当前索引、目标高度层、舰艏朝向、航速、指定目标舰、弹种、交战距离和目标类别优先级。移动与巡逻命令按航点生成机动输入；指定攻击会向目标靠近并在所定交战距离内制动，但 I9 不自动选择武器或生成开火事件。命令完成或失败后回到编队相对位置；无有效编队锚点时保持稳定航行。目标丢失与本舰失能已经使用稳定原因结束命令，航路受阻和弹药耗尽需等待对应系统提供事实状态后接入。

直控舰转为 `falling / exited` 或失去 `scene_command` 时，指挥状态进入不可逆的 `command_defeat_withdrawal`，保留原直控舰 id 而不切换，并把幸存玩家舰的普通命令改为搜救撤离。直控对象为分旗舰时只保存 `withdrawal_extra_loss_pending=true`，实际额外舰艇、物资和人员损失仍由战略层结算，不在战术合同中伪造概率。当前《阶段I舰队指挥技术替身配置.v1.json》的航点容差、满舵航向误差与速度误差响应均为 `prototype_unbalanced`，配置引用和内容指纹随指挥状态锁定。

### 5.6 船坞库存与战略工时

扩展接口为 `gaotian.shipyard-logistics/v1alpha1`，战略工时策略为 `gaotian.shipyard-work/confirmed-reserved-strategic-time/v1`。`ShipyardFacilityProfile` 保存精确版本、支持的建造/改装/维修/重新武装/补给、受损模块拆卸与归零残骸清理能力和工作吞吐；`ShipyardInventoryState` 保存逐材质整数单位、维修材料整数、逐模块原型整数件数、逐弹药整数单位与双精度燃料；`ShipyardWorkOrder` 保存完整报价、设施精确引用、确认时间、最后推进时间、剩余工作量及队列状态。

建造报价从当前规范船壳几何重新计算逐材质质量，并逐模块实例汇总库存模块；改装报价只允许同一精确船壳，比较来源与目标编译后模块实例，预留新增模块并把完好拆下模块列入完成产出；维修报价绑定当前实例内容指纹，以缺失船壳和非残骸模块的质量等效量生成维修材料需求；重新武装与燃料补给分别绑定目标弹药状态和目标燃料量。报价、订单和完成产物均检查设计/实例内容指纹，不能把资源用于已经变化的舰艇状态。

玩家在暂停战略时间的特殊界面确认订单时即预留库存；同一世界时间内订单保持 `queued`，只有返回战略地图后绝对战略时间增加，订单才消耗工作量并进入 `in_progress`、`completed_unsettled`。新舰完成后继续调用既有实例初始化链并嵌入设计状态；维修只恢复船壳及未归零模块，归零残骸保持占位；重新武装和燃料补给只修改实例当前状态。

改装、受损模块拆卸成功和残骸清理属于设计变更订单：必须先原子更新模块状态及当前舾装快照，订单进入 `artifact_applied_unsettled` 后才能把完好模块或废金属返库。精确未变模块保留当前耐久/模式，新增或更换模块采用原型完好初态；拆换前必须卸空相关弹药库和武器待发弹。受损模块不能被普通改装隐式移除，需由外部概率结算注入成功/失败；失败时实例与库存不变。耐久归零模块只能走残骸清理，废金属材质与整数产量由外部回收结算注入。

当前一千克取整材料单位、缺失质量等效维修材料、模块质量安装工作量和设施吞吐只属测试政策。涂装材料、模块内部物料表、安装耗材、工资和资金价格、受损模块拆卸概率、残骸清理/废金属产率仍明确未定；I3 只冻结外部结果注入和原子结算契约，不得把回归中的成功/失败或七单位废金属夹具解释为正式经济平衡。

正式战术动力学接口为 `gaotian.tactical-dynamics/runtime-adapter/v1alpha1`，固定步策略为 `gaotian.tactical-dynamics/semi-implicit-euler-60hz/v1`。它不接收调用者手填的整舰质量、惯量、推力或结构安全值，而由 `RuntimeShipParameters` 和对应派生快照取得当前质量/惯量、逐方向执行器聚合、结构安全值、乘员安全锁、气动缓存与 RCS 缓存。当前高度层决定环境；换层保存起始层、目标层和剩余时间，完成前继续使用起始层环境。相邻层耗时与三层介质参数均为可替换的 `prototype_unbalanced` 配置。外挂模块 RCS 未标定时，查询结果明确标为不完整并返回已知船壳下界。

## 六、规范化规则

读取 `HullBlueprint` 后执行：

1. 拒绝重复闭合端点、零长度边、自交、退化面积和非法半格坐标；
2. 将多边形统一为逆时针；
3. 旋转顶点序列，使字典序最小的坐标成为首顶点；
4. 通过无向端点对重建逐边装甲，使改变绕序不会错配装甲；
5. 按 `level`、甲板 `id` 和区域 `id` 规范排序；
6. 验证每条边的镜像边具有相同装甲材质、版本和厚度；
7. 输出排序后的确定性 JSON。

显示名称不参与几何或引用比较。浮点输入必须为有限数；长度、密度和强度等物理量不得使用字符串保存。

## 七、错误与警告

编译器错误使用稳定代码与可读消息分离。首切片至少包括：

- `schema.unsupported`；
- `resource.kind_mismatch`；
- `resource.reference_missing`；
- `hull.slice_unsupported`；
- `hull.polygon_degenerate`；
- `hull.polygon_self_intersection`；
- `hull.coordinate_off_half_grid`；
- `hull.symmetry_geometry`；
- `hull.symmetry_armor`；
- `hull.cic_origin_outside`；
- `hull.cic_origin_cell_incomplete`；
- `hull.deck_levels_non_contiguous`；
- `hull.base_deck_invalid`；
- `hull.regions_overlap_or_touch`；
- `hull.upper_deck_unsupported`；
- `coating.runtime_multiplier_missing`；
- `coating.unresolved_multiplier_present`；
- `coating.default_count`；
- `coating.not_runtime_usable`；
- `module.footprint_not_centered`；
- `module.half_cell_parity`；
- `module.power_category_missing`；
- `module.damage_control_power_category`；
- `module.default_operating_mode`；
- `module.activation_event_default_mode`；
- `module.damage_response_model`；
- `module.damage_response_endpoints`；
- `module.damage_response_function_set`；
- `module.damage_control_operating_policy`；
- `module.remote_core_operating_policy`；
- `module.departure_minimum_category`；
- `module.remote_core_host`；
- `module.unmanned_variant_invalid`；
- `outfit.hull_reference_mismatch`；
- `outfit.module_reference_missing`；
- `outfit.rotation_invalid`；
- `outfit.internal_cell_overlap`；
- `outfit.side_slot_overlap`；
- `outfit.clearance_conflict`；
- `outfit.host_slot_occupied`；
- `outfit.cic_origin`；
- `outfit.insufficient_lift`；
- `sortie.outfit_reference_mismatch`；
- `sortie.minimum_crew_shortfall`；
- `sortie.crew_capacity_exceeded`；
- `sortie.remote_core_instance_invalid`；
- `sortie.fuel_capacity_exceeded`；
- `sortie.storage_instance_missing`；
- `sortie.storage_not_cargo_hold`；
- `sortie.cargo_capacity_exceeded`；
- `sortie.insufficient_lift`；
- `instance.power_category_permutation`；
- `instance.height_layer`；
- `instance.crew_type_duplicate`；
- `instance.cargo_id_duplicate`；
- `instance.crew_capacity_exceeded`；
- `instance.fuel_capacity_exceeded`；
- `instance.storage_instance_missing`；
- `instance.storage_not_cargo_hold`；
- `instance.cargo_capacity_exceeded`；
- `instance.module_state_duplicate`；
- `runtime.derived_snapshot_hash_mismatch`；
- `runtime.sortie_hash_mismatch`；
- `runtime.module_state_set_mismatch`；
- `runtime.module_durability_exceeded`。

测试断言优先检查错误代码，不依赖可能调整的中文消息全文。

## 八、首个规范夹具

《舰艇数据/船壳蓝图夹具/标准155x20单层船壳.v1.json》是第一个 `canonical_blueprint_fixture`。它复用既有 `155×20m` 单层装甲钢、无基础装甲结构锚点，但其身份仍是 L1 船壳编译夹具，不是最小合法舰、舾装方案或教程舰。

它必须满足：

- 规范化往返文本稳定；
- 面积 `2650m²`；
- 长度 `155m`、宽度 `20m`；
- 内部骨架质量 `2,080,250kg`；
- 纵向结构安全值 `8G`；
- 完整内部格和单层露天顶挂格各 `75` 个；
- 五米侧挂步长 `64` 个；
- 改变顶点起点或绕序后派生结果不变。

测试与首轮编译报告见《高天荒野舰艇规范数据与船壳编译器测试》。

《舰艇数据/船壳蓝图夹具/标准155x20双层分离上层船壳.v1.json》是第二个
`canonical_blueprint_fixture`。它在装甲钢基底层上放置两块互相分离但整体轴对称的
铝合金上层区域，用于验证：

- 上层多区域按甲板整体进行几何与装甲镜像；
- 上层结构体积采用 `0.10+0.02m` 等效厚度；
- 支撑关系拒绝越出相邻下层的悬挑；
- 基底层被部分覆盖的格不再作为露天顶挂格；
- 改变甲板数组或同层区域数组顺序不改变规范指纹和派生结果。

气动方向缓存的关系与运行时公式测试见《高天荒野舰艇气动缓存测试.py》。其中平艏、
尖艏和尖艉测试体均先构造为 `HullBlueprint` 再调用同一个船壳编译器，测试不保存或
回填任何手工聚合阻力面积。

船壳 RCS 缓存的规范输入、方向关系、等价几何、材质独立性、涂料边界和运行时插值测试见
《高天荒野舰艇RCS缓存测试.py》。测试体全部由 `HullBlueprint` 编译产生；临时倍率只用于
验证线性缩放关系，不会写入正式涂料目录。

《舰艇数据/模块/测试夹具/最小模块目录.v1.json》是阶段 D/E 的 `contract_fixture`，只验证九类
最小模块的字段、单位、安装语义和关系。其质量、推力、功率与耐久不是正式内容数值。
规范化、偶数格中心、嵌入槽位、供电优先级、最低船员和稳定错误码测试见
《高天荒野舰艇模块原型契约测试.py》。

《舰艇数据/舾装方案夹具/标准155x20最小舾装.v1.json》绑定上述单层船壳和九类最小模块目录，保存十个真实模块实例。它覆盖 CIC 原点、内部模块、货仓、左右侧挂转向发动机、主机尾焰、遥控核心舱宿主槽、升力、发电和船员汇总；仍是 `contract_fixture/prototype_unbalanced`，不是正式最小合法舰。占格、旋转、跨层、净空、引用、质量惯量与执行器回归见《高天荒野舰艇舾装编译器测试.py》；中轴主机、非对称配平、单侧损失、物理残余副作用与派生快照指纹测试见《高天荒野舰艇执行器聚合与派生快照测试.py》。阶段 D 综合入口为《高天荒野舰艇阶段D综合回归.py》，确定性保存报告为《舰艇数据/报告/阶段D舾装编译接口.v1.json》。

《舰艇数据/模块/测试夹具/战斗系统模块目录.v1.json》保存 E5 四类扩展模块；《舰艇数据/舾装方案夹具/标准155x20战斗系统舾装.v1.json》把它们与基础模块共同安装在同一规范船壳上，《舰艇数据/出航配置夹具/标准155x20战斗系统出航.v1.json》提供不可变出航来源。炮塔实际同时占据内部格与顶挂格，搜索雷达只占顶挂格，弹药库与火控占据内部格。I1 已在不改动这些模块布局的前提下，把兼容弹药标识落实为实例库存、待发弹与武器动作状态；连续回归及确定性报告见《高天荒野舰艇阶段I弹药与武器动作测试.py》和《舰艇数据/报告/阶段I弹药与武器动作接口.v1.json》。

《舰艇数据/出航配置夹具/标准155x20载货出航.v1.json》精确绑定上述舾装方案，把 `50,000kg` 大宗货物绑定到实际货仓实例，并配置有人模式、标准船员和初始燃料。它用于验证货物位置惯量、人员/燃料质量排除、容量、最低船员、遥控核心、乘员安全锁和初始升力余量，不是正式出航平衡基准。

《舰艇数据/舰艇实例夹具/标准155x20完好实例.v1.json》由上述设计态和出航配置初始化，保存初始当前资源状态、全舰船壳满耐久、十个模块的满耐久状态与默认类别顺序；损管及未选中的遥控核心舱均默认待机。运行时回归会在不修改出航配置的情况下改变当前燃料、船员、货物、高度层和船壳耐久，并验证质量、惯量、安全锁、执行器、换层及 OverG 回写随之变化，同时验证按需激活、类别重排/关闭和关键最低岗位优先。供电、缺员、损毁、宿主连带失效和执行器重聚合测试见《高天荒野舰艇运行时参数编译器测试.py》；固定步机动、分层气动、方向 RCS、换层与 OverG 测试见《高天荒野舰艇战术机动求解器测试.py》；四类战斗模块的能力字段、安装、事件、P1～P3 供电、自动化和战损测试见《高天荒野舰艇战斗系统契约测试.py》。阶段 E E1～E5 综合入口为《高天荒野舰艇阶段E运行时回归.py》，冻结报告为《舰艇数据/报告/阶段E运行时参数接口.v1.json》。
