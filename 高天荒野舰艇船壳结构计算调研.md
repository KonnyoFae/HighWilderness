# 《高天荒野》舰艇船壳结构计算调研

## 一、目的与既定边界

本文研究如何从自由绘制的多层船壳、逐甲板结构材质和逐边基础装甲，近似计算：

- 船壳结构质量；
- 局部装甲耐久；
- 全舰船壳最大耐久；
- 完整船体的纵向、横向与偏航结构安全值；
- `OverG` 状态下的船壳耐久损失。

本文不要求玩家逐根布置龙骨、纵梁、横梁、舱壁和立柱，也不进行有限元求解。结构公式必须使用现有的二点五维船壳数据，并满足以下既定约束：

- 每层甲板层高固定为五米；
- 每层分别选择一种结构材质，同层多个合法多边形共用该材质；
- 每条船壳边独立选择基础装甲材质和厚度；
- 上层船壳必须完全受相邻下层支撑，不允许悬挑；
- 设计质量包括船壳和模块，当前质量另外加入货仓内大宗货物；
- 正常控制禁止超过结构安全 G，`OverG` 可以解除限制但持续削减全舰船壳耐久；
- 只要舰上存在任何人员，乘员安全锁就把主动机动硬性限制在 `12G` 以内；`OverG` 不解除该限制；
- 完全无人舰艇不受 `12G` 乘员上限限制，但不启用 `OverG` 时仍受结构限制；
- 局部装甲耐久与全舰船壳耐久分开；
- 船壳蓝图、舾装方案与舰艇实例使用各自缓存，战术模式不能逐帧重新分析全部多边形。

## 二、现实结构关系可以提供什么

### 2.1 总材料量不能代替结构强度

现实舰体常被视为整体船体梁。纵向弯曲强度不只取决于总材料量，还取决于有效结构在截面上的位置、截面惯性矩和截面模量。美国海军对受损舰艇的分析表明，同样数量的加强材料安装在不同位置，对恢复截面模量的效果可能差异很大；中部结构受损会同时降低有效截面和剩余强度。[美国海军《USS Houston War Damage Report》](https://www.history.navy.mil/research/library/online-reading-room/title-list-alphabetically/w/war-damage-reports/uss-houston-cl81-war-damage-report-no-53.html)，[美国海军《USS O'Brien War Damage Report》](https://www.history.navy.mil/research/library/online-reading-room/title-list-alphabetically/w/war-damage-reports/uss-obrien-dd415-war-damage-report-no-28.html)

因此，只使用“结构材料总体积×平均强度”会把中部狭窄、前后宽大的舰体误判为与连续舰体同样坚固，不适合自由船壳系统。

### 2.2 完整截面模量法也不能直接照搬

现实船体梁计算需要知道哪些甲板、外板、纵骨和加强件属于有效纵向构件，并依据确定的波浪弯矩或其他载荷计算应力。现有游戏船壳只保存每层面积代理和边装甲，没有内部构件方向、接头效率、局部屈曲和真实载荷路径。

多种材料共同工作的梁还需要使用弹性模量确定中性轴与弯曲刚度，或使用转换截面法。NASA 的复合梁资料也以各层弹性模量与截面惯性矩共同计算弯曲刚度；只给一个“强度系数”不足以复现完整复合梁应力。[NASA：Initial Dynamic Load Estimates During Configuration Design](https://ntrs.nasa.gov/api/citations/19880004239/downloads/19880004239.pdf)，[NASA：Composite Beam Neutral Axis and Bending Stiffness](https://ntrs.nasa.gov/api/citations/20080042332/downloads/20080042332.pdf)

因此，截面模量适合作为“结构瓶颈很重要”的物理依据，但没有必要为第一版增加弹性模量、中性轴、局部屈曲和完整弯矩求解。

### 2.3 安全 G 应对应允许载荷，而不是破坏载荷

航空航天结构区分正常使用的限制载荷与发生破坏的极限载荷，并使用安全系数覆盖载荷、材料和制造不确定性。NASA 的结构设计资料明确把限制载荷、屈服载荷和极限载荷分开。[NASA：Moving Aerospace Structural Design Practice to a Load and Resistance Factor Design](https://ntrs.nasa.gov/api/citations/20160007733/downloads/20160007733.pdf)，[NASA：Structures and Materials Technology](https://ntrs.nasa.gov/api/citations/19900009436/downloads/19900009436.pdf)

本项目可以把结构安全 G 对应为正常允许载荷：不启用 `OverG` 时控制系统不主动超过它。`OverG` 不是另一套静态上限，而是进入超限损伤区间。

## 三、候选近似方案比较

| 方案 | 尺寸和材料 | 多层混合材质 | 狭窄腰部 | 方向差异 | 舾装与货物 | 成本 | 结论 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 总结构体积×平均强度 | 是 | 可平均 | 否 | 否 | 只看总质量 | 最低 | 只用于耐久，不用于安全 G |
| 完整船体梁/转换截面 | 是 | 是 | 是 | 主要沿预设梁轴 | 可扩展 | 高，且需要不存在的数据 | 不采用 |
| 五米结构格最小割/流网络 | 是 | 是 | 是 | 可任意扩展 | 可扩展 | 较高，载荷路径解释困难 | 保留为以后扩展方案 |
| 纵向/横向材料加权切面 | 是 | 是 | 是 | 只计算前后轴与左右轴 | 可以增量重算 | 较低 | 推荐作为第一版安全 G 模型 |

推荐采用混合模型：

1. 质量和全舰最大耐久按结构材料等效体积线性累加；
2. 结构安全 G 分别使用纵向和横向材料加权切面容量，并除以切面需要承载的舰体质量；
3. 逐边基础装甲作为外部承载壳体，对切面容量提供有限贡献；
4. 不使用完整有限元、转换截面或应力分布，但保留截面瓶颈和材料位置的影响。

## 四、规范化几何与材料字段

### 4.1 逐层结构几何

甲板层 `l` 的全部合法船壳多边形面积之和为：

`A_l = Σ_k Area(P_l,k)`

结构质量读取精确多边形面积，而不是可用内部格数量。不完整边界格虽然不能安装模块，仍属于真实船壳面积。

每层的等效内部结构体积为：

`V_struct,l = t_deck × A_l + δ_l × t_joint × A_l`

其中：

- 基底层 `δ_l=0`，其他层 `δ_l=1`；
- `t_deck` 抽象甲板板材、纵横骨架、舱壁和基础连接；
- `t_joint` 抽象本层向下连接的立柱、支撑与加强结构；
- 上层不存在悬挑，因此本层向下支撑面积等于 `A_l`；
- `t_deck` 与 `t_joint` 是全局结构几何参数，不属于材料性能。

正式值为 `t_deck=0.100m`、`t_joint=0.020m`。现实大型钢船的普通壳板和主要板材通常只有约十余至二十余毫米，但一个五米高船体层同时具有多张水平与竖直板面、纵横骨架、舱壁和梁，因此这里采用按钢材总体积折算到平面面积上的等效厚度，而不是单块壳板的字面厚度。`155×20m` 基准轮廓精确面积为 `2650m²`；装甲钢基底层据此具有 `265m³` 内部结构体积和 `2,080.25t` 质量，同轮廓非基底层另有 `53m³`、`416.05t` 的层间连接结构。

定义本层用于载荷切面的等效承载厚度：

`t_eff,l = t_deck + δ_l × t_joint`

### 4.2 结构材质字段

每种甲板结构材质至少保存：

| 字段 | 符号 | 用途 |
| --- | --- | --- |
| 密度 | `ρ_struct` | 结构质量 |
| 允许结构应力或强度系数 | `σ_allow` | 方向切面承载能力与安全 G |
| 单位体积船壳耐久 | `E_hull` | 全舰船壳最大耐久加成 |
| 单位质量成本系数 | `K_cost` | 相对装甲钢的材料取得成本；材料费按质量计算 |
| 加工维护难度 | `K_work` | 同等效结构体积下的建造、拆换和完全维修工时倍率 |

`σ_allow` 可以使用游戏化的有效允许应力，而不必等于现实材料的屈服强度。材料安全系数可以预先包含在该字段中，也可以使用一个全局 `FS_struct`；两种方式只能选择一种，避免重复降低。

不建议第一版额外加入弹性模量。只有未来需要弹性变形、振动或完整转换截面时，它才成为必要字段。

### 4.3 逐边基础装甲字段

船壳边 `e` 已经保存：长度 `L_e`、固定高度 `H=5m`、厚度 `t_e`、装甲材质密度、结构强度参数和局部耐久参数。

边装甲体积与质量为：

`V_edge,e = L_e × H × t_e`

`m_edge,e = ρ_edge,e × V_edge,e`

共线、同层、同材质、同厚度的连续边应先合并为规范化边组。拆分或合并同一条直边不得改变质量、局部耐久、全舰耐久或结构强度。

## 五、质量与全舰船壳耐久

### 5.1 内部骨架质量

`m_struct,l = ρ_struct,l × V_struct,l`

`m_internal = Σ_l m_struct,l`

船壳设计质量中的船壳部分为：

`m_hull = m_internal + Σ_e m_edge,e`

舾装模块质量在舾装方案中另行加入，不重复计入船壳质量。

### 5.2 局部装甲耐久

已敲定的线性公式为：

`H_edge,max = K_local × L_edge × 5m × t_edge × E_edge`

线性使用边长、厚度和材料参数能够保证规范化边被拆分后，总局部耐久不变。

### 5.3 全舰最大耐久

推荐让内部骨架和外部承载壳体都对全舰最大耐久作出贡献：

`H_internal = K_H × Σ_l(V_struct,l × E_hull,l)`

`H_shell = K_H × K_shell,H × Σ_e(V_edge,e × E_shell,e)`

`H_hull,max = H_internal + H_shell`

其中 `K_shell,H` 用于控制外部装甲已经提供局部防护后，对全舰耐久的第二重贡献，避免重甲材料在局部耐久与全舰耐久上获得未经控制的双倍收益。

建议保持总体线性，不为大型舰艇使用隐藏的舰种倍率。尺寸增加后，耐久随有效材料量增长，武器后效与命中面积负责形成实际生存性差异。

## 六、纵向与横向有效切面

### 6.1 切面定义

结构负荷只建立两组平动切面：

- **纵向通道：** 前进与后退共用，受力轴为舰体 `Y` 轴，切面为 `y=s`；
- **横向通道：** 向左与向右共用，受力轴为舰体 `X` 轴，切面为 `x=s`。

前进与后退、向左与向右分别使用相同结构容量；具体发动机在正反方向的推力差异仍由推进系统处理。系统不再为任意方位生成结构方向表。

对载荷通道 `c∈{long,lat}` 的受力轴单位向量 `e_c`，建立垂直切线：

`r · e_c = s`

沿舰体在该方向上的投影范围，以五米为步长在每个区间中心采样 `s_j`。使用区间中心而不是直接使用多边形顶点，避免切线恰好经过尖角时得到虚假的零宽度。此外，每个载荷通道都必须加入一条令当前质量分布在切面两侧各占一半的质量平衡切面；五米宽度较小的舰体不能只依赖常规区间中点。

对于甲板层 `l`，计算切线与该层全部船壳多边形相交所得线段的总长度：

`W_l(s_j,c) = Σ length(P_l,k ∩ cut_j)`

该层内部结构穿过切面的等效承载面积为：

`A_cut,l = t_eff,l × W_l(s_j,c)`

内部骨架的允许承载力代理为：

`C_internal(s_j,c) = Σ_l[σ_allow,l × A_cut,l]`

### 6.2 基础装甲壳体贡献

逐边基础装甲不仅是局部防护，也属于外部承载壳体。对于与切面相交的规范化边组 `e`，令边切向单位向量为 `u_e`，其沿受力方向的有效程度为：

`q_e = |u_e · e_c|`

边装甲对该切面的承载面积代理为：

`A_cut,edge = 5m × t_e × q_e`

壳体贡献为：

`C_shell(s_j,c) = K_shell,S × Σ_e[σ_allow,e × A_cut,edge]`

边与受力方向平行时最能跨越切面传递载荷；边与切面平行时不跨越切面，其贡献趋近零。`K_shell,S` 是外部装甲参与整体承载的全局效率系数，用于吸收接头、局部屈曲和并非所有装甲都作为主承载构件工作的简化。

总切面容量为：

`C_cut(s_j,c) = C_internal + C_shell`

这不是正式的屈服载荷预测，而是单位为力、具有正确材料与几何单调关系的游戏代理量。

### 6.3 切面采样稳定化

纵向和横向两组切面都使用以下规则：

- 采样点位于五米区间中心；
- 额外评估一条当前质量分布的质量平衡切面，使 `M_-=M_+`；其结构容量可以在低频重算时精确求取，也可以从相邻的蓝图缓存切面插值得到；
- 忽略切面一侧质量低于全舰质量固定比例的端部样本，防止尖艏最后一个极小切面支配全舰；
- 对连续三个切面容量使用中值滤波，再寻找结构瓶颈；
- 一个持续至少五米的真实狭窄腰部仍然必须降低结构强度；
- 只沿对应坐标轴采样，不计算斜向切面或角度插值。

## 七、从切面容量得到结构安全 G

### 7.1 质量分布

安全 G 不能只除以全舰总质量，否则无法判断狭窄连接两侧实际需要带动多少质量。舾装方案保存时，把质量分配到舰体坐标：

- 内部结构质量按各层多边形面积均匀分布；
- 边装甲质量按边长均匀分布；
- 模块质量按实际占用格均匀分布；
- 货仓内大宗货物在舰艇实例中集中于货仓锚点；
- 人员、燃料、已装填弹药与已装载导弹继续不计质量；
- 损毁但尚未清除的模块残骸继续计质量。

对切面 `j`，分别计算两侧质量 `M_-` 与 `M_+`。推荐的有效传递质量为：

`M_transfer(s_j,c) = min(M_-, M_+)`

它表示在自动配重、推力配平并把合力视为通过 CIC 的简化下，切面至少需要带动较小一侧的舰体质量。该选择是游戏载荷代理，不声称复现每台发动机到每个构件的真实载荷路径。

### 7.2 单切面允许加速度

`a_safe,j(c) = C_cut(s_j,c) / [FS_struct × M_transfer(s_j,c)]`

该载荷通道的结构安全加速度为全部有效切面中的最小值：

`a_safe,long = min_j a_safe,j(long)`

`a_safe,lat = min_j a_safe,j(lat)`

`G_safe,long = a_safe,long / g_ref`

`G_safe,lat = a_safe,lat / g_ref`

该公式自然产生以下结果：

- 同材料、同尺度下，狭窄腰部降低安全 G；
- 细长船体需要通过切面带动更多远端质量，安全 G 低于短粗船体；
- 增加同材质甲板会同时增加承载面积和质量，安全 G 不会无条件倍增；
- 使用更高比强度的结构材质可以提高安全 G；
- 船壳相同而重型舾装或大宗货物增加时，安全 G 下降；
- 货物位置变化可能改变具体瓶颈切面；
- 前后机动共用纵向安全 G，左右机动共用横向安全 G。

### 7.3 为什么不直接使用截面模量

材料加权切面容量本质上近似“沿纵向或横向传递惯性载荷”的能力。它不计算中性轴和弯曲应力，因此不奖励材料远离中性轴的真实梁效应。

这是有意的第一版取舍：当前编辑器没有纵骨方向、连接刚度与弯矩载荷，硬套截面模量会制造无法验证的精确结果。纵向与横向载荷切面仍能保留本项目最需要的差异——材料比强度、层数、舰体长度、局部宽度、狭窄连接、舾装质量和货物位置。

若测试发现多层深船体的强度收益不足，可以在不改变主公式的情况下加入一个受限的层深修正，但不应直接升级为完整船体梁求解。

## 八、平动、偏航与结构载荷比

### 8.1 前后与左右平动

将 CIC 处实际平动加速度转换到舰体坐标，得到横向分量 `a_x` 与纵向分量 `a_y`。前后和左右分别使用对应通道；同时存在纵向与横向负荷时采用椭圆交互式：

`R_trans = sqrt[(a_y/a_safe,long)² + (a_x/a_safe,lat)²]`

因此纯前后、纯左右机动分别读取一个安全值；斜向加速只组合两个分量，不生成新的斜向结构切面。

### 8.2 偏航产生的局部加速度

偏航角加速度 `α` 和角速度 `ω` 会使远离 CIC 的船体具有附加切向和向心加速度。对舰体代表点 `r_k`：

`a_local,k = a_CIC + α × r_k + ω × (ω × r_k)`

二维实现中，第二项为与 `r_k` 垂直的切向加速度，第三项为指向 CIC 的 `-ω²r_k`。

代表点不需要包含每个多边形内部点。船壳蓝图可以在保存时生成有限的外轮廓极值点和最长半径点；舾装方案再加入重型模块与货仓锚点。把每个代表点的局部加速度转换为舰体纵向和横向分量 `a_y,k`、`a_x,k`：

`R_k = sqrt[(a_y,k/a_safe,long)² + (a_x,k/a_safe,lat)²]`

全舰结构载荷比为：

`R_struct = max(R_trans, max_k R_k)`

这一方式把偏航转向作为第三类负荷处理：角加速度产生切向负荷，角速度产生向心负荷；二者最后仍投影到已经计算好的纵向与横向承载通道，不需要第三套任意角度切面。设计界面可以分别在 `ω=0` 时求解安全偏航角加速度，在 `α=0` 时求解安全偏航角速度。

### 8.3 正常限制

未开启 `OverG` 时，舰艇控制器预测下一固定物理步的 `R_struct`，并按比例限制主动发动机输出，使自身指令不把 `R_struct` 推到一以上。只要舰上存在任何人员，控制器还必须同时执行不可由 `OverG` 解除的 `12G` 乘员安全锁。外部爆炸、碰撞或上一时刻已经存在的角速度仍可能造成实际超限；结构损伤应读取实际载荷，而不是只读取 `OverG` 开关。

## 九、OverG 损伤

定义：

`R = R_struct`

当 `R≤1` 时不产生结构过载损伤。当 `R>1` 时，推荐使用平方增长的归一化损伤率：

`dH_hull/dt = -(H_hull,max / T_ref) × [max(0,R-1)/(R_ref-1)]²`

其中：

- `R_ref` 是标定参考超载比；
- `T_ref` 表示舰艇在持续保持 `R_ref` 时，从完整船壳耐久下降到零的参考时间；
- 平方曲线允许轻微超限维持较长时间，但会让严重超限迅速变得危险；
- `R_ref`、`T_ref` 属于全局平衡参数，不按舰种填写。

`OverG` 开启时，控制器不再把主动指令限制在 `R=1`；损伤公式本身始终根据实际 `R` 工作。若舰上存在任何人员，主动机动依然不能超过 `12G`。完全无人舰艇不受该乘员上限限制，可以在推进能力允许的范围内继续提高过载，同时承受对应结构损伤。船壳耐久可以被持续削减到零并导致结构崩溃，不设置最低保护。

第一版不让当前船壳耐久反向降低 `G_safe`，否则 OverG 会形成难以控制的正反馈：耐久下降、允许 G 再下降、相同动作造成更高超载比。船壳耐久作为结构失效倒计时，安全 G 仍由当前几何、材料和质量决定。以后若需要表现战损结构衰退，可以在耐久阈值处使用离散修正，而不是每帧连续反馈。

## 十、缓存与更新

### 10.1 船壳蓝图

保存：

- 各层 `A_l`、`V_struct,l`、结构质量与耐久贡献；
- 规范化边组的质量、耐久和壳体强度贡献；
- 纵向和横向五米切片上的 `C_cut(s_j,long)` 与 `C_cut(s_j,lat)`；
- 用于偏航负荷的结构代表点。

不保存一度、五度或其他任意角度的结构方向表。

### 10.2 舾装方案

加入模块质量分布，确定纵向与横向的质量平衡切面，并计算常规切面与质量平衡切面两侧设计质量、`G_safe,long`、`G_safe,lat`，以及设计状态下的安全偏航角加速度和安全偏航角速度。

### 10.3 舰艇实例

大宗货物数量变化、模块被实际拆除或替换时更新质量分布、两个质量平衡切面及纵向、横向与偏航安全值。普通耐久下降、模块受损或模块成为仍未清除的残骸时不更新质量。

结构切面只在这些低频事件中重算，战术模式逐帧只进行纵横分量组合、代表点局部加速度和 `R_struct` 计算。

## 十一、标准测试

正式确认公式前至少完成以下测试：

1. **拆边不变性：** 同一直边拆成多段后，质量、两类耐久和安全 G 不变；
2. **单层缩放：** 相似船体等比例放大后，质量按面积与周长对应项增长，安全 G 呈合理尺度效应；
3. **同形加层：** 增加同材质同形甲板后，质量、最大耐久和切面容量增长，安全 G 不会无条件成倍提高；
4. **混合材料：** 把某一层替换为轻而弱或重而强的材料时，质量、耐久和安全 G 分别按对应字段变化；
5. **狭窄腰部：** 面积与质量接近的情况下，狭窄连接船体的安全 G 低于连续船体；
6. **载荷通道：** 细长船体纵向与横向安全 G 可以不同；前后读取同一纵向值，左右读取同一横向值；斜向平动按纵横分量的椭圆交互式连续组合；
7. **尖角稳定：** 切面经过多边形顶点时不产生单点零容量；
8. **重型模块：** 在船艏或船艉安装重型模块后，相应切面安全 G 降低；
9. **货物位置：** 相同货物在 CIC 附近与远端货仓时，瓶颈切面和安全 G 可以不同；
10. **偏航半径：** 相同角加速度下，长舰的端部结构载荷比高于短舰；
11. **角速度：** 无推力但保持高速自旋时仍产生向心结构载荷；
12. **OverG 曲线：** `R≤1` 无损伤，轻微超限可维持，严重超限迅速消耗船壳耐久；
13. **乘员安全锁：** 任意有人状态下，无论是否启用 OverG，主动机动都不能超过 `12G`；完全无人状态下不读取该上限；
14. **窄舰采样：** 十米宽舰体加入质量平衡切面后，不得因横向只有两个五米区间中点而明显高估横向安全值；
15. **时间步稳定：** 固定物理步缩小时，OverG 累计损伤和限制结果保持在允许误差内。

## 十二、已确认结论

正式第一版推荐：

- 逐层材料体积线性计算质量；
- 内部骨架与外部壳体线性生成全舰最大耐久；
- 局部装甲继续使用已经敲定的独立线性耐久；
- 纵向与横向切面容量分别除以切面两侧较小质量，生成前后安全 G 与左右安全 G；
- 偏航通过代表点局部加速度进入统一载荷比；
- 正常控制限制 `R_struct≤1`，OverG 使用平方曲线持续削减当前船壳耐久；
- 任意有人状态始终受 `12G` 乘员安全锁限制，完全无人状态不受该上限限制；
- 第一版不让当前船壳耐久反向降低安全 G。

已经确认：

1. 本层结构材质负责本层向下连接的层间支撑；
2. 外部基础装甲保留对全舰最大耐久和切面强度的贡献，并分别使用效率系数；
3. 有效传递质量采用 `min(M_-,M_+)`；
4. 结构负荷只计算前后、左右和偏航转向，不建立任意角度方向表；
5. 第一版当前船壳耐久不反向降低安全 G；
6. `OverG` 不解除有人舰艇的 `12G` 乘员安全锁，完全无人舰艇没有乘员过载上限。

至此公式接口已经全部确认，可以进入原型测试；测试通过后再写成最终定案并开始材料参数标定。

现实舰艇尺度测试与第二轮变体测试已经完成。结果确认尺度效应、长宽比、叠层、狭腰、混合材质、基础装甲和货物位置均按预期进入公式。普通结构材质正式命名为“装甲钢”；密度采用现实钢材基准 `7850kg/m³`，强度、耐久、成本和加工维护难度系数以 `1.000` 为相对基准。`155×20m` 单层、空载、无基础装甲、全部使用装甲钢结构的基准船壳，纵向结构安全值敲定为 `8G`。对应比强度尺度为 `K_struct,a=5197.5245m²/s²`，装甲钢的游戏化有效允许应力为约 `40.801MPa`。详细数据分别见《高天荒野舰艇船壳结构公式现实尺度原型测试》和《高天荒野舰艇船壳结构公式第二轮变体测试》；两份可复现程序分别为《高天荒野舰艇船壳结构公式原型测试.py》和《高天荒野舰艇船壳结构公式变体测试.py》。
