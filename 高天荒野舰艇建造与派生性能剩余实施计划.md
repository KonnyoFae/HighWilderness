# 《高天荒野》舰艇建造与派生性能剩余实施计划

## 一、修订原因

现有船壳结构、气动、雷达反射截面积、甲弹对抗和机动闭环已经分别建立公式原型，但当前机动测试舰仍由测试脚本直接填写质量、转动惯量、推力、结构安全值和阻力面积。它只能验证“给定派生参数后，运动求解器是否正确”，不能验证以下完整链路：

`玩家绘制船壳 → 生成安装空间 → 安装具体模块 → 计算质量与质量分布 → 计算升力、推力、力矩、惯量、结构和气动 → 生成战术运行参数`

因此，任何声称使用“实际模块布局”的测试舰，都必须以规范船壳蓝图和舾装方案作为唯一源数据。没有船壳与舾装派生链时，不得用手填聚合参数代替真实布局，也不得据此正式标定 `K_turn`、舵盘响应、模块推力、教程舰速度或其他内容参数。

玩家可见的船壳建造器和舾装建造器不必成为第一项实现，但它们背后的规范数据结构、校验器和无界面编译器必须先存在。未来编辑器只是这套数据的可视化编辑前端，不能另建一套几何、占格或派生性能逻辑。

## 二、测试层级重新划分

### L0：公式与求解器单元测试——已经存在

直接填写质量、惯量、推力或阻力面积的脚本属于 L0 测试。它们负责验证：

- 公式量纲和单调关系；
- 固定步稳定性；
- 舵盘、制动、安全锁与 OverG 等控制语义；
- 极端输入和边界条件。

L0 夹具不是舰艇蓝图，不要求能够由编辑器打开，不得用于正式平衡标定。现有《高天荒野舰艇机动性能公式原型测试.py》保留在这一层。

### L1：几何与模块组件测试——部分已经存在

使用简单多边形、装甲边、五米格模块和单独执行器验证：

- 船壳拓扑、支撑、严格整格和安装空间生成；
- 船壳质量、结构强度、局部装甲、惯量、气动与 RCS；
- 模块旋转、跨层占格、顶挂、侧挂、净空和射界；
- 单台发动机的推力向量、力臂、力矩和受损输出。

这一层可以使用人为构造的最小几何，但输入必须已经符合正式数据对象的字段口径。

### L2：规范蓝图集成测试——完成

测试输入必须是完整的：

`材料与模块原型 → 船壳蓝图 → 舾装方案 → 出航配置`

测试运行时由同一派生链生成舰艇设计结果和战术运行参数。测试文件不得保存一套能够覆盖计算结果的手填质量、惯量、推力或结构安全值。当前三条阶段 F `contract_fixture` 已贯通材料、船壳、舾装、出航初态、舰艇当前状态、`RuntimeShipParameters` 和正式战术动力学适配器，并覆盖双向转向、多层非对称战斗舾装、标准船员、零船员遥控、自动损管限制与 OverG。发动机位置、模块质量、结构材质、甲板几何和货仓载荷的改动均由同一编译链自动反映到相关派生结果，L2 已完成。

### L3：编辑器往返测试——完成

当前引擎无关编辑器领域层已经能够读取 L2 测试舰，未作修改直接保存后满足：

- 船壳几何语义一致；
- 甲板、材质、逐边装甲和 CIC 一致；
- 模块原型、锚点、旋转、占格和武器组一致；
- 重新派生的全部性能在规定数值误差内一致。

不强求序列化文件逐字节相同，但规范化数据和派生结果必须相同。

### L4：教程舰与正式平衡测试——最后进行

只有 L2 与 L3 均通过后，教程舰才能成为第一条正式平衡基准舰。正式模块质量、推力、功率、人员、成本、装甲、火炮穿深、气动系数、RCS 系数和操控响应在这一层联合标定。

## 三、唯一数据源与禁止事项

### 3.1 唯一源数据

正式链路继续使用已经确定的数据关系：

`材料定义与模块原型 → 船壳蓝图 → 舾装方案 → DerivedShipSnapshot`

`DerivedShipSnapshot + 出航配置 → 含当前资源状态的舰艇实例初态`

`DerivedShipSnapshot + 出航配置 + 舰艇实例状态 → RuntimeShipParameters`

其中材料/模块、船壳、舾装和出航配置是内容/部署输入；`SortieConfiguration` 在离港后保持为不可变初态与来源记录。`ShipInstanceSnapshot` 保存战斗中会变化的高度层、燃料、船员、大宗货物、当前全舰船壳耐久、逐模块状态与供电策略。派生性能快照和舰艇实例都不能手工填写聚合性能覆盖计算结果。

每种数据对象都必须具有：

- 稳定标识；
- 数据版本或模式版本；
- 明确单位；
- 对其他对象的精确版本引用；
- 规范化顺序与确定性输出规则；
- 旧版本迁移或明确拒绝读取的处理方式。

### 3.2 禁止事项

在 L2 完成前禁止：

- 把 L0 聚合夹具称为最小合法舰、常规有人舰或无人旗舰；
- 用手填惯量代替模块位置与货仓位置派生；
- 用汇总推力代替具体发动机模块、方向、位置和当前可用状态派生；
- 用假定正向阻力面积代替船壳蓝图气动缓存；
- 正式标定 `K_turn`、舵盘满偏目标角速度或响应时间；
- 用尚不存在的教程舰布局联合标定装甲、火炮、RCS 或气动正式参数。

## 四、剩余实施阶段与依赖

### 阶段 A：冻结现有公式原型边界

**状态：基本完成。**

保留现有结构、惯量、气动、RCS、甲弹与机动原型，补充统一标记：

- `synthetic_fixture`：聚合参数或简单几何夹具；
- `canonical_blueprint_fixture`：正式数据对象组成的蓝图夹具；
- `balance_reference`：通过编辑器往返后的正式平衡基准。

验收条件：任何测试报告都能明确说明自己属于哪个层级，不再把公式夹具与舰艇设计混为一谈。

### 阶段 B：规范数据契约

**状态：基础契约族、E5 四类战斗模块、运行时派生接口及 I1～I9 的弹药、时间线、弹丸、场景、生命周期、交战边界与舰队指挥合同已经完成；I10a/I10b 已补充导引头配置、可持久化状态和固定步制导闭环，I11a 已补充显式点燃、持久火灾与损管队闭环；二次爆炸、人员伤亡与医疗仍在后续扩展。**

已经落地 `gaotian.ship/v1alpha1` 的共同资源信封、精确版本引用、确定性 JSON、
结构/基础装甲材质目录、船体涂料目录、最小模块原型目录、`HullBlueprint`、`OutfitPlan`、`SortieConfiguration` 和 `ShipInstanceSnapshot`。当前可以读取、规范化、保存并再次读取
`155×20m` 规范船壳夹具，且得到相同语义与相同 SHA-256 指纹。运行时契约会拒绝
未知字段、非法单位值、非法资源标识和失效材质引用。

先定义以下对象的机器可读结构，不制作玩家界面：

1. `MaterialDefinition`；
2. `HullCoatingDefinition`；
3. `ModulePrototype`；
4. `HullBlueprint`；
5. `OutfitPlan`；
6. `SortieConfiguration`；
7. `DerivedShipSnapshot`；
8. `ShipInstanceSnapshot`。

这一阶段同时统一坐标、角度、格坐标、边标识、甲板标识、模块锚点、旋转、引用版本和单位。是否使用 JSON、其他文本格式或引擎原生资源，可在实施时选择；无论外部格式为何，领域字段和语义必须保持唯一。

验收条件：可以建立一个不含正式数值的最小示例文件，通过读取、规范化、保存和再次读取而保持语义一致。

### 阶段 C：无界面船壳编译器

**依赖：阶段 B。**

**状态：完成。首版接口已经冻结为 `gaotian.hull-compiler/v1alpha1`，确定性综合报告已保存并由回归脚本校验。**

输入 `HullBlueprint`，依次执行：

1. 多边形闭合、自交、孔洞和连通性校验；
2. 基底层唯一连续区域和逐层支撑校验；
3. 强制轴对称与逐边装甲镜像校验；
4. 严格完整覆盖的五米内部格生成；
5. 局部露天甲板格和单边侧挂步长生成；
6. 船壳结构质量、基础装甲质量和船壳耐久计算；
7. 纵横结构安全值和偏航代表点生成；
8. 船壳部分转动惯量计算；
9. 气动与船壳 RCS 方向缓存生成；
10. 规范化派生结果和错误列表输出。

验收条件：现有简单结构、气动和 RCS 测试体能够改由 `HullBlueprint` 输入产生，拆边、顶点顺序和等价几何规范化后得到一致结果。

当前已经完成：

- 简单多边形退化、自交、半格坐标、CIC 原点和强制轴对称校验；
- 顶点绕序与起点规范化，并保持逐边装甲正确绑定；
- 单层凹形船壳的严格完整覆盖内部格；
- 单层露天顶挂格和每边独立的五米侧挂步长；
- 唯一基底层、连续层级、上层多个分离区域和逐层完全支撑；
- 按甲板全部区域而不是单一区域执行的强制几何与逐边装甲轴对称；
- 根据相邻上层正面积投影生成的多层局部露天顶挂格；
- 非基底层 `0.02m` 层间支撑质量，以及多层混合材质结构切面和惯量求和；
- 船壳结构/装甲质量、耐久代理量、绕 CIC 惯量和纵横/偏航结构安全值；
- 三百六十方向的投影面积、迎风/背风钝度、流向长度、波阻面积变化指标和湿表面积缓存；
- 由外部环境与待标定系数驱动的运行时形状阻力、摩擦阻力、波阻和动压结算函数；
- 普通涂料倍率锁定为 `1.0`、隐身涂料倍率保持未定且不可装备的可执行涂料目录；
- 三百六十方向的多层船壳镜面、投影底项、凹角与普通涂料基准 RCS 缓存；
- 同层共线边合并、多区域遮挡、方向插值、材质/装甲厚度独立性和涂料线性缩放测试；
- 现有 RCS 标准关系测试体全部迁移为 `HullBlueprint` 输入；
- `155×20m` 单层与双层基准舰确定性往返、几何修改联动和非法输入回归测试；
- 阶段 C 三套回归统一入口、接口版本和可逐字段复验的保存报告。

底层船壳与舾装编译器继续采用首个硬错误即停止的简单契约；阶段 H 编辑器领域层已经补充彼此独立的编辑态结构预检，并把多个预检错误与正式编译错误合并显示，不改变底层编译器的权威裁决。

### 阶段 D：最小模块表与无界面舾装编译器

**依赖：阶段 B、阶段 C。**

**状态：完成。`OutfitPlan`、九类最小测试目录、无界面舾装编译、执行器聚合与设计态 `DerivedShipSnapshot` 均已通过确定性回归。**

先只建立打通核心链路所需的模块原型：

- CIC；
- 灵烷贮槽；
- 前后左右主发动机；
- 侧挂转向发动机；
- 发电设备；
- 最低限度损管设备；
- 人员舱或最低船员容量来源；
- 遥控核心舱测试件。
- 货仓测试件（阶段 E 载荷链所需）。

这些模块的首轮数值只用于接口与相对关系测试，仍不是正式平衡值。

模块契约首切片已经实现：

- 五米格轮廓与半格整数锚点偏移，可表达偶数格宽模块；
- 内部、顶挂、侧挂、跨层、净空、尾焰净空、直角旋转和甲板规则；
- CIC 固定原点与 `cic_internal` 嵌入槽位；
- 质量、耐久、待标定外部 RCS、升力、容量、推力、发电与负载字段；
- 稳定供电类别、默认 P0～P3 顺序、分类型最低/标准船员、自动化子功能和无人改进版接口；
- 九类模块 `contract_fixture` 的确定性往返和负面回归。

舾装编译器的责任边界为：

1. 原型引用、甲板限制、锚点和直角旋转；
2. 内部、顶挂、侧挂和复合模块占用；
3. 跨层形状、外部净空和主发动机尾焰净空；
4. 模块质量与惯量贡献；
5. 升力、发电、负载、最低船员和标准船员；
6. 各发动机当前可用推力、作用方向和绕 CIC 力矩；
7. 设计硬错误、警告和解释字段；
8. `DerivedShipSnapshot` 输出。

当前已经完成第 1～8 项的设计态部分：

- `grid/side/hosted` 三类实例放置和确定性规范化；
- 内部、顶挂、跨层、侧挂、嵌入槽位、本体重叠、外部净空和尾焰净空校验；
- 设计质量、模块位置惯量、升力余量、发电/稳定类别负载、最低/标准船员和人员容量；
- 主发动机与转向发动机逐实例的作用点、方向、推力、响应、耗油和绕 CIC 力矩；
- 中轴主机独立输出、离轴两侧等总推力配平、真实残余偏航和转向机残余平动力；
- 四个平动方向与顺/逆时针回转方向的最终设计态执行器聚合；
- 保留船壳、舾装、模块目录和涂料目录精确引用及指纹的 `DerivedShipSnapshot`；
- 绑定 `155×20m` 船壳的十实例规范舾装夹具，以及多层、跨层、顶挂和全部主要负面路径回归；
- 非对称推力、单侧损失、中轴主机、横移分组和转向残余合力回归；
- 接口 `gaotian.outfit-compiler/v1alpha1` 与阶段 D 确定性保存报告。

设计态快照同时保留逐实例执行器与聚合结果：前者用于战损后重算，后者作为完好设计的解释与能力上限。静态设计质量不包含大宗货物；出航初态由 `SortieConfiguration` 保存，战损、实时装载、当前燃料、当前船员和供电分配由 `ShipInstanceSnapshot` 与运行时适配处理。执行器策略冻结为 `gaotian.actuator-aggregation/physical-residuals-v1`。

### 阶段 E：运行时状态、求解器适配与换层接口

**依赖：阶段 D。**

**状态：完成。E1～E5 已全部落地：当前状态边界、设备运行策略、逐功能战损响应、机动/气动/RCS 与换层适配，以及最小战斗系统契约均已进入确定性回归。安全供电解锁资格明确留待成长系统接入，不再阻塞阶段 E。**

建立唯一转换：

`DerivedShipSnapshot + SortieConfiguration + ShipInstanceSnapshot → RuntimeShipParameters`

其中 `SortieConfiguration` 保存离港时的不可变初态；`ShipInstanceSnapshot` 保存当前高度层、燃料、船员、大宗货物、当前全舰船壳耐久、逐模块状态与供电策略，但不能保存或覆盖整舰聚合性能。完好实例由前两项自动初始化，后续资源消耗、伤亡、换层和 OverG 损伤只改变实例当前状态，不改变或重绑原始出航配置指纹。

正式机动求解器不得接受由测试随意填写的整舰聚合对象。只有专门标记为 L0 的单元测试可以绕过这一转换。生产接口 `gaotian.tactical-dynamics/runtime-adapter/v1alpha1` 只接受可追溯的 `RuntimeShipParameters`、派生快照和实例快照；早期 L0 积分器继续作为隔离的公式单元测试，不承担生产接线。

高度层环境和换层状态已经接入运行时。第一版采用可配置的相邻层基础耗时之和；上升必须有正升力余量，切换完成前仍使用起始层环境。当前环境值与相邻层 `30s` 基础耗时均标记为 `prototype_unbalanced`，留待教程舰共同标定。

验收条件：运行时质量、惯量、结构安全值、升力、执行器能力、阻力缓存和 RCS 缓存都能追溯到具体源对象及其版本。

当前已经完成：

- `SortieConfiguration` 的机器可读结构、规范化、JSON Schema 与精确舾装引用；
- 逐项大宗货物必须绑定实际货仓实例，并按货仓位置增加 `m·r²` 惯量；
- 设计质量只含船壳与模块，当前质量只额外加入大宗货物；燃料、人员、已装弹药和导弹明确排除；
- 当前升力余量、逐仓容量、燃料容量、人员舱容量和有人最低船员硬校验；
- 遥控核心模式的实际模块引用，以及由是否有人员决定的 `12G` 安全锁状态；
- 接口 `gaotian.sortie-compiler/v1alpha1`、正负面回归与阶段 E 首切片确定性保存报告。
- `ShipInstanceSnapshot` 的逐模块当前耐久、`off/standby/active` 状态、宿主连带失效和确定性往返；
- `ShipInstanceSnapshot.operational_state` 保存当前高度层、燃料、船员和逐货仓大宗货物；修改这些状态不会使不可变的出航配置来源失效；
- `damage_control / weapons_and_active_defense / fire_control / sensors` 四个稳定类别按默认 P0～P3 顺序排列，可重排、整类跳闸、整类关闭；安全供电模式按 CIC 二维直线距离逐台供电；
- 模块原型声明 `default_operating_mode` 与自动激活事件；运行时编译接收瞬时事件集合并只临时唤醒待机模块，显式关闭保持最高优先级；完好实例不再把损管和未选中的遥控核心舱错误初始化为工作状态；
- 人员先按 CIC、主发动机、转向发动机、发电、损管的顺序保障关键最低岗位，再保障其他模块最低岗位，剩余人员按标准岗位缺口成比例分配；按原型自动化子功能仍逐功能豁免缺员削减；
- 模块原型逐功能声明归零失效、分段线性或阶梯式战损响应；`gaotian.module-damage-response/per-function-curves/v1` 把耐久倍率与人员/自动化倍率相乘，并据此重算推进、转向、发电、升力、CIC 与遥控核心能力；
- 发电机、CIC、遥控核心舱、灵烷贮槽、主发动机和转向发动机损毁后的当前能力重算；
- 复用 `gaotian.actuator-aggregation/physical-residuals-v1` 的当前执行器重聚合；
- 带设计态、出航、实例、气动与 RCS 来源指纹的 `gaotian.runtime-ship-parameters/v1alpha1`；
- 当前全舰船壳耐久进入实例快照；归零生成 `hull_structure_collapsed`，OverG 损伤可回写实例；
- 正式适配器 `gaotian.tactical-dynamics/runtime-adapter/v1alpha1` 与固定 `1/60s` 半隐式欧拉策略；
- 当前质量、惯量、结构限制、乘员锁、燃料状态和战损后执行器聚合实际进入每个战术固定步；
- 船壳方向气动缓存按当前层介质参与阻力，船壳 RCS 缓存按观察方位与涂料查询；待标定外挂模块 RCS 以“不完整/已知下界”显式返回，不以零值冒充完整结果；
- 上层、云层、雨层环境表、参数化相邻层耗时、换层进行中状态、上升升力门槛及实例回写；
- `gaotian.combat-system-modules/v1alpha1`：武器、弹药库、火控和传感器的最小能力、安装、供电、人员、自动化、事件唤醒与逐功能战损契约；
- 绑定真实五米网格的战斗系统舾装与出航夹具，验证炮塔内部/顶挂复合占用、顶挂传感器、内部弹药库和内部火控；
- 阶段 E E1～E5 正负面回归和确定性保存报告。

阶段 E 之外仍需处理：安全供电模式的核心舱升级解锁资格接入成长系统；供电算法本身已经完成。I1 已在不改动 E5 模块原型布局的前提下接入弹药当前库存、待发弹和原子开火/装填；I4 又接入射速/装填计时、连续射击和齐射队列；I5 已将成功开火事件连接为弹丸实体，并完成非制导弹丸阻力、真实船壳边命中、甲弹判定和模块/船壳回写；I6 已把这些状态与实际舰艇固定步机动放入同一可持久化战术时钟；I7 已接入舰艇失能、指挥权、坠落和显式离场；I8 已接入阵营/舰队归属与自动交战/脱离边界；I9 已接入主旗舰/分旗舰/普通舰角色、唯一直控对象与普通舰命令仲裁；I10a/I10b 已接入导引头配置、弹丸内持久状态与确定性固定步制导；I11a 已接入模块火灾与损管压制。传感器/EW 对观测事实的自动生成、火势蔓延、弹药殉爆、人员伤亡与医疗仍属后续战斗求解阶段。

阶段 E 已按以下内部顺序完成：

1. **E1 当前状态边界：** 拆分出航初态与当前资源状态，允许耗油、伤亡、卸货和换层而不修改出航配置。**已完成。**
2. **E2 设备运行策略：** 默认工作模式、事件驱动负载、稳定供电类别及关键岗位优先的人员分配。**已完成。**
3. **E3 战损响应：** 按模块功能分别定义耐久—输出曲线或失效阈值，不使用全模块统一线性倍率。**已完成。**
4. **E4 战术求解器适配：** 把当前质量、惯量、执行器、结构限制、气动缓存和 RCS 缓存真正接入运行循环，并加入参数化高度环境与换层状态机。**已完成。**
5. **E5 战斗系统契约夹具：** 只为验证接口加入武器、弹药库、火控与传感器，不在此阶段正式平衡。**已完成。**

全部 Python 测试与阶段回归统一由《高天荒野舰艇测试总入口.py》以 `python -X utf8` 子进程执行，避免 Windows 默认 GBK 控制台因 `m²` 等单位字符产生与公式无关的假失败。

### 阶段 F：三条规范蓝图测试舰

**依赖：阶段 E1～E5。**

**状态：完成。三条测试舰、无人化模块目录、初始化实例、集成测试与确定性报告均已落地。**

建立三条仅用于验证规则的规范蓝图夹具：

1. **最小合法舰：** 另建具备双向转向能力的新夹具，覆盖最低船壳、CIC、升力、推进、转向、发电与最低人员约束；现有仅有逆时针力矩的 `contract_fixture` 保留为组件测试，不直接升级；
2. **常规有人战舰：** 覆盖多层船壳、非对称舾装、供电优先级、标准船员、武器和战损接口；
3. **完全无人主旗舰：** 覆盖无人模块、遥控核心舱、无乘员 `12G` 锁、OverG 和自动损管限制。

每条测试舰只保存规范蓝图、舾装方案、出航配置和由它们初始化的舰艇实例状态。报告中的质量、惯量、推力和性能表必须在测试运行时生成。

验收条件：改变任一发动机位置、模块质量、结构材质、甲板几何或货仓载荷后，所有相关派生性能自动变化，不存在需要人工同步的第二套数据。

当前已经完成：

- `80×15m` 最小合法舰，四台侧挂转向机组成顺、逆时针双向零残余平动力回转，最低与标准船员均由模块岗位派生；
- `155×20m` 双层常规有人战舰，分离上层甲板承载非对称武器与传感器，标准船员、P0～P3 供电、战损响应和 `25t` 货载进入正式运行时；
- `100×15m` 完全无人旗舰，以及 CIC、推进、转向、发电、损管、火控、传感器和武器的八类完全无人化契约原型；
- 遥控核心零船员出航、无 `12G` 乘员安全锁、OverG 船壳耐久损失，以及普通手动损管在零船员时输出为零的正负面回归；
- 三舰各自的规范船壳蓝图、舾装方案、出航配置和由同一链路初始化的完好实例快照；
- `gaotian.stage-f-three-canonical-ships/v1` 集成接口与《阶段F三舰集成接口.v1.json》确定性保存报告。

### 阶段 G：端到端集成测试

**依赖：阶段 F。**

**状态：完成。阶段 F 规范舰艇已经进入连续状态演化、战损配平、环境查询与解释字段一致性回归。**

至少验证：

- 蓝图编译结果确定且可复现；
- 远端模块和货物正确改变惯量；
- 发动机位置与方向正确改变力矩；
- 一侧受损后推力配平和转向能力正确变化；
- 满载质量同时影响升力余量、平动加速度与惯量；
- 同一出航来源下的燃料消耗、船员伤亡、货物变化与换层能够改变运行时结果；
- 结构限制、乘员安全锁和 OverG 读取派生安全值；
- 船壳方向缓存实际进入阻力与 RCS 计算；
- 设计器解释字段与运行时采用的数值一致。

本阶段只锁定相对关系、数据追溯和运行正确性，不锁定教程节奏。

当前已经完成：

- 对同一规范蓝图重复执行船壳、舾装、派生快照、出航和运行时编译，逐层结果与内容指纹保持确定；
- 把远端火控模块向 CIC 移动，验证质量不变而惯量按实际位置自动下降；`25t` 货载按货仓位置增加 `3,125,000kg·m²` 惯量；
- 常规有人舰改为左右成对主机，完好时两侧各输出 `100kN`；单侧耐久降至 `50%` 后按战损曲线得到 `75kN` 可用推力，另一侧自动配平到 `75kN`，总推力 `150kN` 且残余偏航为零；
- 摧毁一台逆时针转向机后，对应方向力矩减半并保留物理上真实存在的 `10kN` 横向残余力，另一回转方向不受影响；
- 在同一出航配置和同一派生快照来源下，连续回写燃料消耗、人员伤亡、卸货和上层至云层的换层结果；人员不足真实降低装填、持续制导与目标跟踪等非自动化子功能；
- 满载与空载的质量、惯量、升力余量和零速首步加速度保持一致的单调关系；
- 有人舰普通机动读取船壳派生安全值，OverG 仍受 `12G` 乘员安全锁；无人旗舰解除乘员锁后能够超越结构安全值并损伤船壳；
- 船壳方向气动和 RCS 缓存进入正式战术查询，云层阻力大于上层，侧向已知 RCS 大于正向；
- `DerivedShipSnapshot`、出航解释与 `RuntimeShipParameters` 对质量、惯量、升力、结构、执行器聚合和来源指纹的展示保持一致；
- `gaotian.stage-g-end-to-end-integration/v1` 与《阶段G端到端集成接口.v1.json》确定性保存报告。

### 阶段 H：玩家船壳与舾装编辑器

**依赖：阶段 C、阶段 D。可在阶段 E 至 G 期间并行制作界面，但不能复制计算逻辑。**

**状态：当前数据合同范围内的引擎无关编辑器领域层与三舰 L3 往返门槛已经完成。具体游戏引擎中的交互画面尚未制作；后续界面必须直接调用本领域层，不能另写规则。**

编辑器只负责：

- 把玩家操作转换为规范蓝图修改；
- 调用同一个无界面编译器获取校验、派生性能和解释；
- 显示占格、净空、射界、错误和警告；
- 保存规范数据对象。

编辑器不得自行计算另一套质量、惯量、推力、结构、气动或 RCS。L3 往返测试通过后，三条规范测试舰才可视为与落地后的实际编辑器布局一致。

当前已经完成：

- `gaotian.ship-editor-domain/v1alpha1`、船壳视图与舾装视图接口；编辑文档只保存规范输入，无任何手填整舰派生字段；
- 船壳逐层区域、结构材质、逐边装甲、严格内部格、局部露天格、侧挂槽以及船壳编译器全部派生结果的显示模型；
- 船壳单侧区域修改与 Y 轴镜像操作；只修改单侧会由正式编译器报出 `hull.symmetry_geometry`，镜像后才能恢复合法；
- 舾装网格、侧挂和嵌入模块的增删、移动、旋转与可选镜像放置；网格镜像转换锚点与直角旋转，侧挂镜像按船壳编译器生成的实际五米槽位匹配，关闭辅助后非对称布局仍然合法；
- 内部、顶挂、侧挂、本体与净空五套占用视图，逐模块原型、锚点、旋转、质量点、执行器和警告视图；
- 编辑态批量结构预检与正式编译诊断合并；非法草稿可以预览错误，但保存入口必定先调用正式编译器并拒绝非法数据；
- 船壳与舾装版本派生不修改原对象，规范保存只写编译器归一化结果；
- 当前无更高甲板遮挡的顶层武器以模块中心为原点显示完整 `360°` 射界。安装在较低露天甲板、可能受更高船壳遮挡的武器会明确返回 `requires_higher_deck_hull_raycast`，在补充正式船壳射线算法前不伪造射界；
- 最小合法舰、常规有人战舰、完全无人旗舰均已完成读取—完整显示模型—原样保存—重新读取，规范船壳、逐模块布局、全部占用与全部既有派生输出逐字段一致；
- `gaotian.stage-h-editor-l3-round-trip-regression/v1` 与《阶段H编辑器与L3往返接口.v1.json》确定性保存报告。

### 阶段 I：教程舰与共同正式标定

**依赖：阶段 G、阶段 H 的往返测试。**

**状态：I0 教程舰基准包与正式化门禁、I1 弹药实例状态及武器动作闭环、I2 船坞库存与战略工时合同、I3 舰艇自持设计快照与改装/残骸结算、I4 武器时间与射击队列、I5 弹丸生成—飞行—命中与损伤回写、I6 统一战术场景时间线、I7 战术生命周期、I8 战术交战边界、I9 战术舰队指挥仲裁、I10a/I10b 导弹制导状态与运行时闭环及 I11a 持久火灾/损管闭环已经完成；正式教程舰的船型、尺度、武备和布局仍待用户确认，尚未开始正式数值标定。**

以编辑器可打开、可修改、可原样保存的教程舰为正式基准，联合标定：

- 主发动机、转向发动机、灵烷贮槽和发电设备；
- `K_turn`、舵盘目标角速度、控制响应与换层时间；
- 三高度层气动环境和跨音速曲线；
- 常见装甲厚度、炮弹穿深、局部装甲承弹次数；
- 隐身涂料和外部模块 RCS；
- 功率、人员、弹药、材料、成本、工时和维护参数。

阶段 I 以前已经确定的现实密度、公式结构和相对材料定位继续有效；尚未联合验证的原型数值不得因为写入测试脚本而自动升级为正式值。

I0 当前已经完成：

- `gaotian.ship-calibration/v1alpha1` 教程舰基准包，精确记录船壳、舾装、出航、实例、材料、涂料、模块目录及组合目录身份；
- `gaotian.tutorial-baseline-compiler/v1alpha1`，从基准包重新执行船壳、舾装、派生快照、出航、实例、运行时和阶段 H 编辑器视图，不接受手填整舰性能；
- 在正式教程舰尚未确定时，只把阶段 F 常规有人舰标记为 `technical_surrogate`，并固定输出 `technical_surrogate_is_not_formal_balance`，不复制或改名生成伪正式教程舰；
- 标定清单只保存来源路径、依赖、状态、阻塞原因和验收条件，不重复保存质量、推力、升力、装甲厚度、RCS 等已有数据值；
- 标定项目依赖必须引用已知项目且不得成环；全部来源路径必须存在于工作区内；候选资源文件必须与清单中的精确 id 和版本一致；
- 正式化门禁要求候选身份为 `approved_tutorial_baseline`、获得 `user_approved`，且全部关键项目均为 `frozen_formula` 或 `calibrated`；提前修改身份会被 `tutorial.formal_gate_failed` 拒绝；
- 当前只有船壳与性能公式基础处于 `frozen_formula`。正式教程舰定义仍为 `pending_user_decision`；推进/升力/供电、操纵/换层、气动、甲弹、RCS/传感器、功率/人员/自动化、弹药/开火以及建造/维修经济均为 `prototype_unbalanced`；
- 《阶段I教程舰技术替身基准包.v1.json》、阶段 I 首切片正负面测试，以及 `gaotian.stage-i-tutorial-baseline-gate-regression/v1` 与《阶段I教程舰基准门禁接口.v1.json》确定性报告。

I1 当前已经完成：

- `SortieConfiguration.ammunition_loadout` 与 `ShipInstanceSnapshot.ammunition_state` 采用可选字段接入；旧资源省略字段时规范 JSON 与既有内容指纹不变，但必须先建立完整弹药初态才能执行武器动作；
- 弹药库保存具体物理库存，武器保存具体待发弹；全部兼容弹药库构成全舰共享装填池，不计算管线、通道或距离损耗；已装弹药继续不计舰艇质量；
- `gaotian.ammunition-actions/v1alpha1` 实现原子开火与原子装填。开火只消耗待发弹，装填按弹药库实例 id 的确定性顺序从仍可工作的兼容弹药库取弹；
- 武器瞄准、击发、装填，弹药库供弹/库存访问及火控解算均读取同一运行时的战损、供电、人员与自动化结果；射程、目标域、兼容弹药和火控需求继续来自模块原型；
- 阶段 F 常规有人舰的精确舾装完成“标准弹开火—特种弹装填—特种弹开火—标准弹再装填—保存重载”连续回归，并覆盖无弹药状态、缺火控、越射程、待发弹不符、火控/弹药库损毁、武器/火控断电、装填缺员与弹药库超载；
- 当前弹量只属 `contract_fixture`。I4 已实现射速、装填时间和连续射击/齐射队列的执行合同，但技术替身时间配置及战略补给资源换算均未据此标定。

I2 当前已经完成：

- `gaotian.ship-logistics/v1alpha1` 定义船坞设施、库存快照和可持久化作业订单；材料、维修材料、模块和弹药采用整数库存，燃料保持双精度连续量；
- 新造舰船壳材料直接从规范船壳逐层结构体积与逐边基础装甲体积、真实材质密度生成；舾装逐实例汇总为精确模块原型库存需求，不允许手填一份脱离布局的整舰造价覆盖派生结果；
- 建造、改装、维修、重新武装和燃料补给均先生成绑定设计/实例内容指纹的报价；确认时一次性预留库存，库存不足或设施缺少对应能力时拒绝创建订单；
- 订单只读取战略世界绝对时间推进。确认特殊事件内世界时间不变时保持 `queued`，返回战略地图并推进时间后才进入 `in_progress` 与 `completed_unsettled`；普通产出只在最终结算时返库，改装订单则在当前设计迁移完成前禁止结算拆下模块；
- 建造完成继续调用既有舰艇实例初始化链；维修恢复船壳及仍未归零的模块，归零残骸保持占位；重新武装和燃料补给应用到实例当前状态，并拒绝订单确认后已经变化的来源实例；
- 同一精确船壳上的改装能够比较实际模块实例、预留新增模块并把完好拆下模块列为结算产出；受损模块不能被改装隐式移除，归零残骸不能被直接替换；
- 当前一千克取整材料单位、缺失质量等效维修材料、模块质量安装工作量与设施吞吐均为 `contract_fixture/prototype_unbalanced`，不构成正式材料损耗、产能、工期或价格。

I3 当前已经完成：

- `ShipInstanceSnapshot.design_state` 作为可选向后兼容字段，嵌入建造时完整规范船壳快照、其内容指纹、当前规范舾装快照、当前舾装/派生指纹与独立修订号；旧字段继续冻结建成/首次出航历史，不随改装覆写；
- 新造舰由船坞完成接口直接嵌入设计状态；旧实例可无损补建。删除外部蓝图后，可以从舰艇内嵌船壳和当前舾装重建并编译出相同派生快照；
- 改装原子迁移模块状态：精确未变模块保留当前耐久与工作模式，新增或更换模块以原型完好初态加入；弹药库或武器存在库存/待发弹时禁止拆换，带货货舱等运行态依赖继续由目标设计载荷编译校验；
- 改装、清残和成功拆卸必须先把舰艇实例迁移到 `artifact_applied_unsettled`，之后才能把拆下模块或废金属结算入库存，避免旧布局未改变却重复产出；
- 完好模块拆卸必定成功；受损模块拆卸成功/失败由外部概率结算注入，失败时消耗工时但舰艇和库存不变；归零模块只走残骸清理，废金属材质与整数产量同样由外部回收结算注入；当前没有伪造拆卸概率或残骸产率；
- `gaotian.ship-instance-design-state/v1alpha1` 与《阶段I实例设计与改装接口.v1.json》已在阶段 F 常规有人舰上验证连续改装、历史来源不变、保存重载、蓝图重建、运行时再编译、清残及拆卸正反结果。

I4 当前已经完成：

- `ShipInstanceSnapshot.weapon_timeline_state` 作为可选兼容字段保存精确时间配置目录引用/指纹、当前战术时刻、每件武器的下次可开火时刻及活动射击序列；旧实例省略字段时规范文本与既有哈希不变；
- `gaotian.weapon-action-timeline/v1alpha1` 按下一事件时刻和稳定序列 id 推进，只通过 I1 的 `resolve_weapon_fire/resolve_weapon_reload` 扣减待发弹和弹药库库存，没有第二条隐藏弹药消耗路径；
- 连续射击把一条多发命令展开为交替的原子开火/逐发装填事件；循环射速受当次 `weapon.fire` 效率缩放，装填时长受武器装填、弹药库供弹与库存访问的最低效率缩放；
- 齐射要求至少两件已经装入待发弹且完成冷却的不同武器，在同一战术时刻生成独立原子开火事件；每件武器随后保留自己的冷却时钟；
- 活动序列可取消，尚未完成的装填不会提前扣除弹药。战损、断电、缺员或火控失效会在事件到期时由原子动作重新校验，失败事件不改变弹药状态并终止该武器序列；
- 改装会保留未变化武器的冷却与队列；涉及被移除/替换武器的活动序列必须先取消。新增武器从当前战术时刻建立新时钟；
- 《阶段I武器时间技术替身配置.v1.json》中的 `60rpm/0.25s` 仅为时间合同夹具；`gaotian.weapon-action-timeline/v1alpha1` 与《阶段I武器时间与射击队列接口.v1.json》已验证 `0/1/2s` 连续射击、同刻双炮齐射、取消、战损中断、保存重载与时间配置指纹锁定，未将其升格为正式炮塔性能。

I5 当前已经完成：

- `gaotian.tactical-projectile-world/v1alpha1` 只接受 I4 已结算成功的单发原子开火事件，从精确舀装中取武器模块中心作为发射原点，并继承舰体线速度及回转造成的局部表面速度；
- `ProjectileWorldState` 保存战术时刻、弹丸 id、来源舰/武器/弹药、目标舰、显式选定甲板层、位置、速度、寿命和路程，并锁定 `ProjectileProfileCatalog` 的精确引用与内容指纹；
- 固定时间步以弹丸相对介质的世界速度和既有跨音速 `Cd` 曲线计算阻力；雨层不隐式增加速度衰减，三层只通过显式介质参数区分；
- 命中检测把弹丸世界轨迹转换到常速平动/回转目标的局部坐标，与所选甲板层的真实多边形边求交，再用相对命中速度、入射角、边装甲材质/厚度与局部耐久调用既有甲弹公式；
- `ShipCombatState` 独立持久化每条规范船壳边的当前/最大局部装甲耐久与舰艇实例。局部耐久归零前穿深所需有效厚度不变，归零后才完全失去防护；
- 未击穿后不损伤内部模块和全舰船壳完整度，但可根据真实顶挂占格/同边侧挂步长损伤外部模块；击穿后只检索所选甲板层上的真实内部占格，按弹种后效距离回写模块耐久与船壳完整度；
- 100 毫米装甲钢舰尾击穿/左主机损伤和 110 毫米轻质碳化物复合装甲左舷未击穿/左后转向机表面损伤两条路径已使用阶段 F 常规有人舰的真实布局通过回归；弹丸最大寿命自毁、时间倒退和配置指纹错配同样已覆盖。

I6 当前已经完成：

- `gaotian.tactical-scene-timeline/v1alpha1` 保存唯一的 `60Hz` 场景步号/战术时刻、弹丸世界，以及各舰机动状态、实例/局部装甲战损状态和精确设计/出航来源指纹；
- 每步固定为“步首武器事件与发射—舰艇机动预计算—沿实际步首/步末轨迹推进弹丸并结算命中—步末武器事件与发射”，步末新生弹丸年龄为零；
- 弹丸介质参数来自其目标舰本步实际高度层；目标位姿由场景实际机动首末状态插值，I6 不再从测试调用者注入独立常速目标夹具；
- OverG、燃料消耗与弹丸战损合并回同一舰艇实例，命中所在步不回滚已经预计算的运动，下一步开始前通过既有运行时编译链重算模块、供电、人员、升力和执行器；
- 会生成战术实体的 I4 事件必须精确落在固定步边界，不对齐时拒绝而不取整；成功单发开火必须匹配唯一显式发射指令，缺失、多余、重复或跨步指令均拒绝整步提交；
- 完整场景保存重载后能够继续产生相同演算结果；实际运动发射、移动目标命中、战损后推力重编译、步末发射不偷跑及错误边界均已通过阶段 I6 回归。

I7 当前已经完成：

- 每舰持久化 `operational / falling / exited` 物理状态，以及 `scene_command / local_only / uncommanded` 指挥权状态、稳定故障原因和最后转换步号；
- 船壳完整度归零、CIC模块耐久归零、当前升力不足分别独立触发不可逆坠落；CIC关闭、断电或缺员只撤销指挥权，不被误判为物理摧毁；
- 遥控核心失联时，有实体人员且CIC仍工作的舰保留本舰控制，完全无人舰则无人控制；两者都不会只因通信失联而凭空坠毁；
- 坠落在步末武器事件前生效，立即清空活动武器序列并拒绝新的操纵输入；二维层不伪造未确定的垂直坠落耗时；
- `fell_below_scene / distance_disengaged / scripted_transfer` 退出均由上层在固定步边界显式确认；离场舰保留战斗结果快照但不再运动或成为目标，以其为目标的在途弹丸以 `target_left_scene` 终止；
- 实际侧向炮击摧毁CIC、三类物理终止条件、CIC暂时不可用、无人遥控失联、坠落后退出、弹丸清退和保存重载均已通过阶段 I7 回归。

I8 当前已经完成：

- 每舰在统一场景中持久化稳定阵营与舰队归属；活动场景继续用外部绑定校验，不允许静默改换阵营或舰队；
- `gaotian.tactical-engagement-boundary/v1alpha1` 配置保存上层 `50000m`、云层/雨层 `25000m`，并以 `prototype_unbalanced`、精确版本与内容指纹明确其可替换性；
- 场景保存主动接战方与被动应战方；每一组敌对舰都按被动方该舰当前层级选择距离阈值，不使用主动方或全场统一层级；
- 边界点计入接触。多舰战只要任意一组敌对舰仍在阈值内就保持 `active`，双方均有可战舰但全部超距才转为 `disengaged`；任一方没有 `operational` 舰则转为 `resolved`；
- 战术场景创建门禁、每步末重新评估及结束后禁止继续推进已经接入 I6/I7 时间线；正常整体脱离保留全部舰艇结果，不伪造成逐舰离场或坠毁；
- 上层/云雨层临界值、主动/被动层级互换、多舰逐对判定、生命周期联动、配置指纹、归属绑定和保存重载均已通过阶段 I8 回归。

I9 当前已经完成：

- `gaotian.tactical-fleet-command/v1alpha1` 独立保存玩家阵营、主旗舰/分旗舰/普通舰角色、编队锚点、唯一冻结的直控对象、当前命令与精确场景/技术配置指纹；
- 常规战斗只允许主旗舰或分旗舰成为直控对象；普通舰不能直控，玩家舰也不能借 NPC 控制输入旁路取得第二条直接操纵通道；最终动员只允许普通舰接受命令，不保存直控对象；
- 普通舰命令首版保存多段航路、目标高度、舰艏、航速、指定目标、弹种、交战距离和目标类别优先级，并覆盖移动、对空巡逻、反舰巡逻、区域自由开火、指定攻击、稳定航行及搜救撤离意图；
- 命令仲裁器把当前航点、移动目标或编队相对位置确定性转换为既有 `TacticalControlInput`，自动命令不会开启 OverG；攻击命令只在本阶段产生接敌机动，自动选择武器/开火与制导仍留给后续合同；
- 直控旗舰失能、离场或遥控链路丢失后，直控对象保持冻结而不能切换，幸存舰放弃一般命令并进入搜救撤离；分旗舰损失只留下“额外损失待战略层结算”标记，不伪造概率；
- 命令保存重载、配置指纹、移动/换层、编队回退、普通舰旁路拒绝、分旗舰失效撤离及最终动员无直控均已通过阶段 I9 回归。

I10a 当前已经完成：

- `gaotian.missile-guidance/v1alpha1` 按弹药保存被动雷达、主动雷达、反辐射与光电四类导引头的火控支持、开机距离、搜索范围、高度层及目标丢失策略；
- 被动雷达持续照射、反辐射最后已知位置和光电仅上层等已敲定边界进入严格正反校验，全部距离与时间继续标记为技术替身；
- `MissileGuidanceState` 保存精确配置引用/指纹、弹丸与目标绑定、当前阶段、跟踪对象、最后位置及丢失/自毁计时槽位；
- `ProjectileState.guidance_state` 采用可选字段接入，旧非制导弹丸的规范 JSON 形状不变；直接弹丸生成与 I6 统一场景生成均能注入并保存该状态；
- `gaotian.missile-guidance-state/v1alpha1` 与《阶段I导弹制导状态接口.v1.json》验证四类配置约束、初态、身份错配拒绝、旧弹丸兼容、保存重载及统一场景传递。

I10b 当前已经完成：

- `gaotian.missile-guidance-runtime/v1alpha1` 以逐弹丸显式运行时事实输入目标跟踪可用性、目标雷达辐射状态与母舰持续照射状态，不在制导层暗造传感器或电子战概率；
- 惯性、搜索、跟踪、记忆和丢失状态在弹丸固定子步内确定性迁移；被动雷达受持续照射门控，反辐射受辐射源门控，光电受高度层门控，主动雷达受跟踪事实与搜索距离门控；
- `gaotian.missile-guidance/fixed-step-limited-lateral-acceleration-pursuit/v1` 在弹道积分前以配置的最大横向加速度限制航向变化，保持当次制导修正前的速度模长，随后继续走既有阻力积分与真实船壳命中链；
- 目标丢失可进入最后位置记忆、重捕、不可逆丢失及固定步边界自毁；状态转换事件由弹丸世界和统一战术场景共同向上返回，非制导弹丸的旧输出形状继续保持；
- 《阶段I导弹制导运行时接口.v1.json》覆盖四类导引头、确定性重复、限过载转向、丢失/重捕/自毁、缺失事实拒绝以及统一场景闭环。

I11a 当前已经完成：

- `ShipInstanceSnapshot.continuous_damage_state` 作为可选字段保存精确配置引用/指纹、持续毁伤时钟、活动模块火灾和损管队槽位分配；旧实例继续省略字段并保持原规范形状；
- `gaotian.continuous-damage/v1alpha1` 只保存技术替身燃烧、自然衰减和灭火速率。点燃强度与可燃物量由 `FireIgnitionOutcome` 显式注入，且只能绑定 `fire` 后效弹丸的实际命中、目标舰和真实受损模块；没有结果就不会暗造起火概率；
- 既有火灾在场景步末按步首强度确定性损伤目标模块与船壳、消耗燃料并衰减；新命中火灾只在出生步登记，从下一步开始结算，避免偷跑持续伤害；
- 损管分配严格受模块 `team_capacity / simultaneous_incidents` 限制，实际灭火率读取统一运行时的供电、人员、自动化和 `damage_control.firefighting` 战损效率；活动火灾自动触发 `ship.damage_control_required`；
- 火灾开始、持续损伤、压制、熄灭、燃尽和分配改变均生成稳定事件；火灾对 CIC、升力与船壳造成的结果继续进入既有生命周期重评估；活动火灾会阻止船坞维修报价和改装迁移；
- 《阶段I持续毁伤与损管接口.v1.json》覆盖旧实例兼容、保存重载、确定性燃烧、正常/关闭损管、容量门禁、真实命中点燃、出生步不偷跑及非 `fire` 后效拒绝。

I0 至 I11a 均不等于阶段 I 完成。下一步仍可走两条互不冒充正式内容的路径：其一由用户确定正式教程舰定义，然后在新蓝图上依次联合标定推进、操纵、环境、甲弹、RCS、人员功率、武器弹药及船坞成本；其二继续 I11b，建立当前在岗/负伤/死亡人员账本、显式伤亡结果、运行时人员效率回写与后续医疗边界。火势蔓延、弹药殉爆、传感器/EW 自动观测、正式散布、火控瞄准修正和甲板层概率仍不得由技术夹具冒充正式规则。

### 阶段 J：扩展内容

在教程舰闭环后，才由基准向小型舰、大型舰、航母、导弹舰、航空设备和特殊古文明模块扩展。每次新增模块类型都必须先进入模块原型和无界面编译器，再进入编辑器与内容表。

## 五、下一次实施应从哪里开始

阶段 C 至阶段 H 的当前数据合同范围已经完成，三条规范测试舰已经形成从蓝图、舾装、编辑器往返、出航、实例到连续战损与固定步战术动力学的可追溯闭环。后续应依次完成：

1. 阶段 I 的基准包与联合标定表已经建立；下一项教程舰内容工序是确认正式船型、尺度、武备与布局，再生成独立的教程舰船壳、舾装、出航与实例资源；
2. 舰艇实例已经持有建造时船壳与当前舾装快照，改装、清残及受损拆卸已形成原子迁移—库存结算闭环；不得为其补写未经标定的概率、废金属产率或工时；
3. 射速/装填队列、弹丸生成—飞行—命中—损伤回写、I6 统一场景时钟、I7 生命周期、I8 交战边界、I9 舰队指挥仲裁、I10a/I10b 导弹制导及 I11a 火灾/损管闭环已经完成；若继续独立于正式舰型推进战斗领域层，下一项是 I11b 人员伤亡账本与运行时回写；
4. 在成长系统接入时，为安全供电模式增加核心舱升级资格校验，不修改已经冻结的供电分配算法。

当前不再存在阶段 H 的数据往返门槛，阶段 I 的技术替身基准包、弹药动作、船坞后勤、实例设计/改装结算、武器时间队列、弹丸/甲弹回写、统一战术场景时钟、舰艇生命周期、交战边界、舰队指挥仲裁、导弹固定步制导及持久火灾/损管闭环均已就位。正式教程舰内容仍需明确授权和定义；在此之前仍可实现不依赖正式舰型的人员伤亡合同。具体游戏引擎的可视化编辑界面仍需在确定技术栈后接入现有领域层。换层时间、环境参数、无人高推力主机、战斗模块、弹量、时间配置、弹种性能/损伤换算、指挥自动驾驶参数、导弹开机/搜索/转向参数、燃烧/灭火速率、材料单位、拆卸概率、残骸产率与船坞吞吐都仍只是未定或可替换原型，不因进入阶段 I 而升格为正式平衡值。

## 六、完成标准

只有同时满足以下条件，才能宣称“测试舰使用了与落地后相同的实际模块布局”：

- 测试舰保存的是规范船壳蓝图和舾装方案，而不是整舰聚合参数；
- 测试、运行时和编辑器调用同一套校验与派生代码；
- 派生性能不能由蓝图中的手填字段覆盖；
- 测试舰能够被玩家编辑器完整读取和显示；
- 未修改往返保存后，规范几何、模块布局和派生结果一致；
- 战术求解器读取的运行参数可以追溯到该蓝图、舾装、出航配置和舰艇实例状态的精确版本/内容指纹。
