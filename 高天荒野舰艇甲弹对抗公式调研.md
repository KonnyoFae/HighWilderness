# 《高天荒野》舰艇甲弹对抗与穿后效应公式调研

## 一、目标与既定边界

本公式用于战术模式中已经发生的炮弹或侵彻战斗部命中，输入实际命中位置、炮弹相对目标命中点局部表面的速度与二维方向、命中船壳边、命中甲板层和该边当前装甲状态，输出：

- 原始入射角与弹体归一化后的有效角度；
- 当前速度下的可用法向装甲钢穿深；
- 当前材质、厚度和角度下所需的等效穿深；
- 跳弹概率以及跳弹、未击穿、击穿三种结果；
- 本次命中对局部装甲耐久造成的损伤；
- 击穿后的剩余速度、剩余动能与穿后效应入口。

继续遵守以下边界：

- 炮弹在战术场景中实际飞行，距离不会在命中公式中重复生成一套隐藏穿深衰减；
- 命中多层舰艇时先由现有规则确定命中甲板，之后只在该层进行二维计算；
- 每次命中只读取实际相交的玩家绘制船壳边；
- 局部装甲耐久大于零时保持完整厚度和完整抗穿系数，归零后完全失去基础装甲防护；
- 穿透后才伤害内部模块和全舰船壳耐久；未击穿与火灾只能损伤局部装甲和外部模块；
- 第一版不进行有限元、流体侵彻或逐片陶瓷破坏计算；
- 第一版装甲材质只有一条抗穿通道，是否拆分动能与化学能防护留待正式导弹战斗部内容验证。

## 二、为何采用参考穿深代理

完整的现实侵彻模型需要弹体质量、直径、长径比、头型、硬度、材料失效、装甲背板、速度区间和具体破坏模式。同一材料也不存在脱离威胁类型后仍普遍成立的固定等效值。当前游戏已经要支持半穿高爆弹、高爆弹、燃烧弹、尾翼稳定脱壳穿甲弹、制导高爆弹和导弹战斗部，直接选择一条现实经验式并不能覆盖全部内容。

因此，弹药原型直接保存“参考速度下、法向命中装甲钢时的穿深”。弹体质量、口径、头型和内部结构已经折算进这个可由内容设计标定的锚点；命中公式只负责从当前速度、角度和装甲材质稳定外推。

现实试验和弹道极限公式仍支持以下骨架关系：速度、入射角、板厚和材质都会改变弹道极限；穿透后可以用残余速度或残余能量描述后续破坏；斜向命中必须考虑速度法向分量或等价的余弦修正。[NASA：Impact Damage to Various Materials for LDEF Applications](https://ntrs.nasa.gov/api/citations/19940006210/downloads/19940006210.pdf)、[NASA：Theoretical and Experimental Investigation of Impact Damage](https://ntrs.nasa.gov/api/citations/19870019149/downloads/19870019149.pdf)、[美国陆军：A Potential Methodology for Evaluating Ceramic Quality](https://www.govinfo.gov/content/pkg/GOVPUB-D101-PURL-gpo124239/pdf/GOVPUB-D101-PURL-gpo124239.pdf)

这些资料只用于选择单调关系与输出接口，不把航天器超高速微粒试验或小口径陶瓷试验的系数直接套用到高天战舰火炮。

## 三、数据字段

### 3.1 弹药原型字段

| 字段 | 符号 | 说明 |
| --- | --- | --- |
| 参考法向钢板穿深 | `P_ref` | 在参考速度下法向命中装甲钢的穿深，单位毫米 |
| 参考速度 | `v_ref` | `P_ref` 对应的命中速度，单位米每秒 |
| 速度指数 | `n_v` | 当前速度对可用穿深的影响 |
| 斜面指数 | `q_obl` | 对斜向装甲的敏感程度 |
| 归一化角 | `δ_norm` | 弹体咬入装甲后抵消的入射角 |
| 跳弹起始角 | `θ_start` | 从装甲法线量起，开始出现跳弹概率的有效角度 |
| 完全跳弹角 | `θ_full` | 角度因素达到最大跳弹概率的有效角度 |
| 能否跳弹 | `can_ricochet` | 高爆、燃烧等接触即产生表面效应的弹种可关闭 |
| 参考撞击装甲损伤 | `D_impact,ref` | 参考速度下纯撞击对局部装甲耐久的损伤 |
| 表面效应装甲损伤 | `D_surface` | 未击穿时爆炸、烧蚀等不随速度平方变化的额外损伤 |
| 穿后效应 | `effect_type` | 延迟爆炸、表面爆炸、动能直线或引燃等 |

弹体质量、口径和气动参数仍需由弹道飞行与模块内容保存，但甲弹结算不再从它们重新计算一遍 `P_ref`。这样可以允许同口径的半穿、高爆和特殊弹使用完全不同的内部结构。

### 3.2 命中时动态字段

- 当前二维位置与炮弹世界速度向量；
- 目标舰世界线速度、角速度以及命中点相对目标转动中心的位置；
- 炮弹相对命中点局部表面的速度向量与其大小 `v_hit`；
- 命中甲板层；
- 命中船壳边的两个端点及其法线；
- 该边实际装甲厚度、材质和当前局部耐久；
- 用于跳弹判定的一次性稳定随机数。

跳弹随机数由“炮弹实例＋本次碰撞序号”的稳定哈希生成。暂停、读档或重复打开预测界面不能让同一次碰撞反复重掷。

## 四、结算顺序

1. 外部主动防御、反应装甲和外附装甲先按各自规则处理；
2. 依据多层命中规则确定实际甲板；
3. 二维碰撞求得命中的船壳边和命中点；
4. 读取命中瞬间速度，不读取射程生成第二套衰减；
5. 计算原始入射角与弹体归一化后的有效入射角；
6. 计算当前可用穿深与所需穿深；
7. 计算一次跳弹概率并判定跳弹；
8. 未跳弹时比较穿深，得到未击穿或击穿；
9. 结算本次局部装甲耐久损伤；
10. 击穿时生成剩余速度、剩余能量和对应穿后效应；未击穿时只生成表面效应；
11. 爆炸、火灾、模块损伤、全舰船壳耐久和人员伤亡进入现有损伤系统。

## 五、入射角

命中点相对目标舰转动中心的位置为 `r=(r_x,r_y)`，目标舰世界线速度为 `v_target`，二维角速度为 `ω_target`。命中点局部表面的世界速度为：

`v_surface = v_target + (-ω_target r_y, ω_target r_x)`

炮弹用于甲弹结算的相对命中速度向量为：

`v_hit,vec = v_projectile - v_surface`

`v_hit = |v_hit,vec|`

令这一相对命中速度的单位向量为 `v_hat`，命中边任一世界空间单位法线为 `n_hat`。因为只需要法线夹角而不需要区分内外法线：

`θ_hit = arccos(clamp(|v_hat · n_hat|, 0, 1))`

角度从装甲法线量起：法向直击为 `0°`，沿装甲表面擦过趋近 `90°`。这套定义必须在数据、UI 和调试工具中统一，避免同时存在“从装甲面量起”的另一种角度口径。

弹体归一化后的有效角度为：

`θ_eff = clamp(θ_hit - δ_norm, 0°, 89.9°)`

第一版归一化角是弹种字段，不由装甲材质再次修正。

## 六、当前速度下的可用穿深

`P_available = P_ref × (v_hit / v_ref)^n_v`

速度指数由弹种原型保存。第一版常规完整弹体可以从约 `1.2–1.5` 开始标定，长杆动能弹可以从约 `1.5–1.7` 开始；这些只是内容标定范围，不是所有弹种共用的隐藏常数。

炮弹飞行模型先生成命中瞬间的世界速度，碰撞适配层再扣除目标命中点局部表面速度得到 `v_hit`，所以命中公式不再读取距离。若以后采用抽象战略导弹命中，其进入战术损伤系统前同样必须先生成末端世界速度，再与目标局部表面速度求相对值。

这一区分对于高天战舰不可省略：炮弹和目标同向各以 `900 m/s` 与 `300 m/s` 运动时，相对命中速度是 `600 m/s`；目标反向以 `300 m/s` 迎面撞来时则是 `1200 m/s`。目标回转造成的局部表面速度同样参与计算。碰撞扫掠仍使用双方世界运动，穿深、入射角、撞击装甲损伤和穿后剩余能量统一使用相对命中速度。

## 七、装甲所需穿深

局部装甲耐久大于零时，法向装甲钢等效为：

`T_normal = t_edge × K_protection`

斜向命中所需穿深为：

`T_required = T_normal / max(cos θ_eff, 0.05)^q_obl`

其中 `0.05` 只用于避免接近九十度时数值发散；在这种角度下通常已经先发生跳弹或明确未击穿。`q_obl=1` 接近简单视线厚度，较大的指数表示弹种更怕斜面，较小的指数表示弹种对斜面更稳定。

若局部装甲耐久已经归零：

`T_required = 0`

此时炮弹以完整剩余速度进入该甲板内部，不能继续读取被摧毁装甲的名义厚度。

## 八、跳弹

第一版使用一个连续概率带，避免在单一角度产生突兀的百分之百跳弹。先计算角度因子：

`u = clamp((θ_eff - θ_start) / (θ_full - θ_start), 0, 1)`

`P_angle = u² × (3 - 2u)`

为了避免穿深远高于装甲的弹体仍以同样概率跳飞，再计算法向穿深比：

`R_normal = P_available / T_normal`

`S_power = clamp((4 - R_normal) / 3, 0, 1)`

最终：

`P_ricochet = P_angle × S_power`

法向穿深达到装甲等效厚度四倍时，第一版完全抑制跳弹；介于一倍与四倍时逐渐降低跳弹概率。常数 `4` 是可全局调整的游戏参数，而不是现实普适定律。

关闭 `can_ricochet` 的高爆弹、燃烧弹和其他接触触发弹种直接跳过这一步。第一版跳弹结果不产生第二次完整伤害碰撞，只生成反射方向的短时视觉弹道后销毁弹体，以避免同一炮弹在密集舰队中产生难以预测的连锁伤害；若后续确实需要危险跳弹，再单独扩展二次碰撞规则。

## 九、穿透与剩余能量

未发生跳弹时：

- `P_available < T_required`：未击穿；
- `P_available ≥ T_required`：击穿。

击穿后的穿深储备比例为：

`r = clamp(1 - T_required / P_available, 0, 1)`

由于本模型假设同一弹种的穿深与速度的 `n_v` 次方成正比，剩余速度比例可由同一关系反推：

`v_res / v_hit = r^(1/n_v)`

剩余动能比例为：

`E_res / E_hit = (v_res / v_hit)² = r^(2/n_v)`

阈值击穿的剩余能量为零；装甲越薄或弹体穿深储备越大，剩余能量单调增加。现实的 Recht–Ipson 类关系同样从弹道极限速度推导残余速度，本项目使用穿深储备作压缩代理，以便直接接入已经存在的“毫米等效穿深”内容字段。[NASA：Theoretical and Experimental Investigation of Impact Damage](https://ntrs.nasa.gov/api/citations/19870019149/downloads/19870019149.pdf)

## 十、局部装甲耐久损伤

撞击损伤与表面爆炸损伤必须分开：

`D_impact = D_impact,ref × (v_hit / v_ref)²`

第一版结果倍率为：

- 跳弹：`0.25 × D_impact`；
- 未击穿：`0.60 × D_impact + D_surface`；
- 击穿：`1.00 × D_impact`。

高爆弹的 `D_surface` 表示在装甲外表面起爆造成的额外剥落、开裂和烧蚀，不随炮弹剩余平动速度平方缩放；半穿弹未击穿时也可以配置较小的 `D_surface`。穿透后发生在舰内的爆炸进入内部模块与全舰船壳损伤，不再把完整爆炸伤害第二次施加给已经穿过的边。

火灾对装甲的持续损伤使用现有火灾强度按时间结算，不塞进一次炮弹撞击公式。最终从局部耐久中扣除：

`H_edge,current = max(0, H_edge,current - D_armor - D_fire)`

局部耐久在本次判定之后归零，不追溯改变本发炮弹已经完成的穿透结果；后续炮弹才把该边视为无装甲。

`D_impact,ref`、`D_surface` 与局部耐久全局常数必须在教程舰常见装甲厚度和主炮确定后共同标定，以“同一边平均能承受多少发对应威胁”为设计锚点。

## 十一、穿后效应接口

### 11.1 半穿高爆弹

- 击穿：沿剩余速度方向进入当前甲板，经过弹种设定的短延迟后在舰内产生爆炸、破片与起火概率；
- 未击穿：在船壳外表面产生较弱表面爆炸，只伤害局部装甲和外部模块；
- 剩余动能影响弹体能否在引信延迟结束前继续穿过内部模块，但不缩减装药的完整爆炸威力。

### 11.2 高爆弹与制导高爆弹

- 制导能力只影响命中前弹道，不改变甲弹公式；
- 未击穿时在命中点外侧起爆，使用较高 `D_surface` 清除传感器、转向发动机等表面设备；
- 击穿极薄装甲时把爆心放到船壳内侧，但不生成长距离动能贯穿线。

### 11.3 燃烧弹

- 通常没有足以击穿主装甲的 `P_ref`；
- 未击穿时在命中边和邻近外部模块生成火灾；
- 意外击穿薄弱区时在当前甲板内部生成火灾；
- 火灾后续按现有蔓延与损管规则运行。

### 11.4 尾翼稳定脱壳穿甲弹

- 击穿后沿剩余速度方向生成窄直线伤害；
- 每命中一个内部模块，就依据该模块的抗穿阻力继续消耗穿深储备与剩余能量；
- 没有爆炸范围，但直接穿过弹药库、灵烷贮槽或 CIC 时仍能造成致命功能损伤；
- 若穿过舰体另一侧边缘，使用剩余速度对出口边再次执行同一装甲结算，允许过穿。

### 11.5 导弹侵彻战斗部

导弹突破拦截后可以复用同一接口：导弹末端状态提供 `v_hit`，战斗部原型提供参考穿深、角度参数、局部装甲损伤和穿后效应。大型爆破战斗部若不属于刚性侵彻体，可以关闭跳弹并以表面爆炸为主。

## 十二、缓存、UI 与调试输出

弹药原型保存静态参数；炮弹实例只保存当前运动状态和一次性碰撞序号。装甲边已经保存材质、厚度与当前局部耐久，因此单次碰撞只进行常数次向量和幂运算，不需要重新分析全舰多边形。

武器设计与弹种选择界面至少显示：

- 参考速度下的法向装甲钢穿深；
- 若选定具体武器，可显示若干典型距离对应的预计命中速度与预计穿深；
- 对当前锁定目标和命中边，可在允许信息范围内显示预计入射角、所需穿深、击穿可能与跳弹概率；
- 装甲设计器显示每条边的实际厚度、法向钢板等效和局部耐久。

调试日志每次命中记录：命中层、命中边、速度、原始角度、有效角度、可用穿深、所需穿深、跳弹概率与随机数、结果、装甲损伤和剩余能量。

## 十三、原型测试结论

第一版原型已经验证：

1. 实际速度降低时可用穿深单调下降；
2. 入射角增大时所需穿深单调上升；
3. 相同 `50mm` 装甲下，装甲钢、轻质碳化物与积层烧蚀装甲按各自 `K_protection` 形成不同结果；
4. 局部耐久归零后装甲阻力严格变为零；
5. 跳弹概率在角度带内连续增加，并能被极高法向穿深抑制；
6. 阈值击穿的剩余能量为零，穿深储备越高则剩余能量越高；
7. 撞击装甲损伤与表面爆炸装甲损伤能够独立变化；
8. 五种既定主炮弹种以及导弹侵彻战斗部可以共用同一结算入口。

详细测试结果见《高天荒野舰艇甲弹对抗公式测试》，可复现程序为《高天荒野舰艇甲弹对抗公式测试.py》。

首轮武器阶梯已经确定为30毫米近防炮、76毫米速射主炮、120毫米滑膛炮、155毫米重型榴弹炮与255毫米超重型榴弹炮；弹丸质量、初速、参考射速、现实依据和项目推导值见《高天荒野舰艇首轮火炮口径与弹道锚点》。普通炮弹第一版二维点质量阻力模型与三层速度—距离测试已经完成，详见《高天荒野舰艇炮弹飞行阻力计算调研》。本子系统不再扩展命中或飞行公式字段；待规范蓝图集成、玩家编辑器往返与教程舰建立后，再以实际命中速度和教程阶段常见钢装甲厚度共同标定各弹种 `P_ref`、局部装甲耐久全局常数和单发装甲损伤。当前实施优先级见《高天荒野舰艇建造与派生性能剩余实施计划》。
