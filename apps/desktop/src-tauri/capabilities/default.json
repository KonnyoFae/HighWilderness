{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "W1 main diagnostic window with no frontend shell or filesystem permission.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
