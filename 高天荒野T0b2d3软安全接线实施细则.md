# T0b.2d3 软安全接线实施细则

> 当前实施基线：`def414c`，d3.1/d3.2 已提交并推送到 `origin/main`。
>
> 状态：d3.3 四阶段及退出门已完成；独立受控黄金、12 场景三次重放、331 步连续/重载矩阵和全回归均已关闭。下一切片为 d4 硬故障与方向切换，实施前先复核资源、状态机和迁移边界。
>
> 范围：离散软保护；不实现 d4 硬故障、紧急断推或方向互锁，不开放正常玩法，不运行正式性能矩阵。

## 一、前置检查与切片顺序

d2b.4 已闭合实际推进场景。d3 不能直接将 c1 判定结果交给现有时间入口：

1. d1 `advance_propulsion_time_boundary` 先提交到期阶段，再应用命令；软保护必须能在升阶提交前否决候选。
2. d1 主机命令只接收六档车钟，而软上限可以是 5%、35% 等任意权威阶段。不能把安全目标量化回车钟，也不能覆盖玩家原命令。
3. c1 纯判定器使用单个通道的标量阶段；真实场景含异步执行器和多个同时到期的通道。后续整舰候选必须保留完整实际输出向量，不能用组内最大阶段代替物理，也不能将分别安全的候选未经合成复核同时提交。

因此在 d3 内分成以下独立验收切片，上一切片验收与经授权的版本检查点完成后再开始下一切片：

| 切片 | 内容 | 退出门 |
| --- | --- | --- |
| d3.1 | 可预览、可否决升阶的纯时间边界；原命令与有效目标分离 | 全阶段、原命令保留、否决/恢复、逐阶降档、精确排程、重放及旧基线回归通过；不接场景 |
| d3.2 | 整舰实际向量候选与定向软 governor | 当前整舰载荷复核、同时到期合成校验、有界降档、滞回、原命令恢复、求值次数及 overg 边界通过；不接场景 |
| d3.3 | 显式新场景策略、持久化、安全事件与独立黄金 | 12 场景三次重放、长序列与重载、武器/毁伤重合边界、旧两套黄金不变；完成后进入 d4 |

该细化不改变主线 d3 → d4 → e1/e2。d3.1 不等于软 governor 已完成或已接线。

## 二、d3.1 冻结规则

- 只新增独立生产模块；旧 d1 入口、engine v2 字段、c1 判定行为、v5 场景和两套黄金保持不变。
- 预览是无副作用值：保存源 engine v2 状态、精确 capability 指纹、边界步号、原命令及到期候选/原生事件。生成候选不等于提交；调用方必须在整舰判定后显式提交。
- 提交重新校验预览与精确时间内核，同时绑定调用方当前状态及当前边界。过期、跨执行器、跨能力或篡改候选拒绝。
- `effective_target_percent` 必须是 22 个权威阶段之一，且不高于原始请求。主机 `commanded_notch` 始终保存原车钟，不能用安全目标代替。
- 到期升阶须显式授权，且候选不得超过当前有效目标。否决时保留原实际输出，有效目标不得高于该输出；从当前阶段重新建立降档或保持排程，不保留过期 due，不产生被否决的阶段事件。
- 已到期的下降阶段和启动完成沿用 d1 顺序；新的有效目标随后从已提交阶段排程。软降档不得瞬时归零，一边界最多一个相邻输出事件。
- 稳定有效目标不重置响应锚点；原车钟变化但有效目标未变也不重置。释放由上层提高有效目标，仍按精确响应时间爬升。
- 本层不求安全载荷、不计油、不修改物理交付、不调用 c1、不产生安全介入/释放事件。`overg` 和硬可用性不能由本层猜测，留给上层明确计算有效目标。
- 预览/提交结果使用新 interface 与独立 schema；解析负责严格形状和局部不变量，带 capability 的重放验证负责精确语义。指纹用于一致性校验，不是安全签名。

## 三、实施与验收记录

已实现独立模块 `高天荒野舰艇受控推进时间边界.py`。只复用 d1 的精确排程原语，不修改旧公开入口；原语依赖通过源文件指纹、全阶段矩阵和 364 个无限制边界的逐状态/事件对照锁定。预览与提交结果分别为 `gaotian.governed-propulsion-time-preview/v1alpha1` 和 `gaotian.governed-propulsion-time-result/v1alpha1`。

验收证据：

- 两类执行器各 22×22，共 968 条完整升降路径，在 `response_time_s=1.1` 下精确到达目标；共 7084 个相邻阶段事件，无跳级。
- 两类执行器全部 21 种相邻升阶分别覆盖保持当前阶段和降至零目标，共 82 条否决路径。否决后没有瞬时掉推、虚假升阶或过期排程。
- 35% 与 5% 有效目标保留主机 `full` 原车钟；35→5 经过 18 步，原命令变化但有效目标不变时保留原响应锚点。
- 冷启动、零时启动、启动取消、启动完成同边界停车与无限制 d1 对照通过；到期升阶与新停车命令同现时，调用方可否决该升阶。
- 226 边界显式授权序列三次一致，其中两次在第 3/96 边界序列化 engine 状态后续跑。规范 trace SHA-256 为 `e4e9d97b1d02b450b3cf489b7fad16ed16c4e29326d155e3b4f111eb46576b1f`。
- 69 个严格负例拒绝非法阶段/类型、跨执行器/能力、过期边界、未授权跳级、坏指纹、字段缺失/额外及可解析但错误的精确排程。
- 两个 schema 的字段及 6 条引用检查通过，两个冷导入顺序通过。未声称使用外部 JSON Schema 引擎验证。

完整回归和实现指纹保存于 `舰艇数据/报告/阶段T0b2d3a受控时间边界接口.v1.json`。本轮只实施 d3.1：不调用 c1，不建立整舰载荷候选，不产生安全事件，不修改 v5 场景或两套黄金。上层必须对整舰同时到期向量作出授权，不能把本模块的单执行器授权当成安全保证。

下一切片唯一为 d3.2。特别需要覆盖：异步同组执行器不能用最大阶段代表整个组；多个分别安全的升阶合并后可能不安全；滞回保持步数只能按一次权威安全判定推进，不能在 opening/closing 重复计数。d3.2 验收及授权检查点完成后才开始 d3.3。

## 四、d3.2 实施前冻结

d3.1 已建立本地检查点 `8ea1438`，本轮不推送。d3.2 只实现无场景副作用的整舰判定和真实载荷采样，以下策略先于代码冻结：

1. 输入包含完整且稳定排序的执行器身份、d3.1 预览、逐执行器显式硬上限、六通道原命令与既有 governor 历史。候选是完整实际阶段向量，不把异步发动机替换成公共阶段，不把安全目标当成已经交付的输出。
2. 每步至少复核一次当前整舰合成载荷。平稳无变化只采样一次；正常同时到期过渡只额外采样一个完整候选。载荷缓存只在本次判定内按完整向量复用，不能跨步保留运动相关结论。
3. 真实下降与启动完成沿用 d3.1。当前载荷失安时不允许升阶；当前安全时，先合成全部符合命令/软/硬上限的到期升阶，再整体判定。联合不安全则整批否决，不进行排列搜索或赋予某通道隐含优先级。被否决通道的上限收紧到本批被拒执行器的最低当前阶段，以使同通道有效目标与各时间提交保持一致。
4. 待提交实际向量仍不安全时，沿规范阶段从高到低搜索：每个执行器的假设阶段为 `min(本机待提交实际阶段, 通道旧上限, 原命令, 本机硬上限, 候选上限)`。最多访问 22 个上限，各候选均重新计算整舰物理，不假设载荷单调。选取这条固定路径上最高的安全上限，不声称是六通道组合空间的全局最优解。
5. 搜索只更新后续响应目标；实际阶段不得瞬降。强制下降引起的失衡、响应滞后或零推力仍不安全必须显式输出残余软风险及“无安全候选”结果，不能伪装成本步已安全。软降档期间仍按步复核，但同向量不重复求值；正常稳态禁止全阶扫描。
6. 结构/乘员阈值与原因排序复用 c1；原车钟不被改写。每通道只在严格递增边界更新一次历史；漏步重置连续释放计数。所有现存有效软原因均满足释放阈值与保持步数后才能释放；释放当步仍不能绕过联合候选检查。只按最终状态发出一个通道事件意图。
7. `overg` 清除软限制，不修改显式硬上限；关闭乘员锁只移除乘员原因。d3.2 不实现跳闸、紧急断推、复位或方向互锁，对向/未停车换向继续拒绝。空油、不可用执行器及部分燃料的实际交付沿用现有 runtime 和积分口径。
8. 真实采样器从精确资源、runtime、模型、运动状态构建本步只读上下文，冻结逐执行器已配平系数，按各自阶段稳定求和，再用原阻力与 `_load_metrics` 公式求值。不得再次乘损伤效率、重新配平、调用连续分配/限幅或执行运动积分。上下文指纹覆盖运动、资源、几何、环境、调谐及本步系数。
9. 新结果只作为严格、可重放的纯边界合同；不写入现有 v5 场景，不改变旧两套黄金。真实场景时序、安全事件包络和安全存档仍留给 d3.3。

## 五、d3.2 实施与验收记录

新增 `高天荒野舰艇整舰推进安全判定.py` 和 `高天荒野舰艇推进向量载荷.py`。前者以完整向量执行一次权威边界的判定与 d3.1 提交，后者从精确资源和 runtime 冻结已经应用效率/可用性/配平的逐机系数。采样复用原载荷公式与独立燃料交付比例，不执行积分；零值聚合探针仅用于复用既有 runtime/use 验证，不是修改真实引擎就绪状态。

四个新合同分别保存输出向量、绑定上下文的载荷样本、通道安全事件意图和完整整舰安全结果。结果包含原输入指纹、当前/提交向量、六通道 governor、各执行器时间提交、实际载荷求值、下降搜索轨迹及残余风险；带原始输入和真实回调可以精确重放。现有 engine/governor 字段和 c1/d1/d3.1 生产模块均未修改。

验收结果：

- 分别安全而联合失安的升阶整批拒绝；分别失安但联合抵消的升阶可以通过。异步同组 `[100, 0] → [100, 2]` 正确触发联合检查，不被组内最大阶段掩盖。
- 合成动态失安的最高安全上限为 30%，实际从 50% 经过 12 步下降至 30%；稳定限幅不重复发出变化事件。非单调夹具找到固定路径上的最高安全上限 35%。零输出仍不安全时返回明确残余原因。
- 最大下降搜索访问 22 个上限；常态每舰一次载荷求值，普通同时到期过渡两次。异常的联合否决、强制下降与下降搜索共同最多允许 25 个去重样本，不假装所有紧急情况都只有两次求值。
- 结构/乘员原因均满足连续释放条件后，第 6 边界释放；实际仍为 25%，第 9 边界才升阶。漏步重置保持计数，关闭乘员锁只删除乘员原因，`overg` 不突破逐执行器硬上限、不复位 tripped。
- 181 边界三次重放一致；第 4/45 边界分别恢复 engine 与 governor 后续跑一致。每次有 6 个介入、6 个释放事件，指纹为 `7711c48d4cfcb96199ffb4af385216fb013f697bab099c5017176077fd10aff2`。这是纯边界重放，不是场景存档证据。
- 三类真实资源共 15 个向量的结构/乘员载荷与现有实际积分诊断完全一致。异步主机得到 `105000 N` 纵向力和 `-475000 N·m` 力矩；损伤、单侧喷口失效、空油、部分燃料、关闭、缺员和断电保持原交付规则。
- 真实无人旗舰全输出得到 60% 安全目标；高推力有人夹具得到 10% 安全目标且同时报告结构/乘员原因。两者实际阶段均未瞬降；overg 只改变软目标。
- 12 场景、224 艘舰、1224 个执行器各做两个纯边界探针，三次结果一致，源资源包不变。`target_20.motion_only` 的稳态/过渡 `_load_metrics` 次数分别为 20/40；没有推进场景、弹丸、毁伤或运动，也不是正式性能运行。
- 69 个严格负例、四个 schema 的字段与 7 条引用、双向冷导入和旧场景不隐式导入新模块检查通过。schema 仍使用严格解析及引用检查，未声称使用外部 JSON Schema 引擎。

完整回归与源文件指纹见 `舰艇数据/报告/阶段T0b2d3b整舰向量安全接口.v1.json`。历史报告只维护实际变更的三份计划文档的顶层实现指纹，旧数值证据与嵌套审计不改。

下一切片唯一为 d3.3：在授权检查点后，为新场景策略确定唯一 governor 求值边界，接入安全事件、完整控制历史、存档重载和独立新黄金。必须继续保留 v5 无软保护技术场景与两套历史黄金，且明确硬故障/互锁仍待 d4。

## 六、d3.3 接线前复盘与冻结决策

本轮基于已推送检查点 `def414c` 只读复核 v5 场景、实际积分诊断、首尾推进边界、d3.1/d3.2 纯接口及 12 个已知场景，没有修改生产代码或黄金。现有 v5 场景策略明确是 `actual-propulsion-unprotected/v1`，单步结果 v4、积分诊断 v1 和存档 v1 也都固定声明软 governor 未接线；因此不能原地替换这些 interface/policy 的含义。

12 个已知 v5 初态共 224 艘舰、1224 个执行器和 1344 个通道 governor。所有 governor 均为未求值、100% 上限、零 revision，所有执行器均无早于或等于场景步的待提交转换。使用 d3.2 真实采样器对 224 艘舰在第 0 边界逐舰初始化，恰有 224 次载荷求值，结构比最大值为 0、乘员过载最大值为 1g，没有限制、残余原因或安全事件。这证明可以由具名初态构建器建立已求值的受保护初态，但不能据此开放任意 v5 存档迁移。

### 6.1 版本和所有权

1. 新路径发布独立的场景 v6、单步结果 v5、受保护场景存档 v1 和受保护积分诊断 v2；v5/v4/旧存档/诊断 v1 继续按原策略运行并保持严格字节语义与黄金结果。
2. 场景 v6 除原 `propulsion_execution` 资源血缘外，必须保存显式的软 governor 执行策略标记，绑定 d3.1 时间策略、d3.2 整舰向量策略和下述闭边界所有权。不能根据 governor 当前是否限幅来猜场景版本。
3. 只允许 12 个指纹锁定初态通过新构建入口建立 v6；运行中 v5 状态和任意旧存档不得自动升级。新存档重载必须重建原完整资源/runtime/模型缓存，并重新校验安全配置和策略血缘。

### 6.2 唯一 governor 边界

受保护场景继续保存“`S_n` 已提交边界 n”的状态，不改成携带到期未提交转换的新语义。每个非退出舰的 governor 在已保存 `S_n` 中必须满足 `last_evaluated_step_index == n`。

1. 构建已知 v6 初态时，以持久 stop 控制、显式中性硬上限和第 0 边界真实载荷执行一次 d3.2 判定，建立 governor 时钟；已审计的 224 艘舰均不会在此产生限制或事件。
2. 开边界 n 只接收/持久化新控制，并用 d3.1 预览/提交在既有软上限和显式中性硬上限下重排有效目标。开边界不得推进滞回、复核整舰载荷或提交到期升阶；源 `S_n` 若仍有到期转换即拒绝。
3. 使用 `S_n` 已提交的实际输出积分 `[n,n+1)`。弹丸命中、持续毁伤、人员、生命周期和武器闭边界处理完成后，用 n+1 的最终实例、同步运动/燃料/船壳、重建 runtime 与模型执行唯一一次 d3.2 整舰判定；它决定到期候选、软目标、governor 历史和 `S_(n+1)`。
4. 下一步开边界不得再次求值 n+1。新控制中的 overg 若在开边界才发生变化，只能影响第一个可能提交实际升阶的闭边界；当既有限幅使尚无升阶排程时，解除限幅可能额外延后一边界，测试和文档须明确，不得暗中双重推进同一步 governor。
5. 已退出舰冻结推进状态、governor 和控制，不再要求安全时钟追随场景；仍在场但交付被生命周期抑制的舰使用同一“零推进交付、保留阻力”的载荷上下文判定，不能以未抑制向量产生假过载。

### 6.3 记录、事件和诊断

1. 旧 `ActualPropulsionBoundaryRecord` 只能用未保护时间内核重放，不能伪装为安全授权记录。新的开边界命令记录必须同时保存源/结果控制、六通道 governor 命令和逐执行器 d3.1 提交，使没有执行器绑定的通道、自动制动改令及“只有 governor 命令变化”的边界同样可审计；开边界不得改变安全 revision、限幅原因或滞回历史。闭边界另存逐舰 d3.2 完整安全决策，并以原输入、精确 runtime/运动上下文重新验证从源状态到结果状态的完整链。
2. 原生发动机 phase/输出事件继续使用执行器级场景事件；d3.2 安全意图是通道级事件，新增独立场景包络，包含 ship、closing、command_channel、前后上限和原因，不虚构 actuator id。排序固定为步号、边界、舰艇、规范通道、事件种类；每舰每通道每边界至多一个最终安全事件。
3. 现有实际积分诊断 v1 固定声明未保护，受保护路径必须发布新诊断版本，绑定本步所使用的源推进状态/governor 指纹，并明确 `soft_governor_status=wired`、`hard_fault_status=unwired`、`direction_interlock_status=unwired`；不得改写旧诊断常量。
4. d3.3 对每个非 tripped 执行器显式传入 100% 中性硬上限。构建器和存档在 d4 前拒绝 tripped 状态；燃料、供电、人员和损伤仍由现有 runtime 交付系数反映，但不冒充硬故障状态机或复位逻辑。

### 6.4 实施次序与退出门

实施仍保持一个 d3.3 切片，但严格按测试先行的四段推进：

1. 先冻结 v6/v5/新存档/诊断/通道事件及两类边界记录的严格正负合同，并证明 v5/v4 原路径无变化。
2. 再实现具名 v6 初态构建、开边界命令提交、闭边界整舰判定、交付抑制采样和逐舰重放验证；修改统一场景前先用无场景适配器覆盖命令、限幅和事件链。
3. 然后接入统一场景及新存档，覆盖首次命令、联合候选拒绝、逐阶降档、滞回释放、overg、乘员锁、退出/坠落、闭边界命中与毁伤同现，以及限幅中和释放候选中的连续/重载续跑一致性。
4. 最后生成独立 `t0-governed-propulsion-step-golden.v1.json`，对 12 场景至少三次重放并分项解释相对 v5 的策略、状态、事件、运动和燃料差异；旧权威黄金和 d2b.4 实际推进黄金必须逐字不变。

退出门为：新增合同严格往返及篡改拒绝；每个非退出舰每个严格递增边界恰一次安全判定；事件无重复且顺序稳定；当前积分请求与源已保护实际向量一致；闭边界判定使用命中/毁伤后的最终 runtime；存档续跑逐步等于连续运行；12 场景三次一致；Python/TypeScript/Rust 全回归通过。正式性能运行、d4 硬故障/互锁、正常玩法和编辑器接入均继续后置。

## 七、d3.3 第一阶段合同与旧版门禁实施记录

本阶段只完成四段顺序中的第一段，没有提前执行场景安全适配。新增轻量 v6 版本标记和独立严格合同模块，冻结场景 v6、单步结果 v5、受控存档 v1、积分诊断 v2、治理标记、开/收边界记录及舰艇/通道级安全事件共八个 schema。治理标记显式绑定 d3.1 时间策略、d3.2 整舰安全策略和“开边界命令、收边界安全”的唯一所有权；诊断 v2 同时绑定源推进状态与六通道 governor 指纹，并继续明确硬故障和方向互锁未接线。

开边界记录保存源/结果控制、完整六通道源 governor 与结果命令、源/结果推进状态指纹以及按执行器稳定排序的全部 d3.1 结果。其合同要求源 governor 已在同一步完成安全提交，且只允许替换命令，不提供修改 safety revision、限幅原因或滞回历史的字段。收边界记录绑定最终 runtime、运动状态、交付状态、乘员安全锁和完整 d3.2 结果；场景安全事件逐项由其最终通道意图导出，固定为 closing，不包含 actuator id。

`TacticalSceneState` 已能严格往返 v6 状态，并要求非退出舰 governor 时钟等于场景步、d4 前拒绝 tripped。受控存档合同只接受 v6、校验内容指纹、控制/governor 链和安全时钟。为避免半接线状态误运行，统一场景推进器和旧实际上下文当前都显式拒绝 v6；v5 携带治理字段、v6 缺少或篡改治理字段、v4/v5 单步字段混用均被拒绝。版本标记单独放在轻量模块中，旧统一场景冷导入不会隐式加载 d3.2 或向量采样器。

专项验证覆盖 8 个 schema、57 条引用、6 类严格合同形状、24 个记录/结果篡改负例和 7 个版本/未接线路径负例；旧 `functional_6.motion_only` v5 初态指纹继续为 `4df89aedc6b51bb913b0d376bc6dfe5177a473b27c232a1c02945d07af63ad3d`，旧 v4 单步仍声明 soft governor `unwired`。下一阶段只实现无场景适配器及逐舰精确重放，不在该适配器验收前解除 v6 推进门禁。

## 八、d3.3 第二阶段无场景适配器实施记录

新增无场景受控推进适配器，独立组合 d3.1、d3.2 和实际推进积分，不导入统一战术场景。适配器先校验精确资源、执行器集合、控制/governor 链、已提交时间状态和单一 governor 时钟；d4 前统一拒绝 tripped，并为每个执行器显式建立 100% 中性硬上限。边界 0 初始化只接受未求值、无限幅、零 revision 的 governor，12 个具名场景 224 艘舰、1224 个执行器和 1344 个 governor 均以恰一次安全判定建立无事件、无限幅初态。

开边界只在 `S_n` 既有软上限下提交新控制和 d3.1 目标排程，拒绝源状态中的到期转换，实际输出、限幅原因、滞回时钟和 safety revision 均不得改变；没有绑定执行器的通道命令仍完整写入六通道审计记录。区间积分由开边界结果状态逐执行器聚合，诊断 v2 绑定该状态及 governor 指纹。首个 full 命令在边界 0 只建立排程，`[0,1)` 与 `[1,2)` 仍使用 0% 实际输出，2% 到期升阶只由收边界 2 提交并供下一积分区间使用。

收边界要求最终同步 runtime、模型和运动状态，且只执行一次 d3.2 整舰判定。交付为 falling 或 uncommanded 时，积分请求归零推进力/力矩/燃料，同时保留原阻力；安全采样也用同一零推进交付口径，但记录仍保留原候选向量。高推力真实资源夹具产生一次通道级介入事件；开边界 overg 不改安全历史，下一收边界才释放并产生一次释放事件。开边界、积分和收边界均提供精确输入重放校验，篡改状态指纹、最终运动指纹、过期时钟、到期未提交状态、退出或 tripped 输入均被拒绝。

专项证据见 `舰艇数据/报告/阶段T0b2d3d无场景受控推进适配器接口.v1.json`。统一场景 v6 门禁仍保持关闭，本阶段没有实现具名 v6 场景构建器、受控存档加载或独立新黄金；下一阶段才消费该适配器接入完整场景时序。

## 九、d3.3 第三阶段统一场景与受控存档实施记录

新增具名 v6 场景构建器，只复用十二个指纹锁定 T0 初态的完整资源链，并在边界 0 对 224 艘舰、1224 个执行器和 1344 个 governor 执行真实初始化。统一场景现在显式区分 v5 未保护路径和 v6 受控路径；v6 每个活动舰在 opening 只接令和重排目标，使用开边界后的实际向量积分，随后在武器、弹丸、毁伤、人员、生命周期与退出处理完成后，以最终同步 runtime、模型和运动执行一次 closing 安全提交。实时路径不为“验证”重复求值 governor；专项计数确认首步 6 艘活动舰恰有 6 次收边界安全判定。

单步结果现在发布 v5 审计包络，分别保存逐舰开边界记录、收边界完整 d3.2 结果、既有执行器级 phase/输出事件和舰艇/通道级安全事件。诊断 v2 绑定开边界结果推进状态与六通道 governor。低阈值场景探针证明两个主机 2% 联合候选可整批否决；overg 在下一收边界释放软上限后不会暗中补跑 opening，首次重新升阶延至边界 5。坠落舰的推进交付与安全采样均归零，源边界已退出舰不产生开/收记录，后续步推进状态、governor 与控制保持冻结。近船壳弹丸命中同一步的 closing 记录绑定命中后 runtime，而积分聚合仍绑定命中前源 runtime。

受控存档只写完整 v6 已提交状态，不保存 runtime、模型或缓存；加载时从具名初态重建全部静态资源和缓存，再校验执行血缘、安全配置、逐舰资源、控制/governor 链和冻结时钟。人为建立的限幅状态及其释放候选边界分别在连续运行和重载续跑中逐步一致，释放事件与完整结果哈希相同。旧统一场景冷导入仍不加载 d3.2 或向量采样器，v5/v4 语义和两套历史黄金未修改。

专项证据见 `舰艇数据/报告/阶段T0b2d3e统一场景受控推进与存档接口.v1.json`。本阶段没有生成独立受控黄金、没有执行正式性能矩阵，也没有把正常玩法或 Web/编辑器切换到 v6。最后阶段将生成 `t0-governed-propulsion-step-golden.v1.json`，完成十二场景三次重放、连续/重载长序列和 Python/TypeScript/Rust 全回归后再关闭 d3.3。

## 十、d3.3 第四阶段独立黄金与完整矩阵完成记录

独立 `contracts/web_bridge/t0-governed-propulsion-step-golden.v1.json` 已生成，固定场景 v6、单步结果 v5、三次重放和十二个具名场景的初态、输入流、资源包、逐步轨迹、最终场景、最终运动以及事件计数哈希。每次重放覆盖 2688 个舰步和 2688 次 closing 安全记录；所有非退出舰的 governor 时钟逐边界严格跟随场景步。默认安全配置下十二个场景均未产生虚假安全事件，运动、燃料和原生执行器推进事件与实际推进 v5 逐项相同；场景/结果哈希因 v6 治理状态、开收边界审计、诊断 v2 和接口版本而有意不同。

独立 331 步序列从合法具名配置与已提交限幅状态起步，连续运行及在第 121、200 边界重载的三条轨迹逐步一致，轨迹哈希为 `dd32904752f35115337f7b7198c6aa6ecedfa20f23293a12d061afafc32dab12`。序列产生一次限幅释放事件和 100 个原生推进事件；第 2 边界提交 2% 实际输出，第 331 边界稳定在 25%，重载不改变控制、governor、燃料、运动、事件或最终场景哈希。

旧权威黄金继续为 `e7e5cf3dd494e5d8390f2aeabfbb4d2e6f4426562901879dbc7268ee68b5f095`，旧实际推进黄金继续为 `6636afd396df0dd1ec17906f4b8fbe0d23d1a88e49d47dae64d1872f259d78ed`；新受控黄金哈希为 `eb253fa94e691505564071d00716104f19443d1ef7f76719d8a49c719f725882`。专项及最终回归证据见 `舰艇数据/报告/阶段T0b2d3f受控黄金与完整矩阵接口.v1.json`。正式性能运行仍为 0，正常玩法、Web/编辑器、d4 硬故障和方向互锁均未提前接入；d3.3 至此关闭。
