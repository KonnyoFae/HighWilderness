# T0b.2d2b 推进力学接线实施细则

> 编排日期：2026-09-03
>
> 审计基线：`973ec4f`，`main`；审计开始时工作区干净，与本地 `origin/main` 跟踪引用一致，本次未重新 fetch。
>
> 状态：`T0b.2d2b.3` 已实现，验收证据见阶段报告。唯一下一切片：`T0b.2d2b.4`；本切片提交与推送按用户授权执行。
>
> 上位计划：《高天荒野T0b2推进响应与权威性能优化规划》第六、二十四、二十五章；本文件细化 d2b，不改变 d3 及其后的主线顺序。

## 一、当前实现与调整结论

已完成 d1 时间内核、d1r 状态合同修复、d2a 资源与控制桥。d2a 已验证 3 类目录/舾装的具名迁移，以及 12 场景、224 艘舰、1224 个推进执行器的精确资源绑定；其中主发动机 328 个、机动喷口 896 个。推进场景仍由 `propulsion_unwired` 阻止进入力学计算，这符合上一阶段边界。

但“资源能绑定、合同能往返”尚不等于“新场景可以正确推进”。本次检查确认资源链、控制语义、事件边界还有接线前置缺口，因此将 d2b 拆为四个独立验收的小切片：

| 顺序 | 切片 | 主要产出 | 边界与退出门 |
| --- | --- | --- | --- |
| 1 | `T0b.2d2b.1` 合同与依赖准备 | 分离平移/转向控制，修正制动路由，冻结首尾事件归属，解除潜在循环依赖 | 严格解析、版本迁移、边界合同和双向冷导入测试通过；不接入力学，保留接线门禁 |
| 2 | `T0b.2d2b.2` 新场景资源链 | 生产侧的显式新场景构建入口，贯通目录到场景实例的完整引用与哈希链 | 12 场景/224 艘舰/1224 执行器全量通过绑定与篡改拒绝；不推进运动 |
| 3 | `T0b.2d2b.3` 实际输出聚合与积分 | 逐执行器实际阶段驱动的纯力/力矩/油耗聚合器，以及接受实际执行量的积分入口 | 单元与合成夹具证明不重复缩放、不隐式软限幅、燃料守恒；不接入统一场景 |
| 4 | `T0b.2d2b.4` 场景接线与新黄金 | 真实固定步推进、推进状态持久化、边界事件、新版本黄金 | 12 场景三次确定性重放、长序列和解释性差异审核通过；旧 12 黄金不变 |

每个切片独立报告、独立回归、独立提交；上一切片验收与版本检查点完成后才开始下一切片。提交/推送按当轮用户授权执行，不跨切片积累未提交代码。规划轮只新增本文；实施轮范围及证据记录在第七节。

## 二、接线前检查证据

以下位置均以 `973ec4f` 为基准。只读探针用于定位问题，尚未替代后续自动化回归用例。

### 2.1 资源迁移尚未贯通完整场景

- `高天荒野T0b2d2a推进资源与控制桥测试.py:143` 的 `d2a_profile_resources` 只构建 v3 目录、v2 舾装和 `CompiledOutfit`，它是测试辅助函数。
- `benchmarks/t0/scenario.py` 仍使用旧资源构建场景；c2b→d1 状态迁移也没有重新编译快照、出航与实例链。检查 12 个迁移场景，首舰仍持有各自旧场景的派生快照哈希。
- 用新舾装编译新派生快照，再搭配旧出航输入，得到 `sortie.outfit_reference_mismatch`。出航编译器在 `高天荒野舰艇出航配置编译器.py:182` 检查精确舾装引用。
- 实例中还保存设计状态、出航哈希和舾装引用；统一场景在 `高天荒野舰艇统一战术场景.py:1531` 检查快照、出航与模型绑定。仅替换目录或哈希字符串不能完成迁移。

结论：1224 个执行器的绑定证据继续有效，但不能作为新场景完整资源链已经就绪的证据。

### 2.2 自动侧向制动与转向通道混用

- `高天荒野舰艇推进资源与控制桥.py:570` 将正侧向速度转换为 `left / stop / 25%`。
- 同文件 `:663` 的绑定逻辑把逆时针转向组也映射到 `left`。
- 最小舰夹具中，侧向速度为 3、纵向速度为 0 时，制动命令命中的左转喷口组净力为 `[0, 0]`，满输出力矩为 `450000 N·m`。按 25% 输出会形成转向力矩，而不是侧向制动力。
- 当前绑定统计中反向主发动机数量为 0。即使发出了反向命令，也不能假定存在反向制动力。

结论：必须显式区分主发动机平移方向与机动喷口转向方向，并对缺少相应推力能力给出可观测结果。

### 2.3 严格输入检查存在类型漏洞

`DirectionPropulsionCommand.__post_init__` 在 `高天荒野舰艇推进资源与控制桥.py:349` 附近仅通过集合成员关系检查阶段值。只读探针确认 `DirectionPropulsionCommand("forward", "stop", False)` 被接受并序列化为布尔值；原因是 Python 中 `False == 0`。

结论：整数阶段必须显式排除布尔与非整型输入；迁移量化入口也应明确拒绝非有限数，不能依赖截断或夹紧吞掉非法输入。

### 2.4 当前结果合同没有覆盖真实单步的两个边界

- 场景在 `高天荒野舰艇统一战术场景.py:2586` 处理源步边界 n，在 `:2892` 处理结束边界 n+1。
- `高天荒野舰艇推进资源与控制桥.py:808` 的结果封装却要求全部推进事件的步号等于结果场景步号。
- d2a 的结果测试在 `高天荒野T0b2d2a推进资源与控制桥测试.py:473` 附近，将未推进的初始场景与同一步号事件封装在一起，尚未覆盖真实 n→n+1。
- 场景内部检查不允许保存已到期却未提交的推进转换；当前旧结果场景构造也没有传递新增的推进安全配置引用与哈希。

结论：接线前要冻结首尾边界归属、实际阶段的生效区间及结果场景字段保留规则。

### 2.5 聚合和集成的结构性风险

- 控制桥在 `高天荒野舰艇推进资源与控制桥.py:36` 附近直接导入统一场景。若统一场景反向导入控制桥，会形成循环依赖。
- runtime 的 `_scaled_actuator` 在 `高天荒野舰艇运行时参数编译器.py:1071` 已将效率应用到推力、力矩和油耗；新的聚合器不能再次施加同一效率因子。
- `高天荒野舰艇战术机动求解器.py:853` 的旧积分入口会调用连续分配器和 `_choose_command_scale`，再叠加燃料比例。把离散实际阶段直接塞回此入口，会重新引入未声明的软限幅。

结论：先整理无环依赖，再建立实际执行量入口；沿用经验证的积分公式，不沿用旧连续控制分配和隐藏软限幅。

## 三、各切片实施清单

### 3.1 d2b.1：先冻结可接线的合同

1. 将资源/控制/事件基础合同置于场景下层；场景结果封装留在场景相关层或独立适配层，避免基础桥反向依赖场景。保持旧有效载荷的既有序列化行为。
2. 新控制模型分别表达主发动机平移车钟 `forward/reverse/left/right` 与机动喷口转向目标 `counterclockwise/clockwise`；绑定必须保留执行器类别和明确的物理用途。禁止再用裸 `left/right` 同时表示两类物理意图。
3. 审核控制、执行器绑定、governor、推进事件及组合状态中所有通道引用。涉及字段集合或语义变化的合同同步升级，并提供明确来源的迁移；不只改控制 DTO 而留下旧状态歧义。独立 engine 时间状态无变化时不升级。
4. 自动线性制动只请求与线速度相反的平移发动机组，有能力时请求 `quarter`。没有对应能力时实际推力为 0，输出缺失轴诊断；被动阻力仍正常计算。侧向速度不得触发转向喷口。角速度制动若后续引入，另设独立意图和测试。
5. 为旧连续控制→新控制、d2a 控制→新控制建立具名迁移。主车钟与机动输出的已有量化选择保持可追溯；不能将历史 `wheel` 迁移描述为物理等价。
6. 冻结真实 n→n+1 的结果合同，至少显式记录源步、结果步和可验证的首尾事件归属；更新 schema、策略版本及排序/去重规则。
7. 加入严格类型、非有限数、未知键、缺键、重复通道、错误引用、跨舰事件及未知版本负例。新路径在 d4 之前拒绝同轴对向同时请求，动态反向切换明确为尚未支持，不伪造互锁实现。

建议控制采用下一个独立 alpha 版本（当前为 v1），结果采用下一个独立 alpha 版本（当前为 v2）；具体完整 interface id 与受影响的组合状态版本在本切片统一登记并测试。若新场景执行策略发生变化，也必须有明确版本标识，不能只凭“带有推进状态”就切换语义。

退出门：新合同与迁移测试、两个导入顺序的全新 Python 子进程测试、旧黄金及完整回归通过；统一场景仍不能执行新力学。此阶段修复合同和路由缺陷，不改变旧路径运动。

### 3.2 d2b.2：建立完整的新资源与场景入口

1. 将新资源构建能力放入生产模块，测试引用它；生产代码不得依赖 d2a 测试辅助函数。
2. 完整构建：目录 v3 → 舾装 v2 → 派生快照 → 精确引用新舾装的出航配置 → 含正确设计状态的实例 → 场景绑定和静态/runtime 缓存。
3. 新入口使用明确版本和已知初始夹具来源。旧场景构建器和旧黄金保持不变；不把任意进行中的旧存档作为可自动迁移对象。
4. 保留原始编成、模块布局、人员、燃料、弹药、武器队列及脚本毁伤配置；所有有意变化仅限已声明的推进资源、控制与状态合同。新初始推进状态必须与模块模式、源步和新能力一致。
5. 核对三类资源、12 个场景、224 艘舰、1224 个执行器的完整 lineage，分别验证目录、舾装、快照、出航、实例、场景任一环节被篡改都会拒绝。
6. 明确缺少反向/侧向平移能力的场景诊断；不为测试通过增添发动机或调整布局。

退出门：完整新资源链可重复构建、严格往返、三次哈希一致；新路径仍不推进场景；旧 12 黄金不变。

### 3.3 d2b.3：纯聚合与实际执行量积分

1. 输入为精确静态绑定、当前 runtime 的可用执行器及配平 use、逐执行器实际离散阶段。静态绑定保留全部执行器身份；runtime 不可用执行器交付 0，不删除其时间状态身份。
2. 固定运算顺序：runtime 效率/可用性 → 对应方向的配平 use → 每个执行器自身实际输出比例 → 稳定求和。实际阶段只乘一次，损伤/人员/供电效率也只应用一次。
3. 不把整组最大能力乘某一个公共阶段；不在实际输出之后重新配平以消除本应出现的不对称力矩。同一个执行器被多个意图引用时必须有明确冲突/合并规则，禁止重复计力与计油。
4. 新增接受实际力、力矩和耗油请求的积分入口。保持既有阻力、质量/惯量、单位调谐、运动积分、层级转换和结构过载损伤公式，避免顺带重写物理。
5. 新路径不调用连续分配器或 `_choose_command_scale`；用调用拦截断言验证，而不只比较结果数值。
6. 先将下节过渡安全/燃料政策写入测试，再实现相应计算。

退出门：零输出、2%/5%/100%、异步阶段、非对称效率、单侧失效、空油和末步部分燃料测试通过；诊断可解释每一级缩放与最终交付量；不接入统一场景。

### 3.4 d2b.4：接线、状态持久化与新黄金

1. 只对显式的新版本完整资源链开放推进；不能全局删除 `propulsion_unwired`，也不能自动让历史 c2b/d1 场景使用新力学。
2. 按冻结的首尾边界语义推进 engine 内核，计算实际力/力矩/油耗，接入统一场景的运动、实例回写与既有武器/命中时间线。
3. 结果必须保留每舰推进状态、配置引用与哈希、资源绑定、已提交时间锚点；存档重载后续跑与不间断运行一致。
4. 推进事件保留真实发生步号，稳定排序并去重；没有新增推进事件时仍须完整保留其他场景事件。
5. 为 12 场景创建独立版本的新黄金，并增加冷启动、同向改令、停车和恢复的长序列。覆盖至少 121 步的启动/响应边界，并用超过 260 步的组合序列覆盖多次目标变化。
6. 三次重放比较每步推进状态、事件顺序、燃料、运动以及最终哈希；单独做连续运行与中途存档重载的等价比较。
7. 新旧差异分项解释后再冻结黄金，不能只记录“hash 有意变化”。

退出门：完整回归通过、旧 12 黄金不变、新 12 黄金及长序列三次重放一致，报告明确标注软 governor/硬故障仍未接线。到此只完成 d2b 的技术基线，不宣称正式玩法、安全系统或 T0 性能验收完成。

## 四、需先写入测试的接线政策

### 4.1 时间边界与实际输出生效

采用以下已由 d2b.1 纯时间及结果合同测试冻结的约定：`S_n` 为已提交边界状态；接收 n 时刻命令，提交相应边界事件；使用此时实际输出积分 `[n,n+1)`；最后提交 n+1 到期转换，保存 `S_(n+1)`。与武器/毁伤等系统的实际调度仍在 d2b.4 接线时验证。

- n+1 才升到的新阶段只影响下一个积分区间，不追溯修改刚结束的一步。
- 一次结果允许包含 n 的命令边界事件和 n+1 的到期事件，分别验证来源；不得为了通过旧结果检查而改写事件步号。
- 下一步开头不得重复发出上一结果已提交的到期事件。同期武器事件、runtime 更新、推进转换和运动使用的状态次序需要固定并有冲突边界测试。
- 对启动 60 步、响应 60 步的冷机夹具，检查 0、59、60、61、119、120、121 等边界；仍沿用 d1 的 `0、2、5……100` 阶段排程规则。
- 有源/结果步歧义的旧结果不能自动迁移为新结果。保留旧合同既有处理能力，必要时拒绝无法确定来源的迁移。

### 4.2 d2b 的过渡安全与燃料政策

d2b 新路径仅用于显式的力学技术夹具，软 governor 尚未接线，诊断必须如实标记，不能把 `overg=false` 解释为软安全已经得到保证。正常玩法入口不在此阶段切换；旧路径软安全行为不变。

- 保留现有 runtime 对燃料、供电、人员和损伤可用性的约束；不可用执行器不得交付推力。
- 单独公开末步燃料交付比例 `fuel_delivery_fraction`：请求耗油量大于 0 时取 `min(1, 可用燃料 / 本步请求耗油量)`，否则取 1。相同比例应用于本步交付推力、力矩和耗油，保证不透支燃料。
- 指令目标、离散实际阶段与资源约束后的交付量分别可观测；燃料比例不得伪装成新的离散阶段，也不得与软安全比例混为一个隐式 scale。
- 断油/断电导致的跳闸状态、紧急断推、恢复、重启及方向切换互锁事件继续留在 d4。d2b 的零交付不代表这些状态机已经完成。
- 遇到需要动态反向互锁才能正确执行的输入，显式拒绝/报告未支持，不以同轴对向同时推力继续计算。

### 4.3 新旧黄金差异解释

至少区分四类来源：资源版本与初始状态、控制意图迁移、启动/响应阶段、实际力/力矩/油耗及其后续运动和命中影响。

尤其要写清：旧前进输入 `0.1` 迁移为 `dead_slow`（2% 主车钟），不再等于旧 10% 连续主推力；旧 `wheel=±0.05` 是连续求解器的目标转向语义，迁移后的 5% 机动输出并不与之物理等价。不能把这两类变化统称为“响应延迟造成的差异”。

## 五、验证、报告与停止条件

### 5.1 规划轮已经执行的检查

- 工作区/提交检查：基线 `973ec4f`，审计前干净。
- d2a 定向测试通过，输出 `actuator_bindings=1224`、`authority_golden=12_of_12_PASS`。
- 完成第二节所列资源链、类型与制动路由只读探针，以及事件/依赖/缩放路径代码检查。
- 本次未执行全套回归、正式性能跑或新的力学黄金。d2a 历史报告中的 Python 68/68、TypeScript 4/4、Rust 15/15 是上一实施轮证据，不作为本轮全量复测结果。

### 5.2 每个实施切片的最低验证

在仓库根目录运行定向测试及 Python 总入口；新增切片测试按现有 `*测试.py` 命名加入自动发现。桌面测试使用既有依赖，不为测试升级工具链。

```powershell
python -X utf8 '高天荒野T0b2d2a推进资源与控制桥测试.py'
python -X utf8 '高天荒野舰艇测试总入口.py'
npm --prefix apps/desktop run check
cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml --locked
```

若当前全局 npm 包装器或 Cargo PATH 仍不可用，使用本机已安装运行时的显式路径与项目本地测试入口，记录实际命令；不把环境失败记为测试通过。每个报告记录实际测试数，新增测试后不机械沿用 68/4/15。

每个切片还需记录：实现基线、合同/策略版本、具名迁移范围、旧黄金结果、新增测试结果、是否推进新场景、尚未接线项和唯一下一切片。d2b.4 才能生成本阶段的新运动黄金。

### 5.3 停止门

- 旧等价路径 hash 改变：先定位原因，不更新旧黄金消除差异。
- 任一资源环节无法精确重建、未知来源无法迁移：停止该迁移，不关闭校验或补写哈希。
- 同一合同 id 需要承载不同严格字段或控制语义：先完成版本升级，不能暂时双解读。
- 时间状态、事件、交付力、燃料或三次重放不一致：不冻结新黄金，不进入下一切片。
- 若需要开放正常玩法、接入软 governor、硬故障或方向互锁才可继续：回到计划边界报告，不把 d3/d4 混入 d2b。

规划轮将本细则单独落盘，未改写原实现、主计划及阶段报告。实施文件变化触发仓库现有报告哈希检查时，必须先验证行为断言，再按原有规则单独审计和维护受影响记录；不得仅刷新哈希掩盖回归，也不顺带重构报告系统。

## 六、d2b 之后的顺序

`d2b.1 → d2b.2 → d2b.3 → d2b.4 → d3 → d4 → e1/e2 → f → g → T0b.3/T0c`

- d3：离散软限幅、自动降档、滞回释放、原车钟恢复，验证 `overg` 只绕过软保护。
- d4：硬故障、紧急断推、恢复与方向切换互锁，形成完整的新权威基线。
- e1：在 d4 基线上优先评估 Python 弹丸批处理；e2：单独开展场景事件驱动与多速率实验。两者都不能提前挤入推进接线。
- f/g：以同一负载的正确性、调用数、P95/最大步耗时与资源数据决定后续路线，不提前决定 Rust 重写。
- T0b.3/T0c：按决策恢复正式 T0 验证和后续编辑器工作；短程诊断或本阶段回归不代替正式性能矩阵。

## 七、d2b.1 实施记录

2026-09-03 基于 `973ec4f` 完成合同、路由、事件边界和依赖准备。发布版本如下：控制 v2、执行器绑定 v2、governor v2、推进组合状态 v3、合同专用场景 v4、推进事件 v2、带边界归属的场景推进事件 v2、单步结果 v3；engine 状态继续使用 d1 v2。

六通道统一采用 `translation.forward/reverse/left/right` 与 `yaw.counterclockwise/clockwise`。旧版本保持既有整数载荷读写；新控制拒绝布尔/浮点阶段、混合用途、重复通道和同轴对向请求。动态反向切换仍返回未接线错误，不冒充 d4 互锁。已知 d2a 输入中的 forward 10% 机动字段没有执行器绑定用途，具名迁移明确退役，不将它改成转向输出。

完成 224 条已知旧控制迁移、1224 个绑定的用途拆分、12 个指纹锁定初始场景的状态合同迁移和严格往返。新场景仍引用原快照/出航链，此项仅作为合同夹具，不计为 d2b.2 完成。自动侧向制动只选平移主机；无对应能力时零请求并返回缺失通道。旧自动制动因缺少速度上下文，拒绝自动迁移。

首尾结果已覆盖真实 0→1 步号、不同边界事件、源/结果指纹与事件变化链；独立内核的 281 步三次重放证明 closing 升档不会提前作用于刚结束的区间。八份新 schema 做 id、形状和本地引用检查，领域不变量由 Python 严格解析及反例测试验证；未新增 JSON Schema 引擎依赖。

回归最终结果及历史报告源码指纹维护明细见 `舰艇数据/报告/阶段T0b2d2b1定向控制与边界合同接口.v1.json`。未接新力学、软 governor、硬故障或方向互锁，未改旧黄金、未开展正式性能跑。

下一轮范围唯一为 d2b.2：完整资源链及明确版本的新场景构建入口；本切片完成验收和经授权的版本检查点后再进入，不提前实施 d2b.3/4。

## 八、d2b.2 实施记录

2026-09-03 先经授权将 d2b.1 建立本地检查点 `04e27f6`（未推送），再独立实施本切片。生产模块 `高天荒野舰艇推进场景构建器.py` 接收已知 d1 初态和旧绑定，不依赖测试辅助函数或基准生成器；通过目录/舾装具名迁移重新编译三类完整资源链，再重新初始化含内嵌设计的新实例。出航和实例版本均升为 v2，原 id 保留，所有新引用与内容指纹严格对应；不接受任意存档的“补哈希”迁移。

新增 `prepare_tactical_scene_bindings` 只做严格验证与缓存重建，不推进时间线。新入口显式保留原始场景内容、编成、布局和实例运行字段，并核对推进初态与模块模式、能力、源步一致。缺少 reverse/left/right 平移能力继续公开诊断。旧生成器、资源文件和十二份旧黄金不修改。

资源包 interface 为 `gaotian.directional-scene-resource-bundle/v1alpha1`，policy 为 `gaotian.directional-scene/known-initial-resource-chain/v1`，具名迁移 id 为 `gtw.migration.propulsion.d1-initial-to-directional-resource-chain`。保存的是可审计资源包，重载需要明确提供来源场景、绑定及资源目录并重新编译；它不是通用场景存档。schema 声明边界、已有资源引用及新绑定形状；编译对象全字段等价和跨资源不变量由严格上下文加载器验证，不声称 schema 单独可验证派生量。

验收记录见 `舰艇数据/报告/阶段T0b2d2b2完整推进资源链接口.v1.json`，覆盖三类资源、12 场景、224 艘舰、1224 执行器、三次构建同 hash、场景和资源包严格往返、逐链篡改/旧缓存拒绝及冷导入隔离。新力学推进步数为 0，`propulsion_unwired` 保持，未运行正式性能矩阵。

下一步唯一为 d2b.3：先冻结实际输出聚合、缩放顺序与燃料交付测试，再实现纯聚合器和独立积分入口，不接入统一场景。进入前仍需本切片完成回归和经授权的独立提交，不跨切片累积修改。

## 九、d2b.3 实施记录

先经授权将 d2b.2 建立本地检查点 `9ab2e9a`（未推送），再实施本切片。测试先冻结零/2%/5%/100%、非对称阶段、效率与配平顺序、部分燃料及旧积分公式等价边界。新增下层实际执行量合同和无场景依赖的 `aggregate_actual_propulsion`：精确静态绑定保留全部执行器；读取 runtime 已完成可用性、效率和配平的逐执行器 use，再各乘一次实际阶段，稳定求和。目标阶段只作诊断，不能代替实际阶段。每执行器仅允许唯一物理意图，重复 use、错误来源及对向实际/目标请求均拒绝，未实现 d4 互锁。

新增 `integrate_actual_tactical_step` 接受绑定当前 runtime 和源步的实际力、力矩及耗油请求，不调用连续控制分配器或 `_choose_command_scale`。新旧入口共享原有半隐式积分、阻力、单位调谐、层级转换和结构过载毁伤公式；旧入口仍保留自己的分配与软限幅语义。新诊断明确标记 soft governor、hard fault、direction interlock 均为 unwired，不把乘员锁或 OverG 解释为新路径安全保证。燃料交付比例按本节冻结公式计算，同一比例施加到力、力矩和耗油，耗尽后保留时间状态身份、交付归零；运行字段变化后调用者必须重新编译 runtime。

新增实际执行量请求、实际推进聚合结果及实际积分诊断三个 v1alpha1 合同和 schema。请求严格解析；聚合和诊断是领域计算输出，schema 检查形状和本地引用，跨来源/时间、配平及物理数值关系由 Python 回归验证。本轮未增加 JSON Schema 引擎依赖。缺员/断电使用独立合成目录夹具验证：原夹具的油门本来自动化，不改写其缺员行为，也不新增正式推进供电类别或修改落盘资源。

覆盖全部 12 场景、224 艘舰和 1224 执行器的纯输入；另有四步燃料耗尽序列三次重放、非对称力矩/侧向残余力、单侧失效、布尔/非有限数、过期模型与步号、重复 use 和错误资源负例。统一场景本身推进步数仍为 0，`propulsion_unwired` 保持，旧 12 黄金不变。最终回归及历史报告指纹维护见 `舰艇数据/报告/阶段T0b2d2b3实际推进聚合与积分接口.v1.json`。

下一步唯一为 d2b.4：经本切片验收及授权版本检查点后，只在显式新资源链上接首尾 engine 边界与实际力学，保留旧路径门禁，建立解释性新黄金和存档连续性回归。软 governor、硬故障和动态方向互锁仍不在 d2b.4 实施范围内。
