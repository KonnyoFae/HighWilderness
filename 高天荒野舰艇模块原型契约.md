# 《高天荒野》舰艇模块原型契约

新增型号、已有型号改版及规则调整统一参考[模块制作与战斗接入 Checklist](./高天荒野模块制作与战斗接入检查清单.md)，记录从编辑安装到真实战斗和存档往返的接入证据。

## 一、当前边界

阶段 D/E 已经实现 `ModulePrototypeCatalog`、十三类最小模块原型、`OutfitPlan`、首版无界面舾装编译、`SortieConfiguration`、`ShipInstanceSnapshot` 与运行时参数派生。当前目录、舾装方案、出航配置与舰艇实例仍是 `contract_fixture`，只验证数据结构、安装关系和派生链，不是正式模块内容，也不提供教程舰平衡参数。

基础目录实现货仓、CIC、灵烷贮槽、主发动机、侧挂转向发动机、发电设备、损管设备、人员舱和遥控核心舱；E5 扩展目录实现武器、弹药库、火控和传感器。救生与航空设施仍在后续内容阶段扩展。

## 二、锚点与半格坐标

船壳安装格仍是 `5m×5m`。模块原型不保存整舰绝对位置，只保存相对模块锚点的格心偏移：

`physical_offset_m = half_cell_offset × 2.5m`

例如一个单格轮廓为 `[[0,0]]`；横向二格轮廓为 `[[-1,0],[1,0]]`。这样二格轮廓的中心自然位于两个格心之间，不需要伪造某个中心格。轮廓包围盒中心必须为 `[0,0]`。

`OutfitPlan` 同样以全舰半格坐标保存栅格模块锚点。内部或顶挂轮廓变换到船壳格后，实际占用格心必须落在船壳编译器输出的完整五米格上；旋转定义为从局部 `+Y` 朝舰艏起算的顺时针 `0/90/180/270°`。

侧挂轮廓使用所属船壳边的局部坐标：一轴沿边切向，一轴沿边法向。它不与内部/顶挂轮廓强制共享全局半格奇偶性。主发动机的内部本体与尾焰净空、转向发动机的侧挂本体与尾焰净空则必须在各自局部框架中保持一致。

## 三、安装几何

每个原型能够分别保存：

- 连续跨层数和每层相同的内部二维轮廓；
- 顶挂本体、顶挂层偏移与顶挂净空；
- 单边侧挂步长、侧挂外形与侧挂净空；
- 主发动机或转向发动机尾焰净空；
- `0/90/180/270` 允许旋转集合；
- 任意层、仅基底层或局部露天甲板安装规则；
- 提供的嵌入槽位或需要的宿主槽位。

嵌入模块不直接占用船壳格。历史目录中 CIC 提供 `cic_internal`，遥控核心舱 v1 嵌入该槽位；舾装编译器将其质量归到宿主 CIC 锚点，并拒绝一个槽位被多个实例占用。普通 CIC＋v1 遥控核心的历史组合继续按精确版本解释；新安装规则见下文。

### SCIC 与核心舱宿主规则（2026-09-20，6a）

正式名称为 **SCIC（Stem CIC）**。SCIC 在舰体原点替代普通 CIC，兼具全舰核心与舰队指挥职责，旗舰无需同时安装两套；普通舰仍可使用 CIC。SCIC 型号提供旗舰以外的随伴舰容量，首批基础 3 艘、高级 8 艘，各有有人/无人化变体，含旗舰共 4～9 艘，敌我规则相同。核心损毁沿用既有 CIC 坠毁语义，舰队后续指挥与结算另随工作包 6 接通。

SCIC 沿用 `cic` 类别，`capability.fleet_companion_capacity` 表示随伴上限，普通 CIC 省略该字段。SCIC 同时提供 `cic_internal` 和 `scic_internal`；遥控核心 v2 使用后者，v1 仅为历史兼容保留前者，二者不得叠装占用两个名额。新安装或更换遥控核心宿主仅允许 SCIC；现有“无人化 CIC”是自动化变体，不等于嵌入的“遥控核心舱”。本轮受限设备为遥控核心舱。旧 CIC＋核心舱可保留读取、单舰出战；多舰旗舰须由玩家在编辑器明确改装。容量只取指定旗舰，不叠加其他 SCIC。换旗舰后超额仍保留编队，但不能入战。新安装校验、旧设计兼容、编队容量、战后保存须同步验证。SCIC 随伴容量与战略分舰队控制带宽、指挥机/数据链功能分别处理。

完整决策、待细化边界和实施顺序见[SCIC 与舰队编制容量](./contracts/web_bridge/tactical-scic-fleet-capacity-design.md)；接入检查见[Checklist T8/T9 与 S1～S5](./高天荒野模块制作与战斗接入检查清单.md)。6a 已接入模块目录、安装与编队资格；见[实施及验证记录](./contracts/web_bridge/tactical-stage6a-scic.md)。

### 跨层部件的安装基准与画布操作

2026-09-17 VLS 安装修正：必须分别说明持久化的内部起始甲板、顶部所在甲板，以及玩家点击的画布甲板。不能因为存档从底层向上描述模块，就要求玩家在露天甲板点击后继续向上寻找空间。

- 数据仍保存最下层锚点。内部占用为 `base_level` 至 `base_level + internal_deck_span - 1`；顶部位于 `base_level + top_deck_offset`。
- 有顶挂部分的跨层部件，在画布添加或点选新位置时，点击顶部所在的局部露天甲板；提交前换算 `base_level = clicked_level - top_deck_offset`。甲板通过层级查找，不能对甲板 ID 或列表下标做加减。
- 当前 VLS 的内部跨层数为 2、顶部偏移为 1：点击第 N 层露天甲板，内部占第 N−1、N 层，顶部占第 N 层外部格。局部露天甲板与全舰最高甲板都使用同一规则；不能在只有一层内部空间的船壳上放置该型号。
- 每个实际内部占格、顶部露天格和相应占用冲突都要检查。预览与提交使用相同的层级换算；不得通过取消越界、露天或重叠检查来修正操作问题。缺少下层甲板时直接说明空间不足。
- 从任一占用甲板拖动已有部件只改变平面位置，保持原内部起始甲板；精确编辑明确填写内部起始甲板。保存、重开、撤销和重做不得再次偏移层级。

此次故障中，预先写好底层甲板的 VLS 测试舰可以合法编译，但画布把玩家点击的露天甲板直接当作底层：局部露天区域报“内部格不可用”，最高甲板报“跨层超出船壳”。因此，新型号必须通过真实目录选取、画布放置和保存重开验证，不能以导入预制舰成功替代。检查项见 [Checklist B6–B8](./高天荒野模块制作与战斗接入检查清单.md#b-编辑器与安装)，本次证据见 [5c 安装修正记录](./contracts/web_bridge/tactical-stage5c-missile-logistics.md#vls-画布安装修正2026-09-17)。

### 安装空间与发射方向限制分别判定

2026-09-17 VLS 射界修正：安装几何合法不等于所有武器都使用炮塔射界。按发射器明确的类型选择工作规则，不能仅因模块类别是“武器”、有内部占位或顶部外形就套用水平射线。

- 普通炮塔与炮塔式导弹发射器继续检查水平转角、瞄准和上层舰体遮挡。
- VLS 是垂直发射设备，不计算炮塔水平净空角度、回转时间或水平禁射扇区。其内部起始层上方存在自身经过的甲板是正常安装关系，不能据此判成 360° 禁射；也不能用“改从最顶层算水平射界”代替正确区分发射方式。
- VLS 仍须满足内部占格、顶部出口露天、空间不重叠等安装要求；实际发射仍须满足设备可用、库存、装填、冷却、射程、作用层及适用目标情报条件。取消水平射界不会豁免这些条件。
- 编辑器及运行时使用同一份精确型号、版本和内容绑定的 `launcher_kind`。当前来源是导弹准备政策，保留旧原型指纹和存档；未知导弹发射方式应显示尚未接入，不擅自当成炮塔或垂发。
- 垂发界面显示“垂直发射，不受水平射界限制”，不显示炮塔射界开关、红色禁射圈或水平遮挡投影。验证须同时覆盖真实界面和不同方位的实际发射，不能只隐藏红圈而保留错误运行条件。

### 探测视界与弹药容量校准

2026-09-17：火控雷达、高容量火控雷达、红外搜索修正版（同型号版本 2）取消周围八格硬性净空，保留一个露天外部占格。采用与炮塔相同的上层船壳射线/角度遮挡；探测盲区限制发现与跟踪，不导致整部设备停机。编辑器预览和战斗使用相同几何，切线归入盲区。旧版原型和存档保留，编辑器提供可撤销的显式设备升级。

弹药资源初值为 10 升/点；标准弹药库有效仓容 100 立方米，即 10,000 点。30 毫米近防炮待发弹仓为 2,000 发，每 50 发消耗 1 点，完整装填消耗 40 点。50/75/120 毫米每点分别装填 30/8/1 发；50 和 75 毫米的待发弹仓及单批装填量同步为 30、8 发。资源容量与待发发数分别记录，不能混为一个弹仓；特殊材料另按配方扣除。这些是已确认的游戏初值，不是现实装备规格。

新准备政策对已知旧标准弹药库和 30/50/75 毫米炮采用具名版本升级，保留安装位置、武器组及其他性能；历史交战仍按保存的旧政策恢复，不静默换算、补满或覆写库存。未列入升级的原型必须保持精确版本，不能仅凭 ID 取目录中的第一个版本。实现和验证见[修正记录](./contracts/web_bridge/tactical-sensor-ammunition-revision.md)。

## 四、物理、供电与人员

模块质量使用 `kg`，推力使用 `N`，功率使用 `kW`，响应时间使用 `s`。模块耐久仍是游戏点数。外露模块的 `base_external_rcs_m2=null` 表示尚未完成教程雷达联合标定，而不是零 RCS。

供电保存发电量、待机负载、工作负载与稳定 `consumer_category`。当前类别为损管、武器与主动防御、火控、传感器，默认顺序分别显示为 `P0～P3`；P0～P3不再写进模块原型。存在工作负载时必须指定类别，损管工作负载固定属于 `damage_control`。模块同时保存默认工作模式和自动激活事件；供电不足后的整类跳闸与安全供电模式属于舾装派生/运行时规则，不在原型内手填整舰结果。

船员按军官、技术官、飞行员、资深损管和普通船员分别保存最低工作人数与标准人数。只有 CIC、推进、发电和损管类别计入最低出航船员；全部模块需求都计入标准船员。完全无人化模块的标准船员必须为零。

部分自动化必须列出具体自动化子功能。普通原型可以精确引用同类别的完全无人改进版，并同时保存工程微机团块成本；目录会拒绝失效引用、跨类别引用或并非完全无人化的目标。

每个模块原型还必须按实际子功能声明 `damage_responses`，而不是由运行时按模块类别套用一条全局曲线。当前支持三种模型：`binary_until_destroyed` 表示耐久大于零时保持完整功能、归零时失效；`piecewise_linear` 在严格递增的耐久—输出节点之间线性插值；`stepwise` 按节点形成阶梯输出。分段模型必须从 `(0,0)` 开始并以 `(1,1)` 结束，输出不得随耐久增加而下降。自动化只豁免人员需求，不能豁免战损倍率。

货仓的 `bulk_cargo_capacity_kg` 是它能够保存的大宗货物质量上限，不是静态模块质量的一部分。出航配置中的初始大宗货物和舰艇实例中的当前大宗货物都必须绑定本舰一个实际货仓实例；当前惯量按该货仓锚点计算。人员、燃料、武器已装弹药和已装导弹不计舰艇质量，不能借货仓字段重复计入。

## 五、E5 最小战斗系统契约

战斗系统接口为 `gaotian.combat-system-modules/v1alpha1`。武器原型保存武器类别、目标域、兼容弹药标识、待发容量、最小/最大射程和火控需求；弹药库保存容量、兼容弹药标识，并固定使用 `ship_shared` 全舰共享池语义；火控保存支持的解算/持续制导需求、并行通道和最大锁定距离；传感器保存雷达/红外/光电通道、工作模式与仪表距离。

这些字段只建立依赖接口，不在 E5 计算弹道、命中或毁伤，也不把夹具中的距离和功率当作正式内容。I1 已由 `ShipInstanceSnapshot.ammunition_state` 保存实际弹药库库存与武器待发弹，并由 `gaotian.ammunition-actions/v1alpha1` 结算开火和装填；I4 又由可选 `weapon_timeline_state` 与 `gaotian.weapon-action-timeline/v1alpha1` 接入连续射击、逐发装填和齐射时间。I5 不回填 E5 武器原型中尚未标定的弹种值，而是通过独立且带内容指纹的 `ProjectileProfileCatalog` 将成功开火事件转换为弹丸实体，再按真实模块中心、船壳边、装甲材质/厚度与模块占格处理命中和损伤回写。I6 只负责编排固定步机动、边界开火、弹丸命中和战损后重编译；I7 再读取运行时的实际船壳、CIC、升力和遥控能力派生生命周期；I8 的阵营、舰队和距离边界属于场景状态；I9 的旗舰/普通舰角色、唯一直控对象和命令意图属于独立指挥状态；I10a/I10b 由弹丸内可选状态和显式观测事实实现制导；I11a/I11b 由舰艇实例可选状态实现持久火灾、损管与人员伤亡回写；I11c 再以独立人员操作和救援清单合同实现医疗、舰间转移与弃舰救援；I11d 以独立二次毁伤结果合同绑定实际火灾事件和实际弹药库存；I12a 再读取现有传感器频道/模式/仪表距离、火控支持项/锁定距离/并行通道及其实际运行时效率，生成雷达辐射、火控支持和逐弹丸制导事实，不把观测或航迹状态塞回模块原型。以上各层均不把弹道、毁伤、阵营、指挥角色或整舰状态字段塞回模块原型。雷达探测/EW 公式、爆炸能量/半径、发生概率及救生艇硬件类别、容量、质量与生还率尚未定义。

火炮与导弹发射器必须同时占据内部格和顶挂或侧挂外露空间；`active_defense` 类主动防御设备可仅有外露本体，首版传感器必须是顶挂设备。5f 五型干扰发射器采用纯露天顶挂、零内部占用、无人员、无炮塔射界；已通过真实目录放置与保存重开。它们复用待发弹和装填账目，只消耗配方材料，不强制扣炮弹资源；零弹药点数配方必须包含非零材料费用。完整制作记录见[5f 制导与电子对抗](./contracts/web_bridge/tactical-stage5f-guidance-ew.md)。

夹具炮塔自动化 `weapon.aim` 与 `weapon.fire`，但未自动化 `weapon.reload`，因此在没有实体装填人员时仍可使用一发待发弹，却无法续装；I1 已用实际动作回归验证这一边界。I4 用独立时间配置目录的基准装填时长除以武器装填、弹药库供弹和库存访问的最低当前效率，形成实际队列时长；配置目录仍是合同夹具，未将效率换算结果标为正式炮塔性能。I5 同样把弹种性能与损伤换算留在独立合同夹具中，未经教程舰联合标定不得升格。I6 要求会生成弹丸的武器事件精确落在 `60Hz` 场景边界，非网格时刻直接拒绝而非取整；以后正式射速与装填配置必须同时满足这一离散时间合同，或显式升级场景求解策略版本。

需要用电的武器、火控和传感器分别强制归入 `weapons_and_active_defense`、`fire_control` 和 `sensors` 供电类别；完全无电气负载的手动武器可以不占供电类别，但仍需要实体人员执行未自动化功能。夹具使用 `ship.weapon_fire_requested`、`ship.fire_control_required` 与 `ship.sensor_scan_required` 唤醒待机模块；显式关闭和整类断电仍优先于自动事件。每类模块继续逐功能声明独立战损响应。

## 六、当前夹具与编译边界

机器夹具为《舰艇数据/模块/测试夹具/最小模块目录.v1.json》，测试为《高天荒野舰艇模块原型契约测试.py》。目录内全部质量、耐久、升力、推力、功率、容量和人员数只用于契约关系测试。

E5 战斗扩展夹具为《舰艇数据/模块/测试夹具/战斗系统模块目录.v1.json》，实际布局为《舰艇数据/舾装方案夹具/标准155x20战斗系统舾装.v1.json》，出航来源为《舰艇数据/出航配置夹具/标准155x20战斗系统出航.v1.json》。四个战斗模块与九类基础模块通过 `merge_module_prototype_catalogs` 确定性合并为舾装编译器使用的精确目录；重复精确引用仍会被拒绝。保存的舾装方案只引用具体模块原型，不保存聚合性能。契约、占格、事件、P1～P3 供电、人员/自动化和战损回归见《高天荒野舰艇战斗系统契约测试.py》。

规范舾装夹具为《舰艇数据/舾装方案夹具/标准155x20最小舾装.v1.json》，编译与负面回归为《高天荒野舰艇舾装编译器测试.py》。当前已经验证精确船壳/模块/涂料引用、栅格/侧挂/嵌入实例、直角旋转、跨层、顶挂、重叠、外部净空与尾焰净空，并由同一布局派生模块质量、惯量、升力、供电、船员、逐实例执行器和配平后方向能力。

执行器规则现已冻结：中轴主机独立全额输出，离轴主机按推力作用线两侧等总推力配平，主机残余偏航和转向机残余平动力均按真实力臂/合力保留。编译器同时生成带完整来源指纹的 `DerivedShipSnapshot`。

阶段 E 已加入《舰艇数据/出航配置夹具/标准155x20载货出航.v1.json》、《舰艇数据/舰艇实例夹具/标准155x20完好实例.v1.json》、《高天荒野舰艇出航配置编译器.py》和《高天荒野舰艇运行时参数编译器.py》。静态舾装不读取载荷；出航编译器校验离港初态，舰艇实例的 `operational_state` 保存离港后变化的高度层、燃料、船员与逐货仓大宗货物，运行时据此重新派生当前质量、惯量和安全锁。实例与运行时编译器同时处理逐模块耐久/工作状态、整类或安全供电、缺员比例、自动化子功能、宿主连带失效、逐功能战损倍率和当前执行器重聚合。测试夹具中的推进采用非线性分段响应、发电采用线性响应、升力/CIC 等采用归零失效阈值；这些节点只验证接口，不是正式平衡值。外部模块 RCS 在原型值为 `null` 时只输出待标定警告，同时具有内部和侧挂本体的复合原型在锚点关系冻结前明确拒绝。

## 2026-09-20 转向发动机数值修订

有人／无人化转向发动机新增同 ID v3，推力由 10,000 N 提高为 100,000 N；质量、资源、安装位置和响应规则不因增推另改。旧原型不可就地改写；新战术准备通过 v16 政策显式升级，已有战术舰艇保留库存与战损进行有备份迁移，战斗占用和待保存结果延后。详见[6b 校准与兼容记录](contracts/web_bridge/tactical-stage6b-fleet-navigation.md)。
