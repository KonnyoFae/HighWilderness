# 《高天荒野》T0b.2 推进响应与权威性能优化规划

> 建立日期：2026 年 8 月 31 日
> 状态：`implementation_in_progress / T0b.2d2a_complete_resource_control_bridge / next_T0b.2d2b`
> 当前代码基线：`T0b.2d2a` 推进资源与控制桥状态（基于 `c48d1c7`，本切片与报告同提交）
> 输入证据：`舰艇数据/报告/阶段T0b1f短程诊断决策门接口.v1.json`
> 正式性能运行：0
> 本文性质：RFC 与切片编排；不授权修改权威算法、黄金结果或引入 Rust 热核

## 一、结论与目标

T0b.1f 已证明 `target_20` 纯运动和制导弹丸短跑都没有达到实时因子 1.0：纯运动三次为 `0.556494 / 0.543660 / 0.551840`，制导弹丸三次为 `0.298540 / 0.279388 / 0.280402`。观察器 drain 已经很小，差距主要位于权威推进内部。

当前差距不能只归因于弹丸，也不能直接用降低权威频率或整体迁移 Rust 解决。现有实现仍包含可在 60 Hz 下消除的局部重复计算；同时，发动机 `response_time_s` 已存在于模块、执行器和聚合能力中，但尚未进入战术求解，推力仍会在一个固定步内瞬时变化。

本规划的目标依次是：

1. 建立可公平比较的修正后 60 Hz Python 基线，先清除不改变权威语义的已知浪费；
2. 将主发动机冷启动、离散车钟和 5% 权威响应阶段纳入正式规则，而不是只作为表现动画；
3. 在新推进规则上比较事件驱动、多速率、Python 数据布局优化和批量 Rust 热核；
4. 保持 60 Hz 权威事件时钟、稳定排序、可重放输入和版本化存档边界；
5. 只有在短程目标、正式负载覆盖和长跑资源门禁全部满足后才恢复 T0c。

## 二、现状与问题来源

### 2.1 已完成且不得回退的成果

1. 设计快照完整指纹固定步重算为 0；
2. 稳态静态战术模型重建为 0；
3. 完整 runtime 编译在六个诊断场景中为 0；
4. 到期事件索引、弹丸批量生成、目标几何缓存和增量实体计数已经建立；
5. 权威事件摘要不再物化整份场景结果，观察器 drain 独立计量；
6. 12 个 T0 黄金场景和 18 次 T0b.1f 短跑的权威 hash 保持稳定；
7. 当前 Python 56/56、TypeScript 4/4、Rust 15/15 回归通过。

### 2.2 纯运动剩余成本

`target_20.motion_only` 单步探针显示：

- `_load_metrics` 1300 次，即 20 舰 × 固定 65 个候选比例；
- `runtime_cache_resolve` 80 次；
- `canonical_sha256` 与 `canonical_json` 各 222 次；
- `dataclass.replace` 541 次；
- 武器时间线、模型绑定和生命周期派生各 40 次或以上。

当前 `_choose_command_scale` 即使满额请求已经安全，也会先构造 65 个比例及其载荷结果。runtime 缓存虽然命中，却仍会刷新完整 `RuntimeShipParameters`、重算实例 hash，并触发 runtime 自身的规范 hash。

### 2.3 制导弹丸剩余成本

`target_20.guided_projectiles` 单步探针显示：

- `_geometry_hit` 2696 次；
- 制导推进 296 次；
- `dataclass.replace` 4054 次；
- `canonical_sha256/json` 各 307 次；
- 弹丸子步内仍有字典构造、稳定排序、配置查找和逐弹对象替换。

`stress_30.guided_projectiles` 进一步达到 6680 次几何调用、680 次制导推进和 9172 次 `dataclass.replace`。弹丸宽相已经按预编译目标几何工作，剩余成本主要是弹丸数量 × 子步数的线性积分、制导、碰撞和 Python 对象开销。

### 2.4 覆盖缺口

T0b.1f 仍是 10+60 步短跑。制导档未达到规划的合法武器事件率，尚未覆盖 30 秒弹丸寿命下的生成、积累、命中、过期稳态，也没有覆盖 Web 传输队列和正式 600+3600 步内存趋势。当前性能只能用于选择优化方向，不能视为正式容量结论。

## 三、推进响应规则规划

### 3.1 已有扩展点

主发动机和姿态推进器能力已经强制声明 `response_time_s`。该字段会进入 `ActuatorInstance`、`MainDirectionCapability` 和 `TurningDirectionCapability`，多执行器聚合时保留参与执行器中的最慢响应时间。当前求解器尚未消费该字段。

`response_time_s` 继续定义为发动机在已经可以工作的前提下，从 0% 爬升到 100% 名义推力所需的总时间。冷机点火或预热不得混入该字段，另设 `startup_time_s`。

### 3.2 玩家车钟与物理响应阶段

玩家或自动驾驶可下达的主发动机车钟冻结为六个位置：

| id | 名称 | 目标推力比例 |
| --- | --- | ---: |
| `stop` | 停车 | 0% |
| `dead_slow` | 微速 | 2% |
| `quarter` | 四分之一 | 25% |
| `half` | 二分之一 | 50% |
| `three_quarter` | 四分之三 | 75% |
| `full` | 全速 | 100% |

玩家指令离散，但发动机不能瞬间切档。权威实际输出使用以下响应阶梯：

`0%、2%、5%、10%、15%……95%、100%`

2% 是微速特殊阶段，其他中间阶段按 5 个百分点递增。实际输出只能沿相邻阶段向目标移动；表现层可以在两个权威阶段之间插值，但不得反向决定权威推力。

### 3.3 固定步排程

响应排程不得每步累加浮点误差。发动机收到指令时记录命令固定步、起始输出百分比和目标档位。任一累计输出变化 `delta_percent` 的生效边界定义为：

`command_step + ceil(response_time_s × fixed_step_hz × delta_percent / 100)`

因此 0→100% 的总步数恰为 `ceil(response_time_s × 60)`；无法整除的 5% 阶段会以确定性的 1/2 步或 2/3 步间隔分布。中途改令时以当前已经生效的权威输出为新起点重新排程，不追溯尚未生效的表现插值。

### 3.4 冷启动状态机

权威推进状态至少包含：

- `actuator_instance_id`；
- `phase = off | starting | ready | running | stopping | tripped`；
- `commanded_notch`；
- `target_output_percent`；
- `actual_output_percent`；
- `ready_at_fixed_step`；
- `next_transition_step`。

冷机收到非零车钟后先进入 `starting`。在 `ready_at_fixed_step` 之前实际推力为 0；启动完成后才按 5% 阶段向目标爬升。`startup_time_s` 与 `response_time_s` 必须分别校验为正数或按明确的零值语义处理。

现有模块 `off / standby / active` 是设备管理与供电/人员可用性状态，不能直接充当发动机时间状态。二者关系必须在合同切片中单独冻结；现有自动激活事件不得继续把发动机从 standby 瞬时提升到 active 并立即输出推力。

### 3.5 每执行器状态与聚合

状态按执行器实例保存并按 `actuator_instance_id` 稳定排序，不能只保存整舰总油门。原因是同一发动机组内各模块可能具有不同启动时间、响应时间、损伤、供电、人员和故障状态。

单步先推进各执行器状态，再按当前实际输出、损伤效率和配平策略聚合力、力矩及油耗。允许对成员与实际阶段完全相同的发动机组缓存聚合结果，但缓存必须由成员 revision 和阶段共同失效。

### 3.6 燃料、供电、损伤与反推

RFC 必须逐项冻结：

1. `starting`、`ready` 和 `running` 的燃料与功率消耗；
2. 启动期间失去燃料、供电、人员或宿主模块时的失败状态；
3. 模块损伤是否同时降低最大推力、延长启动时间和延长响应时间；
4. 紧急停机是立即断推还是按 `shutdown_time_s` 衰减；
5. 前进与反向发动机组切换时，是否必须先降推、再启动另一组；
6. 战略戒备状态如何决定进入战术场景时的初始 `off / starting / ready` 状态。

首轮实现可以推迟启动附加油耗、热机保持成本和关机衰减的平衡数值，但不得省略状态字段和事件边界的扩展空间。

### 3.7 安全限推

离散车钟、软安全保护、硬可用性和物理响应是四层不同概念：

1. `commanded_notch` 保存玩家或自动驾驶下达的车钟，不因安全介入被静默改写；
2. `safety_ceiling_percent` 是软安全系统给出的离散输出上限；
3. `availability_ceiling_percent` 由燃料、供电、人员、模块/宿主损伤和跳闸等硬条件决定；
4. `actual_output_percent` 按发动机响应阶段追随有效目标。

有效目标冻结为：

`effective_target_percent = min(commanded_notch_percent, safety_ceiling_percent, availability_ceiling_percent)`

#### 3.7.1 冻结策略：保留离散降档

安全保护采用离散限幅/降档，不改回连续比例调节：

1. 升推前只预测相邻的下一响应阶段；下一阶段不安全时拒绝进入并锁定当前安全上限；
2. 当前阶段因速度、阻力、姿态或损伤变化变得不安全时，从当前阶段向下寻找最高安全阶段，立即降低 `safety_ceiling_percent`；
3. 安全上限变化是当前固定步生效的控制决定，实体推力仍按 `response_time_s` 逐阶段下降；只有硬跳闸或后续明确规定的紧急断推可以立即归零；
4. 限制期间保留原 `commanded_notch`。安全释放后，发动机自动重新向原车钟爬升，无需玩家重复下令；
5. UI 和诊断同时显示“命令车钟、软安全上限、硬可用上限、实际输出、限制原因”，不得只显示一个模糊的油门百分比。

软保护首轮覆盖结构载荷和乘员过载。`overg` 只绕过软安全上限，并继续使用既有结构过载损伤规则；它不得绕过燃料耗尽、断电、人员不可用、发动机/宿主损毁或 `tripped` 等硬可用性限制。

#### 3.7.2 介入、保持与释放

安全判定器设计为无副作用纯函数，输入为当前舰艇状态、当前/候选输出阶段、安全配置和 `overg`，输出为离散上限、原因集合与状态转换意图。状态至少包含：

- `safety_ceiling_percent`；
- `safety_reasons`，按稳定枚举序排列；
- `safety_limited_since_step`；
- `release_candidate_since_step`；
- `safety_revision`。

为避免阈值附近在相邻固定步反复降档/升档，软保护必须具有滞回：介入阈值、释放阈值和释放保持步数由版本化安全配置提供，且释放阈值必须比介入阈值保守。具体数值留给负载夹具和教程舰设计阶段标定，不在本 RFC 中臆定。

限制原因可以同时存在；只有全部软限制都连续满足释放条件后，才提高或移除安全上限。释放也只允许后续响应排程尝试相邻升阶，不能在一个固定步跳回车钟目标。

#### 3.7.3 固定步判定与事件顺序

单个固定步内的顺序冻结为：

1. 提交该边界的玩家/AI 车钟与 `overg` 输入；
2. 更新燃料、供电、人员、模块损伤和跳闸形成的硬可用上限；
3. 生成该步到期的启动/响应候选阶段，但暂不提交升阶；
4. 复核当前实际阶段，并在需要升阶时预测相邻下一阶段；
5. 更新软安全上限，拒绝不安全升阶或排程离散降档；
6. 提交允许的实际输出阶段，聚合执行器力、力矩和油耗；
7. 积分运动并提交该步事件、损伤和生命周期变化。

由第 7 步产生的新状态在下一固定步复核；若它同时触发硬跳闸，则按硬故障规则立即处理。所有执行器按 `actuator_instance_id` 稳定排序，事件至少包括：

- `engine_safety_limit_engaged`；
- `engine_safety_limit_changed`；
- `engine_safety_limit_released`；
- `engine_output_stage_changed`。

事件排序键冻结为 `(fixed_step_index, actuator_instance_id, event_kind_order)`；同一输入三次运行必须产生相同事件、状态和 hash。

#### 3.7.4 安全保护的性能边界

安全保护不能重新引入当前固定 65 候选/舰/步的成本：

1. 稳态每舰每步对当前实际阶段调用一次 `_load_metrics`，因为速度、阻力和姿态仍会变化；
2. 到期升阶时只额外检查一个相邻候选阶段，不扫描整条阶梯；
3. 只有当前阶段已不安全时，才在不高于当前/有效目标的最多 22 个权威阶段中向下搜索；
4. 降档搜索只在安全状态 dirty 或实际阶段变化时运行；不得永久缓存随运动状态变化的安全结论；
5. 多舰可以对同一固定步的不可变快照批量求值，但必须先完成整批读取，再按稳定舰艇/执行器顺序提交结果。

`target_20.motion_only` 的阶段性调用数目标为：正常稳态 `_load_metrics` 不高于 20 次/步；有升阶的典型过渡步不高于 40 次/步；紧急降档只对受影响舰艇产生有界的离散搜索。缓存键若保存安全派生结果，必须包含实际输出阶段、安全 revision、硬可用性 revision 和发动机组成员 revision。

### 3.8 权威合同与编辑器字段

推进响应属于权威规则扩展，将有意改变运动状态、燃料、事件和最终 hash，必须：

1. 新增版本化 `EngineRuntimeState` 与解析/序列化合同；
2. 为旧实例和旧场景定义迁移，不以字段缺省隐式猜测热机状态；
3. 冻结发动机启动、阶段生效、限推、停机和故障事件的稳定排序键；
4. 在模块编辑器中暴露 `startup_time_s`、`response_time_s` 与引用的车钟/响应策略；
5. 区分规则字段与表现插值字段；表现插值不得进入权威 hash；
6. 归档现有 T0 黄金结果，以明确版本升级生成新黄金，不把预期变化记作回归漂移。

## 四、其他优化方向与优先级

### 4.1 P0：修正后的 60 Hz Python 等价基线

这一层必须保持现有权威结果逐步一致，先于任何规则变更实施或至少形成可执行原型。

1. **推力安全搜索快速路径**：先检查当前请求的最高比例；安全即返回，只有不安全时才运行既有搜索。当前 T0 低推力输入下，目标是把 `target_20.motion_only` 的 `_load_metrics` 从 1300 次降到不高于 20 次；
2. **runtime 稳定核心与轻量视图分离**：缓存命中不再 `replace` 完整 `RuntimeShipParameters` 并重新规范序列化；实例 revision/hash 在变更边界生成；
3. **精确边界级 `ShipStepContext`**：上下文只在一个固定步内生存，只有舰艇 id、边界、精确实例值、实例代次和自动事件集合都匹配时才复用 runtime/模型；稳定核心相同不得单独授权复用包含旧实例快照的模型；
4. **无到期时间线安全快进**：只建立步内临时到期投影，并区分同刻无操作、仅推进权威时钟和完整事件推进；不把持久 next-due 索引写入存档；
5. **生命周期同一步投影复用**：步首仍完整派生，步末只在由正式生命周期规则共同生成的语义投影相同时复用；不建立跨步骤通用 dirty 布尔缓存；
6. **临时对象削减**：b3a—b3c 稳定后的画像没有发现集中、可测的场景级临时容器热点，因此 `T0b.2b3d` 已取消场景级 scratch 实现；弹丸世界中明确集中的批量对象成本仍留给 `T0b.2e1`，且只能使用不跨步逃逸的 scratch。

P0 的意义是给后续架构方案提供公平基线。不得用仍包含已知 65 倍安全搜索和缓存命中全量 hash 的结果证明多速率或 Rust 必要。

### 4.2 P1：推进响应与离散安全求解

在 P0 权威等价基线冻结后，按第三章实施推进响应。这是有意的权威规则版本升级，不与 P0 放在同一提交。

性能目标不是省略物理，而是让推力分配和安全搜索只在命令、5% 阶段或 dirty 事件上变化。即使推力阶段不变，速度、阻力和姿态仍会变化，所以每个运动积分步至少复核一次当前实际载荷；不得永久缓存结构安全结论。

### 4.3 P2：弹丸世界数据布局与批处理

优先在 Python 中验证不会改变规则的批量布局：

1. 维持弹丸稳定有序容器，避免每个子步重复 `tuple → dict → sort → tuple`；
2. 发射时预绑定弹种运行参数与制导配置 revision；
3. 按目标、弹种、制导类型和剩余寿命分组；
4. 一个权威固定步内部使用可变 scratch buffer，末尾只冻结一次规范状态；
5. 命中候选继续按 `(event_time, projectile_id)` 稳定提交；
6. 保留几何 revision 校验和保守宽相，不以减少精确求交为由漏碰撞。

### 4.4 P3：事件驱动与多速率权威

先区分两种方案：

- **等价跳过**：仅在输入和依赖未变化、数学结果可证明不变时跳过 runtime、生命周期、时间线等派生计算；必须保持现有逐步 hash；
- **真正多速率物理**：舰艇、损管或传感器按低于 60 Hz 的频率更新并使用采样保持；这会改变权威结果，必须版本化规则并生成新黄金。

RFC 优先采用 60 Hz 权威事件时钟加事件驱动慢系统：

- 60 Hz：输入接收、事件排序、必要的运动积分和场景提交；
- dirty/event-driven：runtime、生命周期、无事件武器时间线、发动机阶段；
- 独立子步：弹丸积分；
- 可评审低频：损管、人员和部分慢变量；
- 非权威：Web 表现插值和快照降采样。

真正多速率不得只把全部舰艇计算集中到每第三帧，否则平均成本下降但峰值仍超过预算。必须说明分批、同步提交、采样保持、突发唤醒和事件精确时间。

### 4.5 P4：批量 Rust 热核

只有在 P0—P3 数据证明 Python 仍无法满足目标时评审 Rust。候选范围限定为：

1. 弹道子步积分；
2. 导弹制导数值推进；
3. 保守宽相和精确几何碰撞；
4. 必要时批量舰艇载荷/机动数值核。

Python 继续负责规则编排、事件排序、模块/人员/毁伤合同和错误语义。FFI 必须以“一次世界步一个批次”为边界，不能逐弹丸跨语言调用，也不能在 Python 端继续为每个子步构造数千个 dataclass 后只把一个公式交给 Rust。

Rust 方案必须提供跨语言黄金语料、输入输出 Schema、浮点策略、稳定排序、错误映射和 Python 参考实现等价测试；不得迁移整套规则。

### 4.6 P5：正式负载与长跑

优化方向确定后，必须拆分：

- **代表性战术场景**：合法事件率、可持续弹丸数量和实际战斗节奏；
- **饱和压力场景**：按 24/120/300 事件率与目标弹丸数施压；
- **正式 T0 矩阵**：600 步预热、3600 步测量、三次重复、传输模式和快照频率；
- **资源门禁**：进程内存、队列深度、最大步耗时、拒绝请求和丢帧/合帧统计。

不得通过修改舰艇编成、射速、弹舱、寿命或目标实体数来让基准通过。

## 五、候选方案比较矩阵

| 方案 | 权威规则变化 | 主要收益 | 主要风险 | 是否必须保留 |
| --- | --- | --- | --- | --- |
| A. P0 修正 60 Hz Python | 否 | 清除已知局部浪费，形成公平基线 | 缓存漏失效 | 必须 |
| B. A + 推进响应/5% 阶段 | 是 | 正确发动机手感，削减连续比例搜索 | 黄金升级、状态迁移 | 必须 |
| C. B + Python 弹丸批处理 | 否或极小 | 减少排序、字典和对象开销 | 稳定顺序、scratch 越界 | 必须评估 |
| D. B/C + 事件驱动慢系统 | 可保持不变 | 降低 runtime/生命周期重复成本 | 漏唤醒、边界错误 | 必须评估 |
| E. 真正多速率物理 | 是 | 降低慢系统平均成本 | 峰值、物理变化、重放语义 | 数据决定 |
| F. 批量 Rust 热核 | 否，目标为等价 | 解决弹丸和数值对象瓶颈 | FFI、浮点、双实现维护 | 数据决定 |
| G. E + F 混合 | 是 | 最大容量潜力 | 复杂度最高 | 最后比较 |

预期最有希望的最终方向是“事件驱动舰艇/runtime + 版本化推进响应 + 批量弹丸热核 + 非权威表现插值”，但在 A—D 数据完成前不得预先冻结结论。

## 六、子切片与依赖编排

实施拆为以下可独立复核、独立回退的阶段。P0 等价优化、纯判定器、合同版本升级和场景接线不得混入同一提交。

| 顺序 | 子切片 | 范围 | 权威 hash | 退出条件 |
| --- | --- | --- | --- | --- |
| 0 | `T0b.2-pre` 版本检查点 | 核对既有 `290d044` 汇总提交及远端状态，记录未逐切片提交的偏差，并提交本文、主计划入口和检查点报告 | 不变 | 基线可回退，报告与实现 commit 对应，三套回归通过 |
| 1 | `T0b.2a` RFC 冻结 | 冻结推进状态机、5% 阶段、离散安全降档、软/硬限制边界、事件顺序和 P0 基线 | 不适用 | 本文评审清单除明确延期项外全部关闭 |
| 2 | `T0b.2b1` 安全搜索快速路径 | 在现有连续求解器中先检查请求比例，只有不安全才执行原搜索；补齐调用数埋点 | 必须逐步一致 | 12 黄金场景和六短跑 hash 一致；正常 `target_20.motion_only` `_load_metrics` ≤20 次/步 |
| 3 | `T0b.2b2` runtime 稳定核心 | 轻量 runtime 视图、revision/hash 仅在变更边界生成，补齐各失效源负例 | 必须逐步一致 | 缓存命中不再全量规范序列化；黄金、迁移和负例通过 |
| 4a | `T0b.2b3a` 精确边界上下文 | 只引入步内 `ShipStepContext`、精确实例/自动事件门禁和按需模型绑定；不跳过时间线或生命周期 | 必须逐步一致 | 武器边界变更不会被运动提交覆盖；20 舰 runtime resolve ≤60、模型绑定 ≤20 |
| 4b | `T0b.2b3b` 时间线安全快进 | 步内到期投影；同刻无操作、clock-only、full-advance 三态；任一时间线 mutation 使投影失效 | 必须逐步一致 | 到期/失败/链式事件不遗漏；无事件时权威时钟仍同步 |
| 4c | `T0b.2b3c` 生命周期同一步复用 | 抽取唯一生命周期语义投影；步首完整派生，步末投影未变时复用 | 必须逐步一致 | 命中、损伤、人员、供电、遥控、升力和自动事件负例通过 |
| 4d | `T0b.2b3d` 画像与可选 scratch | 重画像前三项收益，只允许无逃逸的单步临时容器；无收益可明确取消 | 必须逐步一致 | 调用数、P95、最大步耗时与内存报告完成，无跨步增长 |
| 5 | `T0b.2c1` 纯安全判定器 | 实现但不接入场景的离散阶段、软/硬原因、滞回和事件意图纯函数 | 不变 | 正负例、属性测试、`overg` 边界与确定性测试通过；现有场景不调用它 |
| 6a | `T0b.2c2a` 推进资源合同 | 版本化 `PropulsionSafetyProfile`；模块推进 capability v2 的 `startup_time_s`；具名旧目录迁移 | 资源合同升级，旧场景不变 | 严格载入、资源往返、具名迁移和非法组合拒绝通过；场景仍不持有推进状态 |
| 6b | `T0b.2c2b` 推进场景状态与迁移 | `EngineRuntimeState`、`PropulsionGovernorState`、`TacticalPropulsionState`、场景引用、事件 schema 与旧场景迁移 | 有意场景合同版本升级 | 存档往返、旧场景迁移、排序/引用/非法状态拒绝通过；仍不接入力学 |
| 7a | `T0b.2d1` 启动/响应内核 | 实现独立时间线内核：冷启动、5% 相邻阶段、中途改令和停车，暂不接入场景力学 | 新合同、旧运动不变 | 固定步边界、60 Hz 总响应时间和三次重放测试通过 |
| 7b | `T0b.2d1p` 接线前复盘与重排 | 量化能力可排程性、资源绑定、控制输入和合同版本缺口；只调整计划 | 不变 | 12 场景全量审计、调整决策、退出门和独立报告完成 |
| 7c | `T0b.2d1r` 推进状态合同修复 | 为 d1 新增排程锚点升级 engine/ship/scene alpha interface，并提供 c2b→d1 具名状态迁移 | 有意合同升级，旧运动不变 | 新旧状态严格解析、显式迁移、未知/篡改拒绝和旧黄金通过 |
| 8a | `T0b.2d2a` 推进资源与控制桥 | 建立可排程 capability 新版本、三类场景目录绑定迁移、逐执行器精确绑定和离散控制合同 | 资源/控制合同升级，运动不变 | 1224 个执行器全部精确绑定且可排程；连续旧输入只能具名迁移 |
| 8b | `T0b.2d2b` 推进场景接线 | 用实际阶段驱动力、力矩与油耗，输出推进事件并建立版本化新黄金 | 有意权威升级 | 运动/燃料/事件新黄金经解释性差异复核；v1 旧路径保持可读 |
| 9 | `T0b.2d3` 安全保护接线 | 接入离散软限幅、自动降档、滞回释放、原车钟恢复和安全事件 | 使用新黄金 | 不安全升阶被拒绝；动态变不安全能降档；`overg` 只绕过软限制 |
| 10 | `T0b.2d4` 硬故障与方向切换 | 断油、断电、人员/损伤、跳闸、紧急断推、前进/反向切换 | 使用新黄金 | 所有硬限制不能被 `overg` 绕过；故障、恢复和重放测试通过 |
| 11 | `T0b.2e1` Python 弹丸批处理 | 稳定容器、配置预绑定、批量数值布局、单步 scratch | 必须与 T0b.2d4 一致 | 弹丸状态/事件顺序一致，target/stress 调用与 P95 改善 |
| 12 | `T0b.2e2` 场景事件驱动实验 | 在 60 Hz 权威边界上跳过无到期慢系统，并单独验证真正多速率原型 | 等价项不变；多速率原型单列 | 漏唤醒负例、峰值和确定性报告完成，不直接替换正式路径 |
| 13 | `T0b.2f` 架构对比 | 比较修正后 60 Hz、事件驱动、多速率、Rust 批量热核及混合方案 | 各方案声明语义 | 同一机器和负载输出数据化比较与唯一建议 |
| 14 | `T0b.2g` 决策门 | 选择继续 Python、多速率、Rust 热核或混合方案 | 权威门禁通过 | 形成唯一下一切片，不冒充正式 T0 |
| 15 | `T0b.3/T0c` | 按决策实施并恢复正式 T0/T0c | 由决策规定 | target_20 三次实时因子 ≥1，正式负载与资源门禁通过 |

每个实施子切片必须独立提交、独立报告、独立回归。T0b.2-pre 之后不得继续累积多个未提交切片。

阶段依赖调整为：`pre → a → b1 → b2 → b3a → b3b → b3c → b3d → c1 → c2a → c2b → d1 → d1p → d1r → d2a → d2b → d3 → d4`。`d1p` 只冻结复盘证据与后续计划；`d1r` 只修复已变化但未升级 id 的状态合同；`d2a` 只解决能力/控制/绑定，不推进场景；`d2b` 才允许产生新的运动、燃料、推进事件和黄金。`b3d` 可在画像证明收益不足时以“不实施”关闭；跨步骤 lifecycle dirty、持久 next-due 索引和一般事件驱动跳过统一留到 `e2`。`e1` 和 `e2` 只有在 d4 新权威基线稳定后才允许开始；二者可分别形成实验分支，但汇总结果必须共同进入 `f → g` 决策门。若任一等价阶段改变旧 hash，立即停止后续阶段并先解释差异。

## 七、共同验收门禁

### 7.1 权威与合同

1. P0、P2 和声明等价的事件驱动优化必须保持逐步状态、事件集合、事件顺序和最终 hash 完全一致；
2. 推进响应和真正多速率属于声明的权威版本升级，必须归档旧黄金、提供迁移并生成新黄金；
3. 发动机状态、命令和阶段事件必须使用稳定 id 与固定步边界；
4. 严格载入、存档、IPC 和资源替换继续执行内容指纹验证；
5. 任一缓存和 dirty 跳过必须具备损伤、人员、供电、燃料、模块模式和资源 revision 负例。

### 7.2 推进响应

1. 冷机在启动完成边界前输出严格为 0；
2. 0→100% 在 `ceil(response_time_s × 60)` 步内完成，允许的误差只来自固定步向上取整；
3. 权威实际输出只取 `0、2、5、10……100`；
4. 安全限制期间 `commanded_notch` 保持不变，实际输出和软安全上限可分别观测；
5. 相邻下一阶段不安全时不得升阶；当前阶段动态变得不安全时必须产生离散降档排程；
6. 所有软原因满足滞回和保持步数后才释放，释放后只能逐阶段恢复原车钟；
7. `overg` 可以绕过结构/乘员软保护，但不能绕过燃料、供电、人员、损毁或跳闸等硬限制；
8. 中途改令、停车、反向、失去燃料/供电、损伤和紧急 overg 都有确定性用例；
9. 三次重放的安全事件、发动机阶段事件、燃料、运动状态和最终 hash 相同；
10. 表现插值变化不得改变权威结果。

### 7.3 性能与资源

1. 每一候选方案使用同一机器元数据、输入、实体负载、预热、测量和三次重复；
2. 报告固定步 P95/最大值、观察 drain、实时因子、内存、调用数和权威 hash；
3. P0 目标场景先证明 `_load_metrics` 不再固定执行 65 候选/舰/步；
4. 任何方案达到 target_20 短跑实时因子 1.0 后仍必须检查最大步耗时和资源有界性；
5. 正式性能通过仍以既有 T0 计划的 600+3600 步、三重复和完整矩阵为准。

## 八、决策规则

1. 若 P0 已使 `target_20.motion_only` 三次短跑实时，而制导仍失败，记录弹丸世界为首要性能候选；由于推进响应会有意改变运动、燃料和弹丸输入，先完成 `c1 → c2a → c2b → d1 → d1r → d2a → d2b → d3 → d4` 的版本化新权威基线，再优先执行 Python 弹丸批处理，最后才评审批量 Rust 热核；
2. 若 P0 后纯运动仍失败，但推进响应/离散搜索能消除主要载荷搜索成本，则完成推进新基线后再判断多速率，不提前迁移整体规则；
3. 若事件驱动优化能保持 hash 且满足目标，不采用真正多速率物理；
4. 若只有真正多速率能满足目标，必须先接受权威规则变化、峰值调度和新黄金，再实施；
5. 若弹丸数值核是唯一剩余瓶颈，只迁移弹丸积分、制导和碰撞批次；
6. 若需要混合方案，Python 仍保留规则编排和事件提交，Rust 不拥有模块、人员、毁伤和存档语义；
7. 任何候选未达到负载覆盖时只能记录 `NOT_COVERED`，不得根据漂亮的实时因子提前恢复 T0c。

## 九、明确非目标

T0b.2 不得：

- 修改 T0 舰艇编成、武器事件率、弹丸目标数、弹舱或寿命来降低压力；
- 把表现插值写入权威状态；
- 在 RFC 冻结前改变现有 `response_time_s` 语义；
- 把发动机响应只实现为 UI 动画；
- 以全局低频 tick 粗暴替代事件精确时间；
- 逐弹丸跨 FFI 调用；
- 迁移整套 Python 规则到 Rust；
- 在正式 600+3600 步执行前宣称 T0 性能通过；
- 在 T0b.2-pre 之后跨多个子切片积累未提交更改。

## 十、T0b.2a 评审结论

开始代码实施前的 12 项评审已经关闭：

1. 六档主发动机车钟与 `0、2、5……100` 实际响应阶梯冻结；
2. `startup_time_s` 与既有 `response_time_s` 的定义冻结；
3. `off/standby/active` 与发动机 phase 的关系冻结；
4. 首轮启动、热备、运行、停机的供电与燃料边界冻结；
5. 反向推力冻结为独立定向发动机组，禁止同轴对向组同时输出；
6. 离散安全降档、滞回配置归属和无隐式默认策略冻结；
7. 首轮损伤只影响可用性和最大推力，不同时拉长启动/响应时间；
8. P0 等价优化的调用数目标、正反夹具和停止门冻结；
9. 事件驱动等价优化与真正多速率权威实验严格分离；
10. Rust 候选的批次所有权、排序和浮点要求冻结；
11. 模块资源、场景存档和黄金结果的版本迁移边界冻结；
12. `T0b.2-pre` 已完成，从此执行一子切片、一报告、一提交。

详细决定见第十二章。当前权威算法和旧黄金结果仍保持不变；上述规则只有进入对应的 `T0b.2c/d` 子切片后才成为代码行为。

## 十一、T0b.2-pre 完成记录与 T0b.2a 开工边界

`T0b.2-pre` 已完成以下核对：

1. T0a—T0b.1f 已由既有提交 `290d04483b3688f377e5cdb437a9aa7df21d4abb` 汇总，检查时 `HEAD == origin/main`，不存在此前规划所写的未提交实现工作树；
2. 该历史提交跨越多个计划子切片且使用汇总提交信息，未遵循逐切片提交纪律；将此记为既有偏差，不重写已推送历史；
3. 当前基线复跑结果为 Python 56/56、TypeScript 4/4、Rust 15/15 全部通过；Rust 仅有 F: 文件系统增量缓存硬链接失败后回退复制的既有警告；
4. 当前终端全局 `npm` 包装器指向缺失的 `npm-cli.js`，`cargo` 未加入 PATH；本次分别通过项目本地 Vitest 入口和已安装 Cargo 的显式路径完成等价回归，未下载或升级依赖；
5. 检查点记录保存于 `舰艇数据/报告/阶段T0b2pre版本检查点.v1.json`；正式性能运行仍为 0，权威算法、黄金结果和性能结论均未改变；
6. 从本检查点之后恢复“一子切片、一报告、一提交”。不得为修正历史纪律而拆分、变基或强推已经推送的 `290d044`。

本检查点完成时的下一子切片为 `T0b.2a`；其冻结结论现已记录于第十二章。T0b.2a 完成前不得开始 `T0b.2b1`，不得修改权威算法或更新黄金结果。

## 十二、T0b.2a 推进与优化 RFC 冻结决定

### 12.1 输入、模块模式与发动机时间状态

1. 六档车钟只用于 `main_engine`。命令由方向组与档位组成，方向组为 `forward / reverse / left / right`；同一轴的两个对向组不得同时接收非零命令。
2. `maneuver_thruster` 保留现有轮舵/制动分配输入，但实际目标向下量化到同一 `0、2、5……100%` 阶梯；它不暴露六档车钟 UI。
3. `RuntimeModuleStateInput.operating_mode` 仍表示管理、人员和供电资格；高频 `EngineRuntimeState.phase` 不写回该字段：
   - `off` 强制 phase 为 `off`，命令无效；
   - `standby` 不直接输出推力，但非零命令可产生 `ship.engine_thrust_requested` 自动激活事实并进入 `starting`；
   - `active` 允许 `starting / ready / running / stopping / tripped`；
   - 自动激活只改变该边界的有效模式，不改写存储模式；只要命令非零或发动机仍在启动/运行/停机过渡中，激活事实就持续存在。
4. 旧实例进入新场景时，存储模式为 `active` 的执行器显式迁移为 `ready + 0%`，`standby/off` 显式迁移为 `off + 0%`。这是一项具名迁移策略，不是解析器字段缺省。
5. `main_engine.startup_time_s` 必须大于 0；`maneuver_thruster.startup_time_s` 首轮固定为 0，获得有效 active 资格后视为 ready。二者的 `response_time_s` 都必须大于 0。

### 12.2 首轮能耗、故障、损伤与反推

1. 第一版不新增推进专用供电类别，也不把发动机误归入现有战斗/损管供电类别。发动机继续使用已有 `ModulePowerProfile`：`starting/running/stopping` 请求 active load，`ready` 请求 standby load，`off/tripped` 不请求负载；当前零电气负载夹具因此保持合法。
2. 第一版不增加启动附加燃料、热机维持燃料、热量或 `shutdown_time_s`。`starting/ready` 推进燃料为 0，`running/stopping` 燃料严格按 `actual_output_percent` 乘既有 `fuel_units_per_s`。
3. 燃料耗尽、供电/人员资格丢失、执行器或宿主损毁会触发硬 `tripped` 并在当前固定步把实际推力归零；`overg` 不得绕过。重新满足条件也不会自动复位，必须有显式 reset/start 命令。
4. 首轮损伤继续通过 `engine.throttle`/`thruster.throttle` 输出倍率降低最大推力；不同时改变 `startup_time_s` 或 `response_time_s`，避免同一次损伤受到重复惩罚。未来若需要，必须新增独立损伤函数 id 并版本升级。
5. 反向推力沿用当前几何：旋转安装后指向反向的独立执行器组成 `reverse` 组，不允许同一执行器瞬时反转。前进/反向换向必须先让原组沿响应阶梯降到 0，再启动或升高对向组；自动制动也服从同一互锁和安全保护。

### 12.3 安全配置所有权与 overg

1. 结构/乘员软保护参数属于版本化 `PropulsionSafetyProfile`，按场景/规则集引用，不属于单个发动机模块。
2. 配置至少包含 `structure_engage_ratio`、`structure_release_ratio`、`crew_engage_g`、`crew_release_g` 和 `release_hold_steps`。两个 release 阈值必须位于更安全的一侧，保持步数必须为正整数。
3. 权威加载不得提供隐式默认配置；旧场景必须通过具名迁移绑定兼容 profile。测试夹具可以使用测试数值，但不得冒充正式教程舰标定。
4. `overg` 冻结为绕过结构和乘员两类软保护；当前 `crew_safety_lock_enabled = false` 仍表示不启用乘员软保护。无论哪种情况，硬可用性和既有过载损伤公式继续执行。
5. 安全上限按主方向命令通道保存，载荷判定使用该舰本步全部主机、转向推力、阻力和运动状态的合成结果，不能把单台发动机局部安全误当作整舰安全。

### 12.4 状态所有权、序列化与迁移

1. 高频状态定义为 `TacticalPropulsionState`，包含按 `actuator_instance_id` 排序的 `EngineRuntimeState` 和按命令通道排序的 `PropulsionGovernorState`，由 `TacticalSceneShipState` 持有并进入场景 hash。
2. 发动机阶段不在每个固定步写入 `ShipInstanceSnapshotInput`，避免为 5% 阶段变化重算整份实例/runtime hash。未来战略模式只在进入/离开战术场景的边界通过显式 readiness import/export 转换；该转换不在 T0b.2 首轮实现。
3. 新场景合同使用新的 interface/policy 标识；旧 `gaotian.tactical-scene-timeline/v1alpha1` 只能通过显式迁移器进入新合同，严格解析器不得静默接受缺失推进状态。
4. 模块原型的推进 capability 升级后强制携带 `startup_time_s`。现有版本 1 夹具由具名迁移表转换；兼容值只用于合同夹具，不作为正式平衡值。
5. `T0b.2d2b` 接入场景时归档旧 12 场景黄金，生成带迁移说明的新黄金；旧接口继续可读但不再与新规则 hash 混比。

### 12.5 P0 等价优化的证据门

`T0b.2b1` 只改现有连续安全搜索的求值顺序，不引入离散车钟：

1. 先计算请求比例一次；若 `_command_allowed` 为真立即返回，`target_20.motion_only` 正常步目标为 20 舰共不高于 20 次 `_load_metrics`；
2. 请求比例不安全时必须运行原 65 样本与二分收敛路径，逐步状态、诊断、事件和 hash 与修改前一致；
3. 至少增加“全部请求安全”“部分请求不安全”“无任何样本安全”“overg”“乘员锁启用”五类夹具；
4. 调用数下降但任一旧黄金 hash 改变，或 fallback 结果与旧实现不一致，都立即停止 `b2`；
5. 报告同时记录每场景 `_load_metrics` 次数、固定步 P95/最大值、实时因子和三次 hash，不以单步调用数代替正式性能结论。

### 12.6 事件驱动、多速率与 Rust 候选边界

1. 事件驱动等价优化仍在 60 Hz 权威固定步上积分运动，只允许对无到期、无 dirty 的 runtime、生命周期和时间线工作跳过重算；所有唤醒仍落在原固定步边界。
2. 真正多速率会改变采样/保持和积分结果，只能作为 `T0b.2e2` 独立原型，使用独立接口与黄金，不得借事件驱动名义进入正式路径。
3. Rust 候选仅接受 Python 在单个弹丸子步开始时冻结的批量平面数组；输入按 `projectile_id`、目标 id/revision 稳定排序，一次 FFI 调用处理整批，禁止逐弹丸调用或 Rust 回调 Python。
4. Rust 只返回数值状态和碰撞候选；Python 继续拥有规则判断、伤害、事件 id、排序和提交。
5. Rust 使用标量 IEEE-754 `f64`，固定运算顺序，禁用 fast-math、隐式并行归约和 `mul_add` 替换；在正式支持平台上必须达到逐步状态、事件顺序和最终 hash 一致，否则该候选不具备等价替换资格。

### 12.7 T0b.2a 退出结论

T0b.2a 只冻结规则，没有修改代码、现有 schema、场景接口或黄金结果。下一子切片唯一为 `T0b.2b1` 安全搜索快速路径；该切片必须保持当前权威语义与 hash 完全一致，不得提前引入本章的新推进状态。

## 十三、T0b.2b1 完成记录与 T0b.2b2 开工边界

`T0b.2b1` 已按等价优化边界完成：

1. `_choose_command_scale` 先且只计算一次请求比例；安全时立即返回，不再预先构造 65 个比例；
2. 请求比例不安全时，把已计算的 100% 结果作为原 65 样本的最后一项复用，仍按 `0/64……64/64` 顺序筛选并执行原 50 次二分，因此 fallback 数值与旧实现一致；
3. 新增 `_load_metrics` 热路径探针和独立测试，覆盖全部安全、部分安全、无安全样本、既有 `overg` 与乘员锁语义；单元调用数分别为 1、115、65；
4. 12/12 单步黄金结果保持一致；六个短程场景各三次的初态、输入流、最终状态和事件 hash 全部重复一致；
5. 单步探针的 `_load_metrics` 次数由固定每舰 65 次降为每舰 1 次：`functional_6 / target_20 / stress_30` 分别为 6 / 20 / 30；
6. 同机同软件环境的短程诊断中，`target_20.motion_only` 最差 P95/最大值由 `35.5546/35.8759 ms` 降至 `27.3490/27.7318 ms`，最小实时因子由 `0.543660` 升至 `0.799687`；
7. `target_20.guided_projectiles` 最差 P95/最大值由 `67.5758/74.3283 ms` 降至 `52.6663/58.5154 ms`，最小实时因子由 `0.279388` 升至 `0.344185`；
8. Python 57/57、TypeScript 4/4、Rust 15/15 回归通过；正式性能运行仍为 0，短程诊断仍未覆盖正式武器事件率和 600+3600 步资源门禁。

详细证据保存于 `舰艇数据/报告/阶段T0b2b1安全搜索快速路径接口.v1.json`。目标规模纯运动仍未达到实时因子 1.0，下一子切片唯一为 `T0b.2b2` runtime 稳定核心；不得提前进入推进状态或多速率实现。

## 十四、T0b.2b2 完成记录与 T0b.2b3 开工边界

`T0b.2b2` 已按权威等价边界完成：

1. `RuntimeShipParameters` 拆为只保存 revision/事件派生值的 `RuntimeShipParametersCore` 与绑定精确实例的不可变轻量视图；燃料精确量、弹药、武器时间线和持续毁伤等透传域变化不再复制整份稳定核心；
2. 每个自动事件变体最多保留 4 个最近实例视图。同值实例和确定性回放命中直接返回既有视图，不执行 `dataclasses.replace`、实例规范 hash 或 runtime 规范 hash；缓存仍以值语义 revision 为权威键，不以对象地址决定正确性；
3. 精确实例内容真的变化时只生成一次实例 hash，并在不同自动事件变体之间复用；视图 `source_sha256` 只在新视图边界生成。模块、船体、高度层、燃料可用边界、人员、货物、供电和设计来源任一变化仍清空全部核心与视图历史；
4. 严格模式继续先执行来源、弹药、人员和模块合法性校验，再允许视图复用；无效弹药不能借同 revision 或历史视图绕过合同；
5. 场景结果直接读取已生成的 `runtime.source_sha256`，不再对同一 runtime 再做一次规范序列化。该替换的输出字段、值和最终权威 hash 保持不变；
6. 独立哈希边界夹具确认：首个事件变体只产生 1 次实例 hash 与 1 次 runtime hash；同值命中产生 0 次；透传实例变化产生各 1 次；同实例新增事件变体只产生 1 次 runtime hash；
7. `target_20.motion_only` 单步剖析中，`runtime_cache_resolve` 保持 80 次、完整 runtime 编译保持 0 次，规范 `canonical_sha256/json` 各由 222 次降为 62 次，下降 `72.0721%`；剖析重复从同一初态回放时 `runtime_view_bind` 为 0；
8. 最终工作树的六案各三次 10+60 步短程诊断中，`target_20.motion_only` 最差 P95/最大值为 `17.6037/18.0185 ms`，三次实时因子为 `0.990861 / 0.996894 / 0.985127`；相较 b1 的 `27.3490 ms` 与最小 `0.799687` 已明显改善，但仍未稳定跨过实时线；`target_20.guided_projectiles` 为 `57.2726/66.3568 ms` 与 `0.320656 / 0.319346 / 0.430476`，继续不实时；
9. 同一实现过程中的较早本地运行曾让目标纯运动达到约 `1.44×`，但最终复跑未复现，因此不选取较快结果宣称通过。最终结果复用了 T0b.1f 的旧分支判定器并得到 `branch_3_author_multirate_authority_rfc`；它只证明纯运动仍接近门槛、制导明显失败，不覆盖本规划已冻结的 `b2 → b3 → c/d → e/f/g` 依赖；下一步仍先完成 P0 的 `b3`；
10. 12/12 权威黄金与六案三重复状态/事件 hash 保持一致，8/8 dirty 域、弹药/时间线透传与严格负例通过；Python 58/58、TypeScript 4/4、Rust 15/15 回归通过。正式性能运行仍为 0，短程诊断仍未覆盖正式武器事件率和 600+3600 步资源门禁。

详细证据保存于 `舰艇数据/报告/阶段T0b2b2运行时稳定核心接口.v1.json`。原定下一切片为宽范围 `T0b.2b3`；风险审计确认旧实例覆盖、无事件时钟遗漏和跨步 dirty 漏唤醒不能由一个通用布尔标志安全解决，因此后续按 `b3a → b3b → b3c → b3d` 独立提交和回退。

## 十五、T0b.2b3 风险收敛与 b3a 完成记录

`T0b.2b3` 不再以“通用 dirty 跳过”为实现单元。dirty 只能触发重新计算，不能单独证明可以跳过；任何未知变化默认走完整路径。跨步骤缓存、持久到期索引和真正事件驱动仍由 `T0b.2e2` 独立实验承担。

`T0b.2b3a` 已完成精确边界上下文：

1. 新增不序列化的冻结 `ShipStepContext`，生命周期严格限制在单次 `advance_tactical_scene_step`；它保存舰艇 id、边界步号/时间、精确实例、实例代次、规范自动事件、runtime 视图和可选战术模型；
2. 上下文复用同时校验边界、实例代次、精确实例值和自动事件集合；实例变化会增加步内代次。runtime stable core 相同不再足以复用携带旧 `instance_snapshot` 的战术模型；
3. 步首生命周期、观测上下文和机动共享同一精确 runtime；战术模型只在实际进入运动积分时按需绑定；结果物化不再执行无用途的第二次模型绑定；
4. 步首武器事件改变弹药/时间线后，运动阶段会因精确实例不同建立新上下文，`commit_tactical_state_to_instance` 始终基于最新模型实例；技术夹具确认 4 艘舰同刻开火后的弹药与时间线变化在运动提交后仍保留，最终 runtime 精确等于场景实例；
5. `target_20.motion_only` 单步剖析中，runtime cache resolve 由 b2 的 80 次降为 60 次，战术模型绑定由 40 次降为 20 次；完整 runtime 编译和 runtime 视图重绑定保持 0，生命周期与武器时间线调用仍分别保持 40 次，未提前混入 b3b/b3c；
6. 12/12 权威黄金保持一致，统一场景时间线、生命周期和全部回归通过；独立的 `target_20.motion_only` 三次 10+60 步复跑最差 P95/最大值为 `11.5236/11.7713 ms`，最小实时因子为 `1.529980`，样本内常驻内存最大值为 `39907328` 字节；这仍是短程诊断，正式性能运行保持为 0。

详细证据保存于 `舰艇数据/报告/阶段T0b2b3a精确边界上下文接口.v1.json`。后续依次进入 b3b 时间线安全快进和 b3c 生命周期同一步复用；不得接入跨步 dirty、推进响应或多速率。

## 十六、T0b.2b3b 武器时间线安全快进完成记录

`T0b.2b3b` 已按“不遗漏事件、不遗漏权威时钟、不持久化到期索引”的边界完成：

1. 新增只绑定当前精确 `WeaponTimelineStateInput` 的 `WeaponTimelineAdvancePlan`，严格校验后保存源时间、目标时间、最早到期时间和三态模式；计划不序列化、不跨边界复用；
2. 三态固定为 `same_time_noop / clock_only / full_advance`：同刻且无到期事件时返回同一精确实例；时间前进但无到期事件时只更新 `timeline.tactical_time_s`；存在 `next_event_time_s <= target + EPS` 时必须进入完整事件引擎，包括最终会失败的开火或装填；
3. 统一场景在每个边界先建立步内舰艇计划表，再稳定按舰艇 id 消费。快速路径复用 `ShipStepContext` 已持有的精确实例 SHA-256，不再为无事件时间线额外规范哈希；有到期事件时继续走原堆、原稳定排序、原事件上限和原失败语义；
4. 计划应用前逐值核对源时间线。enqueue、cancel、reload、坠落暂停或其他时间线 mutation 后旧计划会以 `weapon_timeline.advance_plan_stale` 拒绝；到期计划误走快速应用会以 `weapon_timeline.advance_plan_requires_full` 拒绝；
5. 专项测试覆盖同刻 identity no-op、clock-only 时钟同步、EPS 内外、精确到期、完整路径强制、cancel 后 stale、实际 4 舰同刻开火以及 12/12 权威黄金；阶段 I 原有链式 fire/reload、失败、取消、齐射、离场和存档回归继续通过；
6. `target_20.motion_only` 单步剖析中，完整 `advance_weapon_timeline` 由 40 次降为 0，规范 `canonical_sha256/json` 各由 62 次降为 22 次；runtime resolve 保持 60、模型绑定保持 20、生命周期派生保持 40，未混入 b3c；
7. 独立三次 10+60 步复跑最差 P95/最大值为 `11.5092/12.3333 ms`，最小实时因子为 `1.627203`，样本内常驻内存最大值为 `41828352` 字节；Python 60/60、TypeScript 4/4、Rust 15/15 回归通过。该结果仍是短程诊断，正式性能运行保持为 0。

详细证据保存于 `舰艇数据/报告/阶段T0b2b3b武器时间线安全快进接口.v1.json`。下一子切片唯一为 `T0b.2b3c` 生命周期同一步投影复用：步首继续完整派生，步末仅在共享语义投影未变化时复用；不得扩展为跨步骤 dirty 缓存。

## 十七、T0b.2b3c 生命周期同一步投影复用完成记录

`T0b.2b3c` 已按“继续检查正式依赖、仅复用未变化语义、不建立跨步缓存”的边界完成：

1. 生命周期规则拆为唯一的 `project_tactical_ship_lifecycle` 语义投影与状态物化；原 `derive_tactical_ship_lifecycle` 直接调用这两层，不保留第二份判定规则；
2. 步首继续经完整派生入口建立本步状态；步末仍解析当前精确 runtime 并重新执行正式语义投影，只有物理状态、指挥状态、失败原因和退出字段全部相等时才复用当前生命周期对象；
3. 投影变化时仍在当前步末物化新状态、记录转移步号、产生原生命周期事件并暂停坠落舰的武器队列。CIC 命中、船壳崩溃、升力不足、CIC 关闭、遥控核心失效以及坠落/退出不可逆语义均由同一规则覆盖；
4. 本切片没有以 dirty 标志替代依赖读取，没有跨固定步保留投影，也没有跳过步末 runtime 解析。未知变化因此仍会进入正式投影，不能因遗漏 dirty 唤醒而静默沿用旧状态；
5. `target_20.motion_only` 单步剖析中，正式生命周期投影仍为 40 次，状态物化仅为步首 20 次，完整 `derive_tactical_ship_lifecycle` 入口由 40 次降为 20 次；runtime resolve、规范哈希和完整武器时间线分别保持不高于 60、22 和 0；
6. 专项夹具覆盖 6 类投影/派生等价状态、20 舰无变化步末复用以及实际弹丸摧毁 CIC 后同一结算步转入坠落。12/12 权威黄金不变；独立三次 10+60 步短程复跑最差 P95/最大值为 `10.7528/13.1486 ms`，最小实时因子为 `1.689098`，样本内常驻内存最大值为 `40046592` 字节；
7. Python 61/61、TypeScript 4/4、Rust 15/15 回归通过。上述仍是短程诊断，正式性能运行保持为 0，不能据此恢复 T0c 或宣称正式容量门通过。

详细证据保存于 `舰艇数据/报告/阶段T0b2b3c生命周期同一步投影接口.v1.json`。下一子切片唯一为 `T0b.2b3d` 画像与可选 scratch：先重新画像 b3a—b3c 的累计收益，只有存在明确、可测且不逃逸单步的临时容器热点时才实现 scratch；否则以证据取消该优化，不提前进入推进响应。

## 十八、T0b.2b3d 画像复盘、scratch 决策与 P0 关闭记录

`T0b.2b3d` 已完成画像与决策，不新增生产代码。P0 从 b1f 到 b3c 的累计结果如下：

| 检查点 | `target_20.motion_only` 最小实时因子 | 最差 P95 | 关键变化 |
| --- | ---: | ---: | --- |
| T0b.1f | 0.543660 | 35.5546 ms | 优化前短诊断基线 |
| T0b.2b1 | 0.799687 | 27.3490 ms | 65 候选安全搜索快速路径 |
| T0b.2b2 | 0.985127 | 17.6037 ms | runtime 稳定核心与轻量视图 |
| T0b.2b3a | 1.529980 | 11.5236 ms | 精确步内上下文与按需模型 |
| T0b.2b3b | 1.627203 | 11.5092 ms | 无到期武器时间线安全快进 |
| T0b.2b3c | 1.689098 | 10.7528 ms | 生命周期投影与未变化复用 |

相较 T0b.1f，目标规模纯运动最小实时因子提高到约 `3.107×`，最差 P95 降低 `69.76%`。当前单步画像为约 `8.79 ms`：`_load_metrics=20`、runtime resolve `60`、规范 hash `22`、完整时间线 `0`、完整生命周期派生 `20`。剩余成本分散在规范哈希、边界 runtime/生命周期与 20 舰机动积分；`step_context` 自耗时和场景内临时集合都没有形成足以抵偿新复用协议、清理义务及状态逃逸风险的集中热点。因此场景级 scratch 按既有停止门明确取消，P0 等价优化在此关闭。

制导档仍未实时。当前 `target_20.guided_projectiles` 单步画像约 `68.65 ms`，弹丸世界累计约 `48.40 ms`，占约 `70.50%`；单步包含 2696 次几何求交、296 次制导推进和 3922 次 `dataclasses.replace`。三次 10+60 步短跑实时因子为 `0.527043 / 0.414480 / 0.409571`，最差 P95/最大值为 `44.1455/51.6384 ms`，状态与事件 hash 三次相同。相较 b1f，最小实时因子提高约 `1.466×`、最差 P95 降低 `34.67%`，但差距仍明确位于弹丸批量路径。弹丸的稳定容器、配置预绑定、平面批次与单步 scratch 继续归 `T0b.2e1`，不得借 b3d 提前混入场景调度。

这一结果触发第八章规则 1，但不改变推进规则升级先于弹丸优化的依赖：推进响应会改变运动、燃料、命中输入和新黄金，若现在先按旧权威标定弹丸优化，将在推进接线后重复建立基线。d1 后复盘进一步把顺序细化为 `c1 → c2a → c2b → d1 → d1r → d2a → d2b → d3 → d4 → e1`，其中 d4 后优先处理 Python 弹丸批次，再决定是否需要 Rust；场景事件驱动、多速率和跨步 dirty 继续留在 e2。

详细证据保存于 `舰艇数据/报告/阶段T0b2b3d画像与Scratch决策接口.v1.json`。下一子切片唯一为 `T0b.2c1` 纯推进安全判定器，具体实施边界冻结如下：

1. 新建独立纯规则模块，不由现有战术场景、机动求解器或 runtime 调用；本切片不得改变旧场景状态、运动、燃料、事件或黄金 hash；
2. 定义停车、微速、四分之一、二分之一、四分之三、全速车钟，以及 `0、2、5……100%` 权威阶段的严格映射、相邻阶段和非法值拒绝；
3. 定义未序列化的安全策略、判定输入、governor 草稿状态、判定结果和事件意图。持久合同、版本号与迁移仍留给 c2，c1 不得抢先把草稿类型写入场景存档；
4. 纯判定器只按需请求“当前阶段”和“相邻升阶”载荷；只有当前阶段已不安全时才有界向下检查离散阶段。测试必须记录求值次数，禁止重新引入稳态全阶扫描；
5. 软限制覆盖结构与乘员，保留原车钟并实现介入、保持、释放候选及释放保持步数；硬可用上限与稳定原因作为显式输入，`overg` 只能绕过软限制，不能提高硬上限；
6. 输出只包含确定性的安全上限、有效目标、允许阶段、稳定排序原因和事件意图，不直接修改发动机、舰艇或场景；同输入三次结果与规范 hash 必须一致；
7. 专项测试至少覆盖安全稳态、相邻升阶被拒、当前阶段动态失安后的最高安全降档、结构/乘员多原因、滞回保持与释放、原车钟恢复、硬限制、`overg` 和非法阶段；
8. 退出门为专项与属性测试、12/12 旧黄金、Python/TypeScript/Rust 全回归通过，并由调用探针证明现有场景对新判定器调用数为 0。完成前不得进入 c2 合同或 d1 时间内核。

## 十九、T0b.2c1 纯推进安全判定器完成记录

`T0b.2c1` 已完成且保持未接线。新增独立接口 `gaotian.propulsion-safety-governor/v1alpha1`，冻结六档车钟到 `0、2、25、50、75、100%` 的命令映射，以及 `0、2、5……100%` 共 22 个权威输出阶段。判定器接收调用方提供的整舰合成载荷回调和显式硬可用上限，只返回草稿状态、相邻许可阶段和最多一个稳定事件意图；它不读取、修改或序列化现有场景对象。

按需求值门禁已经通过：安全稳态只求值当前阶段 1 次，安全或被拒绝的升阶均只求值当前与相邻阶段 2 次；当前 `50%` 失安而 `25%` 首次安全时共求值 6 次，直到 `0%` 仍不安全的边界共 12 次。同一判定内阶段结果会复用，不存在稳态全阶段预计算。软限制原因固定为结构、乘员顺序，并覆盖连续固定步释放、漏步重置、原车钟恢复和乘员锁关闭；`overg` 可立即移除软限制，但断油、断电、人员不可用、执行器/宿主损毁、跳闸和方向互锁形成的硬上限仍参与最终目标。

专项测试包含 22×22 个相邻阶段对、5 个严格拒绝负例和 3 次规范哈希重放；调用探针确认现有战术场景对新函数调用数为 0，12/12 旧黄金不变。完整回归为 Python 63/63、TypeScript 4/4、Rust 15/15；正式性能运行仍为 0。证据保存于 `舰艇数据/报告/阶段T0b2c1推进安全判定器接口.v1.json`。

下一子切片唯一为 `T0b.2c2a` 推进资源合同，实施边界冻结如下：

1. 将 c1 草稿安全参数落实为独立、版本化、严格载入的 `PropulsionSafetyProfile` 资源合同，冻结唯一 id、阈值、释放保持步数与内容指纹；非法字段、未知字段、非有限值及反向滞回阈值必须拒绝；
2. 新增模块推进 capability v2，并强制显式携带 `startup_time_s`：`main_engine > 0`，首轮 `maneuver_thruster = 0`，二者既有 `response_time_s > 0`；不得由解析器静默补默认值；
3. 只为仓库内已知 v1 模块目录建立按文档 id 命名、可审计且确定性的 v1→v2 迁移表。兼容启动时间仅是技术夹具值，不宣称正式平衡；未知目录不得猜测迁移；
4. 保持现有 v1 目录、舾装、实例、场景和 12 个黄金可继续按旧入口使用；c2a 不批量改写既有夹具，不把 `startup_time_s` 注入 v1 schema，也不让场景引用新资源；
5. c2a 不定义或序列化 `EngineRuntimeState`、`PropulsionGovernorState`、`TacticalPropulsionState` 和推进事件，不修改战术求解、运动、燃料或时间线。上述高频场景合同和旧场景迁移唯一留给 `c2b`；
6. 退出门为资源严格正负例、规范往返/指纹、全部具名迁移、未知迁移拒绝、12/12 旧黄金及三套完整回归通过。完成前不得进入 c2b，更不得提前接入 d1 时间内核。

## 二十、T0b.2c2a 推进资源合同完成记录

`T0b.2c2a` 已完成且保持未接线。新增独立 `gaotian.propulsion-safety-profile/v1alpha1` 资源合同与技术替身配置，严格冻结资源 id/version、结构与乘员介入/释放阈值、连续释放保持步数、夹具等级及规范内容指纹；缺失、额外、未知、非有限、非正数和反向滞回阈值均会显式拒绝。

模块目录另增 `gaotian.module-prototype-catalog/v2`，只在 v2 推进 capability 中强制显式 `startup_time_s`：主发动机必须大于 0，首轮姿态推进器必须严格等于 0，二者 `response_time_s` 继续大于 0。旧 `gaotian.ship/v1alpha1` Schema 和三份 v1 目录逐字保持不变，v1 解析器仍拒绝额外启动字段，不存在静默默认或批量原地改写。

迁移只接受仓库内三份 id、版本和规范 SHA-256 全部匹配的具名目录；共迁移 4 个推进模块，模块资源版本随 capability 升为 2，非推进模块逐字段不变。每份目录连续三次迁移得到同一目标指纹；未知目录和内容被篡改的已知目录均拒绝。推进安全配置往返与指纹、20 个资源/能力严格负例、12/12 旧黄金及 Python 64/64、TypeScript 4/4、Rust 15/15 回归通过；正式性能运行仍为 0。证据保存于 `舰艇数据/报告/阶段T0b2c2a推进资源合同接口.v1.json`。

下一子切片唯一为 `T0b.2c2b` 推进场景状态与迁移，实施边界冻结如下：

1. 定义并严格序列化 `EngineRuntimeState`、`PropulsionGovernorState` 与 `TacticalPropulsionState`，明确版本、所有权、不变量及规范指纹；不得在 c2b 内推进时间或计算推力；
2. 场景只新增推进安全配置引用、逐舰推进状态和确定性推进事件合同；引用必须精确到资源 id/version，资源解析失败不得静默回退；
3. 为仓库内已知旧场景建立具名、指纹锁定的迁移，初始化状态必须由旧场景与已知模块能力确定性导出；未知或被篡改场景拒绝猜测迁移；
4. 旧 v1 场景继续可由旧入口读取，12 个既有黄金保持不变；不得批量覆写旧夹具，也不得把新状态塞入旧 Schema；
5. c2b 不接入发动机启动、响应、燃料、运动、安全判定或硬故障。时间内核、力学与事件产生分别继续留给 d1—d4；
6. 退出门为状态/事件严格正负例、规范往返与指纹、全部具名场景迁移、未知迁移拒绝、12/12 旧黄金及三套完整回归通过。

## 二十一、T0b.2c2b 推进场景状态与迁移完成记录

`T0b.2c2b` 已完成场景合同升级但仍未接入时间与力学。新增版本化 `EngineRuntimeState`、`PropulsionGovernorState`、`TacticalPropulsionState` 与 `PropulsionStateEvent`：执行器按实例 id 排序，四个主方向 governor 按冻结通道顺序保存，发动机阶段、离散目标/实际输出、就绪/下次转换步、原车钟、软限制滞回状态和事件排序键全部进入严格合同与规范指纹。

统一场景采用兼容式双版本：旧 `gaotian.tactical-scene-timeline/v1alpha1` 的字段、Schema、序列化和哈希保持不变；显式迁移后才使用 `gaotian.tactical-scene-timeline/v2alpha1`，并强制携带推进安全配置精确引用/内容指纹及每艘舰的推进状态。v2 缺失配置、缺失任一舰状态、错误 policy、坏指纹或向 v1 偷塞新字段均拒绝；配置解析必须由引用和内容指纹同时匹配，不提供隐式回退。

12 个现有 T0 初始场景按场景名与旧规范 SHA-256 建立具名迁移，共生成 1224 个执行器状态和 896 个 governor 状态，每案连续三次迁移得到同一目标指纹。当前 T0 执行器存储模式均为 `active`，因此这 12 案迁移为 1224 个 `ready + 0%`；独立模式映射用例同时确认 `active → ready`、`standby/off → off`。未知场景名、内容被篡改的已知场景和错误兼容配置均拒绝。

状态合同 13 个、事件合同 6 个、场景合同 5 个严格负例均通过；新场景调用既有 `advance_tactical_scene_step` 会以 `propulsion_unwired` 明确拒绝，实际推进步数为 0。12/12 旧黄金及 Python 65/65、TypeScript 4/4、Rust 15/15 回归通过，正式性能运行仍为 0。证据保存于 `舰艇数据/报告/阶段T0b2c2b推进场景状态与迁移接口.v1.json`。

下一子切片唯一为 `T0b.2d1` 独立推进时间内核，实施边界冻结如下：

1. 新建无场景副作用的纯时间内核，只接收当前 `EngineRuntimeState`、精确 capability、固定步边界与命令，返回下一状态及排序事件；不得在 d1 调用战术机动或修改场景；
2. 主发动机冷启动严格在 `ceil(startup_time_s × 60)` 对应边界完成，完成前输出为 0；首轮姿态推进器的显式零启动时间直接进入 ready；
3. 0→100% 总响应步数严格为 `ceil(response_time_s × 60)`，只经过相邻离散阶段；不能整除时使用确定性的整数步分配，不依赖墙钟或浮点累积误差；
4. 中途改令以当前已提交实际阶段重新排程，停车逐阶段降到 0；同一步不得跨越多个输出阶段；
5. d1 只实现启动、响应、改令和停车事件，不接入力、力矩、油耗、c1 安全 governor、硬跳闸或方向互锁；这些继续留给 d2—d4；
6. 退出门为固定步边界、全部阶段、总响应时间、中途改令、停车及三次重放通过；旧 12 黄金不变，c2b 新场景仍不得由旧场景推进器执行。

## 二十二、T0b.2d1 独立推进时间内核完成记录

`T0b.2d1` 已完成但仍未接入场景。新增纯边界函数 `advance_propulsion_time_boundary` 与严格结果接口 `gaotian.propulsion-time-boundary-result/v1alpha1`：输入只包含单个 `EngineRuntimeState`、已解析的推进 capability v2、当前固定步边界和完整命令，输出同一执行器的下一状态及稳定排序事件。模块不导入统一战术场景，也不计算力、力矩、油耗，不调用 c1 安全 governor、硬故障或方向互锁。

响应排程不做逐步浮点累加。状态补充 `response_started_at_fixed_step` 与 `response_start_output_percent` 两个显式锚点；任一相邻阶段仍按 `command_step + ceil(response_time_s × 60 × delta_percent / 100)` 计算。主发动机冷启动在 60 Hz 下严格于 `ceil(startup_time_s × 60)` 边界完成，完成前输出恒为 0；姿态推进器显式零启动时间在命令边界完成就绪。中途改令先提交该边界已到期阶段，再从当前已提交输出重新排程；停车反向穿过全部相邻阶段，同一边界最多产生一个输出阶段事件。

旧兼容目录中的 `response_time_s=0.5/0.2` 只是 c2a 合同迁移值，完整上升/下降时会让多个相邻阶段落在同一 60 Hz 边界，不能同时满足精确总时长与单步单阶段。d1 因此以 `propulsion_time.response_unschedulable` 显式拒绝这两类能力，不跳级、不偷偷延长响应；正式平衡值必须通过可排程门禁后才能接入 d2。

定向用例确认：1 秒冷启动在第 60 步完成，随后 0→100% 依次产生 21 个相邻阶段事件并在第 120 步到达全输出；100→0% 从第 200 步开始，在第 260 步完成并进入 `ready`。第 12 步到期的 20% 阶段与四分之一中途改令在同一边界确定提交，新排程从 20% 起点于第 15 步到达 25%。三次 161 边界混合命令重放指纹一致，12/12 旧黄金及 Python 66/66、TypeScript 4/4、Rust 15/15 回归通过；正式性能运行仍为 0。证据保存于 `舰艇数据/报告/阶段T0b2d1推进时间内核接口.v1.json`。

下一子切片原定为 `T0b.2d2` 推进场景接线；d1 后全量审计证明资源、控制和合同版本尚不足以安全接线，因此该安排由下节正式取代，不再作为当前授权入口。

## 二十三、T0b.2d1p 接线前复盘与计划重排记录

本检查点只复核既有代码与 12 个 T0 场景，不修改推进生产代码、旧场景、运动、燃料或黄金。审计覆盖 12 个场景、224 艘舰和 1224 个执行器：主发动机 `1.0s` 共 280 个可通过 d1 门禁；无人主发动机 `0.5s` 共 48 个、姿态推进器 `0.2s` 共 896 个不能同时满足“精确总响应时间、百分比边界公式、同一步最多一个相邻阶段”。不可排程执行器共 944 个，占 `77.124183%`；每艘舰均有 4 个 `0.2s` 姿态推进器，因此 224/224 艘舰都不能直接进入原 d2。

场景绑定仍包含 3 种 v1 模块目录：最小目录可命中 c2a 原目录具名迁移，常规有人舰和无人旗舰使用的 2 种动态组合目录没有直接具名迁移；所有推进模块原型引用仍为 version 1，仓库没有落盘的 v2 推进目录或已迁移舾装方案。当前 `TacticalControlInput` 仍为连续 `move_body/wheel`，12 场景基准使用 `move_body.y=0.1`、`abs(wheel)=0.05`，而 d1 主发动机入口只接受六档车钟。能力来源与控制语义都不能在接线器里临时猜测。

d1 还在原 `v1alpha1` engine/tactical propulsion interface 下增加了两个必需响应排程锚点；这使同一合同 id 对应两种严格字段集合。由于推进场景仍被 `propulsion_unwired` 阻断、尚未建立新运动黄金，现在必须先升级 alpha interface 并保留 c2b→d1 显式迁移，不能把同 id 双语义带入 d2。场景单步结果目前仍输出旧 scene interface，且没有推进事件集合；这些输出合同留给 d2a 冻结、d2b 接线。

后续唯一顺序改为 `d1r → d2a → d2b → d3 → d4`，边界如下：

1. `d1r` 只升级 `EngineRuntimeState`、`TacticalPropulsionState` 和推进场景 alpha interface，恢复旧 c2b 状态可读并提供具名、指纹锁定的 c2b→d1 迁移；不改变 d1 排程算法，不推进场景；
2. `d2a` 建立可排程推进 capability 新版本。保留 c2a v2 历史，不原地改值；技术替身通过显式新版本迁移获得可排程响应值，并为 3 类场景目录、舾装原型引用和 1224 个执行器建立精确绑定；
3. `d2a` 同时定义版本化离散控制合同：玩家/自动驾驶主推只提交方向组车钟，姿态推进器目标按冻结规则量化；旧连续基准输入只能通过具名迁移进入新合同，自动制动的档位选择也必须显式冻结；
4. `d2a` 冻结场景单步结果的新 interface、推进事件集合与排序，但不修改力学。退出门要求 1224/1224 执行器精确绑定、可排程，3 类目录和旧输入迁移三次重放一致，未知/篡改资源拒绝；
5. `d2b` 才解除 `propulsion_unwired`，先提交每执行器时间状态，再按实际阶段、损伤效率和配平 use 聚合力、力矩与油耗；推进状态、配置引用和事件必须贯穿结果场景。旧连续 `_choose_command_scale` 在新路径不得形成不可观测的二次软限幅，d2b 的过渡政策必须在实现前写入测试；
6. `d3` 接入 c1 离散安全 governor，`d4` 再接硬跳闸、复位、紧急断推和方向互锁。`e1/e2`、编辑器和 T0c 继续暂停到 d4 新权威基线稳定。

证据保存于 `舰艇数据/报告/阶段T0b2d1p接线前复盘与计划重排接口.v1.json`。下一子切片唯一为 `T0b.2d1r` 推进状态合同修复：只处理 interface 版本、严格旧解析和具名迁移，不实现资源/控制桥或场景力学。

## 二十四、T0b.2d1r 推进状态合同修复完成记录

`T0b.2d1r` 已把 d1 排程锚点从 c2b 历史合同中分离。c2b 的 `gaotian.engine-runtime-state/v1alpha1`、`gaotian.tactical-propulsion-state/v1alpha1` 与 `gaotian.tactical-scene-timeline/v2alpha1` 恢复为原严格字段集合和原 12 案目标指纹；d1 状态分别升级为 `engine-runtime-state/v2alpha1`、`tactical-propulsion-state/v2alpha1` 和 `tactical-scene-timeline/v3alpha1`，两个响应排程锚点只在 d1 engine interface 下必需。旧、新解析器均拒绝额外字段、缺字段、父子 interface 混用和未知 interface；d1 时间内核也拒绝未显式迁移的 c2b engine state。

12 个 c2b 推进场景按场景名和 c2b 规范 SHA-256 建立具名 `v2→d1 v3` 迁移，连续三次重放一致，共升级 1224 个执行器状态并原样保留 896 个 governor 状态。当前 c2b 初态全部为 `ready + 0%`，因此两个 d1 排程锚点确定初始化为 `null`；若旧状态已处于 `running/stopping`，因 c2b 没有足够信息恢复响应排程起点，迁移器明确拒绝而不猜测。未知迁移、篡改源场景、重复迁移、错误 policy 和同场景混用 c2b/d1 状态全部拒绝。

本切片没有改变 d1 排程算法，没有解除 `propulsion_unwired`，也没有修改力、力矩、油耗、运动、推进事件结果或旧黄金。12/12 旧黄金及 Python 67/67、TypeScript 4/4、Rust 15/15 回归通过，正式性能运行仍为 0。证据保存于 `舰艇数据/报告/阶段T0b2d1r推进状态合同修复接口.v1.json`。

下一子切片唯一为 `T0b.2d2a` 推进资源与控制桥：建立可排程 capability 新版本、3 类场景目录/舾装引用迁移、1224 个执行器精确绑定、离散控制合同和新场景单步结果/推进事件合同，但仍不得接入力学、安全 governor 或硬故障。

## 二十五、T0b.2d2a 推进资源与控制桥完成记录

`T0b.2d2a` 已新增 `gaotian.module-prototype-catalog/v3`。该版本沿用 c2a v2 的显式 `startup_time_s` 字段集合，但增加 60 Hz 精确离散响应可排程门禁；c2a v2 历史值没有原地修改。三类场景目录先由原始内容目录确定性复现 v2 组合指纹，再经具名、指纹锁定的 v2→v3 迁移把技术替身推进响应统一为 `1.0s`。三份舾装只把推进原型精确引用从 version 1 升至 version 3，非推进引用、布置和其余字段保持不变；目录与舾装均连续三次重放一致，未知或篡改来源拒绝。

12 个 T0 场景的 224 艘舰已逐场景、逐舰、逐执行器建立精确绑定：共 `1224/1224`，包含 328 个主发动机和 896 个姿态推进器；全部响应为 60 步，主发动机启动为 60 步，姿态推进器启动为 0 步。命令通道由既有配平 use 反查并冻结为 forward 328、left 448、right 448、reverse 0，绑定同时保存 v3 原型精确引用、目录精确引用和目录内容指纹。

新离散控制接口为 `gaotian.tactical-propulsion-control/v1alpha1`。四个方向组按固定顺序提交主机车钟和姿态推进目标；姿态推进采用最近离散阶段、同距向上，主机采用最近车钟、同距向上。224 份旧 T0 连续输入只能命中两条具名迁移：`move_body.y=0.1` 映射为 forward `dead_slow + 10%`，`abs(wheel)=0.05` 映射为 left/right `5%`。自动制动显式冻结为速度反向通道的主机 `quarter` 与姿态推进 `25%`，不再留给接线器猜测。

场景输出新增 `gaotian.tactical-scene-step-resolution/v2alpha1` 包络及带 `ship_id` 的场景推进事件，排序键冻结为固定步、舰艇、执行器、事件枚举序。该包络目前只负责合同和稳定收集；`propulsion_unwired` 未解除，力、力矩、油耗、运动、安全 governor、硬故障和方向互锁均未改变。12/12 旧黄金保持不变，正式性能运行仍为 0。证据保存于 `舰艇数据/报告/阶段T0b2d2a推进资源与控制桥接口.v1.json`。

下一子切片唯一为 `T0b.2d2b` 推进力学接线：按离散控制与逐执行器时间状态提交实际输出，再通过冻结的配平 use 聚合力、力矩和油耗，并建立可解释的新版本黄金。d2b 不得提前接入 d3 软安全 governor 或 d4 硬故障/方向互锁。
