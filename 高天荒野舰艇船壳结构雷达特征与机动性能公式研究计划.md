# 《高天荒野》舰艇船壳结构、雷达特征与机动性能公式研究计划

## 一、研究目标

本轮研究用于敲定三组能够直接服务船壳设计器、舾装设计器和战术运动求解器的性能公式：

1. 船壳质量、局部装甲耐久、全舰船壳耐久与结构强度；
2. 随船体朝向、船壳几何、船体涂料和外部模块变化的雷达反射截面积；
3. 从质量、升力、主发动机、转向发动机、气动阻力和转动惯性推导实时机动表现。

本轮首先敲定公式结构、输入字段、缓存层级和验证标准。首轮七种结构材质参数已经完成第一版标定；模块推力、环境密度及其余全局平衡系数仍在教程舰建立后标定，不用尚未验证的精确数字倒推公式。

## 二、已有基础与实际缺口

| 方向 | 已经形成的基础 | 本轮需要补全的内容 |
| --- | --- | --- |
| 船壳结构 | 五米网格、多层五米甲板、各层独立结构材质、精确多边形边、逐边装甲材质与厚度、严格整格、逐层支撑、全舰船壳耐久与局部装甲耐久分离 | 纵向、横向载荷切面与偏航代表点负荷的推荐公式、标准测试体、七种结构材质、九种逐边基础装甲材质及甲弹对抗第一版公式均已完成；教程武器与常见装甲厚度联合标定推迟到规范蓝图、编辑器往返和教程舰阶段 |
| 气动阻力 | 已形成方向性等效阻力面积：形状阻力、表面摩擦和波阻分别计算，并在船壳蓝图保存时生成方向表 | 标定三个形状系数、表面粗糙度、三高度层介质参数和马赫数响应曲线 |
| 回转 | 已形成复合转动惯性公式、`τ=xF_y-yF_x`、`α=τ/I`、目标角速度舵盘控制器、回中反推制动、主机配平和物理残余副作用聚合，并已接入出航/实例运行时 | 正式模块布局下的回转参数标定 |
| 平动与升力 | 固定步平动与偏航方程、半隐式欧拉积分、制动档、结构/乘员指令限制、OverG、发动机分配、三层环境和参数化换层状态机均已接入正式适配器 | 实际模块与教程舰参数标定，包括环境与换层时间正式值 |
| 雷达特征 | 第一版采用统一 `0.10m` 参考波长与三百六十度水平方位表；规范化可见表面组、相干面积软限制、展宽镜面项、投影底项、凹角项、船体涂料和外挂模块接口均已形成并通过原型测试 | 随教程舰和教程搜索雷达标定隐身涂料倍率、方向瓣、底项与外部模块基准特征；只有形成明确多频段玩法后才扩展频段表 |

现有气动与回转调研不需要推倒重写。本轮机动研究应把两者接入同一个实时求解器，并只补足尚缺的控制和状态接口。

## 三、共同研究原则

### 3.1 物理骨架与游戏代理分开

保留具有明确量纲和相对关系的物理骨架，例如质量、力、力矩、动压、转动惯性、雷达截面积和截面模量。真实计算需要有限元、计算流体力学或全波电磁求解时，改用能够从现有二点五维船壳数据稳定生成的代理量，并明确记录代理量的适用边界。

### 3.2 不按舰种填写隐藏修正

同一材料、几何与模块使用同一套公式和全局系数。舰种差异应来自设计本身，不为“驱逐舰”“巡洋舰”等分类另加不可见的结构、隐身或机动倍率。

### 3.3 保存时预计算，战术中低成本读取

- 只依赖船壳几何的量在船壳蓝图保存时计算；
- 依赖模块布局的量在舾装方案保存时计算；
- 依赖货物、战损、供电或设备状态的量在舰艇实例中增量更新；
- 战术模式不逐帧重新遍历船壳多边形或求解复杂几何。

### 3.4 必须具有绘制不变性

在几何、材质和厚度完全相同的情况下，下列编辑操作不得改变性能结果：

- 把一条直边拆成多条共线边；
- 合并相邻且参数相同的共线边；
- 改变多边形起始端点或绕序；
- 以不同的合法绘制顺序得到同一船壳；
- 仅改变蓝图名称或版本号。

因此，结构和 RCS 计算前都应先建立规范化几何表示，不能直接把“玩家画了多少条边”当作性能来源。结构计算仍按同材质、同厚度合并；RCS 只按共线与连续关系合并，基础装甲材质和厚度变化都不产生新的外部散射表面。

## 四、共用标准测试体

三组研究共用以下无武装标准船壳，以便比较公式是否给出一致的相对关系：

1. 单层平艏平艉矩形体；
2. 同长宽、同质量的尖艏平艉体；
3. 同长宽、同质量的尖艏尖艉体；
4. 长宽比不同、面积相同的矩形体；
5. 中部具有狭窄连接的凹形船体；
6. 上下层完全同形的双层体；
7. 上层逐渐收窄的阶梯形多层体；
8. 上层轮廓突然变化的多层体；
9. 几何相同但装甲材质或厚度不同的船体，用于验证结构、防护发生变化而基础 RCS 保持不变；
10. 船壳相同但外挂模块数量、位置与种类不同的舾装；
11. 总推力相同但发动机力臂不同的舾装；
12. 空载、均匀装载及远端货仓满载的同一舰艇实例。

每个标准体还要生成共线边拆分版本，用于验证绘制不变性。

## 五、船壳结构公式研究

### 5.1 必须先区分的四个结果

结构研究不能用一个“结构值”代替所有用途，至少应分别生成：

- **船壳质量：** 参与设计质量、升力、平动惯性和转动惯性；
- **局部装甲耐久：** 每条规范化船壳边独立保存，归零前保持原有效厚度；
- **全舰船壳耐久：** 只有击穿后的伤害才削减，用于结构崩溃判定；
- **结构强度：** 表示船体承受整体载荷和局部结构瓶颈的能力，并用于计算舰艇的结构安全 G 值。

### 5.2 结构质量的候选骨架

逐边基础装甲可以直接形成有量纲的质量：

`m_edge = ρ_mat × L_edge × H_deck × t_edge`

其中固定甲板层高 `H_deck=5m`。这能够计算各高度带竖直船壳边的材料体积和质量，但不能单独表示甲板、内部骨架和层间连接的质量。

内部骨架仍采用不要求玩家逐根布置骨架的面积代理，但每个甲板层分别选择一种结构材质。一个甲板层即使包含多个彼此分离的合法多边形，也共用该层的结构材质。

对甲板层 `l`，令其全部多边形精确面积之和为 `A_l`，等效结构体积为：

`V_struct,l = t_deck × A_l + δ_l × t_joint × A_l`

其中基底层 `δ_l=0`，非基底层 `δ_l=1`：

- 第一项抽象该层甲板板材、纵横骨架、舱壁和基础连接件；
- 第二项抽象该层与相邻下层之间的立柱、支撑和加强连接。由于禁止悬挑，上层面积全部受到下层支撑，因此可以继续使用 `A_l`；
- `t_deck` 和 `t_joint` 是全游戏统一的等效结构厚度，只负责把精确面积换算为抽象结构材料体积。

现实尺度标定后正式取 `t_deck=0.100m`、`t_joint=0.020m`。现实大型钢船公开资料中的普通壳板与主要板材大致处于 `10–30mm` 量级；这里的 `100mm` 折算了同一五米高度带内多张不同方向的板面、纵横骨架、舱壁和梁，`20mm` 则折算非基底层额外的层间支撑，二者都不是单块钢板的字面厚度。以 `2650m²` 基准轮廓计算，单层装甲钢内部结构质量为 `2,080.25t`，同轮廓非基底层的层间连接额外增加 `416.05t`。

本层内部骨架质量与全舰总内部骨架质量为：

`m_struct,l = ρ_struct,l × V_struct,l`

`m_internal = Σ_l m_struct,l`

本层选择的结构材质负责本层甲板、骨架和舱壁，并负责该层向下连接的层间支撑。其材料字段至少包括密度、强度系数、船壳耐久加成系数、成本与加工难度。它不参与炮弹首先命中的逐边基础装甲穿深、局部装甲耐久或外部 RCS。

结构质量读取精确多边形面积，不读取可用内部格数量；不完整边界格即使不能安装模块，仍然属于实际船壳并计入结构质量。逐边装甲材质也不向内部区域分摊骨架质量。

不同结构材质对全舰船壳耐久的加成先按层线性累加：

`H_struct = K_hull × Σ_l(V_struct,l × E_hull,l)`

其中 `E_hull,l` 是该层结构材质的船壳耐久加成参数。结构强度不对各层系数做简单平均，而把各层材料强度带入纵向和横向载荷切面；各通道由最低有效切面容量与切面两侧较小质量计算安全加速度，使弱材质所在位置、承载面积和狭窄连接真实影响结构安全值。

结构材质需要形成重量、耐久、强度、成本和加工难度之间的取舍，不能存在一种材料在所有维度全面优于其他材料。

### 5.3 局部装甲耐久候选式

局部装甲耐久确定采用基于材料体积与单位体积损伤承受能力的线性形式：

`H_edge,max = K_local × L_edge × H_deck × t_edge × E_mat`

其中 `E_mat` 是装甲材质的耐久参数，`K_local` 是全局战斗尺度换算系数。线性使用边长能够保证一条边被拆分后，总局部耐久不变；厚度既提高穿深防护，也增加局部耐久和质量。

公式中的 `H_deck` 固定为五米。仍需专门验证：极长边的单一耐久池是否导致命中相距很远的位置仍共享损伤。若游戏反馈不理想，应调整局部损伤记录方式，而不是用非线性的边长指数修补，因为后者会直接产生拆边取巧。

### 5.4 全舰耐久与结构强度的方案结论

已经比较以下模型：

| 方案 | 反映尺寸与层数 | 发现狭窄连接 | 计算与解释成本 | 用途 |
| --- | --- | --- | --- | --- |
| 总面积/总质量耐久 | 是 | 否 | 最低 | 作为全舰耐久基线 |
| 沿舰艏轴逐片计算有效截面与截面模量 | 是 | 是 | 中等 | 物理关系较清晰，适合整体弯曲强度代理 |
| 五米结构格连通图与最小承载割面 | 是 | 是 | 中等至较高 | 能处理凹形和多层连接，但需要额外解释与防离散噪声 |
| 纵向/横向材料加权载荷切面 | 是 | 是 | 较低 | 选为第一版结构安全值模型 |

第一版采用“总有效材料量决定全舰耐久＋纵横载荷切面瓶颈决定结构安全值”的混合方案：

- 全舰耐久由船壳和内部结构的总有效材料量线性生成；
- 沿舰体纵向和横向分别以五米步长计算多层船壳的材料加权有效承载切面；
- 切面使用区间中心采样、端部样本排除和三点中值滤波，再读取最低有效容量，避免几何采样误差支配整舰；
- 狭窄腰部、突然变截面和层间支撑不足应降低结构强度；
- 同外形双层船体应比单层船体具有更大质量、全舰耐久和结构强度。

前后共用纵向安全值，左右共用横向安全值。斜向平动只按纵横分量组合；偏航角加速度和角速度通过舰体代表点的局部加速度进入统一载荷比，不为其他角度生成结构切面。

现实舰体整体纵向强度以弯矩、截面惯性矩和截面模量为核心；美国海军舰船救捞手册也以受损前后有效截面、截面模量及弯曲应力评估剩余强度。因此截面模量适合作为本项目结构代理的物理骨架，但具体载荷仍需按飞行战舰的惯性与升力设定重新简化。[NAVSEA《U.S. Navy Salvage Manual》](https://www.navsea.navy.mil/Portals/103/Documents/SUPSALV/Salvage%20Docs/Salvage%20Manual%20Vol%201%20S0300-A6-MAN-010.pdf)

### 5.5 结构研究的验收标准

- 同几何同材质时，质量、耐久和强度不受拆边影响；
- 加厚装甲必然增加质量、穿深防护和局部耐久；
- 增加完整受支撑甲板必然增加质量和整体强度；
- 中部狭窄连接的舰体弱于面积和质量相近的连续舰体；
- 尺寸扩大后的性能变化连续，不因跨过某个五米采样位置突变；
- 公式不要求玩家额外选择一种已经被否定的“船壳基础材质”；
- 战损阶段不需要重新求解完整结构模型；全舰耐久仍按既定规则统一扣减。

## 六、雷达反射截面积公式研究

### 6.1 结果口径

RCS 应作为目标自身的特征，以平方米保存，并可在界面转换为 `dBsm = 10log10(σ/1m²)`。雷达功率、天线增益、噪声、传播损失、天气和高度层属于传感器及传播计算，不能混入船体 RCS。

单站雷达方程中，最大探测距离只按 RCS 的四次方根变化，因此 RCS 与探测距离不能线性换算。MIT Lincoln Laboratory 的雷达课程分别给出了雷达方程与目标 RCS 的定义、波长、尺寸、形状、入射方向及材料对回波的影响，适合用于固定本项目的数据接口。[MIT Lincoln Laboratory：Introduction to Radar Systems](https://www.ll.mit.edu/outreach/web-based-course-radar-introduction-radar-systems)

### 6.2 不采用的方案

- **只按舰体尺寸或投影面积：** 无法奖励倾斜表面，也不能表示装甲材质和外部模块；
- **所有边简单线性相加：** 会受到玩家拆边数量影响；
- **直接把各项 dBsm 相加：** dB 是对数显示量，不能作为功率线性叠加；
- **逐舰进行全波电磁求解：** 频率、极化、相位、边缘绕射和多次反射的实现与标定成本远超游戏需要。

### 6.3 第一版结论：规范化表面组的方向性代理

船壳蓝图保存时，先把共线、连续的相邻边合并为规范化表面组；基础装甲材质和厚度都不作为 RCS 分组条件。随后对每个方位角预计算普通涂料基准表：

`σ_hull,base(β) = Σ[σ_spec,base,i(β)+σ_diff,base,i(β)] + Σσ_corner,base,j(β)`

`σ_total(β) = K_coat σ_hull,base(β) + Σσ_external_module,i(β)`

单个表面组采用以下物理启发式结构：

`Q(A_i)=A_i²/(1+A_i/A_coh)`

`σ_spec,base,i=K_spec [4π/λ_ref²] Q(A_i) μ_i^p`

`σ_diff,base,i=K_diff A_i μ_i`

其中：

- `K_coat` 为全舰统一船体涂料的 RCS 线性倍率，普通涂料为 `1.000`；
- `A_i` 为经过方向可见性处理的实际外露表面组面积；
- `A_coh` 是有效相干面积软限制；
- `λ_ref=0.10m` 为游戏统一参考雷达波长；
- `μ_i=max(0,n_i·s)` 为表面法线朝向雷达的余弦；
- `p` 是展宽后的镜面方向瓣指数，投影底项则避免倾斜表面获得不合理的零回波；
- 凹角另以转角、相邻两面同时可见程度和五米特征长度形成非负基准角项；`K_coat` 在整条船壳基准方向表生成后统一应用一次。

高频 RCS 研究表明，物理光学适合处理大尺度照明表面，边缘绕射和角反射还需要额外方法；矩形平板在近法向与掠射状态的机理和误差也明显不同。因此本项目应只借用“面积平方、波长和表面朝向影响镜面回波”的关系，并用单独的边缘/角部底项避免倾斜表面获得不合理的零回波。[NASA：High-frequency techniques for RCS prediction of plate geometries](https://ntrs.nasa.gov/archive/nasa/casi.ntrs.nasa.gov/19910018126.pdf)，[NASA：A Physical Optics/Equivalent Currents Model for RCS](https://ntrs.nasa.gov/api/citations/19940020143/downloads/19940020143.pdf)

外部模块不重新参与船壳表面求交。每种模块原型保存基准 RCS、必要的方向修正和几何状态规则，在线性平方米口径下与船壳结果相加。顶挂传感器、炮塔和其他明显外露设备可以因此提高特征；断电或功能损毁但残骸仍在时默认保持原几何特征，只有收拢、折断、脱落或清除残骸等确实改变外形的状态才应用不同倍率。

### 6.4 第一版方向表结论

第一版确定只保存三百六十度水平角。当前舰艇只有离散高度层，没有连续高度或俯仰姿态；使用三档相对高度会强行引入与水平距离无关的任意代表俯仰角。不同高度层的探测差异先由传播、遮蔽和天气系统处理，顶挂模块通过自身 RCS 参与。

缓存接口仍预留 `elevation_band` 维度，第一版仅保存 `LEVEL`。将来若加入连续高度或证明跨层俯视/仰视特征具有明确玩法价值，再增加 `ABOVE` 与 `BELOW`，不修改现有船壳蓝图几何。

### 6.5 RCS 研究的验收标准

- 同一几何的拆边与合边版本给出相同结果；
- 大型平直舷侧的正向回波高于倾斜舷侧；
- 尖艏正前方特征低于同尺度平艏；
- 外部模块增加后总特征按平方米线性增加，不按 dB 直接相加；
- 隐身涂料在其他条件相同时降低全部船壳表面贡献，但不自动降低外挂模块贡献；
- 方向变化连续，不在整数角采样边界跳变；
- 船壳尺度、表面积和材料变化对结果保持单调；
- 最终通过雷达方程测试，确保 RCS 的变化不会造成与预期完全不符的探测距离差异。

## 七、机动性能公式研究

### 7.1 机动闭环范围

机动研究负责把现有气动与回转公式接入以下二维刚体状态：

- 世界坐标位置 `p`；
- 世界坐标速度 `v`；
- 舰艏朝向 `ψ`；
- 偏航角速度 `ω`；
- 当前高度层与高度层切换状态。

第一版不加入俯仰、滚转、气动升力、气动压心和气动偏航力矩。船体可以保持舰艏与速度方向不一致，侧滑角通过现有方向性阻力公式产生代价。

### 7.2 平动与回转骨架

固定物理步中使用：

`m_current × dv/dt = F_main + F_turn,net + F_drag`

`dp/dt = v`

`I_current × dω/dt = τ_turn + τ_main`

`dψ/dt = ω`

其中 `F_drag` 使用现有方向性等效阻力面积公式，`I_current` 使用现有复合转动惯性公式。NASA 的刚体飞行动力学同样将质量、推力和气动力用于平动方程，将转动惯性与各项力矩用于转动方程；本项目只取其中的二维水平平动与偏航子集。[NASA/TM–20250008678](https://ntrs.nasa.gov/citations/20250008678)

实现研究需要比较半隐式欧拉与更高阶积分器在固定时间步下的稳定性。第一版优先选择行为稳定、可复现且便于联网或回放的固定步方案，不以理论阶数最高为唯一目标。

### 7.3 主发动机与转向发动机分工

必须建立明确的推力分配规则：

- 主发动机主要响应前进、制动和横移档位，并遵守已经确定的两侧推力配平；
- 转向发动机主要响应偏航指令；
- 每台发动机的实际推力受损伤、供电、人员、自动化、燃料和开关状态影响；
- 如果转向发动机组合无法形成纯力矩，其未抵消的合力是否实际推动舰体，需要敲定；
- 主发动机配平后是否把全部残余偏航力矩强制消除，也需要敲定。

研究阶段应同时计算所有发动机的净合力与绕 CIC 净力矩，以便比较“完全自动抵消副作用”和“保留实际残余力/力矩”两种手感。无论最后选择哪种，设计器都应能解释为什么某种发动机布局加速或转向较差。

### 7.4 舵盘输入与自动反推制动

已经敲定舵盘偏转量映射目标角速度，回中表示目标角速度为零。舵盘不直接改写舰艏角或当前角速度；控制器根据目标角速度与当前角速度之差请求实际偏航力矩，回中后使用反向转向发动机逐步制动。第一版采用受限比例角速度控制器，完整公式、结构安全角速度裁剪和非对称执行器处理见《高天荒野舰艇机动性能计算调研》。

固定步原型已经完成“满舵建立角速度—舵盘回中—反向力矩制动”测试：测试舰回中瞬间角速度约为 `0.501858rad/s`，八秒后收敛到 `0.000000796rad/s`，过程中没有直接清零角速度或永久自旋。

### 7.5 升力、高度层与高 G

升力首先使用无量纲余量显示：

`R_lift = L_available / (m_current × g_ref)`

- `R_lift<1`：无法维持当前舰艇；
- `R_lift=1`：只能勉强维持；
- `R_lift>1`：具有升力储备。

第一版已经选择“参数化固定基础耗时”路线：环境配置表保存相邻层切换时间，跨越多层时求沿途之和；向上切换要求 `R_lift>1`，完成前继续使用起始层环境。当前相邻层基础时间暂取 `30s`，仅用于验证接口与状态机，属于 `prototype_unbalanced`；是否在教程舰阶段加入升力余量修正，作为参数标定问题处理，不再改变状态契约。

人员高 G 判定与船体结构过载判定分开。玩家与 CIC 核心人员的高 G 判定至少应读取 CIC 处的平动加速度：

`G_CIC = |dv/dt - g_environment| / g_ref`

由于玩家位于固定 CIC 原点，偏航产生的局部切向和向心项在 CIC 处为零；若以后需要计算其他舱室或船员位置的局部 G 值，再加入 `α×r` 与 `ω×(ω×r)`。只要舰上存在任何人员，控制系统就启用全舰统一的 `12G` 乘员安全锁，主动机动不能超过该值。

船壳结构强度分别推导纵向与横向安全加速度；前后共用纵向值，左右共用横向值。斜向平动只把纵横分量代入椭圆交互式，偏航则把代表点的切向和向心加速度折算为纵横分量，最终得到统一结构载荷比 `R_struct`。不建立任意角度结构方向表。默认机动控制禁止舰艇主动执行会使 `R_struct>1` 的推力指令；完全无人舰艇虽然没有乘员上限，但未启用 `OverG` 时仍受结构限制。

玩家可以主动开启 `OverG` 选项，临时解除结构安全限制，但不能解除有人舰艇的 `12G` 乘员安全锁。完全无人舰艇没有该乘员上限。实际 `R_struct>1` 的持续时间内，当前全舰船壳耐久持续下降；载荷回落到安全范围后停止扣减。推荐损伤率为：

`dH_hull/dt = -(H_hull,max / T_ref) × [max(0,R_struct-1)/(R_ref-1)]²`

平方曲线允许轻微超限维持较长时间，但使严重超限迅速变得危险。`R_ref` 与 `T_ref` 属于全局标定参数。OverG 可以持续把当前全舰船壳耐久削减到结构崩溃阈值，不提供最低耐久保护；第一版当前船壳耐久不反向降低结构安全值。

### 7.6 设计器应输出的机动指标

- 设计质量与典型载荷下当前质量；
- 升力余量；
- 前进、倒车、横移的零速初始加速度；
- 各高度层正向持续最大速度；
- 指定速度下的制动时间与制动距离；
- 左右最大转向力矩、空载与典型载荷下角加速度；
- 静止状态完成九十度和一百八十度转向的预计时间；
- 指定初速下转向后的速度矢量保持、侧滑和重新加速过程；
- 满载货仓对加速度和转动惯性的影响；
- 纵向安全 G、横向安全 G、安全偏航角加速度和安全偏航角速度；
- 有人舰艇的 `12G` 硬上限，以及完全无人状态解除乘员安全锁后的可用范围。

不宜只显示一个合成“机动性”评分；如需排序评分，也必须允许玩家展开查看上述原始派生量。

### 7.7 机动研究的验收标准

- 同质量下增加有效推力会提高加速度；
- 同推力下增加质量会降低加速度；
- 同力矩下增加转动惯性会降低角加速度；
- 同推力发动机远离 CIC 且作用线合适时产生更大力矩；
- 货物装入远端货仓会同时增加质量与转动惯性；
- 松开或回中舵盘后，舰艇不会在没有持续命令的情况下永久自旋；
- 舰艏转向不会瞬时旋转速度矢量，舰艇会保留惯性并产生侧滑；
- 最大速度来自推力与阻力的平衡，不使用舰种硬编码速度上限；
- 固定时间步变化时结果保持在允许误差内，不出现高推力舰艇穿透或数值爆炸。

## 八、研究顺序与交付物

### 阶段零：先敲定会改变数据结构的接口

已经敲定：每个甲板层分别选择一种结构材质；结构负荷只计算纵向前后、横向左右和偏航转向，斜向平动只组合纵横分量；默认禁止结构载荷比超过一，玩家可开启 `OverG`，以持续损失全舰船壳耐久为代价解除结构限制；只要舰上存在任何人员，主动机动便始终受 `12G` 乘员安全锁限制，完全无人舰艇没有该上限；局部装甲耐久采用线性公式；第一版当前船壳耐久不反向降低结构安全值。RCS 第一版只保存三百六十度水平方位表，并统一使用 `0.10m` 参考波长；缓存结构预留高度档和未来有限频段扩展。舵盘采用目标角速度语义，回中后通过实际反向力矩制动；固定步原型采用 `1/60s` 半隐式欧拉积分。

主发动机中轴独立归组、离轴两侧配平后保留真实残余偏航、转向发动机保留未抵消平动力，以及参数化固定基础换层耗时均已敲定并进入回归。阶段零的数据结构问题已经封闭，余项属于教程舰共同标定，不再阻塞契约实现。

### 阶段一：船壳结构调研文档

已形成《高天荒野舰艇船壳结构计算调研》，提出“材料体积线性耐久＋纵横材料加权载荷切面＋偏航代表点负荷”的推荐第一版。结构接口已经全部确认，可以进入原型测试与参数标定。调研覆盖：

- 船壳和内部结构质量公式；
- 局部装甲耐久与全舰耐久公式；
- 截面采样、结构瓶颈和结构强度公式；
- 蓝图缓存字段与标准测试结果。

### 阶段二：雷达特征调研文档

已形成《高天荒野舰艇雷达反射截面积计算调研》和可复现原型测试，敲定：

- 规范化表面组和可见性处理；
- 方向性表面、边缘/角部底项与船体涂料倍率；
- 外部模块的 RCS 数据接口；
- 船体 RCS 与雷达方程、天气和侦察系统的边界；
- 方向表和标准测试结果。

公式结构已经完成第一版：`155×20m` 单层标准体的全向线性均值约为 `0.89～1.01×10⁴m²`，尖艏主要压低正前方特征而不消除侧向大特征。装甲材质不再参与基础 RCS；普通涂料倍率固定为 `1.000`。隐身涂料正式倍率与外挂模块基准 RCS 必须等待规范蓝图集成测试、编辑器往返测试和教程舰建立后共同标定。

### 阶段三：机动闭环调研文档

已经形成《高天荒野舰艇机动性能计算调研》《高天荒野舰艇机动性能公式原型测试.py》和测试报告。当前已经敲定或实现：

- `1/60s` 固定步与半隐式欧拉平动、偏航求解；
- 目标角速度舵盘控制、回中反推制动和制动档；
- 执行器四分量输出接口，未锁死发动机残余副作用；
- 升力余量、换层回调、高 G、安全锁与 OverG 接口；
- 设计器机动输出、战术状态更新和调试字段；
- 推力、质量、惯量、惯性、制动、最大速度、结构限制、OverG、有人 `12G` 锁和固定步收敛测试。

首轮自动断言全部通过。原始脚本属于直接填写整舰聚合参数的 L0 求解器单元测试，只证明机动方程和控制语义；生产适配器现已另行从规范蓝图、实际模块布局、出航配置和舰艇实例生成战术输入，并通过固定步机动、气动、RCS、换层与 OverG 回归。两类测试保持分层，不把 `contract_fixture` 当作正式内容舰。

### 阶段四：规范数据与无界面船壳编译

先建立材料定义、模块原型、船壳蓝图、舾装方案、出航配置、派生性能快照和舰艇实例快照的统一机器可读数据契约。随后实现不依赖玩家界面的船壳编译器，从规范船壳蓝图生成：

- 拓扑、逐层支撑、轴对称和严格整格校验；
- 内部格、局部露天甲板格和侧挂步长；
- 船壳与基础装甲质量、耐久和结构安全值；
- 船壳转动惯量、气动方向缓存与船壳 RCS 缓存。

现有简单多边形公式测试逐步迁移为读取同一种 `HullBlueprint` 输入。此阶段不建立正式教程舰，也不标定控制手感。

### 阶段五：最小模块表与无界面舾装编译

先建立 CIC、灵烷贮槽、主发动机、转向发动机、发电机、最低损管、人员容量和遥控核心舱等最小模块原型，再从具体模块锚点、旋转、占格和状态派生质量分布、惯量、升力、供电、人员、推力和力矩。

主发动机中轴归组、主机配平后残余偏航力矩和转向发动机残余平动力已经敲定并进入阶段 D 回归。阶段 E 正式适配器已使用参数化相邻层基础耗时完成换层接线；当前数值仍待教程舰标定。

### 阶段六：规范蓝图测试舰与端到端测试

只有阶段四和阶段五完成后，才建立最小合法舰、常规有人战舰和完全无人主旗舰三条规范测试舰。三者保存船壳蓝图、舾装方案和出航配置，不保存能够覆盖派生结果的手填整舰质量、惯量或推力。

端到端测试在运行时从源布局重新生成全部性能，用于验证几何、模块位置、货物、战损、推力配平、结构限制和机动闭环之间的关系。本阶段只确认数据追溯与相对关系，不进行正式节奏标定。

### 阶段七：玩家编辑器与往返验证

玩家船壳编辑器和舾装编辑器只能调用阶段四、阶段五的同一套校验与派生代码。三条规范测试舰必须能够由编辑器读取、显示和原样保存；往返后的规范几何、模块布局和派生结果保持一致，方可称为与落地后相同的实际布局。

### 阶段八：教程舰与共同标定

编辑器往返验证通过后，使用教程舰以及小艇、大型舰等规范蓝图共同标定：

- 模块质量、推力、升力、功率、人员、成本和工时；
- 气动形状系数、表面摩擦、波阻和高度层介质参数；
- `K_turn`、舵盘目标角速度、控制器响应和换层时间；
- 隐身涂料、方向瓣、底项和外部模块基准 RCS；
- 教程装甲厚度、火炮穿深和局部装甲承弹次数。

正式顺序、数据门槛和 L0 至 L4 测试层级统一见《高天荒野舰艇建造与派生性能剩余实施计划》。若规范测试不能满足相对关系，应退回对应专项公式或编译阶段记录原因，不得为单一舰种增加隐藏倍率。

## 九、建议的下一步讨论顺序

现实舰艇尺度、结构材质、基础装甲、甲弹、RCS 和机动闭环的公式级原型已经形成，其中机动首轮自动断言全部通过。统一机器可读契约、无界面船壳编译器、十三类最小模块、`OutfitPlan`、执行器聚合、设计态派生快照、`SortieConfiguration`、`ShipInstanceSnapshot`、`RuntimeShipParameters` 和正式战术动力学适配器现已落地；出航初态/当前资源状态、默认工作模式、稳定供电类别、关键最低岗位优先、逐功能战损响应、气动/RCS 实际查询、参数化换层和 E5 最小战斗系统契约也已完成。下一步进入阶段 F 三条规范蓝图测试舰，再进行端到端测试和玩家编辑器往返验证，最后以教程舰进行 `K_turn`、换层时间、控制响应、气动、RCS、甲弹和模块经济性的共同正式标定。完整依赖见《高天荒野舰艇建造与派生性能剩余实施计划》。
