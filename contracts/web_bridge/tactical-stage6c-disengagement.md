# 6c：正式脱离、单舰撤离与终局结算

2026-09-21。接续 6a/6b、工作包 A 和 D。本轮采用用户确认的单舰判据及旗舰损失概率规则；战略地图、打捞和 NPC 运行仍未实现。

## 行为

- 正式遭遇明确被动应战方。双方旗舰平面距离**严格大于**有效阈值才脱离：上层 50 公里，云层/雨层 25 公里。相等不离场。现有测试准备入口明确以我方作为被动应战方；外部遭遇可指定任一阵营。
- 被动应战方旗舰先到新层时保留旧阈值，仍参与整队撤离的在场成员全部到达该层才更新。已成残骸、已离场及指定单独撤离的成员不参与等待，不改变舰艇实际受击高度层。
- 整队撤离继续使用 6b 的真实执行器和共同目标速度。达到距离边界时按撤离命令指定的舰队离场；未下撤离命令而自然拉开距离时，被动应战舰队脱离。追击失败显示为双方脱离接触，不伪称击毁获胜。
- 随伴舰可“单舰脱队撤离”，方向为下令时远离敌旗舰；以**该舰到敌旗舰**超过同一有效阈值为离场判据。旗舰不能单独脱队，离场前可以取消并归队，仍可受击。整队离场不会替仍在阈值内的脱队舰提前结算；尚能撤离的成员留场时，待修舰继续获得恢复机会。
- 离场舰停止机动、观测、命中、装填及资源时间推进，保留离场时状态。有效火炮/损管在作业批次按既有合法结算规则完成，导弹未完成作业保留进度；已经发射的在途弹、VLS 转向中弹继续视为已使用，不退料。整场结束统一清除残余弹体。
- 全推进失效的舰艇先留场；其余仍可撤离的成员离场后，最终仍未恢复推进的舰艇在原地登记 `propulsion_abandoned` 残骸。CIC/SCIC 损毁、船壳崩溃及升力不足最终坠毁继续使用既有实际事件；下坠读条期间不提前认定旗舰损失。
- **旗舰实际被毁当步立即结束战斗**，不转控其他舰。该方仍在场的幸存舰各自进行概率撤离；基础幸存率按用户确认设为 **50%**。已经成功离场的舰不再次抽取，两方旗舰同一步被毁则判双方战败并分别处理幸存舰。
- 概率成功的幸存舰及单独脱队舰交接为战略 NPC，持久状态为已离场，当前不能直接再次部署。概率失败记录 `withdrawal_loss`，使用最后战术位置作为损失残骸登记位置，**不声称模拟了撤离途中命中**。真正的 NPC 活动、搜救和打捞另行接入战略模式。

## 概率接口与结果合同

正式遭遇版本为 `gaotian.tactical-encounter/6c-v2`，新增必填 `defending_side_id`；可选 `withdrawal_policy` 未提供时使用以下首轮基线。历史 `st0-v1` 遭遇保持原行为，不隐式迁移历史场景。

```json
{
  "interface": "gaotian.withdrawal-survival/v1",
  "base_probability": 0.5,
  "ship_modifiers": [
    {"instance_id": "instance.example", "bonus": 0.1}
  ]
}
```

最终概率为 `clamp(base_probability + bonus, 0, 1)`，例中为 60%。加成为概率百分点，不是乘法系数；基础值限 0～1，加成限 −1～1，逐舰身份必须属于遭遇且不能重复。未来 buff 系统在入战时编译到该接口；本轮不新增 buff 系统或战略效果计算。

随机样本由规则版本、场景、舰艇持久身份和终局步号确定。同一事务回滚后重试不会换结果；待保存结果、重复保存和重启恢复均读取同一结果。概率规则随遭遇和结算保存，并在提交时核对，不允许结算时偷换基础值或加成。

新结算 `gaotian.battle-settlement/6c-v4` 在既有逐舰战损/库存和残骸之外增加 `departures`、`withdrawal_policy` 和 `escape_outcomes`。离场记录包含舰艇与阵营身份、时刻、位置、层级和战略控制归属；概率记录包含实际概率、随机样本和幸存判定。校验离场/残骸互斥、身份唯一、结果与概率可复算、物资对账及入战修订关联。历史 P3 v1～v3 结果仍可读取。

## 时序、界面与验证

每个固定步先处理真实飞行/命中、火灾与资源、损管修复、升力下坠及最终坠毁，随后判定正式离场和终局；最后在同一事务提交世界、库存、导航和离场记录。候选状态投影失败不保留半份离场或消耗。终局不产生下一模拟步，也不重复消费弹药。

舰况面板显示旗舰间距、有效阈值、等待换层舰数；单舰撤离时补充该舰距敌旗舰距离，提供脱队/取消命令。已离场舰退出画布与操纵；旗舰已离场但仍有脱队成员在场时，可以暂停和继续观察。结算区显示整队/单舰离场、NPC 归属及概率结果。原“结束本场交战”明确标为测试入口，不能作为正式距离撤离验收。

后台验收覆盖严格距离边界、真实运动越界、全队换层等待与单舰排除、离场后弹丸穿过且库存冻结、投影失败回滚、推进失能/恢复/最终遗弃、旗舰毁坏优先于当步距离离场、双方同时失旗舰、概率端点/buff/伪造拒绝、原子保存/重复保存/重启及 NPC 禁止重新部署。另回归结算、ST0 遭遇、下坠、实际甲弹伤害、调度、显示生命周期和准备编队。

历史下坠测试中两处过期夹具已修正：伪装 v1 的结果同时移除 v3 阵营/舰名字段；双舰损管样本使用合法 SCIC 旗舰。未放松生产合同，也未恢复普通 CIC 多舰旗舰资格。

真实 React/Python/Edge 页面验证航行与脱队/取消、暂停恢复、两场连续保存，以及双方朝外的 50 公里初始布局中通过真实推力越界并自动结算。浏览器操作不冒充 WebView 原生操作；另构建实际 Tauri 桌面程序。验证通过：[108 项后台回归](../../artifacts/tactical-6c-20260921/backend-tests.txt)、[补充滞留舰检查](../../artifacts/tactical-6c-20260921/stranded-regression.txt)、[197 项前端检查](../../artifacts/tactical-6c-20260921/frontend-tests.txt)、[桌面构建](../../artifacts/tactical-6c-20260921/desktop-build.txt)、[真实页面流程](../../artifacts/tactical-disengagement-1789975042143/result.json)和[自动距离离场截图](../../artifacts/tactical-disengagement-1789975042143/distance-settlement.png)。

下一阶段按[工作包 7 整体计划](./tactical-stage7-integrated-plan.md)推进：7a 自动距离与开局敌情 → 7b 基线 → 7c 性能 → 7d 最大编制联合战斗 → 7e 平衡 → 7f 操作/原生验收。用户已确认任意一方首次发现敌舰即可开战，自动距离限定在现有 50/25 公里边界内，保留本包离场及旗舰损失规则。单场最大预期仍为 15 分钟，不新增强制结束计时，也不恢复两小时测试要求。
