# 《高天荒野》舰艇机动性能公式原型测试

## 一、测试范围

配套脚本为《高天荒野舰艇机动性能公式原型测试.py》。本轮属于 L0 聚合参数单元测试，只验证机动闭环、控制语义和数值稳定性。脚本中的对象不是船壳蓝图或舾装方案，不把其质量、推力、阻力面积、回转响应和 OverG 损伤时间当作正式内容参数，也不据此宣称存在实际模块布局测试舰。

测试求解器采用：

- 二维水平平动与偏航；
- `1/60s` 默认固定物理步；
- 半隐式欧拉积分；
- 舵盘偏转映射目标角速度；
- 舵盘回中后使用实际反向力矩制动；
- 制动档以零舰体相对速度为目标、受实际反推能力限制；
- 正常模式结构安全限制；
- 无人舰 OverG 结构超限；
- 有人舰不可解除的 `12G` 乘员安全锁；
- 可替换的方向性平方阻力接口。

执行器接口保留主机平动力、转向机平动力、主机偏航和转向机偏航四项。正式布局已经采用保留物理残余副作用的聚合规则；本文件作为更早的 L0 积分器单元测试，仍使用主机只平动、转向机只偏航的合成夹具，以隔离验证积分器本身。

## 二、测试舰夹具

| 参数 | 测试值 |
| --- | ---: |
| 当前质量 | `50,000t` |
| 当前转动惯量 | `1.20e11kg·m²` |
| 纵向结构安全值 | `8G` |
| 横向结构安全值 | `5G` |
| 前进推力 | `500MN` |
| 倒车推力 | `400MN` |
| 左右横移推力 | 各 `400MN` |
| 左右偏航力矩 | 各 `60GN·m` |
| 舵盘满偏目标角速度 | `30deg/s` |
| 角速度控制响应时间 | `0.60s` |
| 上层正向等效阻力面积 | `4,000m²` |

结构代表点放在舰艏、舰艉与左右舷，用于同时计算平动、切向和向心负荷。

## 三、加速度与转动惯量关系

| 场景 | 首步结果 |
| --- | ---: |
| 基准舰正向初始加速度 | `10.000m/s²` |
| 推力提高到 `750MN` | `15.000m/s²` |
| 质量提高到 `75,000t` | `6.667m/s²` |
| 转动惯量加倍后的首步角加速度 | `0.250rad/s²` |

自动断言确认：同质量增加有效推力提高加速度；同推力增加质量降低加速度；同力矩增加转动惯量降低角加速度。

## 四、舵盘回中与物理制动

测试舰从静止开始保持满舵两秒，随后舵盘回中八秒：

| 项目 | 结果 |
| --- | ---: |
| 回中瞬间角速度 | `0.501858rad/s`，约 `28.754deg/s` |
| 回中八秒后角速度 | `0.000000796rad/s` |
| 全过程最终舰艏角 | `59.460deg` |

控制器没有直接清零角速度，而是在回中后持续请求反向转向力矩；接近零角速度时请求自然收敛。该测试通过，舵盘语义可以进入后续操控手感标定。

## 五、惯性与制动档

测试舰以世界坐标 `+X` 方向 `100m/s` 飞行，同时只进行舰艏转向：

| 项目 | 结果 |
| --- | ---: |
| 一秒后舰艏变化 | `13.467deg` |
| 同期速度方向变化 | `0.00000000deg` |

速度会因阻力降低，但不会随舰艏被瞬时旋转，说明侧滑与惯性边界成立。

另一测试以正向 `200m/s` 进入制动档。测试舰只有 `400MN` 倒车推力，八秒后速度为 `127.345m/s`。制动档显著减速但没有瞬时停车，结果符合“目标速度为零、实际能力受限”的语义。

## 六、结构限制、OverG 与乘员锁

将测试舰正向推力临时提高到 `10GN`：

| 模式 | 指令倍率 | 结构载荷比 | CIC 过载 | 单个 `1/60s` 步损伤 |
| --- | ---: | ---: | ---: | ---: |
| 无人、正常 | `0.392266` | `1.000000` | 未限制项 | `0` |
| 无人、OverG | `1.000000` | `2.549291` | 未限制项 | `400.050` |
| 有人、OverG | 小于 `1` | 大于 `1` | `12.000000G` | 大于 `0` |

测试确认：

- 正常模式按结构安全值削减主动请求；
- 完全无人舰开启 OverG 后可以使用全部可用推力并承受结构损伤；
- 舰上存在人员时，即使开启 OverG 也仍由 `12G` 安全锁限制主动请求；
- 当前测试的损伤绝对数只来自原型 `R_ref`、`T_ref` 夹具，不能直接用于正式平衡。

## 七、最大速度与固定步收敛

测试夹具的正向推力与平方阻力平衡解析值为 `500.000m/s`。持续加速二百五十秒后的固定步模拟值为 `499.955m/s`，误差约 `0.009%`，说明求解器没有使用硬编码舰种速度上限。

复合场景依次执行四秒加速转向、四秒加速回中和四秒制动：

| 固定步 | 最终位置 `(x,y)` | 最终速度 `(x,y)` | 舰艏角 | 角速度 |
| --- | ---: | ---: | ---: | ---: |
| `30Hz` | `(-226.001,208.194)m` | `(-12.038,3.213)m/s` | `77.998deg` | `0.000022deg/s` |
| `60Hz` | `(-225.524,208.519)m` | `(-12.005,3.225)m/s` | `77.998deg` | `0.000026deg/s` |
| `120Hz` | `(-225.282,208.684)m` | `(-11.988,3.231)m/s` | `77.998deg` | `0.000029deg/s` |

以 `120Hz` 为参照，`30Hz` 与 `60Hz` 的末态误差均处于首轮验收阈值内，没有出现发散、永久自旋或速度矢量瞬时转向。

## 八、结论与下一轮

首轮自动断言全部通过。已经可以暂时封闭以下实施接口：

- 固定步二维刚体状态；
- 半隐式欧拉更新顺序；
- 舵盘目标角速度语义；
- 回中后的实际反向力矩制动；
- 制动档零目标速度语义；
- 结构限制、乘员锁与 OverG 的控制倍率接口；
- 气动缓存、质量缓存、惯量缓存和执行器分配层之间的输入输出边界。

发动机分配、最终执行器聚合、派生快照、唯一运行时适配、参数化换层状态机与 E5 最小战斗系统契约现已完成；它们分别由《高天荒野舰艇战术机动求解器测试.py》和《高天荒野舰艇战斗系统契约测试.py》沿真实规范数据链验证。本文件继续只承担 L0 积分器公式单元测试，不因生产适配器落地而改写成布局测试。下一步建立阶段 F 三条 L2 规范测试舰；这些测试舰通过玩家编辑器往返验证后，才可用于正式标定 `K_turn`、舵盘满偏目标角速度、响应时间与换层耗时。完整顺序见《高天荒野舰艇建造与派生性能剩余实施计划》。
