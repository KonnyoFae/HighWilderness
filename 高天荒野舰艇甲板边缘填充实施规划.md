# 舰艇甲板边缘填充实施规划

> 建立日期：2026-09-07。
> 状态：H5a 只读边缘空间预览已完成；H5b—H5d 的材料选择、净容量和运行效果尚未实现。几何、容量与运行效果按以下切片冻结；候选方案不视为已确认的正式数值。
> 用户要求：允许玩家选择每层填充什么，例如灵烷燃料、防火材料、微机修复材料。

> 2026-09-10 调度更新：H5a 已完成；按[统一交付顺序](./contracts/web_bridge/editor-tactical-delivery-roadmap.md)，先 P1—P3 实例/交战/结算，再 X1a 玩家设计出航；H5b 作为其后编辑扩展，H5c/H5d 按效果依赖接续。O3 已完成，不再使用“接入 O3 时实施”作为 H5b 的排期。货舱共享容积与边缘填充净容积是不同来源，不能重复计容积或库存；战术推进不耗燃料。

## 1. 玩家功能与首版范围

在船壳编辑器中为每层甲板提供独立的“边缘填充”设置，并在舾装视图中显示其区域、用途与代价。填充材料与该层结构材质、逐边基础装甲分别设置。同层多个分离区域首版共用一种填充选择；逐区域混合、逐格涂画和批量编辑后移。

| 选项 | 预期用途 | 实施前需确定的边界 |
| --- | --- | --- |
| 不额外填充 | 保留现有船壳结构，是旧设计的兼容语义 | 不减掉已计入的骨架、甲板质量 |
| 灵烷燃料 | 用边缘空间提供附加燃料储备 | 贮存设施的体积/干质量、容量与装载量、供油、受损泄漏及风险；不因储存灵烷自动获得升力 |
| 防火材料 | 降低填充区域相关的火灾或蔓延影响 | 生效位置、覆盖范围、强度、是否消耗、与现有损管叠加方式；不直接赋予全舰免疫 |
| 微机修复材料 | 以有限材料支持局部修复 | 修复对象、作用范围、消耗/速率、启动条件、供电或损管依赖，以及与工程微机团块的资源关系 |

后三项均为候选材料类别，密度、价格、效果、解锁条件和可用率尚未标定。微机修复材料不默认等同于现有工程、神经或演算微机团块，也不默认可以重建被摧毁模块。

## 2. 填充空间的首版建议

本需求承接“船壳轮廓与模块可安装区域之间的缝隙”。首版建议以**所有可用完整内部格之外、仍处于本层船壳内部的边缘余量**为对象。可安装但尚未放置模块的整格继续留给舾装，拆掉模块不会凭空增加填料或燃料。

几何准备阶段采用 `F_l = P_l − union(C_l)`：`P_l` 为该层船壳区域并集，`C_l` 为该层全部完整内部格的平面区域。返回真实边缘片区及面积，而非把整块不完整边界格都当成船体；矩形对齐时可为零，凹形和多区域须保留各片区位置。

`area(F_l) × 5 m` 仅是几何毛体积上界，不能直接当作可装燃料或填料的净容积。净容积必须明确骨架、甲板、容器、必要隔离设施及空间利用率的扣除口径；已有结构等效厚度不是精确三维构件，不能未经定义就把它当作真实局部几何扣除。新增设施质量与填充物质量分别记账，避免重复计算结构质量。

现行装甲厚度是逐边属性，不会内缩安装空间。H5a 保持这一已验收口径，只显示边缘毛区域；它不意味着装甲占据零体积。H5b 开工前须冻结装甲与填充净容积的关系：若继续使用等效扣除模型，要明确近似范围；若选择实际向内生长，则另立几何规则版本，重新验收装甲内边界、完整安装格、拐角和超厚装甲冲突。不得在填充功能里隐式修改旧船壳空间。

## 3. 数据与权威计算

- 船壳设计保存逐层填充方案和材料精确版本引用；派生面积、净容量、质量分布与惯量由 Python 计算。最终字段位置及接口版本在 H5b 合同中冻结，不往旧严格 Schema 随意加字段。
- 设计容量、出航时实际装载量、运行中的剩余量分开保存。选定材料不代表免费装满；固定填料的建造用量与可消耗填料的装载状态分别定义。
- 原有船壳面积结构质量继续保留，新增填料和设施只计算增量；按填充片区位置计算质量分布与惯量，不把所有增量集中于 CIC。动态消耗在接线时明确质量/惯量更新边界，适配新实时路线的按变化更新，不恢复每步整舰重编译或实时配平。
- 燃料沿用单舰共享池的访问规则，但须保留逐层储存来源、容量和局部受损归属。不能同时按共享池和填充物重复计重，也不能增加容量却没有供油/损失路径。
- 修改船壳后容量不足，应明确提示重新配置或卸载；编辑操作不能静默删除实例中的燃料、填料或修复储备。
- 旧资源、存档和黄金保持原语义及原指纹；显式新建或迁移进入填充合同，新功能的可用性不能靠丢弃未知字段实现。

## 4. 切片与接续顺序

| 切片 | 工作 | 验收与调度 |
| --- | --- | --- |
| H5a 边缘空间准备 | 确认首版区域口径；在船壳派生预览中提供边缘片区、面积及毛体积，加入可开关叠加和分层说明 | 已完成并验收；零余量、斜边、凹形、多层/分离区域、强制网格均有确定结果，既有安装格与资源往返不变；不增加燃料或材料效果 |
| O1/O2 舾装基础 | 模块资源视图、舾装会话与四类安装最小命令 | 最小范围已完成；复用材料目录/精确引用与会话模式，不等待微机修复和防火规则完成 |
| H5b 逐层填充基础 | 冻结净容积/装甲关系、材料与方案合同，提供每层选择、增量质量/惯量、保存恢复与撤销重做 | P1—P3 与 X1a 基础链后接续；同层选择一致，负容量/未知材料拒绝，旧版一致，新版往返一致。技术材料明确标记，未接效果不能作为可生效战术配置出航 |
| H5c 灵烷储备接线 | 填充储存设施、容量、装载/补给、共享池供油、消费与局部损失、保存续跑 | 依赖运行时及 X1a 出航适配；携带灵烷填充的设计必须通过此门才可出航。容量和燃料质量守恒，耗油/受损更新一致，无自动升力加成 |
| H5d 防火与微机效果 | 分别定义并接入现有火灾/损管和维修资源链 | M4/M5 基本闭环后实施，不阻塞无填充设计的舾装/战术主线；覆盖作用范围、消耗上限、受损、暂停及连续/重载确定性 |

H5 是 H4 之后的新增需求，H4/M2 原最小范围验收继续有效，不将新增功能标记为已完成。M3/M4 可先以“不额外填充”完成最小闭环；未实现的候选效果只能展示为规划内容，不能作为有效增益。

## 5. 当前完成与下一步

H5a 已由 `高天荒野舰艇边缘空间.py` 从编译器规范区域与严格内部格派生边缘区域，经显式 v2 编辑器预览传给 `HullViewport`。几何分区、可开关叠加、分层面积与毛体积已接通，旧资源、默认 v1 预览和历史编译黄金保持一致。详细合同与真实桌面验收见 `contracts/web_bridge/h5a-edge-space-preview.md`。

H5a 验收时类型检查与 29 项前端测试、选定 9 / 90 个 Python 脚本及前端构建通过。O1—O3 已完成，O4 用户功能复测认可、规范实机留档待收尾。P1a 已范围内完成，当前主线接 P1b，不因填充参数未冻结阻塞普通武器和战后状态开发。H5b 按本文件顶部排期接续，届时冻结装甲与净容积关系；当前不生成填料库存，不更改既有装甲公式。H5c 的燃料消费指明确支持的动作及未来战略消耗，不增加战术推进耗油。
