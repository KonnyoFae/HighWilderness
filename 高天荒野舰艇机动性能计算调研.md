# 《高天荒野》舰艇机动性能计算调研

## 一、实施状态与边界

本文是机动闭环的第一版实施规格。生产接口 `gaotian.tactical-dynamics/runtime-adapter/v1alpha1` 已把船壳质量、当前质量、方向性气动阻力、主发动机推力、转向发动机力矩、转动惯性、结构安全值和 `OverG` 规则接入固定 `1/60s` 二维求解器；本文中的环境和控制数值仍属于 `prototype_unbalanced`。

本轮正式确认舵盘采用以下语义：

- 舵盘偏转量映射为目标偏航角速度；
- 舵盘回中表示目标角速度为零；
- 当前仍在回转时，控制系统调用反向转向发动机进行物理制动；
- 舵盘不会直接改写舰艏角，也不会直接改写当前角速度；
- 反推制动能力受实际可用转向发动机、供电、受损、缺员、自动化、燃料和安全锁限制。

第一版继续只处理水平面内的二维平动和偏航，不加入俯仰、滚转、气动升力、气动压心或气动偏航力矩。舰艏朝向改变时不旋转既有速度矢量；舰艇会保留惯性并按实际侧滑角读取方向性阻力。

执行器副作用现已敲定：主发动机两侧总推力配平后保留真实残余偏航，转向发动机未抵消合力保留为真实平动力。执行器输出接口继续分别保存 `F_main`、`F_turn,net`、`tau_main` 和 `tau_turn` 四项，以便控制器解释和补偿。早期 L0 积分器测试仍可使用“主机只提供平动、转向机只提供偏航”的合成夹具，但正式布局派生必须采用 `gaotian.actuator-aggregation/physical-residuals-v1`。

## 二、坐标、单位与战术状态

舰体坐标继续使用：

- CIC 固定格中心为原点；
- `+Y_body` 为舰艏方向；
- `+X_body` 为右舷方向；
- 世界坐标舰艏角 `psi=0` 时，舰艏指向 `+Y_world`；
- 正偏航角、正角速度和正力矩统一取逆时针方向；
- 距离使用米，时间使用秒，质量使用千克，力使用牛顿，力矩使用牛顿米，角度求解统一使用弧度。

战术状态至少保存：

| 字段 | 含义 |
| --- | --- |
| `p_world` | 世界坐标位置 |
| `v_world` | 世界坐标速度 |
| `psi` | 舰艏角 |
| `omega` | 偏航角速度 |
| `height_layer` | 上层、云层或雨层 |
| `layer_transition` | 当前换层状态与剩余进度 |
| `hull_hp_current` | 当前全舰船壳耐久 |
| `fuel_units` | 当前燃料单位 |

`overg_enabled` 是每步控制输入，不是持久运动状态。乘员安全锁只能从 `RuntimeShipParameters.crew_safety_lock_enabled` 取得，调用者不能通过战术控制输入伪造“无人”状态。

质量和惯量分别读取舰艇实例的 `m_current` 与 `I_current`。它们已经包含船体、模块和货仓内大宗货物，不再把人员、燃料、已装填弹药和已装载导弹计入质量。

## 三、固定步二维刚体求解

### 3.1 运动方程

每个固定物理步使用：

`m_current * dv/dt = F_main + F_turn,net + F_drag`

`dp/dt = v`

`I_current * d_omega/dt = K_turn * (tau_turn + tau_main)`

`d_psi/dt = omega`

`K_turn` 是全游戏统一的回转节奏标定系数。原型先取 `1.000`，只有在教程舰实测回转明显不符合战术节奏时才统一调整，不允许按舰种填写隐藏倍率。

### 3.2 第一版积分器

第一版采用固定 `1/60s` 物理步和半隐式欧拉积分：

`v_(n+1) = v_n + a_n * dt`

`p_(n+1) = p_n + v_(n+1) * dt`

`omega_(n+1) = omega_n + alpha_n * dt`

`psi_(n+1) = wrap[psi_n + omega_(n+1) * dt]`

渲染帧率与物理步解耦。掉帧时使用累积器补做固定步，并设置单帧最大补步数，防止长时间停顿后一次执行过多物理步。回放与测试记录控制输入发生在哪一个固定步，而不是记录不稳定的渲染帧时间。

采用半隐式欧拉的理由不是追求最高理论阶数，而是其状态少、行为可复现、实现成本低，并且在本项目没有弹簧约束的自由刚体机动中足够稳定。必须同时以 `1/30s`、`1/60s` 和 `1/120s` 运行标准场景，对末态位置、速度、舰艏角和角速度进行收敛检查。

### 3.3 气动接口

生产求解器不重复分析船壳几何，而是读取船壳蓝图保存时生成的三百六十度方向性气动缓存：

`f_D(beta,V,h) = f_form(beta) + f_skin(beta,V,h) + f_wave(beta,M)`

`D = 0.5 * rho_h * |v_rel|^2 * f_D`

`F_drag = -D * v_rel / |v_rel|`

其中 `v_rel=v_ship-v_air`。首轮机动原型只使用可注入的方向性阻力面积查表，以验证求解器接口和固定步稳定性；正式气动系数仍由《高天荒野舰艇气动阻力计算调研》与教程舰联合标定。

## 四、平动控制与制动档

### 4.1 主发动机请求

前进、倒车和横移输入先转换为舰体坐标中的归一化请求 `u_move=(u_x,u_y)`，每轴范围为 `[-1,1]`。执行器分配层依据方向读取受损、供电、人员、自动化与燃料结算后的实际可用推力，再输出 `F_main,body`。

输入不会直接指定速度。持续前进档表示持续请求正向推力，最终稳定速度来自可用推力与气动阻力的平衡。

### 4.2 制动档

制动档采用“目标舰体相对速度为零”的自动反推语义。控制器将世界速度转换到当前舰体坐标，分别请求：

`a_brake,request = -v_body / t_brake,response`

`F_brake,request = m_current * a_brake,request`

各轴请求再受该方向实际可用主发动机推力限制。制动不会瞬间清零速度；当舰体在转动时，控制器每步按新的舰体坐标重新计算需要调用的前后左右发动机。

`t_brake,response` 是统一控制响应参数，不是额外制动力。它只决定控制器多积极地请求现有反推能力，实际减速度始终由实际可用推力、质量、阻力和安全锁决定。

## 五、舵盘目标角速度控制器

### 5.1 输入映射

令舵盘归一化偏转量为 `u_wheel in [-1,1]`：

`omega_map = u_wheel * omega_ui,max`

`omega_ui,max` 是舵盘满偏所映射的操控目标角速度，属于全局操控手感参数或舰艇控制系统参数，不是刚体的硬物理上限。舰艇受到外力或原有惯性影响时，实际角速度可以暂时超过该值，控制器随后会请求制动。

未开启 `OverG` 时，还需要求出当前船体的安全稳态偏航角速度 `omega_struct,safe`。该值通过对现有结构代表点公式做单变量二分得到：

`R_struct(a_CIC=0, alpha=0, omega_struct,safe) = 1`

因此正常模式下：

`omega_target = clamp(omega_map,-omega_struct,safe,+omega_struct,safe)`

开启 `OverG` 后不再用结构安全值裁剪目标角速度；有人舰艇的 `12G` 乘员安全锁仍然保留。

### 5.2 角速度反馈与反推

第一版使用受限比例角速度控制器：

`alpha_request = (omega_target-omega) / t_omega,response`

`tau_request = I_current * alpha_request / K_turn`

然后按请求符号裁剪到当前可用左转或右转力矩。`t_omega,response` 越小，控制器越积极，但不会增加执行器的实际最大力矩。

舵盘回中时 `omega_target=0`。只要 `omega` 仍不为零，`alpha_request` 就与当前角速度反向，因而调用反向转向发动机。角速度接近零时请求力矩自然收敛，不进行角速度瞬时归零，也不依赖虚构的旋转摩擦。

### 5.3 执行器能力不对称

左转和右转分别保存实际可用力矩。某一侧转向发动机受损时，控制器可能出现以下合理结果：

- 向一侧加速转动比另一侧更快；
- 从某一方向回转中制动所需时间更长；
- 极端情况下只能缓慢制动，甚至暂时无法产生所需反向力矩。

控制器不得用较强方向的能力替代缺失的反向执行器，也不得直接修改角速度以掩盖损伤。

## 六、结构限制、乘员安全锁与 OverG

### 6.1 实际局部负荷

设 CIC 处水平加速度转换到舰体坐标后为 `a_CIC=(a_x,a_y)`。结构代表点 `r=(x,y)` 的局部加速度为：

`a_local = a_CIC + alpha cross r - omega^2 * r`

在二维分量中：

`a_local,x = a_x - alpha*y - omega^2*x`

`a_local,y = a_y + alpha*x - omega^2*y`

每个代表点的结构载荷比为：

`R_point = sqrt[(a_local,y/a_safe,long)^2 + (a_local,x/a_safe,lat)^2]`

`R_struct = max(R_point)`

代表点、安全纵向加速度和安全横向加速度直接读取船壳结构缓存，不在机动求解器中重新分析多边形。

### 6.2 主动指令限制

正常模式下，控制器对本步主动平动力和偏航力矩应用统一请求倍率 `s in [0,1]`，使下一步预测的 `R_struct<=1`。气动阻力等外力不随 `s` 缩放；如果外力本身已经造成超限，控制器只允许能够降低预计负荷的制动或修正指令。

第一版原型用有界搜索求取可用的最大 `s`。生产实现可以在确认负荷函数单调的常见区间使用二分，在已经超载且制动会降低负荷时直接允许减载指令。

### 6.3 乘员安全锁

二维水平求解器不显式积分竖直重力与升力，因此 CIC 处总固有过载按以下方式恢复：

`G_CIC = sqrt[1 + (|a_CIC,horizontal|/g_ref)^2]`

只要舰上存在任何人员，主动指令倍率还必须保证 `G_CIC<=12`。该锁不受 `OverG` 影响。完全无人舰艇不执行乘员过载检查。

第一版乘员安全锁以 CIC 作为统一判定点；如果以后为不同舱室记录人员位置，再把局部偏航加速度纳入对应人员过载，而不是在当前阶段虚构人员分布。

### 6.4 OverG 损伤

开启 `OverG` 后，主动指令不再受 `R_struct<=1` 限制，但仍受实际推力、力矩和有人舰艇 `12G` 锁限制。任何原因造成的实际 `R_struct>1` 都使用既有平方曲线扣除全舰船壳耐久：

`dH_hull/dt = -(H_hull,max/T_ref) * [max(0,R_struct-1)/(R_ref-1)]^2`

当前船壳耐久不反向降低结构安全值。耐久降至结构崩溃阈值时，舰艇坠毁。

## 七、升力与高度层接口

本轮将升力余量接入实例数据与合法性检查：

`R_lift = L_available/(m_current*g_ref)`

- `R_lift<1`：当前载荷下无法维持飞行；
- `R_lift=1`：没有爬升储备；
- `R_lift>1`：允许发起上升换层。

高度层继续作为离散状态，当前层决定气动环境参数。换层过程保存起始层、目标层、剩余时间与总时间，切换完成前不瞬间改用目标层。第一版正式适配器采用配置表中的相邻层基础耗时，多层跨越取沿途耗时之和；向上切换要求当前升力余量大于零，向下切换不需要额外升力。当前相邻层均暂取 `30s`，三层介质参数和换层耗时都明确标记为 `prototype_unbalanced`，教程舰阶段可以替换参数而无需改动状态机或实例契约。

## 八、执行器输出接口

每个固定步的执行器分配层向刚体求解器提交：

| 字段 | 坐标与单位 | 说明 |
| --- | --- | --- |
| `F_main_body` | 舰体坐标，牛顿 | 主发动机平动力 |
| `F_turn_net_body` | 舰体坐标，牛顿 | 转向发动机未抵消合力，正式布局保留 |
| `tau_main` | 牛顿米 | 主发动机配平后的真实残余偏航力矩 |
| `tau_turn` | 牛顿米 | 转向发动机偏航力矩 |

四分量接口使控制器能够主动请求补偿，又不会把真实副作用从结构负荷、乘员安全锁或 OverG 计算中抹除。具体分配由发动机聚合层生成，刚体状态、积分器和气动接口不需要另设例外。

## 九、设计器与调试输出

设计器和舰艇详情不显示单一不可解释的“机动性”评分，至少输出：

- 设计质量、典型载荷质量与满载质量；
- 空载、典型载荷和满载升力余量；
- 前进、倒车、左右横移的配平后推力与零速初始加速度；
- 各高度层正向持续最大速度；
- 指定速度下制动时间与制动距离；
- 左右实际可用偏航力矩；
- 空载、典型载荷和满载转动惯性与最大角加速度；
- 舵盘满偏目标角速度、正常结构安全角速度；
- 从静止开始完成九十度和一百八十度原地转向的预计时间；
- 纵向、横向安全 G，有人舰 `12G` 锁状态；
- 当前结构载荷比、当前 CIC 过载和 OverG 耐久损失率。

调试状态还应显示主动指令倍率 `s`、各力来源、净力矩、侧滑角、方向性阻力面积及本步固定步编号，便于解释舰艇为什么未达到玩家请求的加速度或角速度。

## 十、首轮原型与验收

配套脚本《高天荒野舰艇机动性能公式原型测试.py》验证以下关系：

1. 同质量增加有效推力会提高加速度；
2. 同推力增加质量会降低加速度；
3. 同力矩增加转动惯量会降低角加速度；
4. 舵盘回中后会调用实际反向力矩，并把角速度收敛到零附近；
5. 舰艏转动不会瞬间旋转速度矢量；
6. 制动档使用实际反推逐步降低速度；
7. 正常模式把主动结构载荷限制在安全范围内；
8. 无人舰开启 `OverG` 后可以越过结构安全值并持续损失船壳耐久；
9. 有人舰开启 `OverG` 后仍不能越过 `12G`；
10. 最大速度来自推力与平方阻力平衡；
11. `1/30s`、`1/60s` 与 `1/120s` 固定步结果收敛，不出现数值爆炸。

原型中用于演示的质量、推力、阻力面积、控制响应和 `OverG` 损伤时间均是 L0 聚合参数夹具，不是船壳蓝图、舾装方案或正式教程舰参数。规范数据契约、无界面船壳/舾装编译器、最终执行器聚合和唯一运行时适配均已完成；《高天荒野舰艇战术机动求解器测试.py》另用真实契约夹具验证生产接线。正式数值标定仍必须经过 E5 战斗契约、三条规范蓝图测试舰和玩家编辑器往返验证；不能只因 L0 脚本或 `contract_fixture` 已有数值便视为正式布局参数。剩余实施顺序见《高天荒野舰艇建造与派生性能剩余实施计划》。
