# 《高天荒野》舰艇气动阻力计算调研

## 一、目的与现有约束

本调研只解决水平飞行时，舰艇船壳朝向、侧滑和外形如何影响气动阻力的问题，不改动已经确定的升力、质量与操作规则。

现有设计对计算方式提出了以下约束：

- 舰艇由一层或多层甲板组成；基底层只有一个连续船壳多边形，其他层可以具有一个或多个由相邻下层直接支撑、并最终连接到基底层的连续船壳多边形；
- 每层甲板的层高固定为五米；
- 船壳以五米网格绘制，但多边形边长与角度仍按公制几何量计算；
- 舰体坐标原点为固定 CIC 所在格，舰艏为 `Y` 轴正方向，所有甲板外形关于舰体 `Y` 轴强制对称；
- 舰艇具有惯性、舰艏朝向、转向与侧滑，船体朝向必须影响阻力；
- 气动阻力只读取舰体本身，各类外挂装备不纳入气动计算；
- 上层、云层与雨层具有不同的阻力环境；
- 舰艇可以进入跨音速和超音速范围；
- 计算结果既要供设计器显示最大速度，也要供战术模式逐帧结算。

因此，计算模型不能只保存一项全舰阻力系数，也不适合在战斗中实时求解流体场。

## 二、调研结论

### 2.1 可以保留的物理骨架

标准阻力方程为：

`D = 0.5 × ρ × V² × C_D × A`

其中 `ρ` 为空气或等效介质密度，`V` 为物体相对介质的速度，`C_D` 为阻力系数，`A` 为与该阻力系数配套的参考面积。NASA 对该公式的说明明确指出，阻力还受到形状、物体相对来流的倾角、黏性、雷诺数和可压缩性的影响；参考面积可以选用迎风面积或其他一致定义的面积，但对应的阻力系数也必须使用相同口径。

本项目应保留 `0.5ρV²` 这一动压骨架，同时把 `C_D × A` 合并为单位为平方米的“等效阻力面积” `f_D`：

`D = 0.5 × ρ × V² × f_D`

这样可以直接将船壳的形状阻力、表面摩擦与跨音速波阻贡献相加，也不会因为任意选择参考面积而造成数据口径混乱。

### 2.2 不能只使用迎风投影面积

迎风投影面积能够反映船体纵向、横向和斜向运动时暴露面积的变化，但无法区分具有相同最大宽度和高度的尖艏与平艏。NASA 的形状阻力资料显示，在相同参考面积下，不同形状的阻力系数可以相差很大；矩形柱风洞试验也表明，阻力随攻角和雷诺数的变化并不等同于投影面积的简单变化。

因此，投影面积只能作为公式的一部分，还必须加入表示船壳边迎风或背风钝度的几何量。

### 2.3 不采用逐边牛顿正弦平方律作为完整答案

将来流对每块表面的压力写成入射角正弦平方关系，可以形成非常便宜的局部表面计算。但是 NASA 的相关技术资料将修正牛顿撞击理论主要用于高超音速钝体；在低速、亚音速和跨音速范围，边界层分离、尾迹和表面摩擦不能由这一规律统一预测。

本项目可以借用“表面越接近正对来流，钝度贡献越大”的几何形式，但必须把它明确视为经过标定的游戏用代理量，不能声称它是从低速到超音速都成立的真实压力公式。

### 2.4 跨音速阻力必须单独处理

接近音速时会出现激波和额外波阻。NASA 明确指出，低速测得的阻力系数不能直接用于超音速；跨音速阻力上升的位置和幅度还会随外形变化。超音速面积律进一步表明，波阻与物体沿来流方向的截面积分布有关，而不只与最大迎风面积有关。

现有的多层甲板多边形可以近似生成沿任意水平方向的截面积分布，因此可以建立一个比统一马赫数倍率更能反映船形差异、但远比真实面积律简单的波阻代理量。

## 三、候选方案比较

| 方案 | 航向变化 | 区分尖艏和平艏 | 支持任意多边形 | 跨音速 | 运行成本 | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| 单一全舰 `C_D` | 否 | 否 | 是 | 只能统一乘倍率 | 最低 | 不采用 |
| 投影面积 × 固定 `C_D` | 是 | 否 | 是 | 只能统一乘倍率 | 很低 | 不足以作为最终公式 |
| 纵向/横向两项阻力插值 | 有限 | 有限 | 不能充分利用 | 只能统一乘倍率 | 很低 | 不适合自由船壳 |
| 战斗中逐面计算 | 是 | 是 | 是 | 可以扩展 | 中等且随几何复杂度变化 | 无需逐帧执行 |
| 保存设计时预计算方向性阻力面积 | 是 | 是 | 是 | 可以分别预计算 | 战斗中很低 | 推荐 |
| 二维或三维 CFD/LBM | 是 | 是 | 是 | 理论上可以 | 开发、校准和计算成本过高 | 不作为游戏规则 |

## 四、推荐模型总览

推荐把总等效阻力面积拆成三项：

`f_D(β,V,h) = f_form(β) + f_skin(β,V,h) + f_wave(β,M)`

其中：

- `β` 为舰艏方向与相对介质速度方向之间的侧滑角，正前进为零度，正横移为九十度，倒车为一百八十度；
- `h` 为高度层；
- `M = V / a_h` 为马赫数；
- `f_form` 为由船壳迎风投影与前后表面钝度决定的形状阻力面积；
- `f_skin` 为表面摩擦阻力面积；
- `f_wave` 为跨音速和超音速截面积变化产生的波阻代理面积。

运行时的阻力为：

`D = 0.5 × ρ_h × V² × f_D`

阻力方向与舰艇相对介质速度相反：

`F_D = -D × v_rel / |v_rel|`

第一版只计算这一沿相对速度反向的阻力，不同时引入气动升力、压心和气动偏航力矩。舰艏朝向仍会通过 `β` 改变阻力大小。这样能够保留侧滑和横向运动的主要代价，又不会把舰艇操纵扩展成完整飞行动力学。

## 五、从多层甲板生成气动外形

### 5.1 二点五维气动包络

每层甲板的层高固定为五米。系统将该层的一个或多个船壳多边形分别视为五米高的水平棱柱，再将各层合并形成只用于气动预计算的二点五维舰体外形。

气动包络只读取各层船壳多边形并统一使用五米固定层高。顶挂模块、侧挂模块、复合模块的外露部分和其他外挂装备均不并入气动包络，也不提供额外方向性阻力修正。因此，同一船壳蓝图的所有舾装方案共用完全相同的气动数据。

### 5.2 方向采样

船壳蓝图保存时，对 `β = 0°～359°` 的每个整数角进行一次预计算。舾装发生改变时不重新计算。战术模式使用相邻两个角度的数据线性插值。

三百六十个方向即使各保存数个单精度浮点数，单条设计的数据量也只有数千字节；运行时不需要再次遍历船壳边。

### 5.3 暴露表面

对每个高度带和方向，沿来流方向进行可见性检测：

- 每条投射线首先遇到的外表面属于迎风表面；
- 最后离开的外表面属于背风表面；
- 被其他船壳或模块完全遮挡的凹陷内表面不重复计算；
- 投射线覆盖的并集形成该方向的迎风投影面积 `A_proj(β)`。

这样可以避免直接累加凹多边形所有边所造成的重复计数，也能自然处理不同甲板宽度之间的遮挡。

## 六、亚音速形状阻力公式

对于方向 `β`，令舰艇相对速度在舰体坐标中的单位向量为 `e`。对于每个经过可见性处理的外露竖直表面 `i`：

- 表面积为 `S_i = 边长_i × 外露高度_i`；
- 指向外部的水平单位法线为 `n_i`；
- 迎风正对程度为 `μ_i^F = max(0, n_i · e)`；
- 背风正对程度为 `μ_i^R = max(0, -n_i · e)`。

定义迎风和背风钝度面积：

`B_F(β) = Σ[S_i × (μ_i^F)³]`

`B_R(β) = Σ[S_i × (μ_i^R)³]`

三次方来自“投影面积的一次方向余弦”与“钝度的二次方向权重”相乘。它在这里是几何代理量，不是对真实表面压力的逐面预测。

推荐的形状阻力面积为：

`f_form(β) = C_A × A_proj(β) + C_F × B_F(β) + C_R × B_R(β)`

三个全局标定系数的职责分别是：

- `C_A`：所有迎风轮廓共有的分离与尾迹基础阻力；
- `C_F`：平艏、横向平面等迎风钝面的额外惩罚；
- `C_R`：平直舰艉等背风基底造成的额外阻力。

这一公式具备以下行为：

- 舰艇横向飞行时，长船体会暴露更大的投影面积；
- 平艏正对来流时，`B_F` 接近其迎风面积，阻力明显增加；
- 尖艏的表面法线与速度方向夹角较大，`B_F` 较小；
- 尖艉比平直截断舰艉具有更小的 `B_R`；
- 非对称舰艇的前进、倒车和左右侧滑可以得到不同结果；
- 斜向来流不会只在纵向和横向两组数值之间机械插值。

`C_A`、`C_F` 与 `C_R` 属于全游戏统一的标定常数，不由玩家直接修改。初次标定时，可以让一个正对来流的平直钝体满足 `C_A + C_F + C_R` 接近低速平板或钝体的参考阻力系数，再用教程舰的目标速度调整整体尺度。NASA 给出的低速正迎风平板参考值约为 `C_D = 1.28`，但它只适合作为数量级校验，不应直接认定为所有舰艇的最终平衡值。

## 七、表面摩擦阻力

首先计算该方向上的流向长度 `L_flow(β)`，再计算雷诺数：

`Re = ρ_h × V × L_flow / μ_h`

对于尺寸巨大的舰艇，可以把边界层按全湍流处理。采用平滑平板的简化关系：

`C_f = 0.074 / Re_eff^0.2`

其中 `Re_eff` 设置一个统一下限，避免接近静止时发生数值发散。令气动包络总湿表面积为 `S_wet`，表面摩擦阻力面积为：

`f_skin = K_rough × C_f × S_wet`

`K_rough` 是统一的表面粗糙度/细节修正。初版可以只使用一个全局值，不为不同装甲材质增加新的气动属性。NASA 的技术资料给出了同一全湍流平板关系，并指出更完整的摩擦阻力仍与雷诺数、马赫数、表面状态和转捩位置有关。

对于本项目的大型舰艇，表面摩擦主要用于保证流线型船体仍存在基础阻力；钝体和大侧滑时通常由 `f_form` 主导。

## 八、跨音速与超音速波阻代理

### 8.1 方向性截面积分布

对每个方向 `β`，沿速度方向建立坐标 `x`。在垂直于速度方向的切面上，计算气动包络的实体截面积：

`A_β(x) = 该切面与全部船壳甲板高度带相交的总面积`

船艏逐渐扩宽、船体中段保持稳定、舰艉逐渐收窄时，`A_β(x)` 变化平缓；平直船艏和突然增宽的上层甲板船壳会形成陡峭变化。

### 8.2 截面积变化指标

定义方向性截面积变化指标：

`G(β) = (1 / L_flow) × ∫[dA_β(x)/dx]² dx`

它的单位为平方米，可以直接作为一种等效阻力面积。离散实现以五米为最大采样步长。
令：

`N = ceil(L_flow / 5m)`

`Δx = L_flow / N ≤ 5m`

在 `N` 个等长区间的中心采样截面积，并在船艏、舰艉外各补一个零截面积样本：

`G(β) ≈ (1 / L_flow) × Σ{[(A_{j+1}-A_j)/Δx]² × Δx}`

不直接从投影最小值开始每隔整五米采样，是因为斜向流向长度一般不是五米整数倍；
固定起点会使同一外形在来流反转一百八十度后产生不同的余数采样相位。等分为不超过
五米的区间后，`G(β)=G(β+180°)` 能够严格成立，同时仍保持统一的约五米分辨率。

这一指标不是正式的惠特科姆面积律或冯·卡门波阻公式，而是由面积律“波阻受纵向截面积分布影响”的结论形成的稳定代理量。它会自然产生以下结果：

- 相同最大截面积下，变化距离越长、外形越平缓，`G` 越小；
- 平头、平尾和甲板轮廓突然变化会提高 `G`；
- 横向飞行通常具有更短的 `L_flow` 和更急剧的截面积变化，因此波阻更高；
- 不超过五米的统一最大步长使不同设计使用同一数值口径，避免依靠提高或降低采样精度改变结果。

### 8.3 马赫数响应

波阻面积为：

`f_wave(β,M) = C_W(M) × G(β)`

`C_W(M)` 是全游戏统一的马赫数响应曲线：

- 低亚音速区为零；
- 接近跨音速区时平滑上升；
- 音速附近形成明显阻力峰；
- 进入稳定超音速后转入单独标定的超音速段。

该曲线不能直接从一条通用物理公式得出，因为阻力上升马赫数和峰值均依赖具体三维外形与流动状态。应在教程舰与若干标准测试外形完成后填写曲线控制点，而不是现在写死一组看似精确的数字。方向差异由 `G(β)` 提供，马赫数阶段变化由 `C_W(M)` 提供。

## 九、高度层与实时结算

每个高度层至少需要保存：

- 等效介质密度 `ρ_h`；
- 动力黏度 `μ_h`；
- 当地音速 `a_h`。

`ρ_h` 可以作为兼顾真实空气密度与世界观环境效果的等效参数，用来实现上层阻力较低、云层与雨层阻力较高的既定规则。若暂不设计风场，则相对介质速度直接使用舰艇水平速度；以后加入风场时，只需改为：

`v_rel = v_ship - v_air`

每个物理更新步执行：

1. 计算 `v_rel`、速度 `V` 和侧滑角 `β`；
2. 按 `β` 在线性插值后的方向表中读取 `A_proj`、`B_F`、`B_R`、`L_flow` 和 `G`；
3. 计算 `Re`、`M`、`f_form`、`f_skin` 与 `f_wave`；
4. 计算 `D = 0.5ρ_hV²f_D`；
5. 将 `F_D` 作为与相对速度相反的外力加入舰艇平动方程。

由于方向性几何量已经预计算，运行成本基本与船壳复杂度无关。

## 十、最大速度与设计器显示

在固定高度层、固定朝向和稳定推力下，最大速度满足：

`T_available = D(V_max,β,h)`

如果忽略速度相关的表面摩擦和波阻，可以直接开平方求解；完整公式包含 `Re` 和 `M` 后，设计器应使用一维二分法或其他稳定求根方法求取 `V_max`。

设计界面至少显示：

- 正向、横向和倒车的低速等效阻力面积；
- 三百六十度方向性阻力极坐标图；
- 各高度层的正向持续最大速度；
- 音速附近的波阻提示；

战术模式中的瞬时阻力仍按实际侧滑角结算，不能只使用设计界面显示的正向最大速度。

## 十一、缓存与战损边界

船壳气动数据只在船壳蓝图保存时生成，并作为该船壳蓝图的派生数据保存。战术模式直接读取船壳蓝图的方向表，不逐帧重新分析多边形。

舾装安装、拆除或损毁均不改变气动数据，也不触发气动重算。局部装甲耐久下降同样不改变船壳几何，因此不触发气动重算。

当前无界面船壳编译器已经实现 `gaotian.aero.geometry/v1alpha1` 缓存。每个整数方向
保存 `A_proj`、`B_F`、`B_R`、`L_flow` 和 `G`，另保存全舰气动包络湿表面积。
缓存本身不保存 `C_A`、`C_F`、`C_R`、`K_rough`、雷诺数下限、高度层介质参数或
马赫响应曲线；这些仍由运行时和平衡内容表注入。这样可以在不重新编译船壳的情况下
调整全局气动数值，也不会把尚未共同标定的参数伪装成蓝图属性。

## 十二、必须进行的标准测试

正式填写三个形状阻力系数和马赫数曲线前，至少建立以下无武装测试体：

1. 相同长宽高的平艏平艉矩形体；
2. 相同最大宽度的尖艏平艉体；
3. 相同最大宽度的尖艏尖艉体；
4. 长宽比不同的矩形体；
5. 上下甲板外形突然变化的多层体；
6. 前进、倒车、横向和四十五度侧滑状态；
7. 从低亚音速连续加速至超音速的同一船体。

校验目标不是复现某一种现实舰船，而是确保以下相对关系稳定成立：

- 同尺度下，尖艏比平艏正向阻力小；
- 同尺度下，尖艉比平艉正向阻力小；
- 细长舰艇横向阻力显著大于正向阻力；
- 阻力随侧滑角连续变化，不在方向采样边界跳变；
- 跨音速阻力峰不会被发动机推力轻易穿越，也不会使所有舰艇永久无法突破音速；
- 相同舰艇在上层、云层与雨层满足已经确定的阻力大小关系。

## 十三、需要随数值阶段标定、但不再改变公式结构的参数

以下参数必须等待规范蓝图派生链、规范测试舰和玩家编辑器往返验证完成，再随教程舰共同标定；无界面船壳与静态舾装编译链已经打通，当前优先完成执行器聚合、派生快照和唯一运行时适配。

- `C_A`、`C_F`、`C_R`；
- `K_rough` 与雷诺数下限；
- `C_W(M)` 的曲线控制点；
- 上层、云层和雨层的 `ρ_h`、`μ_h`、`a_h`；
- 教程舰的目标持续速度与推力基准。

这些属于数值标定，而不是对气动公式结构的重新选择。

## 十四、资料来源

- [NASA Glenn：Drag Equation](https://www1.grc.nasa.gov/beginners-guide-to-aeronautics/drag-equation/)：标准阻力方程，以及形状、倾角、黏性和可压缩性对阻力系数的影响。
- [NASA Glenn：Shape Effects on Drag](https://www1.grc.nasa.gov/beginners-guide-to-aeronautics/shape-effects-on-drag/)：以相同投影参考面积比较不同外形，并给出低速平板等典型阻力系数。
- [NASA Glenn：Drag Coefficient](https://www1.grc.nasa.gov/beginners-guide-to-aeronautics/drag-coefficient/)：雷诺数、马赫数相似性以及超音速波阻不能由低速阻力系数直接外推。
- [Nguyen 等：3:2 矩形柱的风洞测量](https://doi.org/10.1016/j.jweia.2021.104826)：矩形钝体阻力、升力和力矩随攻角及雷诺数变化的实验研究。
- [NASA TP-2006-213486](https://ntrs.nasa.gov/citations/20060053240)：平板湍流边界层、雷诺数及简化表面摩擦系数关系。
- [NACA TR-1284：Theory of Wing-Body Drag at Supersonic Speeds](https://ntrs.nasa.gov/citations/19930092281)：超音速面积律及波阻与截面积分布的关系。
- [NASA TN D-446：Applications of the Transonic and Supersonic Area Rules](https://ntrs.nasa.gov/citations/19980227964)：跨音速/超音速面积律计算与实验结果比较。
- [NASA TN：Drag of a Supercritical Body of Revolution in Free Flight at Transonic Speeds](https://ntrs.nasa.gov/citations/19720004249)：跨音速附近出现明显阻力上升的实测例证。
- [NASA TN D-652：Hypersonic Newtonian-impact analysis](https://ntrs.nasa.gov/citations/20040008078)：牛顿撞击理论在高超音速任意姿态物体上的使用范围，用于说明本项目不应把正弦平方律当作全速度域真实压力模型。
