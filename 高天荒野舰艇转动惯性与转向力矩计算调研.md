# 高天荒野舰艇转动惯性与转向力矩计算调研

## 一、目的与既定边界

本文只研究俯视战术模式中的舰体偏航转动，不扩展为完整三维飞行动力学。

已经敲定的边界如下：

- 舰体坐标原点为船体中心固定的 CIC 所在格，舰艏为 `Y` 轴正方向；
- 转向发动机力矩与舰体转动惯性都围绕 CIC 原点计算；
- 舰艇默认自动配重，不把重心作为玩家设计约束；
- 舰体由五米网格、多层船壳多边形、船壳边装甲与模块共同构成；
- 设计质量只包括船壳和模块，战局当前质量另外加入货仓内大宗货物；人员、燃料、已装填弹药和已装载导弹不计质量；
- 主发动机先按平动指令进行两侧推力配平；转向发动机明确产生回转力矩，主发动机配平后是否仍计算残余偏航力矩尚未敲定；
- 当前气动模型不计算压心或气动偏航力矩。

因此，本系统需要回答两个问题：

1. 如何从船壳和舾装计算围绕 CIC 的标量转动惯性 `I_CIC`；
2. 如何从各转向发动机的位置、推力和方向计算净力矩，并转化为角加速度。

## 二、物理基础

对于二维刚体绕垂直战术平面的轴旋转，质量元的转动惯性为：

`I = ∫ r² dm`

多个质量部分围绕同一参考轴的转动惯性可以直接相加。某一质量部分已知自身质心惯性 `I_local`、质量 `m`，且其质心距离 CIC 为 `d` 时，可以用平行轴定理移到 CIC：

`I_CIC = I_local + m d²`

矩形质量块的中心惯性为：

`I_rect,local = m(a² + b²) / 12`

这些关系允许把整条舰艇拆成船壳区域、装甲边、模块和动态货物分别计算，再统一相加。[TU Delft 工程动力学教材整理的平行轴定理与复合惯量](https://eng.libretexts.org/Bookshelves/Mechanical_Engineering/Introductory_Dynamics%3A_2D_Kinematics_and_Kinetics_of_Point_Masses_and_Rigid_Bodies_%28Steeneken%29/03%3A_Rigid_Body_Dynamics/10%3A_Kinetics_of_Rigid_Bodies/10.05%3A_Moment_of_inertia)给出了相同的二维表达式。

转向发动机 `i` 的推力作用点相对 CIC 的二维位置为 `r_i=(x_i,y_i)`，推力向量为 `F_i=(F_x,i,F_y,i)`，其绕 CIC 的有符号力矩为：

`τ_i = r_i × F_i = x_i F_y,i - y_i F_x,i`

净力矩与角加速度为：

`τ_net = Σ τ_i`

`α = τ_net / I_CIC`

单位依次为牛顿米、千克平方米和弧度每二次方秒。NASA 对力矩的说明强调，力矩等于力与到参考点的垂直力臂之积；MIT 的二维转动动力学教材给出了 `τ=Iα` 的固定轴关系。[NASA：Torque](https://www.grc.nasa.gov/WWW/K-12/airplane/torque.html)，[MIT OCW：Two Dimensional Rotational Dynamics](https://ocw.mit.edu/courses/8-01sc-classical-mechanics-fall-2016/mit8_01scs22_chapter17.pdf)

本项目把 CIC 原点作为人为固定的转动参考中心，即使不对称舾装会使真实质心偏离 CIC，也不额外加入质心平动与转动耦合项。这是自动配重设定所对应的游戏简化。

## 三、候选简化方案

### 3.1 整舰外接矩形

将整舰视为长 `L`、宽 `W`、质量 `M` 的均匀矩形：

`I_CIC = M(L²+W²)/12`

优点是极其简单。缺点是同样长宽和质量的尖艏舰、细腰舰与实心矩形会得到完全相同的转动惯性；模块安装位置、不同甲板轮廓和大宗货物位置也完全不起作用，不符合自由造舰系统的反馈需求。

结论：只适合作为测试上限或异常回退值，不适合作为正式公式。

### 3.2 全部五米格离散求和

将舰体质量分配给边长 `s=5m` 的方格。质量为 `m_j`、中心位于 `(x_j,y_j)` 的单格，对 CIC 的转动惯性为：

`I_j = m_j(x_j²+y_j²) + m_j s²/6`

其中 `m_j s²/6` 是五米正方形围绕自身中心的惯性。全舰惯性为全部格子的贡献之和。

优点是实现直接、天然支持凹形、多层和模块占格。缺点是船壳边界形状被量化为五米格，斜边与不完整边界格的质量分配需要另设规则。

结论：适合作为第一版原型和模块质量的正式算法，但只用它计算船壳会损失已经存在的精确多边形信息。

### 3.3 多边形船壳加网格模块的复合算法

船壳的连续多边形使用精确面积惯量，模块使用五米占格，边装甲使用线段惯量，最后围绕 CIC 相加。这一方案能直接利用现有数据，并且计算只在保存船壳、保存舾装或大宗货物改变时发生，不需要逐帧积分。

Box2D 等二维刚体引擎同样将刚体质量、质心和标量转动惯性作为独立质量属性，并允许由多个形状的密度和几何共同生成质量属性。[Box2D：Mass Data](https://github.com/erincatto/box2d/blob/main/docs/simulation.md#mass-data)，[Box2D：Polygon Mass](https://box2d.org/documentation/group__geometry.html)

结论：推荐作为正式第一版算法。

## 四、推荐计算公式

### 4.1 均匀船壳区域

对于一个顶点按边界顺序排列的简单多边形，令：

`c_i = x_i y_(i+1) - x_(i+1) y_i`

其围绕 CIC 原点的面积极惯性为：

`J_O = (1/12) Σ c_i [x_i² + x_i x_(i+1) + x_(i+1)² + y_i² + y_i y_(i+1) + y_(i+1)²]`

若该区域的面密度为 `σ`，则质量转动惯性为：

`I_region = σ J_O`

公式对无孔洞的简单凹多边形同样适用，但必须统一顶点绕序并处理有符号结果。各层、各个彼此分离的上层多边形分别计算后相加。利用边界顶点通过 Green 定理计算多边形面积、质心和二阶矩是成熟做法；相关推导与算法可参考[内布拉斯加大学多边形面积推导](https://www.math.unl.edu/~mbrittenham2/classwk/208s04/inclass/areas_of_polygons.pdf)和[慕尼黑工业大学：On the Calculation of Moments of Polygons](https://mv.in.tum.de/_media/members/steger/publications/1996/fgbv-96-05-steger.pdf)。

如果后续船壳结构质量公式不能直接表示为均匀面密度区域，则可以把每层结构质量均匀分配给其五米内部格，退回第 3.2 节的格子算法，而不改变其余计算链。

### 4.2 船壳边装甲

一整段玩家绘制边具有独立材质、厚度和质量。将该段近似为位于战术平面内的均匀细杆。边长为 `L`、质量为 `m_e`、中点距离 CIC 为 `d_e` 时：

`I_edge = m_e L²/12 + m_e d_e²`

这样装在舰艏、舰艉或远离中心的长装甲边会自然提高转动惯性。装甲厚度对惯性的截面内贡献相对舰体尺度很小，第一版不需要另算。

### 4.3 模块

模块总质量按其全部实际占用格平均分配。若模块共占 `n` 个五米格，每格质量 `m/n`，则：

`I_module = Σ[j=1..n] (m/n)[x_j²+y_j²+s²/6]`

跨层模块仍按其各层占格共同分配质量；由于只计算俯视偏航，甲板高度不进入距离项。顶挂和侧挂模块使用其已经敲定的五米格占用轮廓。外部净空格、炮管扫掠范围和尾焰净空格都没有质量，不计入惯量。

如果某些特殊模块后续需要非均匀质量分布，可以在模块原型中另存若干带权质量格；常规模块不需要这一字段。

### 4.4 大宗货物

战局中的当前惯量建议在设计惯量上加入货仓内大宗货物。为避免资源数量变化时反复遍历所有货格，可以把每个货仓当前承载的货物质量视为集中在该货仓锚点：

`I_cargo = m_cargo(x_hold²+y_hold²)`

这会使远离 CIC 的满载货仓显著降低回转响应，同时只需在货物数量变化时更新一个加项。人员、燃料、已装填弹药和已装载导弹依照既定质量规则不进入惯量。

### 4.5 全舰总惯量

`I_design = ΣI_hull_region + ΣI_edge + ΣI_module`

`I_current = I_design + ΣI_cargo`

战术模式使用 `I_current`。如果第一版性能或数据准备不足，可暂时让 `I_current=I_design`，但不建议退回整舰外接矩形公式。

## 五、转向发动机力矩

每台转向发动机使用模块中心作为暂定推力作用点。将发动机局部推力方向随舰体朝向转换后，在舰体坐标中计算：

`τ_i = x_i F_y,i - y_i F_x,i`

同向力矩直接相加，反向力矩相减：

`τ_net = Σ enabled_i τ_i`

`α_raw = τ_net / I_current`

发动机受损、断电、缺员或被关闭时，使用其当前可用推力而非额定推力。因为推力、力臂和惯量均从实际设计派生，相同发动机装得越靠近舰艏、舰艉或舷侧，通常越容易产生较大力矩，但具体效果仍取决于推力方向与力臂的叉积，而不是只看直线距离。

若真实单位下得到的回转速度不符合游戏节奏，可以在所有舰艇统一使用一个无量纲回转标定系数：

`α_game = K_turn × τ_net / I_current`

`K_turn` 只能作为全局节奏标定，不应按舰种随意填写，否则会破坏自由设计带来的相对差异。

## 六、主发动机推力配平的接口边界

主发动机允许朝前、后、左、右安装。对于同一平动指令，将位于舰体两侧、向同一运动方向输出的可用主发动机分别汇总为 `T_A` 与 `T_B`：

`T_A,used = T_B,used = min(T_A,T_B)`

推力较大一侧的多余能力不参与实际输出。受损或断电导致一侧推力下降后，另一侧也同步受限。

现已冻结以下细节：

- “两侧”按发动机推力作用线相对 CIC 的正负力矩侧划分，因此前后推进对应左右侧，横移推进对应前后侧；
- 推力作用线穿过 CIC 的中轴主机不进入两侧配平，独立全额输出；
- 同侧内部各发动机按统一比例缩放，使正负力矩侧的已用总推力都等于 `min(T_A,T_B)`；
- 配平只约束两侧总推力，不伪造力臂相等；配平后仍按各发动机真实位置计算残余偏航力矩；
- 转向发动机按力矩正负分别形成顺/逆时针能力，其未抵消合力真实参与舰体平动。

执行策略标识为 `gaotian.actuator-aggregation/physical-residuals-v1`。控制系统可以调用其他执行器补偿副作用，但不得由编译器直接把真实合力或力矩清零。

## 七、缓存与战损更新

建议采用三级缓存：

1. **船壳蓝图：** 保存各船壳区域与装甲边围绕 CIC 的惯量；
2. **舾装方案：** 在船壳惯量上加入全部模块，保存 `I_design`；
3. **舰艇实例：** 在 `I_design` 上加入各货仓当前大宗货物，得到 `I_current`。

普通受损不会改变模块和船壳残骸的质量，因此不必每次扣除耐久都重算惯量。只有模块被实际拆除、船壳蓝图改变、舾装改变或大宗货物数量改变时才更新。模块彻底损毁后是否仍以残骸质量参与惯量，应与既有“残骸必须清除后才能安装替换模块”规则保持一致，建议在残骸被清除前继续计重。

## 八、标准测试

实现前至少建立以下测试：

1. **中心正方形：** 边长十米、质量 `M` 的正方形应得到 `I=M(10²+10²)/12`；
2. **长宽互换：** 相同质量与尺寸的矩形旋转九十度后，围绕 CIC 的惯量不变；
3. **远端质量：** 同一模块从 CIC 附近移到舰艏时，惯量按 `m d²` 增长；
4. **镜像模块：** 两个关于 `Y` 轴镜像的同质量模块应贡献相同惯量；
5. **凹多边形：** 精确多边形算法与高分辨率数值积分结果接近；
6. **多层船壳：** 各层分别计算再相加，应等于把所有质量单元一次性求和；
7. **力矩符号：** 镜像安装且推力方向相反的两台转向发动机应产生方向一致的力矩；
8. **力臂为零：** 推力作用线穿过 CIC 时力矩应为零；
9. **货物变化：** 远端货仓装满后 `I_current` 增加，卸货后恢复；
10. **主机受损配平：** 一侧主发动机受损后，另一侧实际总推力被同步限制到相同水平。

## 九、调研结论

正式第一版推荐使用“精确船壳区域 + 线段装甲边 + 五米格模块 + 货仓集中质量”的复合惯量公式：

`I_current = Σ(σJ_polygon) + Σ[m_edge(L²/12+d²)] + ΣI_module_grid + Σ(m_cargo d²)`

回转求解使用：

`τ_net = Σ(xF_y-yF_x)`

`α_game = K_turn × τ_net / I_current`

这套方案保留自由造舰中最重要的质量分布差异，计算成本又主要发生在保存设计和资源变化时，适合本项目的五米网格、多层甲板与模块化舾装结构。该惯量与力矩公式现已接入《高天荒野舰艇机动性能计算调研》的 L0 固定步原型，目标角速度舵盘和回中反推制动测试已经通过；无界面舾装编译器也已从真实模块位置派生设计惯量、逐实例力矩和配平后方向能力，并以非对称推力、单侧损失和残余副作用回归锁定结果。剩余工作是实现出航状态与唯一运行时适配，再以通过编辑器往返的规范测试舰和教程舰标定全局 `K_turn` 与控制响应参数。完整依赖见《高天荒野舰艇建造与派生性能剩余实施计划》。
