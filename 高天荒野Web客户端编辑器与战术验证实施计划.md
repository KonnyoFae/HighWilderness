# 《高天荒野》Web 客户端、舰艇编辑器与战术验证实施计划

> 建立日期：2026 年 8 月 27 日
> 更新日期：2026 年 9 月 7 日
> 上一已推送基线：`526efe4`；本次提交范围为 H4/H5a、舾装与验收修复、T1a 及对应计划和证据，提交身份以 Git 历史为准。d4h 曾通过退出门，当前改动后的历史全量门禁未复跑。
> 当前主线：`T1a_completed / T2a_T3a_next / O4_formal_evidence_pending`；两舰 v7 场景、最小表现协议及真实跨进程创建/读取/释放已完成，接续战术画布和实时直控。舾装经用户复测已无已知功能阻塞，O4 三舰规范实机留档仍待补齐，不冒充正式 M3/O4 全项 PASS；历史指纹与全量结果单独保留。
> 调整依据：用户确认侧挂修复，保存疑问源于首次需“另存为”，当前未发现其他问题，并要求复盘后准备后续实施。
> 适用顺序：第零至十三章为现行产品计划；第十四章起为历史实施记录，其中旧“唯一下一项”和性能阻塞 UI 的调度结论已由本次重排替代。历史验收事实、权威规则与黄金保持有效。

## 零、2026-09-07 优先级调整与最小交付

当前目标是让用户能够实际建造舰体、布置模块，并将编辑结果送入可操作的战术视角。已有 Python 编译器、编辑器领域层、战斗内核和 d4 完整推进安全链直接复用，不再先完成整个性能研究分支。

### 0.1 当前交付与扩展范围

下表是本轮范围的裁剪依据。后文 H/O/T/X 原编号保留以便追溯；其完整功能清单属于扩展范围，当前只执行本表及相应最小验收。后移不等于功能取消或已有规则停用。

| 里程碑 | 当前必须交付 | 后移内容 | 最小验收 |
| --- | --- | --- | --- |
| M1 共用编辑基础：W2a/W2b、W3a | 资源索引、串行权威会话、修订校验、撤销/重做、合法保存/另存/重开、草稿恢复；二维画布缩放平移、五米网格、吸附、点选、属性与错误面板 | W3b 框选、通用复杂快捷键、完整前端测试平台；多文档工作区与通用资源管理器 | 真实 UI 打开资源，修改、撤销/重做、保存重开；过期请求及外部文件冲突拒绝，重启后恢复明确标记的草稿 |
| M2 船壳可用：H1—H4 最小范围 | 空白绘制闭合区域，增删甲板，端点增删移动，基底层、镜像、结构材料和边装甲；草稿预览、防抖、错误定位、安装空间与基本派生面板 | 专用拆边工具、复杂批量几何操作、气动/RCS 细分图表与显示润色 | 从空白完成一条合法船壳；三条已有船壳无修改保存重载一致；非法草稿不覆盖合法资源，旧预览不覆盖新修订 |
| M3 舾装可用：O1—O4 最小范围 | 模块列表和关键参数；内部/顶挂/侧挂/嵌入的放置、选择、移动或明确替换、旋转（适用类型）、删除；冲突/宿主/净空显示、武器组基本配置与必要射界提示、派生与保存 | 剪贴板、批量操作、镜像复制、原型批量替换、高级筛选和完整射界可视化 | 在 M2 船壳上装出合法舰；四类安装方式均可完成基本编辑；三条技术舰规范往返与派生一致 |
| M4 战术基本运行：T1a、T2a、T3a、T4a | 两舰、两个阵营，舰体轮廓/模块位置/朝向、选中与镜头、速度与耐久；暂停/单步/继续；直控车钟、转向和制动，指定目标基本开火；普通炮弹、命中和战损可见 | 独立回放播放器、差量快照、尾迹/爆炸美术、云层黑影、完整三层表现、复杂 RTS 航路/编队、制导与持续毁伤专项 UI | 合法资源生成场景；真实输入改变权威运动，合法开火消耗弹药并产生命中/战损；暂停无推进、重复输入不重复消费，界面帧率不改权威结果 |
| M5 编辑到战术闭环：X1a | 从 M2/M3 保存的版本编译出航，以显式技术出航预设配置弹药/燃料/人员及必要规则资源；进入 M4，移动/射击/受损、保存加载续跑、返回编辑器 | 正式部署配置编辑器、战损改装 UI、跨模式高级工作流、X2 无 Python 安装包和发行总验收 | 用户从空白设计完成编辑→保存→出航→战斗→重载；几何与模块实际参与碰撞，参数可追溯到精确资源；同一保存边界与输入续跑一致 |

舾装领域层已有 `place_grid / place_side / place_hosted / move_grid / rotate_grid / remove` 等入口，先复用；缺失的侧挂移动、宿主更换、武器组修改只补当前闭环所需命令。复合替换仍须原子提交，不以删除后放置失败的半状态充当成功。

### 0.2 拆出的共用基础

- **W2a（已完成）**：资源索引、`EditorSession` 与真实 sidecar 串行领域工作队列；接入已有文档读取/预览及最小修改入口，完成修订、撤销/重做、结构化诊断与陈旧结果过滤。交付可在真实诊断界面操作的一条“打开→修改→预览→撤销”链，不单独扩建框架。
- **W2b（已完成）**：宿主受控路径句柄、合法原子保存、另存版本、外部冲突检查、关闭脏状态处理和独立草稿恢复。旧文件与失败保存不得丢失；超时结果未知必须重读核对，不能自动重复保存。
- **W3a（已完成）**：加入 PixiJS 并实现本表所需画布与面板；直接服务 H/O，模拟 transport 只覆盖这些界面的必要测试。W3b 增强交互后移。

W2a 完成后可实施 H1 纯领域命令及 O1 资源视图准备；W2b 与 W3a 在界面保存验收前就绪。依赖允许独立安排，不要求为此引入并行代理或另建项目。

### 0.3 战术桥的首版边界

T1a 不等待 T0c/T0d/T0e 或 e1：沿用 W0 信封，建立独立版本的最小表现全量快照、视觉事件及输入命令。静态几何在资源绑定时发送，动态状态先用有界低频全量快照；不传整份权威存档作为每帧表现数据。差量与二进制仅在真实测量显示需要时恢复。

T3a 在 sidecar 串行权威执行器中按 60 Hz 固定步调度；目标每秒发送 10—20 次表现快照，前端仅插值显示。编辑与战术使用明确模式/场景所有权，首版切换时暂停战术，不要求同时后台编辑与战斗。I/O、心跳与 UI 不能被长批次占住，输入和事件有界排队；可合并可替换完整表现快照，可靠结果/事件不得静默丢失。过载时显式暂停或减慢模拟墙钟进度并显示状态，不能丢弃权威步追赶。

新建试航场景显式选择已验收的 d4 v7 完整安全路径，绑定精确版本模块、推进安全资源和控制；不得隐式升级旧场景、旧保存资源或修改旧默认入口。W2 编辑资源保持其原版本，进入战术所需的能力迁移/适配由 X1a 提供具名、可追溯结果；现有具名迁移不能用于任意新设计时，补受测的资源构建入口，禁止猜测旧历史状态。

T2a 与 T3a 联调形成实时视角，不以“独立回放播放器已完成”为前置。输入带 `scene_id/input_seq/target_step`，显式拒绝迟到、重复和错误场景输入；直控及攻击经已有指挥仲裁与武器时间线处理，不另写规则。选择目标不绕过既有观测/火控条件，验收使用合法、可复现的技术配置。

### 0.4 当前性能验收与恢复条件

当前两舰低弹量场景是独立命名的产品功能样例，不能替代或改写原 6/20/30 舰 T0 基准。T4a 在参考机器上运行 10 秒预热、60 秒测量，记录固定步平均/P95/最大耗时、实时因子、表现频率、输入响应、队列峰值及内存起止/峰值。一次代表性运行加确定性输入重放即可作为首版证据；目标实时因子 >= 1.0，连续运行无持续队列增长，暂停及恢复有效。记录资源/输入身份、实际弹丸数与事件数，不能把短测宣称为长期内存稳定。

若两舰场景不能实时运行，仍可继续完成编辑器与暂停/单步联调，但 M4 实时验收不得标 PASS；仅针对该场景实测瓶颈做最小修复。只有热点落在弹丸批处理时才恢复 e1 的相关部分，不自动启动整个优化路线。六舰扩展验收留到 M5 后；20/30 舰容量结论继续等待正式 T0。

### 0.5 暂缓清单与触发器

| 暂缓项 | 恢复条件 |
| --- | --- |
| T0b.2e1 Python 弹丸批处理 | M4/M5 实测弹丸瓶颈阻碍当前验收，或闭环完成后开始扩大舰数 |
| e2 事件驱动/多速率、f/g 架构比较、Rust 热核 | 闭环完成且画像证明局部 Python 优化仍不能满足下一规模目标；规则变化另立版本 |
| T0c/d/e、252 次正式矩阵、饱和夹具与长跑 | 开始正式容量验收或准备发布性能承诺；原 NOT_COVERED 状态保留 |
| T1b 差量、T2b 回放/完整表现、T3b 高级控制、T4b 六舰全功能与 20/30 舰压力 | M5 通过后按用户体验及规模需求逐项恢复 |
| W3b/H/O 高级批量交互、X2 发行打包 | 基本编辑/试航闭环通过；打包在需要分发给无开发环境机器时实施 |
| 正式教程舰与联合标定、I12b、战略层与扩展舰种 | 本轮不实施；教程舰待完整工具与测试验证能力就绪后另行进入 I-F |

保留所有现有内核规则。后移某项可视化/专项场景不代表关闭对应毁伤、人员、资源、安全或生命周期计算。资源指纹、非法输入拒绝、保存完整性、权威时间与确定性始终属于当前必需部分。

### 0.6 新增需求：逐层边缘填充（2026-09-07）

玩家可为每层选择边缘填充类型，候选包括灵烷燃料、防火材料和微机修复材料，并保留“不额外填充”。首版建议处理船壳轮廓与所有完整内部安装格之间的边缘余量，不随模块增删填满空置整格。材料选择、装甲与净容积关系、装载消耗和战斗效果分片实施，具体方案见[《舰艇甲板边缘填充实施规划》](./高天荒野舰艇甲板边缘填充实施规划.md)。

H5a 已完成边缘片区、面积和毛体积的权威预览与分层叠加；舾装已进入验收收尾，T1a 已接通，当前主线接续 T2a/T3a。H5b 的装甲与净容积关系仍待冻结，冻结后接逐层设置、净容量和质量；H5c 在出航适配接灵烷供油与损失，H5d 防火/微机效果后移至基本闭环之后。H4 既有 PASS 不包含 H5；M3—M5 可用无额外填充设计验收，未接线效果不得冒充有效战术配置。

### 0.7 本次复盘与下一交付（2026-09-07）

用户已确认当前编辑功能未发现其他问题。首次没有可写文件目的地时须另存，之后普通保存覆盖已绑定文件；RCS 缺失不是本次保存疑问的原因。每外挂 1 m² 临时值保留为未标定技术参数，不再将其描述为保存故障根因修复。

M1/M2 已完成；M3 已可用并获得用户功能复测认可，O4 规范三舰实机留档作为非阻塞收尾项保留。现有 13 项 O4 服务测试、60 项前端测试及构建等记录仅代表各自已执行范围，本次复盘不重跑或扩大这些结论。进入 T1a 符合既有 W2a 后可开展协议准备的依赖关系，不修改 X1a 最终需要 H4/O4 与 T4a 的退出条件。

本次复盘后已完成 **T1a：两舰测试场景与最小表现协议**，下一交付联调 T2a/T3a，让用户看到并操纵真实舰艇。实际合同和验证项见 `contracts/web_bridge/t1a-tactical-protocol.md`。T4a 接普通炮弹与命中战损，X1a 再接用户保存设计出航及续跑。填充材料效果、高级批量编辑、独立回放、正式 RCS 标定和大规模性能工作继续按触发条件后移。

## 一、目标与顺序修正

本计划把舰艇规则内核转化为可实际使用和复核的产品工具。当前优先完成第零章 M1—M5；完整产品后续仍按以下方向扩展：

`Web 共用平台 → 船壳编辑器 → 舾装编辑器 → 测试战术场景 → 正式教程舰设计 → 联合标定 → 阶段 I 正式化门禁`

正式教程舰不得在可用船壳编辑器、可用舾装编辑器和测试战术场景之前完成数值标定。阶段 F 的最小合法舰、常规有人舰和完全无人旗舰继续作为 `contract_fixture / technical_surrogate` 支持界面、传输、性能和战斗闭环测试，不得改名或复制为正式教程舰。

I0 已建立的教程舰基准包和正式化门禁继续有效，但其作用是阻止技术替身被误升格，而不是要求当前立即填写正式参数。I1 至 I12a 已完成的弹药、后勤、实例设计、武器时间线、弹丸、战损、指挥、制导、持续毁伤、人员和观测/火控合同继续作为编辑器和战术场景的权威规则内核。

## 二、范围与非目标

本计划交付：

- Tauri 桌面宿主、TypeScript Web 界面和 Python 权威规则 sidecar；
- 玩家可用的船壳编辑器；
- 玩家可用的舾装编辑器；
- 可回放、可实时操纵、可压力测试的二维战术场景；
- 从编辑器设计到出航、战斗、战损和保存重载的垂直闭环；
- 在上述工具完成后设计并联合标定正式教程舰。

本计划暂不交付：

- 模块原型制作编辑器；
- 完整战略地图、据点经济、世界事件和战略 AI；
- I12b 舰载航迹持久化、舰队观测共享与正式 EW 来源；
- 自由三维战场、体积云或三维舰艇物理；
- 航空器、航母、主动防御和阶段 J 扩展舰种；
- 未经教程舰联合验证的雷达/EW、伤亡、蔓延、殉爆、搜救和回收概率。

I12b 与战略模式可以在本计划完成共用平台后接续，但不是编辑器和测试战术场景的前置条件。

## 三、冻结的技术原则

第一版技术栈冻结为：

- 桌面宿主：Tauri 2；
- Web 工程：Vite + TypeScript + React；
- 二维渲染：PixiJS WebGL；
- 权威规则：现有 Python 模块；
- 进程形态：由 Tauri 启动并随应用打包的 Python sidecar；
- 开发期通信：逐行 JSON 请求/响应与事件；
- 正式高频战术传输：先以版本化 JSON 快照/差量验证，只有性能报告证明必要时才替换为二进制；
- 第一发布目标：Windows 桌面版；
- 第一版可视化：调试几何和简化占位资源，不等待正式美术。

TypeScript 只负责交互、显示、视觉插值和非权威操作预测。以下规则只能由 Python 权威内核结算：

- 船壳拓扑、对称、甲板支撑和安装空间；
- 模块安装、重叠、净空、嵌入和派生性能；
- 质量、惯量、结构、气动、RCS、升力、推力、功率和人员；
- 武器、火控、弹丸、命中、装甲、战损、火灾、人员和生命周期；
- 规范保存、资源版本、内容指纹和正式化门禁。

## 四、当前已具备的基础

| 领域 | 当前状态 | 本计划用途 |
|---|---|---|
| `gaotian.ship/v1alpha1` 规范资源 | 已完成 | 前后端唯一数据口径 |
| 无界面船壳编译器 | 已完成 | 船壳编辑器最终裁决 |
| 无界面舾装编译器 | 已完成 | 舾装编辑器最终裁决 |
| 编辑器领域层与三舰 L3 往返 | 已完成 | Web 编辑器后端起点 |
| 运行时、出航和实例状态 | 已完成 | 编辑设计进入战术场景 |
| I1 至 I12a 技术闭环 | 已完成 | 测试战术场景规则内核 |
| Web/Tauri 工程 | W1 真实应用、监管器、诊断页和 sidecar 生命周期已完成 | T0/W2 共用宿主 |
| 可视化编辑器 | 船壳 H1—H4 完成；舾装已有画布、四类安装、射界、武器组和保存恢复，用户复测未发现其他问题 | O4 三舰规范实机留档收尾，不阻塞 T1a |
| 战术渲染和实时桥 | T1a 已接两舰完整安全场景、静态/动态快照及桌面检查入口；实时调度与画布未接 | 下一交付 T2a/T3a，画布、暂停单步与直控 |
| 正式教程舰 | 尚未定义 | X 阶段后进入 I-F |

已测得阶段 F 常规有人舰的现有领域预览成本约为：船壳完整预览 `77ms`，舾装完整预览 `0.9ms`。船壳拖动必须采用草稿显示、后台编译和防抖；舾装可以接近实时调用权威编译器。

## 五、依赖关系

```text
已完成 W0/W1 + Python 领域层 + d4 验收基线
        │
        ▼
W2a → W2b + W3a → H1—H4 最小范围 → O1—O4 最小范围
        │                                    │
        └→ T1a → T2a + T3a → T4a ────────────┤
                                             ▼
                                       X1a / M5 闭环
                                             │
                                             ▼
                    高级交互、六舰/大规模验证、发行与教程舰
```

T1a 依赖 W1 和已验收权威场景；T2a 依赖 W3a，T3a 复用 W2a 串行执行基础。H1/O1 可在 W2a 后开始，H/O 保存验收依赖 W2b。T4a 可先使用技术舰；X1a 必须使用实际编辑结果。T0 优化及正式容量验证不再阻塞 W2、H/O 或 T1a—T4a。

## 六、共用 Web 平台切片

### W0：架构与协议冻结

依赖：G0。

状态：2026-08-28 已落实。《高天荒野Web客户端W0架构与协议决策》冻结架构、目录、生命周期与环境边界；`gaotian.web-bridge/v1alpha1` 提供 Schema、18 个正面示例、45 个可移植负面用例、Python 编解码/握手参考合同与确定性报告。W1 的 18 项运行时验收和 T0 的 6/20/30 舰性能计划均已保存，但未标为执行通过。

任务：

- 建立架构决策记录，冻结宿主、前端、渲染器和 sidecar；
- 冻结 `editor.* / tactical.* / resource.* / save.*` 命名空间；
- 定义请求 ID、接口版本、会话 ID、修订号、命令、结果和结构化错误信封；
- 定义 sidecar 启动、握手、超时、崩溃和关闭策略；
- 定义开发日志、玩家日志和性能报告边界；
- 定义 Windows 开发、测试和打包前置依赖；
- 为未来 `strategy.*` 保留命名空间，但不实现战略模式。

验收：

- 所有跨进程消息均有明确版本；
- 前后端职责无重叠；
- 明确第一版不用纯浏览器 Pyodide，Python 后端不使用本地监听端口；Vite 的仅开发期页面/HMR 服务除外；
- 形成 W1 可直接实现的文件结构和接口样例。

### W1：应用骨架与 sidecar 生命周期

依赖：W0。

状态：2026-08-31 已落实。React/Vite/TypeScript、Tauri 2、ASCII Python sidecar、Rust 进程监管、Channel、心跳、有界队列、结构化故障与诊断页均已建立；18/18 W1 运行时用例通过，真实窗口完成自动握手、Ping、显式重启、停止和关闭回收复核。机器报告见《阶段W1Web应用与Sidecar生命周期接口.v1.json》。

任务：

- 创建 Tauri、Vite、React 和 TypeScript 工程；
- 创建 ASCII 名称的 Python 后端入口，内部复用现有中文模块；
- 实现 sidecar 启动、握手、健康检查、关闭和异常恢复；
- 实现逐行 JSON 调用与 Python 契约错误透传；
- 建立前端开发诊断页和 Python 日志查看入口。

验收：

- Tauri 窗口可以启动 Python 后端并取得接口版本；
- 后端异常不会令界面永久等待；
- 一次请求只能对应一次成功或失败结果；
- 不修改现有 Python 规范资源和测试语义。

### T0：战术性能与传输风险验证（扩容阶段，当前不阻塞 UI）

依赖：W1。基础诊断与 d4 规则基线已建立；以下正式容量任务后移。当前功能性能检查按 0.4 执行。

任务：

- 建立 6 舰功能档、20 舰目标档和 30 舰压力档；
- 分档加入普通炮弹、制导弹和连续武器事件；
- 测量单固定步、弹丸世界、运行时重编译、完整状态序列化和跨进程传输；
- 比较完整快照与差量快照；
- 记录参考机器、场景内容和可重复运行命令。

验收：

- 目标档达到实时因子 `>= 1.0`，或形成明确热点和优化切片；
- 压力档即使降帧也不得改变权威结果；
- 冻结 T1 表现协议的初始数据密度；
- 生成机器可读性能报告。

### W2：资源、文档会话与命令历史（按 W2a/W2b 落地）

依赖：W1。

任务：

- 建立材料、涂料、模块目录、船壳和舾装资源索引；
- 建立后端 `EditorSession`；
- 命令携带 `expected_revision`，拒绝过期修改；
- 保存当前草稿、最近合法编译结果、撤销/重做历史和脏状态；
- 实现新建、打开、保存、另存版本、关闭和恢复草稿；
- 非法草稿不得覆盖磁盘上的合法资源。

验收：

- 撤销/重做后内容指纹确定；
- 过期修订被原子拒绝；
- 未修改资源原样保存后保持规范 JSON 一致；
- sidecar 重启后可以恢复明确标记的草稿。

### W3：共用二维画布与前端测试设施（先 W3a，增强项 W3b 后移）

依赖：W2。

任务：

- 建立 PixiJS WebGL 画布、摄像机、坐标转换和五米网格；
- 实现缩放、平移、吸附、悬停、点选和框选；
- 建立 React 属性面板、诊断面板、状态栏和快捷键；
- 建立模拟 transport，使前端测试不必启动 Python；
- 建立 TypeScript 类型检查、单元测试和端到端测试入口。

验收：

- 世界坐标与屏幕坐标往返无漂移；
- 高 DPI 和窗口缩放正常；
- 相同输入得到相同选择与吸附结果；
- 同一 UI 可以在模拟和真实 transport 上运行。

## 七、船壳编辑器切片

### H1：细粒度船壳编辑命令（最小范围已完成）

依赖：W2。

在现有 `HullEditorDocument` 上增加：

- 新建空白船壳；
- 添加、删除甲板；
- 添加、删除区域；
- 插入、移动、删除端点；
- 拆分或删除边；
- 闭合区域；
- 设置基底层；
- 设置结构材料和逐边装甲；
- 单侧修改后的明确镜像操作；
- 端点数量变化时的边装甲迁移规则。

验收：每条命令独立可撤销，复合命令失败保持原子性，规范形状与旧资源兼容。

### H2：草稿与正式预览

依赖：H1、W3。

当前最小范围已完成：本地折线闭合后提交；拖动松开后延迟 150 ms 提交一次权威命令，提交前可取消；未提交轮廓不显示旧安装格。诊断定位复用甲板/区域/端点路径，专用边选择与细化定位后移。

- 拖动时显示宽松草稿；
- 停止操作后以约 `100—150ms` 防抖执行正式编译；
- 非法草稿保留最近一次合法派生结果；
- 诊断路径映射到甲板、区域、端点和边；
- 过期编译结果不得覆盖新草稿。

验收：船壳全量编译不阻塞拖动，自交、未闭合、重叠、无支撑和非对称错误均可定位。

### H3：船壳编辑器界面

依赖：H2。

当前最小范围已完成：新建、绘制、甲板/区域增删、端点拖动/数值编辑/插入/删除、基底切换、镜像、结构材料与逐边装甲、安装空间与质量/尺寸/惯量。边属性通过选中端点的出边编辑；气动/RCS 细分图表与专用边选择模式继续后移。双层空白绘制及保存已在真实桌面验证，三条技术船壳完整往返及镜像/装甲实机矩阵进入 H4。

- 甲板列表与层级切换；
- 端点、边和区域选择模式；
- 单侧绘制与镜像显示；
- 查看/编辑上层时默认半透明显示正下层船壳，供支撑与连接位置参考（参考显示已接入）；
- 结构材料与边装甲属性；
- 内部格、露天格和侧挂槽显示；
- 质量、结构、惯量、气动和 RCS 派生面板；
- 新建、打开、保存和派生版本。

验收：可从空白文档完成最小合法船壳，可修改三条技术替身，非法船壳不可保存。

### H4：船壳 UI L3 门禁（最小范围已完成）

依赖：H3。

- 用真实 UI 打开三条规范测试船壳；
- 未修改保存并重载；
- 比较规范几何、安装空间、派生结果和内容指纹；
- 覆盖镜像、装甲和多甲板交互。

验收：UI 往返与现有阶段 H 领域报告一致，Python 和 Web 回归同时通过并生成独立报告。

状态：2026-09-07 三条船壳完成真实桌面另存及文件重开，完整权威预览逐字段一致；镜像替换、边装甲、撤销/重做和上下层参考通过。M2 最小范围收口，详见下方 H4 完成记录。

## 八、舾装编辑器切片

### O1：模块资源浏览与显示模型

依赖：W2。可与 H1/H2 并行。

状态：O1/O2 最小范围已完成。21 个精确版本原型、13 类模块可筛选与查看，舾装会话及内部/顶挂/侧挂/嵌入命令已接通，并有精确位置表单。侧挂移动与宿主更换为原子操作；保存、恢复和历史复用共用机制。O3a 已补齐用户船壳快照与空白舾装，复制粘贴、批量及舾装镜像仍后移；O3b 已接可操作画布，O4 执行三舰 UI 往返。

- 按类别、安装方式和版本筛选模块；
- 显示质量、耐久、功率、人员、自动化和能力；
- 显示内部、顶挂、侧挂、嵌入和净空几何；
- 明确标识 `contract_fixture / prototype_unbalanced / balance_reference`。

验收：现有十三类模块均可完整显示，精确版本与内容指纹可追溯。

### O2：补齐舾装操作命令

依赖：O1。

在现有 `OutfitEditorDocument` 上增加：

- 侧挂模块移动；
- 嵌入模块更换宿主；
- 替换模块原型；
- 复制、粘贴、批量选择和删除；
- 镜像复制与实例自动命名；
- 复合操作原子提交。

验收：内部、顶挂、侧挂和嵌入均可完整编辑，失败操作不留下半个镜像或半次替换。

当前最小范围 PASS：四类放置/移除、网格移动与旋转、侧挂移动和更换宿主均接线；后三类批量/复制/原型替换等扩展项继续后移。自动服务验证不代替 O4 的真实画布往返。

### O3：舾装编辑器界面

依赖：O2、W3。最终联调使用 H4 产出的船壳。

O3a 已完成：通过宿主受控文件授权创建空白舾装，固定并内嵌用户船壳的规范快照、精确版本与内容指纹；保存、恢复和冲突检查已接通，不按同名资源静默重绑。O3b 已接点选放置、拖动移动、占用/冲突与宿主选择；O3c 已接同型号默认分组、拆分/改名/合并、保存历史和保守射界提示。下一切片为 O4。H5b 须先冻结净容积与装甲关系，再接逐层填充设置。

- 模块拖放、移动、旋转、删除和可选镜像；
- 内部、顶挂、侧挂、本体与净空五层占用；
- 冲突、嵌入槽和宿主关系显示；
- 质量、惯量、升力、供电、人员、推力和警告面板；
- 武器射界状态显示。

验收：可从空白方案完成最小合法舰，模块预览流畅，非法方案不可保存。

### O4：舾装 UI L3 门禁

临时数值决策（2026-09-07）：所有缺省 RCS 的外挂按每实例 1 m² 派生，明确配置优先；不修改目录身份，保持旧便携设计可打开。正式外挂 RCS 平衡继续后移，当前只保障编辑、派生和保存链路。用户已确认侧挂问题修复，最新复测与自动验证记录见 O4 验收合同。

依赖：O3、H4。

状态：功能复测认可，正式留档收尾。用户确认侧挂已修复，首次保存需另存，当前未发现其他问题；之前已确认跨层、射界、保存重开及已有草稿恢复可用。三舰规范往返已通过服务与 Rust/Python 桥接检查，完整实机逐舰留档仍待补齐。CIC 修复有自动回归覆盖，用户本次为整体认可，未逐项描述重装动作，不补造逐项证据。恢复机制为同一会话覆盖一份记录。剩余证据见 `contracts/web_bridge/o4-outfit-ui-acceptance.md`；不阻塞 T1a，但不能据此宣称正式 O4/M3 全项 PASS。

当前最小范围：三条技术舰真实打开、另存和重载；覆盖四类安装、跨层选择与下层参考、非法安装修复、武器组保存/撤销/恢复。逐字段核对模块、分组、占用和派生，并验证一个用户船壳便携舾装。高级镜像复制和批量操作仍按第 0.1 节后移；下面镜像等清单属于完整扩展门禁。

- 用真实 UI 打开三条规范舰；
- 显示全部模块和五类占用；
- 未修改保存并重载；
- 覆盖镜像、非对称删除、侧挂和嵌入操作。

验收：模块实例、位置、旋转、占用和派生输出逐字段一致，生成舾装 UI 往返报告。

## 九、测试战术模式切片

### T1：战术表现协议

T1a 最小范围已完成：独立命名两舰技术场景、v7 完整推进安全初始化、只读静态/动态投影、三端创建/读取/释放及桌面检查入口。输入边界纯校验已测，输入能力与真实视觉事件随 T3a/T4a 接入；差量仍为 T1b。详见 `contracts/web_bridge/t1a-tactical-protocol.md`，不得据此宣称实时操纵、战术画布或 M4 已完成。

依赖：W1 与 d4 已验收基线；T1a 不等待正式 T0。以下完整协议中的差量为 T1b 后续范围。

建立独立于权威存档的：

- `TacticalRenderSnapshot`；
- `TacticalRenderDelta`；
- `TacticalVisualEvent`；
- `TacticalInputCommand`。

静态船壳/模块/阵营数据与高频位置、朝向、速度和耐久分离。输入带序号和目标固定步，表现快照带权威场景步号。

验收：表现协议不进入权威存档，旧包、重复包和过期包可以识别，相同场景产生相同事件序列。

### T2：战术回放渲染器

依赖：T1、W3。

- 舰艇轮廓、朝向和阵营；
- 三高度层与云层黑影；
- 弹丸、导弹和尾迹；
- 开火、命中、爆炸和火灾占位表现；
- 相机、速度、航向和目标调试覆盖层；
- 从记录快照播放，不接实时控制。

验收：相同记录重复播放顺序一致，回放不修改权威状态，WebGL 资源可正确重建和释放。

### T3：实时仿真桥与控制

依赖：T1a、T2a 的画布显示及 W2a 串行执行基础；独立回放播放器不阻塞 T3a。

- Python 以 `60Hz` 固定步运行；
- 前端以 `20—30Hz` 接收快照并在显示帧间插值；
- 接入舵盘、推力、制动和 OverG；
- 接入 RTS 选择、航点和攻击命令；
- 接入暂停、单步和开发回放；
- 渲染帧率变化不得改变仿真时间线。

验收：重复输入不重复执行，暂停后权威场景不推进，直控和舰队命令继续通过 I9 仲裁。

### T4：功能、目标规模与压力场景

依赖：T3。

功能场景包含两个阵营、约六艘舰艇、一直控旗舰、其他 RTS 舰艇、三高度层、普通炮弹、制导武器、观测火控、局部装甲、模块战损、火灾、人员伤亡、二次毁伤、坠落和离场。

目标规模为十五至二十艘舰艇；压力规模为三十艘舰艇和高弹丸密度。未定义概率继续使用明确、可复现的脚本裁决，不冒充正式概率。

验收：功能场景可确定性回放，目标规模实时因子 `>= 1.0`，压力降帧不破坏权威结果，生成战术功能与性能报告。

## 十、跨模式闭环与打包

### X1：编辑设计直接进入战术场景

当前 X1a 依赖 H4/O4 最小验收与 T4a；原完整 X1 扩展验收依赖 T4b。

固定流程：

1. 船壳编辑器修改并保存新船壳版本；
2. 舾装编辑器在该船壳上布置模块并保存；
3. 通过现有编译链生成出航配置和舰艇实例；
4. 加入测试战术场景；
5. 执行移动、开火、命中和战损；
6. 保存并重载战术状态。

验收：不手填整舰聚合性能，战术参数可追溯到编辑资源精确版本，实际几何和模块位置参与命中与战损。

### X2：Windows 打包与总验收

依赖：X1。

- Python sidecar 随应用打包；
- 目标机器无需预装 Python；
- 完成本地资源权限、存档目录、日志和崩溃恢复；
- 执行 Python、TypeScript、UI 往返、战术功能和性能总回归。

验收：编辑—编译—出航—战斗—保存闭环通过，构建产物与源码边界明确。

## 十一、正式教程舰与阶段 I 收口

本章整体后移。只有扩展后的完整 X1 与测试能力通过后才进入正式教程舰设计和联合标定；X1a/M5 通过不自动解锁正式标定：

| 切片 | 内容 | 主要依赖 |
|---|---|---|
| I-F1 | 确定教程舰船型、尺度、定位、武备、装甲、船员和教学目标 | X1 |
| I-F2 | 用编辑器建立独立正式船壳、舾装、出航、实例和基准包 | I-F1 |
| I-F3 | 标定推进、升力、供电、操纵、换层和三层环境 | I-F2 |
| I-F4 | 标定装甲、甲弹、RCS、传感器、人员、武器、弹药和火控 | I-F2、I-F3 的相关输出 |
| I-F5 | 标定建造、维修、补给、改装和回收经济 | I-F2、I-F4 |
| I-F6 | 编辑器、战术、后勤和保存重载跨域验证 | I-F3、I-F4、I-F5 |
| I-F7 | 获得 `user_approved`，将全部关键项标为 `frozen_formula / calibrated` 并通过正式化门禁 | I-F6 |

编辑器完成前只允许明确教程舰的教学目标和体验边界，不冻结船体几何、模块布局或正式数值。

## 十二、现行执行顺序

1. W2a：真实编辑会话与权威命令已完成，见下方验收记录。
2. W2b 安全保存恢复、W3a 共用二维编辑画布已完成。
3. H1—H4 最小范围已完成：三条船壳真实 UI 往返与镜像/装甲、多甲板验收通过，M2 收口。
4. H5a、O1—O3 最小范围已完成，用户舾装功能复测认可；O4 三舰规范实机留档收尾。首次保存操作已澄清，不再排作功能故障。填充设置与运行效果按第 0.6 节分片。
5. T1a 已完成具名两舰场景与最小表现协议；当前下一交付 T2a/T3a 联调画布、暂停/单步/继续与直控，再以 T4a 完成基本交战和两舰性能验收。
6. X1a：将用户编辑结果送入战斗并保存续跑，完成 M5。
7. 按第 0.5 节触发器恢复扩展交互、性能、打包和正式内容工作。

## 十三、按影响范围验证

每次代码切片保留接口版本、兼容边界、确定性、错误诊断与适用正负例；不为文案、布局等低影响修改机械增加测试。领域修改运行相关专项与必要 quick；前端修改运行类型检查、相关单测及当前真实 UI 路径；宿主/通信修改追加对应 Python sidecar 与 Rust 检查。每个 M 里程碑记录实际运行命令、结果、未覆盖项与可复现操作，不重复制造无新增证据的报告。

全量 86 项重回归不再作为每个 UI 切片前置：在修改共享权威核心、保存/迁移语义或最终 M5 收口时执行 full，并按变更影响验证旧黄金。纯 UI 变更保留已有 d4h 证据，只补必要联调。若发现权威差异，先解释并修复，不更新黄金掩盖回归。

每次提交前检查 `git diff --check`。计划文字调整做文档一致性检查，实际功能验收运行相应专项并记录结果；不改写历史验收报告。当前计划只规定可提交边界，实际提交按开发任务授权执行。

## W2a 完成记录（2026-09-07）

新增只读开发资源索引（12 项，三条船壳可创建编辑副本）、有界内存 EditorSession、真实 sidecar 串行领域队列及独立 I/O。首轮命令复用领域层改名/结构材料修改，具有修订冲突、撤销/重做、脏状态、非法草稿诊断与最近合法预览；inspect 可在冲突后读取当前修订。

Rust 增加白名单编辑桥和后台/会话绑定，并将请求编号与发送入队原子化；前端依据实例、会话、请求票号和修订过滤过期结果。真实桌面界面完成打开常规有人舰、改名、撤销、重做与预览复核。证据见 `舰艇数据/报告/阶段W2a编辑会话与权威命令接口.v1.json`，业务合同见 `contracts/web_bridge/w2a-editor-session.md`。

Python W2a 7 项、包含既有 Web/阶段 H 的 5 个专项脚本、TypeScript 7 项及 Rust 16 项均通过。未改权威内核、规范资源、旧黄金和持久化语义；本轮没有重跑完整 87 脚本矩阵，也不计入正式 T0 性能验收。

W2a 验收当时仅有内存草稿；后续文件与恢复能力已由下述 W2b 接续。二维画布、舾装操作与战术接线仍待实施。

## W2b 完成记录（2026-09-07）

完成 Windows 原生文件选择、宿主一次性路径句柄、合法船壳打开/保存/另存/另存新版本、外部指纹冲突检查与同目录原子替换。项目资源包保持只读，保存后撤销历史保留；失败保存不主动截断旧文件，已提交文件与恢复记录清理失败分别报告。

已应用修改自动持久化为独立恢复草稿，包含修订、撤销/重做和最近合法源数据。恢复时重新校验材料依赖并重建预览，不恢复文件写入授权。脏会话与窗口关闭提供取消入口；未应用的输入框文本不属于恢复内容。会话业务接口升级为 v2alpha1，W0 信封和旧领域/战术存档合同不变。

Python W2b 12 项、相关 Web/阶段 H 六个脚本、TypeScript 8 项和 Rust 17 项通过，前端生产构建通过。真实桌面完成原生另存、修改、后台重启与草稿恢复，确认原文件未被草稿覆盖。未执行完整 88 脚本矩阵；本切片增加编辑器文件持久层，未改共享领域保存规范、迁移或战术存档语义。业务合同见 `contracts/web_bridge/w2b-editor-files.md`，报告见 `舰艇数据/报告/阶段W2b文件保存与草稿恢复接口.v1.json`。

下一切片为 W3a：二维画布缩放平移、五米网格与吸附、点选、属性/错误面板；随后进入 H1—H4 船壳几何编辑最小范围。舾装编辑与战术基本运行仍按 M3/M4 顺序实施。

## W3a 完成记录（2026-09-07）

完成 PixiJS WebGL 共用画布、米制坐标转换、缩放平移、五米网格/候选点吸附、甲板切换、区域与端点选择、属性与错误面板。诊断支持跨甲板定位；非法草稿明确显示当前轮廓，不用历史合法预览掩盖。画布按需重绘并在关闭时释放，不改变权威修订。

TypeScript 类型检查与 13 项测试通过（新增 5 项坐标/选择测试），生产构建通过。真实桌面验证船壳轮廓、端点选择坐标和缩放；独立模拟 transport 入口验证错误定位、平移、800 px 布局和关闭重开。高 DPI 已按设备分辨率适配，未执行多显示器实机矩阵或自动化端到端套件。本次无后台/宿主修改，未重复 Python/Rust 全量回归。合同与复现入口见 `contracts/web_bridge/w3a-viewport.md`。

下一项为 H1 船壳几何权威命令，接续 H2—H4。当前只有查看/选择与坐标吸附提示，尚不能直接绘制、拖动修改端点或增删区域；框选与复杂快捷键继续后移 W3b。

## H1 最小命令集完成记录（2026-09-07）

新增空白船壳创建、甲板/区域增删、基底切换、端点插入/移动/删除、区域镜像、逐边装甲及有界复合命令，沿用结构材料与改名入口。命令通过独立候选原子提交，接入修订、撤销/重做和合法保存。插点继承原边装甲，删点必须显式给出合并边装甲；镜像保留逐边对应关系。

为新建和删除操作补充严格的空容器草稿校验：允许无甲板或空甲板恢复，不降低正式资源编译要求。独立恢复记录写入升级为 v2alpha1 并兼容读取 v1alpha1；新建草稿也在确认前持久化，恢复仍不自动获得文件写入权限。W0 信封、会话 v2alpha1、正式船壳和战术存档格式不变。

H1 Python 14 项、相关 Web/阶段 H/船壳编译器 8 个脚本和 Rust 18 项通过，Rust 新增真实后台空白→几何批次→撤销验收。三条旧船壳规范往返保持一致；未执行完整 89 脚本矩阵。本次没有前端修改，不把命令测试当成绘制 UI 验收。合同见 `contracts/web_bridge/h1-hull-commands.md`，报告见 `舰艇数据/报告/阶段WebH1船壳几何命令接口.v1.json`。

下一项 H2/H3 最小交互：本地绘制折线和拖动草稿，闭合后一次提交区域，防抖权威预览并将命令接入画布。独立拆边/删边工具、复杂批量几何仍按第零章后移；正式多边形采用隐式首尾闭合，不引入未闭合正式资源。

## 2026-09-07 用户试用修正：网格是硬约束

移除吸附开关，采用既有编译器的 2.5 m 半格端点约定。五米安装格以原点为格中心，画布改为亮线表示 5*n+2.5 边界、暗线表示半格辅助线。新增区域、插点、移点和镜像均由后台拒绝离格坐标，不依赖界面开关；原第二甲板的半格坐标合法，不自动重写。

H1 专项增加至 16 项，前端增加至 14 项；相关八个 Python 脚本及生产构建通过。后续 H2/H3 绘制和拖动必须使用同一强制吸附函数，不能引入自由坐标模式。规则只约束端点，对斜边或窄区域仍须显示权威完整安装格，不能以端点对齐代替安装空间检查。

## 2026-09-07 用户试用修正：下层船壳叠加

画布已默认显示相邻下层的淡蓝半透明参考轮廓，当前层保持前景显示和独立选择。切层和适应视野包含上下层，空上层也可依据下层开始绘制；按实际 level 查找，缺层不自动替代。前端 15 项测试、生产构建及模拟交互验收通过。后续 H2/H3 绘制交互须保留此参考层，合法性仍由权威完整支撑检查判定。

## H2/H3 最小编辑交互完成记录（2026-09-07）

画布已接入空白新建、添加甲板、闭合绘制、端点拖动与数值编辑，以及区域/端点/甲板增删、基底、镜像、结构材料和逐边装甲命令。材料选项来自后台只读目录的精确版本引用。绘图与拖动强制使用 2.5 m 网格；正下层半透明参考在空上层和绘图过程中保持显示。

拖动在松开 150 ms 后只提交一次命令，取消和卸载清理待提交操作。未提交草稿明确标记，禁止保存/切层；非法已提交草稿保留最近合法派生结果，画布不叠加过期安装格。安装空间、质量、尺寸、惯量使用权威预览。

真实桌面从空白绘制 25 格基底层，再依据下层参照绘制 9 格上层，通过支撑检查；离格鼠标位置吸附到 ±7.5 m。端点拖动使修订只增加一次，非法非对称轮廓不可保存，撤销恢复合法。双层船壳保存产物位于 `artifacts/h2h3/drawn-hull.json`，规范资源、网格与后台重新编译一致；原生关闭重开后为干净修订 0，双层和派生结果保持一致。

前端 19 项测试、类型检查与生产构建通过；相关 Python 八个脚本通过（H1 专项 17 项，89 个脚本中按影响范围选取 8 个）。本轮没有 Rust 修改，未重复 Rust 或完整矩阵。合同见 `contracts/web_bridge/h2h3-hull-interaction.md`，报告见 `舰艇数据/报告/阶段WebH2H3船壳编辑交互.v1.json`。H4 完整船壳 UI 门禁、舾装与战术仍待接续，M2 尚未最终收口。

## 2026-09-07 用户试用修正：对称绘制与原区域替换

镜像主流程由“复制整个区域到另一侧”改为默认单侧绘制、实时镜像显示、回到中线后一次生成完整区域。现有区域采用“以左侧为准／以右侧为准→替换预览→应用”，轮廓与对应边装甲同步替换，已经对称时不新增区域或修订。自由逐点完整轮廓保留为另一绘制方式。原区域镜像复制命令保留兼容，但不再作为常规船体建造按钮。该修正是 H4 验收前的产品缺口修复；多次穿越中线、分离结构和离格交点须明确提示，不自动猜测几何。

前端 25 项、H1 专项 19 项及相关 Python 八脚本通过；桌面退出权限修正后 Rust 18 项通过，前端与宿主构建通过。专项覆盖无中线接缝、左右轮廓、边装甲反向映射、已对称无操作、原子替换、撤销/重做/恢复和离格拒绝。

## H4 最小 UI 门禁完成记录（2026-09-07）

在 `526efe4` 生产基线上通过真实桌面打开常规有人战舰、最小合法舰和完全无人旗舰，分别另存并关闭会话、用原生文件对话框重开。三者均为合法、干净修订 0，规范几何、全部安装空间、完整派生与源指纹逐字段一致；阶段 H 既有领域报告回归通过。

常规舰切换上层显示两个活动区域及正下层半透明参照。最小舰拖动端点吸附至 `(10, 40)` 后非对称草稿不可保存；舰首边装甲设为 0.1 m，以右侧为准预览并替换后恢复合法单区域，撤销/重做正确恢复非法/合法状态。独立保存并重开编辑产物，装甲对应关系及质量经重新编译核对。

四个真实保存产物位于 `artifacts/h4/`。新增 `tools/verify_hull_ui_roundtrip.py` 供复跑完整数据比较；该脚本不代替实际 UI 操作。独立报告见 `舰艇数据/报告/阶段WebH4船壳UI往返.v1.json`，实际操作、回归命令及未覆盖范围见 `contracts/web_bridge/h4-hull-ui-acceptance.md`。

本轮 TypeScript 类型检查与 25 项测试、相关 Python 8 / 89 个脚本及四文件校验均 PASS。未修改生产规则或宿主，不重复完整战术矩阵和 Rust 构建。M1/M2 最小范围已完成；当前下一切片为 O1/O2，M3 舾装、M4 战术和 M5 闭环尚未完成。

## H5a 边缘空间预览完成记录（2026-09-07）

Python 已根据规范船壳与全部完整内部格派生真实边缘分区、面积及 5 m 层高毛体积；新增显式 v2 编辑视图，默认 v1 预览和历史资源/编译结果保持一致。画布可独立开关橙色边缘余量，分层显示统计；零余量有明确说明，本地/非法草稿不展示陈旧面积。

真实桌面验证常规舰 deck.0 的 775 m² / 3,875 m³、deck.1 的 50 m² / 250 m³，余量开关、上层与下层参照并存、非法拖动隐藏及撤销恢复均通过。应用保留合法、与源资源一致的修订 2。

新增 6 项 Python 专项与 4 项前端测试；类型检查及 29 项前端测试、选定 9 / 90 个 Python 脚本和前端生产构建全部 PASS。合同与 UI 记录见 `contracts/web_bridge/h5a-edge-space-preview.md`，独立报告见 `舰艇数据/报告/阶段WebH5a边缘空间预览.v1.json`。旧 H4 产物和阶段 H 黄金未重写。

下一切片为 O1/O2 模块资源视图、舾装会话和四类安装最小命令；H5b 逐层材料与净容量、H5c 燃料、H5d 防火/微机效果尚未实现。毛体积不能作为实际燃料容量。

## O1/O2 最小范围完成记录（2026-09-07）

新增显式 v2 资源目录视图，显示三份技术目录的 21 个模块原型、13 类模块及精确版本、内容指纹和安装几何。共用会话按资源种类打开船壳或舾装，复用保存/另存、撤销/重做与恢复；舾装保存及恢复额外校验船壳、模块和涂料依赖。

四类安装放置/移除已接通；网格位置与旋转一次提交，侧挂移动和宿主更换在完整候选编译成功后原子替换。前端提供目录筛选、实例选择和位置表单；真实窗口验证常规舰 18 个模块、货仓移动和撤销恢复源设计。用户恢复草稿保持不动。

新增 8 项 Python 与 6 项前端测试，类型检查及 35 项前端测试、选定 11 / 91 个 Python 脚本、前端生产构建全部 PASS，阶段 H 既有报告未改变。合同见 `contracts/web_bridge/o1o2-outfit-sessions.md`，报告见 `舰艇数据/报告/阶段WebO1O2舾装资源与命令.v1.json`。三舰文件往返属于服务层自动验证，本轮没有冒充 O4 的完整真实 UI 验收。

本次 O1/O2 验收时仍限于资源包船壳及其舾装另存；后续 O3a 接续结果见下一记录。M3、M4、M5 尚未完成，H5b—H5d 的填充设置与效果继续按切片推进。

## O3a 完成记录（2026-09-07）

用户保存的合法船壳可通过文件选择器创建空白舾装；快照随舾装文档保存，不再依赖原文件位置。内嵌船壳、舾装引用、目录指纹和全部恢复历史均验证，原文件改变不自动同步，覆盖船壳及外部舾装保存冲突明确拒绝。空白草稿可恢复；补齐 CIC 和升力等既有条件后才能保存。

新增船壳绑定 8 项测试；前端类型检查与 36 项测试、构建、选定 12 / 92 个 Python 脚本通过，80 个未执行。真实桌面创建与文件往返证据另存阶段报告。合同见 `contracts/web_bridge/o3a-user-hull-binding.md`，报告见 `舰艇数据/报告/阶段WebO3a用户船壳与空白舾装.v1.json`。

O3a 完成时的下一步为 O3b 船壳/模块画布；其接续结果见下方完成记录。O3c 接武器组，O4 再做三舰完整 UI 往返。尚未完成 M3—M5；本次文档包装在出航适配时须解包成规范船壳与舾装对。

## O3b 最小画布完成记录（2026-09-07）

用户船壳与资源包舾装均已有可操作画布：点选原型放置、自动实例命名、模块选择与拖动、旋转/移除、甲板切换、下层参考、缩放平移、实际侧挂槽与嵌入宿主选择。精确位置表单收纳在详情中。内部、顶挂、侧挂、本体与净空图层已接，空白和非法草稿仍显示当前布局；占用冲突双方标红，重叠处优先拖动已选模块。

布局通过既有实例编译器生成独立 `gaotian.outfit-layout/v1alpha1`，不会把缺 CIC、升力或有冲突的方案标作合法整舰。原船壳、装甲、填料、战术公式与历史黄金报告保持原状。

新增 6 项 Python 布局测试、8 项前端交互几何测试；44 项前端测试、类型检查与构建通过，选定 13 / 93 个 Python 脚本全部通过（80 个未执行）。真实桌面验证了贮槽拖动、弹药库画布放置、重叠标红和拖开恢复合法；本次不冒充侧挂/嵌入/多甲板全流程 O4 验收。详见 `contracts/web_bridge/o3b-outfit-canvas.md` 与 `舰艇数据/报告/阶段WebO3b舾装画布.v1.json`。

下一切片 O3c 武器组及必要射界提示，随后 O4 补齐三舰、四类安装、跨层画布与保存恢复真实往返；M3—M5 尚未完成。H5b 净容量与逐层填充仍按已冻结的分片规划推进。

## O3c 最小武器组完成记录（2026-09-07）

同型号、同版本武器默认成组，现可改名、拆分和合并，成员可定位到对应甲板画布。分组提交复用原子修订、撤销、重做、保存和恢复；模块增删同步维护组成员。首次应用分组才升级舾装为 `gaotian.outfit-plan/v2alpha1`，旧文件的默认视图不写回或改变规范指纹。

射界提示采用已有保守策略：无更高甲板且安装无冲突时显示 360° 船壳参考方向圈；有更高甲板则提示遮挡待检查；非法安装不显示可用圈。完整遮挡射线、弹药选择和开火接线仍待战术阶段。

新增 9 项 Python 分组专项和 6 项前端测试，50 项前端测试、类型检查及生产构建通过；选定 15 / 94 个 Python 脚本通过，覆盖编辑会话、船壳/舾装编译、运行时参数、保存恢复与阶段 H。真实桌面验证双武器默认分组、拆分提交、撤销/重做、成员切到上层和参考圈；检测到用户接手后停止界面输入，未宣称完成原生保存重开。服务层普通/便携文件保存往返及恢复已自动验证。

全量运行前 19 个历史阶段脚本失败，在第 20 个长矩阵期间停止；已复核 b3a、c1 为历史代码/计划指纹断言失败，其余失败原因未全部分类，后续 74 个脚本未在该轮启动。不得宣称全量 PASS 或改写历史报告掩盖失败。此次最小编辑范围通过不替代 O4/M5 退出门，完整记录见独立报告。

合同见 `contracts/web_bridge/o3c-weapon-groups.md`，证据见 `舰艇数据/报告/阶段WebO3c武器组.v1.json`。下一步 O4 三舰与四类安装真实往返，再接基本战术；M3—M5 尚未完成。H5b 净容量关系仍待冻结。

## O3c 水平射界缺口修正（2026-09-07）

原参考圆未实现真实水平遮挡，已补齐共享船壳射线规则、可射/禁射扇区及上层轮廓投影。按武器基底层以上船壳计算，贴边也禁射；实际弹丸生成与统一场景发射边界强制检查，非法步骤不提交弹药或时间变化。低层无方向弹药动作仍不是空间发射授权。

相关 11 / 95 个 Python 脚本通过，包含真实三层船壳遮挡拒绝/无遮挡发射、转向一致性及既有弹药、队列、弹丸和场景回归；前端 51 项测试和构建通过。完整历史指纹门禁及长矩阵未复跑。实机扇区与重启恢复本次未完成，需在 O4 一并验收。合同与便携验证设计见 `contracts/web_bridge/o3c-horizontal-fire-arc.md`。

下一步仍是 O4；自动停火/重新瞄准与战术 UI 指令过滤在 T 阶段接入，不用改写历史黄金掩盖失败。

下层射界圈跟进：实机确认旧后台仍返回“遮挡待检查”，现把重启提示直接放到画布属性栏；修正武器在顶挂/内部跨层可见时射界被隐藏的问题，投影仍按武器安装层筛选。新增 6 项 SVG 渲染测试，前端 57 项、构建及 6 项 Python 射界专项通过。用户当前草稿为修订 20、20 个模块；原生操作检测到用户输入后中断，后台重启恢复与扇区实机验收仍待完成。

## O4 舾装验收启动记录（2026-09-07）

用户已确认后台重启后下层射界可见。本轮新增三舰、四类安装保存重开、重叠修复与恢复历史、侧挂及武器组共同持久化的 5 项综合测试；新增 Rust 三舰真实子进程文件往返测试。选定 6 / 96 个 Python 脚本全部通过，Rust 原有 18 项及新增专项通过，桌面构建通过。前端未在本轮改动，保留此前 57 项结果。完整历史矩阵未复跑。

修正原生文件对话框将舾装误称为“船壳”的文案，并提供独立保存文件比较器；文件缺失或内容不一致不能通过。实际窗口已观察到三层便携舾装合法打开，但后续原生控制反复失去可用窗口，三舰 UI 另存重开及四类安装画布验收仍待完成。计划保持 O4 执行中，下一步完成剩余实机检查后接 T1a—T4a。记录见 `contracts/web_bridge/o4-outfit-ui-acceptance.md`，当前文件检查结果见 `artifacts/o4/saved-artifact-verification.json`。

## 历史记录阅读说明

以下第十四章起保留实施当时的证据和决策。涉及 W2 暂停、e1 唯一下一项、T0 先于 UI 等调度语句均为历史状态，已被 2026-09-07 第零至十三章取代；技术约束和通过/未覆盖事实不因此改变。

## 十四、W1 完成记录与 T0 开工边界

W0 的架构与协议合同继续有效。W1 已在该合同上完成以下交付：

1. 不修改系统级 PATH 的 Windows 工具链预检、构建、测试与开发入口；
2. 锁定 npm 与 Cargo 依赖的 React/Vite/TypeScript/Tauri 工程；
3. ASCII 模块入口 `backend.high_wilderness_sidecar`，只开放三个 `system.*` 能力；
4. Rust 子进程所有权、严格 JSON Lines、一次结算、代次隔离、心跳、超时与有界队列；
5. 单窗口 Channel、旧代次过滤、64 KiB 前端日志和 200 项事件上限；
6. 自动握手的开发诊断页，显示接口、sidecar、数据契约、能力和实例 ID；
7. Python 8 项 sidecar 检查、TypeScript 4 项测试、Rust 15 项测试和真实窗口复核；
8. `w1-acceptance.v1.json` 18/18 PASS 的机器可读报告。

协议参考代码与测试仍位于《高天荒野Web桥接协议.py》《高天荒野Web桥接协议测试.py》；W1 实现位于 `apps/desktop/`、`backend/high_wilderness_sidecar/` 和 `tools/Invoke-HighWildernessWeb.ps1`。未改动现有舰艇规则或规范资源。

下一切片 T0 按已冻结的性能计划开工：

1. 先建立可重复场景生成器与无界面基线，冻结输入、资源和最终状态 hash；
2. 实现 6 舰功能档、20 舰目标档和 30 舰压力档；
3. 分级测量固定步、弹丸世界、运行时重编译和全状态序列化；
4. 在 W1 桥接上测量全量 JSON 与实验性精简/差量投影，不将实验投影升格为 T1 合同；
5. 保存每档三次重复的平均值、P95、峰值、实时因子、队列等待和最终权威 hash；
6. 若 20 舰目标档未达到实时因子 1.0，先形成有证据的优化切片，再决定是否进入 W2。

W1 的 PASS 只证明桌面宿主和 sidecar 生命周期可用；不代表 T0 性能、W2 编辑会话、编辑器 UI、战术表现协议或发行打包已通过。

## 十五、T0a 完成记录与 T0b 开工边界

T0a 已建立严格计划解析、正式运行矩阵、最近秩 P95、机器与 Git 元数据、结果 Schema、命令行入口和阶段 F / I 技术资源负载审计。正式矩阵按 `3 档 × 4 级负载 × 3 次重复 × 7 个模式/频率变体` 展开为 252 次运行；无界面基线不虚构快照频率，同一档位、负载级和重复序号的七个传输变体共用输入 seed 与输入流 hash。

负载审计形成以下约束：

1. 三档运动负载均可使用现有三舰编成进入 T0b；
2. 阶段 F 保存出航与实例均未装载弹药，T0b 必须物化独立、合法、可复核的基准装载，不得修改保存夹具冒充现状；
3. 完全无人旗舰武器原型尚无阶段 I 武器时间配置，不能直接进入合法射击时间线；
4. 现有合法时间配置下，若按统一场景实际输出的 `fire + reload` 武器动作事件计数，6/20/30 舰档最大持续事件率仅为 4/20/28 次每秒，60 秒容量仅为 240/1200/1680 次，低于计划的 24/120/300 次每秒；对应最大合法开火率为 2/10/14 发每秒；
5. 因此三档武器事件目标均标记为 `NOT_COVERED`。后续只能补充可追溯技术夹具或依据审计修订目标，不能注入脱离弹药、装填、制导和武器时间线的虚构事件；
6. 当前没有执行正式性能运行，五项 T0 门禁仍为 `NOT_COVERED`，`t0_performance_measured = false`。

T0b 的直接范围是确定性 6/20/30 舰场景生成、合法基准装载、分级活动实体计数、输入/资源/最终权威状态与事件 hash，以及只包含权威固定步总墙钟的无界面基线。T0b 不实现 JSON 投影、差量编码、跨进程/WebView ACK 或正式 T1 字段；这些分别留给 T0c、T0d 和 T0e。

## 十六、T0b 完成记录与 T0c 开工边界

T0b 已建立以下可执行边界：

1. 生成 6/20/30 舰三档与运动、普通弹、制导弹、脚本战损重编译四级负载，共 12 个确定性初始场景；
2. 每艘舰使用阶段 F 精确船壳、舾装和运行时链；战斗舰通过独立 T0 技术出航配置装入 100 发弹药库库存和 1 发待发弹，不修改阶段 F 保存资源；
3. 新增仅供 T0 使用的无人武器时间技术替身，使常规有人舰与无人旗舰都能进入合法 60Hz 武器时间线；
4. 普通弹与制导弹压力初态均通过现有 `TacticalSceneState` 持久化解析；后续新增弹丸必须来自合法弹药、装填、射击事件和同边界发射指令；
5. 脚本战损档为每艘舰建立低风险、可追溯的持续毁伤状态，每步继续走现有毁伤结算与运行时重编译；
6. 三次重复复用完全相同的输入流和输入 hash，重复序号只表示再次测量；当前为 12 条唯一输入流、36 个重复比较组和 252 次模式/频率运行；
7. 无界面运行器记录固定步样本、实时因子、驻留内存、活动实体分布、事件数量、最终权威状态 hash 和滚动事件流 hash；`-Resume` 只复用输入、资源、初态和步数均匹配的完整结果；
8. 12/12 初态、4/4 分级单步、合法装载、负面输入、结果合同和重复权威 hash 均已回归通过。

补入无人武器时间配置后，三档合法持续武器动作事件上限提高到 8/28/40 次每秒，但仍低于 24/120/300 次每秒目标。初始压力状态可以达到计划弹丸数；由于弹丸寿命为 30 秒、正式测量窗口为 60 秒，而合法补充射速不足，不能据此宣称全窗口负载已覆盖。正式 600 步预热、3600 步测量尚未执行，T0 五项门禁继续为 `NOT_COVERED`，`t0_performance_measured = false`。

T0c 只在同一权威场景输出上比较完整权威 JSON、实验性表现全量与实验性表现差量，记录序列化耗时、帧字节、差量基线和恢复规则；不得改变权威推进、补造无效负载或把实验字段升格为 T1 稳定协议。跨进程和 WebView ACK 仍留给 T0d。

## 十七、T0b.1 权威热路径优化计划

### 17.1 当前唯一进行项与暂停边界

当前唯一进行项是 `T0b.1`。在其决策门通过前，暂停以下工作：

1. `T0c` 权威全量、实验投影与实验差量序列化；
2. `T0d` 跨进程与 WebView ACK、`T0e` 正式结果汇总和 T1 建议；
3. `W2` 及后续船壳编辑器、舾装编辑器和测试战术界面；
4. 24/120/300 次每秒武器事件目标的修订、专用饱和夹具和弹丸寿命调整；
5. 600 步预热、3600 步测量和 252 次正式性能矩阵。

暂停不等于取消。T0c 及后续阶段保持原有依赖和范围，待 T0b.1 决策门给出稳定的权威基线后恢复。当前不得通过降低负载、关闭规则或修改权威结果来制造性能通过。

### 17.2 诊断基线

以下数据是 2026 年 8 月 31 日在当前工作树执行的短程诊断，不是正式 T0 成绩：

| 诊断项 | 当前结果 | 结论 |
| --- | ---: | --- |
| `target_20.motion_only` 三步平均 | 2265.848 ms/步 | 远高于 60Hz 的 16.667 ms 预算，弹丸不是首要瓶颈 |
| `target_20.guided_projectiles` 三步平均 | 2384.079 ms/步 | 当前约为实时速度的 0.007 倍 |
| `stress_30.guided_projectiles` 首步 | 3998.165 ms | 30 舰与 1650 枚初始弹丸无法用于正式长跑 |
| 30 舰制导单步完整快照哈希访问 | 约 700 次 | 不变设计资源在固定步内被重复完整序列化和哈希 |
| 30 舰制导单步完整运行时编译 | 约 160 次 | 同舰、同边界和相邻阶段存在大量重复编译与校验 |
| 30 舰制导单步战术舰模型构建 | 约 60 次 | 静态几何、气动与 RCS 结构被重复构建和复核 |
| 仅临时缓存设计快照哈希后的 20/30 舰制导首步 | 约 380/747 ms | 静态哈希是第一热点，但移除后仍不足以达到 60Hz |

剖析累计时间会发生调用包含关系重叠，不能相加为总时间；上述调用次数和短跑墙钟只用于确定优化顺序。

### 17.3 不可破坏的约束

1. 60Hz 固定步、输入顺序、事件顺序、弹药消耗、武器边界、弹丸命中、持续毁伤和生命周期语义保持不变；
2. 相同输入和资源必须产生与优化前相同的最终权威状态 hash 与滚动事件 hash；
3. 外部输入、载入、存档、IPC 和资源替换边界继续执行严格内容指纹校验；只允许可信、已验证的进程内对象使用快速路径；
4. 快速路径不能依赖可变但未版本化的嵌套容器。若对象尚未真正不可变，必须先封装、复制或引入显式 revision；
5. 本轮不修改性能目标、舰艇编成、武器射速、弹舱容量、弹丸寿命或正式 T1 表现字段；
6. 在 Python 热路径完成结构性减负前，不直接启动整体 Rust 迁移，也不修改权威固定步频率。

### 17.4 子切片、依赖与验收

| 顺序 | 子切片 | 实施内容 | 可复核交付物 | 退出条件 |
| --- | --- | --- | --- | --- |
| 1 | `T0b.1a` 基线与观测 | 增加默认关闭的阶段计时和调用计数；区分完整性校验、运行时编译、生命周期、武器时间线、机动、弹丸、毁伤、结果物化和观测器开销；冻结 12 个场景的权威黄金 hash | 短跑协议、热点报告、12 场景黄金结果 | 关闭观测时不改变结果或产生可测退化；当前基线可一键复现 |
| 2 | `T0b.1b` 静态指纹与验证边界 | 在编译/载入时一次性计算设计快照、气动/RCS缓存和配置目录指纹；场景绑定保存已验证 token；严格验证移到资源边界 | 指纹缓存接口、严格/快速双路径、篡改与失效测试 | 固定步内设计快照完整哈希计算降为 0；严格与快速路径权威 hash 一致；短跑至少比当前快 4 倍 |
| 3 | `T0b.1c` 静态模型与动态运行时复用 | 分离静态战术舰模型和动态实例状态；按舰艇 revision/dirty domain 复用 runtime；生命周期、机动和输出共享同一边界结果；燃料、模块、人员、供电、弹药和毁伤分别失效 | 缓存键与失效表、运行时复用实现、逐类失效测试 | 稳态固定步不再重建静态模型；完整 runtime 编译调用降低至少 75%；20 舰纯运动短跑 P95 不高于 50 ms |
| 4 | `T0b.1d` 事件和弹丸热路径 | 使用确定性到期事件索引避免无事件舰艇反复扫描；同边界批量生成弹丸并只合并/排序一次；预编译目标几何；活动实体改为增量计数 | 事件索引、批量生成接口、几何缓存、顺序等价测试 | 事件集合、顺序和 hash 与原实现一致；20 舰制导短跑固定步 P95 达到 16.667 ms 目标，或形成明确的剩余热点证据 |
| 5 | `T0b.1e` 测量器去扰动 | 事件 hash 直接读取事件字段，不先物化整份 resolution；环境元数据每次运行只采集一次；内存低频采样；分别报告权威推进墙钟、观测器开销和包含 drain 的实时因子 | 更新后的无界面结果、测量边界测试 | 固定步样本不包含观测器开销；实时因子仍包含计划规定的 drain；结果 Schema 与续跑校验通过 |
| 6 | `T0b.1f` 决策门 | 在固定机器元数据下执行 10 步预热、60 步测量、三次重复的短程诊断；比较优化前后调用数、P95、最大值、内存和权威 hash | T0b.1 优化报告与下一步决策 | 按 17.5 的分支作出唯一决策；仍不冒充正式 600+3600 步 T0 结果 |

子切片必须按表中依赖顺序实施，每个子切片独立提交、独立回归、可以单独回退。不得把结构优化、负载目标调整和 T0c 序列化混入同一个提交。

### 17.5 T0b.1f 决策分支

1. 若 `target_20` 运动和制导短跑三次重复均达到实时因子不低于 1.0，且权威 hash 全部一致：结束 T0b.1，恢复 T0c；
2. 若运动档通过而弹丸档失败：保持 Python 规则编排，新增一个只覆盖弹丸积分、制导和碰撞的 Rust 热核评审，不迁移整套规则；
3. 若运动档仍失败且热点集中在动态运行时、对象分配或 Python 调度：先形成“舰艇/损管低频、弹丸高频、表现插值”的多速率权威 RFC，与继续迁移热核做数据化比较；
4. 若短跑达到目标但内存、队列或最大步耗时无界：继续留在 T0b.1，不进入 T0c；
5. 只有决策分支 1 可以直接恢复 T0c。分支 2 或 3 必须先完成新切片规划和权威等价门禁。

### 17.6 每个子切片的共同回归门禁

1. 舰艇测试总入口全部通过，且不得减少既有用例；
2. 12 个 T0 场景的初态 hash、输入流 hash 和资源 hash 不变；
3. 严格路径与快速路径在相同输入下的逐步事件、最终状态和事件流 hash 一致；
4. 存档重载、续跑和跨进程输入继续经过严格验证；
5. 至少包含一项缓存失效负例，证明模块损伤、人员、供电、燃料、弹药、毁伤和资源 revision 不会复用过期结果；
6. `git diff --check`、Python 静态编译和对应阶段报告通过；
7. 任一权威结果发生变化时立即停止性能比较，先判断是缺陷、未声明规则变化还是黄金结果错误，不以“性能更快”为理由接受漂移。

### 17.7 风险与复核重点

1. `frozen dataclass` 不等于所有嵌套成员都不可变；缓存指纹前必须审计字典、列表和外部引用；
2. runtime 缓存最危险的是漏失效，必须使用显式 revision/dirty domain，不以对象地址作为长期权威键；
3. 批处理可能改变同一边界事件排序，稳定排序键必须继续进入合同测试；
4. 剖析器和逐步内存采样会显著扰动短步结果，观测器必须默认关闭并单列开销；
5. 缓存命中率、完整编译次数和静态模型重建次数必须作为报告字段，防止只看墙钟掩盖错误复用；
6. 24/120/300 武器事件率与 30 秒弹丸寿命的问题继续保持 `NOT_COVERED`，待权威热路径稳定后另行拆分“代表性场景”和“饱和压力场景”，不在 T0b.1 内绕过。

### 17.8 T0b.1a 完成记录与 T0b.1b 开工边界

T0b.1a 已建立以下可执行基线：

1. 新增严格解析的六场景短程诊断协议，覆盖 6/20/30 舰的运动和制导弹丸场景；协议固定剖析与非剖析运行从同一初态推进相同步数，并明确不得作为正式 T0 成绩；
2. 观测器完全位于 `benchmarks/t0`，只在显式执行诊断命令时启用 `cProfile`；权威场景、运行时编译器、武器时间线、机动、弹丸和毁伤代码没有插入常驻计时钩子；
3. 冻结 3 档 × 4 级负载共 12 个场景的初态、单步最终状态、滚动事件、输入流、资源清单、事件数量和实体数量 hash；重新计算结果为 12/12 一致；
4. 建立完整性校验、运行时编译、生命周期、武器时间线、机动、弹丸和毁伤七类函数探针，并单列结果物化、事件摘要和剖析器估算开销；累计时间允许嵌套重叠，不作为可相加的阶段墙钟；
5. 代表性 6 舰运动步基线为 120 次设计快照内容指纹访问、24 次完整 runtime 编译、12 次战术舰模型构建、12 次武器时间线推进和 6 次机动积分；观测前后的权威状态与事件 hash 完全一致；
6. 六场景本地诊断再次确认调用数随舰数近似线性放大：30 舰制导首步达到 700 次设计快照指纹访问、160 次完整 runtime 编译和 120 次静态模型/时间线相关构建或推进；本地时序结果写入忽略目录，不进入确定性阶段报告；
7. 命令行与 PowerShell 已提供 `verify-golden` / `VerifyGolden` 和 `diagnose-hot-path` / `DiagnoseHotPath`；默认本地输出位于 `artifacts/t0-local/`；
8. 新增回归后舰艇测试总入口为 51/51 PASS。正式性能运行仍为 0，`t0_performance_measured = false`。

T0b.1b 只能调整静态指纹计算和严格验证边界。不得同时引入 runtime dirty-domain、事件批处理、弹丸优化或测量器改写。其首要验收是固定步内设计快照完整哈希计算降为 0、严格/快速路径 12 场景黄金结果一致，并在相同短跑协议下达到至少 4 倍加速。

### 17.9 T0b.1b 完成记录与 T0b.1c 开工边界

T0b.1b 已完成静态指纹和校验边界收敛：

1. 递归审计三种基准舰的派生快照，并将模块能力字段内部残留的 `list/dict` 深度冻结；规范 `to_dict()`、资源 hash 和既有黄金结果不变；
2. 派生设计快照、气动缓存、RCS 缓存、武器时间目录、弹丸目录、制导目录和持续毁伤配置均在编译或载入时一次性生成规范指纹；指纹字段不进入序列化和 dataclass 相等性；
3. 场景初始化始终执行严格快照重算，并在每个绑定中保存已验证 token；公共步进默认保持严格模式，T0 内部构造的不可变场景显式使用 `trusted_prevalidated` 快速模式；
4. 新增未验证绑定、过期 token 和快照内容篡改负例；合法资源 revision 会生成新指纹，篡改但未刷新指纹会在严格边界拒绝；
5. 3 档 × 4 负载的 12 个场景严格/快速单步状态 hash 和事件流 hash 均与 T0b.1a 黄金结果一致；六场景固定步内 `compute_derived_ship_snapshot_sha256` 调用均为 0；
6. 在同一本地机器、同一单步短跑协议下，六场景从 `632.7692–4205.1559 ms` 降为 `12.1109–324.7820 ms`，各案加速为 `12.95×–54.85×`，全部超过 4 倍退出门禁；时序明细仍只保存在忽略目录，不冒充正式 T0 成绩；
7. 新增回归后舰艇测试总入口为 52/52 PASS，TypeScript 为 4/4 PASS，Rust 为 15/15 PASS。正式性能运行仍为 0，`t0_performance_measured = false`。

T0b.1c 只能在本切片建立的不可变静态指纹和已验证绑定上，引入静态战术舰模型与动态 runtime 的显式 revision/dirty-domain 复用。不得使用对象地址作为长期权威缓存键，不得在本子切片并入事件批处理或弹丸优化。

### 17.10 T0b.1c 完成记录与 T0b.1d 开工边界

T0b.1c 已完成静态模型和动态 runtime 的显式复用：

1. 新增值语义 `RuntimeStateRevision`，权威键只由设计/出航来源、船体完整度、模块状态、高度层、燃料可用边界、人员、货物和供电策略组成，不使用对象地址作为长期键；
2. 单舰 runtime 缓存对自动激活事件保留有界变体，主 revision 变化时整组失效；场景初始化为已排队武器预热两种规范唤醒组合，固定步内不再冷启动完整编译；
3. 弹药和武器时钟不改变 runtime 派生值，因此命中时只刷新 `instance_snapshot` 及其指纹；模块毁伤、船体毁伤、人员、供电、燃料可用边界、货物、高度层和资源 revision 会按明示 dirty domain 失效；
4. 公共战术步进的严格模式会在缓存命中时重做来源、舰载、弹药、人员和模块合法性校验；只有 T0 内部预验证路径跳过这些重复检查，无效弹药不能借缓存绕过原合同；
5. 新增 `TacticalShipStaticModel`，船壳结构点、气动/RCS 缓存、环境和动力学调参在绑定期构建一次；固定步只将当前 runtime 绑定到静态模型；
6. 六场景单步诊断中，完整 runtime 编译调用从 `24/32/80/108/120/160` 全部降为 `0`，静态模型构建调用从 `12/12/40/40/60/60` 全部降为 `0`；12 个黄金场景权威结果不变；
7. 同机单步短跑中，`target_20.motion_only` 为 `25.2300 ms`，低于本切片 `50 ms` 退出门禁；六场景范围为 `8.6871–292.4550 ms`，制导弹丸档仍明显超过 60 Hz 步长，留给 T0b.1d 处理；
8. 新增回归后舰艇测试总入口为 53/53 PASS；正式性能运行仍为 0，`t0_performance_measured = false`。

T0b.1d 只能优化到期事件查找、同边界弹丸批量生成/排序、目标几何预编译和活动实体增量计数。不得在本子切片改变事件稳定排序键，不得引入 Rust 热核，不得提前执行 T0b.1e 测量器去扰动。

### 17.11 T0b.1d 完成记录与 T0b.1e 开工边界

T0b.1d 已完成事件和弹丸热路径的确定性收敛：

1. 武器时间线以单次推进内的最小堆维护 `(next_event_time_s, sequence_id)` 到期索引，不再在每个原子事件后重新校验、扫描和排序全部序列；既有稳定排序键、失败语义和事件上限保持不变；
2. 新增同边界 `spawn_projectiles_from_weapon_events`，场景将同一边界的成功开火收集后一次构造、一次合并、一次按弹丸 id 稳定排序并只计算一次源世界 hash；旧单发 API 保留为兼容包装；
3. 舰艇绑定按派生快照 revision 缓存甲板边、局部包围盒和包围半径；弹丸子步复用每个目标的起止位姿，并在精确线段求交前执行保守世界/局部宽相剔除；错误 revision 的几何缓存会显式拒绝；
4. T0 基准观察器新增不进入权威场景合同的增量实体计数器，按生成、命中、过期、开火、殉爆、生命周期和受影响武器序列更新；连续 3 步与全量扫描逐字段一致；
5. 4 枚同边界弹丸的批量结果与逐发兼容路径完全一致，重复 id 负例、跨步几何复用和稳定 id 顺序均通过；六个诊断场景中稳态几何重编译和单发生成调用均为 0，三个制导档都只执行 1 次批量生成；
6. 3 档 × 4 负载的 12 个黄金场景状态、事件、输入和资源 hash 保持一致；本地单步诊断从 T0b.1c 的 `8.6871–292.4550 ms` 收敛到本轮 `9.8077–129.3615 ms`，其中 `target_20.guided_projectiles` 为 `68.5421 ms`；这些数字仍是忽略目录中的诊断结果，不是正式 T0 成绩；
7. 20 舰制导档尚未达到 `16.667 ms`，退出条件按“形成明确剩余热点证据”满足：固定步仍有 2696 次弹丸几何宽相/积分调用，同时存在 runtime 校验 hash 和战术机动积分成本；是否评审 Rust 弹丸热核留到 T0b.1f 决策门，不在本切片越界实施；
8. 新增回归后舰艇测试总入口为 54/54 PASS，TypeScript 为 4/4 PASS，Rust 为 15/15 PASS。正式性能运行仍为 0，`t0_performance_measured = false`。

T0b.1e 只能调整基准测量和结果物化边界：事件摘要直接读取事件字段，环境元数据单次采集，内存低频采样，并分别报告权威推进、观察器开销和包含 drain 的实时因子。不得继续改变时间线、弹丸、runtime 或机动权威算法，不得执行 T0b.1f 的短程决策跑。

### 17.12 T0b.1e 完成记录与 T0b.1f 开工边界

T0b.1e 已完成测量器与结果物化边界收敛：

1. 滚动事件摘要直接遍历 `TacticalSceneStepResolution` 的权威事件元组，并保持原合同中六类基础事件始终输出、其他事件仅在非空时输出的规则；不再先调用整份 `resolution.to_dict()`，因此不会为事件摘要额外物化最终场景、舰艇结果和无关运行时 hash；
2. 3 档 × 4 负载的 12 个单步场景中，直接事件载荷、旧完整物化路径和冻结黄金事件 hash 三方完全一致；事件摘要包装负例会在任何越界调用 `resolution.to_dict()` 时立即失败；
3. 无界面结果接口升级为 `gaotian.t0-headless-baseline/v2`，`fixed_step` 只包围 `advance_scenario_step`；逐步事件摘要、增量实体计数、事件总量和内存采样进入独立 `observer_drain` 指标；
4. 运行结果同时报告 `authoritative_advance_wall_s`、`observer_drain_wall_s` 和总 `measured_wall_s`，三者满足严格分解；实时因子明确使用包含观察 drain、末态 hash 和收尾统计的总墙钟；
5. 环境元数据在 CLI 运行边界采集一次并注入运行器；直接调用运行器时也只采集一次。已提供元数据时禁止重复采集，命令不匹配会显式拒绝；
6. 驻留内存默认从逐步采样降为每 60 个固定步一次，并在测量末步补采；正式 3600 步窗口由 3600 次查询降为 60 次。采样间隔与有效样本数同时进入执行结果和正式 `runMetadata`；
7. 结果 Schema 新增测量策略、实时因子范围、内存间隔/样本数和 `observer_drain`；续跑只接受 `v2` 且输入、资源、初态、步数、策略、采样、指标和墙钟分解全部匹配的完整结果，旧 `v1`、缺失观察指标和错误墙钟分解均被拒绝；
8. 六场景本地诊断中，事件摘要由 T0b.1d 的 `1.1409–17.5870 ms` 降为 `0.0158–0.8719 ms`；这些仍是忽略目录中的观察器诊断，不是正式 T0 成绩。新增回归后舰艇测试总入口为 55/55 PASS，TypeScript 为 4/4 PASS，Rust 为 15/15 PASS；正式性能运行仍为 0，`t0_performance_measured = false`。

T0b.1f 只执行计划规定的 10 步预热、60 步测量、三次重复短程诊断，比较调用数、固定步 P95/最大值、观察 drain、包含 drain 的实时因子、内存和权威 hash，并按 17.5 选择唯一后续分支。不得把短程诊断冒充正式 600+3600 步 T0 结果，也不得在取得决策数据前继续修改权威算法。

### 17.13 T0b.1f 完成记录与 T0b.2 开工边界

T0b.1f 已完成短程诊断决策门：

1. 新增 `gaotian.t0b1f-short-diagnostic-decision/v1` 结果合同、JSON Schema、CLI `decide-short-diagnostic` 与 PowerShell `DecideShortDiagnostic`；协议严格冻结为六场景、10 步预热、60 步测量、三次重复，并始终声明 `official_performance_runs_executed = 0`、`t0_performance_measured = false`；
2. 六场景共 18 次短跑复用同一份机器元数据；结果严格校验六案矩阵、1/2/3 重复顺序、机器元数据 hash、运行总数和未知字段。三次重复的初态、输入流、最终权威状态和事件流 hash 在六案中全部一致；
3. 首次跨 60 步执行暴露了基准输入生成缺口：位于固定步末边界的新导弹尚未进入本步弹丸推进，却被提前加入本步制导输入。现已只为当前边界生成物加入当步输入，末边界生成物从下一步开始接收制导输入，并新增跨第 59→60 步回归；权威场景算法和事件排序未改；
4. 历史单步中每案 `12–60` 次静态战术模型构建和 `24–160` 次完整 runtime 编译，在当前六案剖析中均保持为 0；剩余运动热点位于 Python 权威场景调度、runtime 缓存解析、战术机动积分、载荷计算和 dataclass 对象替换；
5. 本机短跑最差重复中，`target_20.motion_only` 固定步 P95/最大值为 `35.5546/35.8759 ms`，三次实时因子为 `0.556494/0.543660/0.551840`；`target_20.guided_projectiles` 为 `67.5758/74.3283 ms`，三次实时因子为 `0.298540/0.279388/0.280402`。两案均未达到实时因子 1.0；
6. 六案观察 drain P95 为 `0.0365–0.6038 ms`，驻留内存最大值为 `41,807,872–49,016,832 bytes`；三次重复最大内存增量 `1,433,600 bytes`，低于短门 `64 MiB` 容差，固定步最大值均有限，无界面模式不存在传输队列，因此短程资源有界性为 6/6 通过；这不替代正式长跑中的内存和队列门禁；
7. 决策门唯一选择 17.5 分支 3：20 舰纯运动本身仍未实时，不能只把问题归因于弹丸热核，也不能直接恢复 T0c。下一阶段改为 `T0b.2` 推进响应与权威性能优化；先建立 60 Hz 等价基线和推进/安全 RFC，再对“舰艇/损管低频、弹丸高频、表现插值”与继续迁移热核做数据化比较；
8. 本轮实际执行的仍只是 18 次短程诊断，正式 `600+3600` 步 T0 运行数保持为 0。不得引用这些本机数字宣称正式 T0 通过。
9. 新增回归后舰艇测试总入口为 56/56 PASS，TypeScript 为 4/4 PASS，Rust 为 15/15 PASS；Rust 仅保留 F: 文件系统不支持增量缓存硬链接而回退复制的既有警告。

`T0b.2a` 只能形成推进响应、安全保护、多速率权威时序、采样/保持、事件边界、插值、确定性复现和等价门禁的 RFC 与验证计划，并用 T0b.1f 的运动/制导数据同弹丸热核方案比较。不得在 RFC 评审前开始 `T0b.2b`、修改 60 Hz 权威算法、实施多速率调度、引入 Rust 热核或恢复 T0c；评审通过后的实施边界以 17.14 的细分切片为准。

### 17.14 T0b.2 推进响应与其他优化规划入口

T0b.2 的详细 RFC 与切片编排已经保存为[《高天荒野》T0b.2 推进响应与权威性能优化规划](./高天荒野T0b2推进响应与权威性能优化规划.md)，状态为 `implementation_in_progress / T0b.2d1p_complete_preintegration_replan / next_T0b.2d1r`。该规划新增以下约束：

1. 既有发动机 `response_time_s` 保留为就绪后 0→100% 的名义响应时间，冷启动另设 `startup_time_s`；
2. 玩家车钟采用停车、微速 2%、四分之一、二分之一、四分之三、全速六个位置，权威实际推力沿 `0、2、5、10……100%` 阶段响应；
3. 推进响应是会改变运动、燃料、事件和 hash 的权威规则扩展，必须与保持现有 hash 的 P0 基线优化分离提交；
4. 在评审多速率或 Rust 前，先建立修正后的 60 Hz Python 基线，处理固定 65 候选推力搜索、runtime 命中全量 hash、边界重复解析、无到期时间线和生命周期重复派生；
5. 后续按推进合同/迁移、推进求解器、Python 弹丸与场景批处理、事件驱动/多速率对比、批量 Rust 热核评审、唯一决策门的依赖顺序推进；
6. `T0b.2-pre` 已确认 `290d044` 是 T0a—T0b.1f 的既有汇总提交且与 `origin/main` 一致；不重写已推送历史，并从此恢复一切片一提交；
7. 安全保护冻结为保留原车钟的离散软限幅/降档：限制解除后逐阶段恢复原命令；燃料、供电、人员、损毁与跳闸属于独立硬可用性边界，`overg` 只能绕过软保护；
8. 实施进一步拆为 P0 等价快速路径、runtime/边界优化、未接线纯安全判定器、推进资源合同、推进场景状态/迁移、启动/响应接线、安全保护接线、硬故障接线及后续批处理/架构比较，禁止把资源、场景状态和力学接线混入同一提交。

`T0b.2a` 已进一步冻结模块模式与发动机 phase 映射、首轮能耗/硬故障、独立反向发动机互锁、高频场景状态所有权、显式迁移、事件驱动/多速率边界和 Rust 批次确定性要求。其退出门只允许先实施 `T0b.2b1` 现有连续安全搜索快速路径；该切片现已完成，期间没有提前接入离散车钟或推进状态，也没有把规则变化伪装成权威等价优化。

### 17.15 T0b.2b1 完成记录

`T0b.2b1` 已增加请求比例安全即返回的快速路径，不安全时仍复用原 65 样本与 50 次二分算法。12 个黄金场景与六案三重复 hash 保持一致，`target_20.motion_only` 的 `_load_metrics` 从 1300 次/步降为 20 次/步；短程最差 P95 从 `35.5546 ms` 降到 `27.3490 ms`，最小实时因子从 `0.543660` 升到 `0.799687`。完整回归为 Python 57/57、TypeScript 4/4、Rust 15/15。

该结果仍是 10+60 步短程诊断，正式性能运行保持为 0；目标规模纯运动尚未实时。下一子切片唯一为 `T0b.2b2` runtime 稳定核心，不得提前接入推进响应或恢复 T0c。

### 17.16 T0b.2b2 完成记录

`T0b.2b2` 已把 runtime 拆为 revision/事件决定的稳定核心与精确实例轻量视图。每个事件变体有界保留最近 4 个视图；同值实例和确定性回放直接复用，透传实例变化只在边界生成一次实例指纹和新视图，8 个既有 dirty 域仍清空整组缓存。严格模式继续在复用前校验来源、弹药、人员和模块合同。

`target_20.motion_only` 单步剖析的规范 hash 调用由 222 次降为 62 次，runtime resolve 仍为 80 次、完整 runtime 编译仍为 0；12 个黄金结果不变。最终工作树的六案三重复短程诊断中，目标规模纯运动最差 P95/最大值为 `17.6037/18.0185 ms`，三次实时因子为 `0.990861 / 0.996894 / 0.985127`；相较 b1 已明显改善但没有稳定通过。目标规模制导为 `57.2726/66.3568 ms`，仍明显失败。同一实现过程中的较早本地运行曾达到约 `1.44×`，最终复跑未复现，因此不引用较快结果宣称通过。完整回归为 Python 58/58、TypeScript 4/4、Rust 15/15；正式性能运行仍为 0。

旧 T0b.1f 分支判定器在最终复跑中建议多速率 RFC；该输出只作为热点证据，不覆盖 T0b.2 已冻结依赖。原定宽范围 `T0b.2b3` 经风险审计后拆为精确边界上下文、时间线安全快进、生命周期同一步复用和可选 scratch 四个独立检查点；跨步骤 dirty 与持久到期索引留到 `T0b.2e2`。编辑器、T0c、推进响应、多速率与 Rust 热核继续暂停。

### 17.17 T0b.2b3a 精确边界上下文完成记录

`T0b.2b3a` 已新增只在单次场景固定步内生存的冻结 `ShipStepContext`。上下文只有在舰艇 id、边界、精确实例值、实例代次和规范自动事件集合全部匹配时才复用；stable core 相同不能单独授权复用携带旧实例快照的战术模型。步首生命周期、观测和机动现在共享精确 runtime，模型仅在运动积分时按需绑定，结果物化不再执行第二次无用途绑定。

武器技术夹具确认：4 艘舰在步首开火改变弹药和时间线后，运动提交仍基于变化后的精确实例，最终 runtime 与场景实例逐舰相等。`target_20.motion_only` 的 runtime cache resolve 由 80 次降到 60 次，战术模型绑定由 40 次降到 20 次；完整 runtime 编译和视图重绑定仍为 0，生命周期和时间线调用仍各为 40 次，未混入后续优化。独立三次 10+60 步复跑的最差 P95/最大值为 `11.5236/11.7713 ms`，最小实时因子为 `1.529980`；12/12 权威黄金及 Python 59/59、TypeScript 4/4、Rust 15/15 回归通过。该结果仍是短程诊断，正式性能运行保持为 0。

后续依次进入 b3b 时间线安全快进和 b3c 生命周期同一步复用；跨步 dirty、推进响应和多速率继续暂停。

### 17.18 T0b.2b3b 武器时间线安全快进完成记录

`T0b.2b3b` 已引入绑定精确源时间线的步内 `WeaponTimelineAdvancePlan`，将推进分为 `same_time_noop / clock_only / full_advance`。同刻无到期事件返回原实例；时间前进但无到期事件仍推进权威时钟；任何位于 `target + EPS` 内的事件，包括最终失败的动作，都继续进入完整事件引擎。计划不写入存档，时间线发生 enqueue、cancel、reload 或暂停后，旧计划会被明确拒绝。

`target_20.motion_only` 的完整时间线推进由 40 次降到 0，规范哈希由 62 次降到 22 次；runtime resolve、模型绑定和生命周期分别保持 60、20、40，证明没有混入 b3c。实际 4 舰步首开火仍生成 4 个事件和 4 枚弹丸。三次 10+60 步复跑最差 P95/最大值为 `11.5092/12.3333 ms`，最小实时因子为 `1.627203`。12/12 黄金及 Python 60/60、TypeScript 4/4、Rust 15/15 回归通过；正式性能运行仍为 0。

下一子切片唯一为 `T0b.2b3c` 生命周期同一步投影复用。步首仍完整派生，步末只允许使用与正式生命周期函数共享的语义投影判定不变；跨步骤 dirty 缓存继续留到 `T0b.2e2`。

### 17.19 T0b.2b3c 生命周期同一步投影复用完成记录

`T0b.2b3c` 已将生命周期判定拆为唯一语义投影与状态物化。步首继续走完整派生；步末仍读取当前精确 runtime 并执行同一正式投影，只在物理状态、指挥状态、失败原因及退出字段全部相等时复用现有状态。投影变化仍在该固定步物化转移、生成事件并暂停坠落舰武器队列。

该实现没有引入跨步 dirty 缓存，也没有以“没有收到变化通知”代替依赖检查。专项测试覆盖 6 类投影/派生等价状态、20 舰无变化复用及实际弹丸摧毁 CIC 的步末负例。`target_20.motion_only` 的正式投影仍为 40 次，但状态物化只有步首 20 次，完整生命周期派生入口由 40 次降到 20 次；runtime resolve、规范哈希和完整时间线调用分别维持不高于 60、22、0。三次 10+60 步短跑最差 P95/最大值为 `10.7528/13.1486 ms`，最小实时因子为 `1.689098`；12/12 黄金及 Python 61/61、TypeScript 4/4、Rust 15/15 回归通过，正式性能运行仍为 0。

下一子切片唯一为 `T0b.2b3d` 画像与可选 scratch。先复核 b3a—b3c 的累计调用数和热点；只有证据表明确有收益时，才引入不序列化、不跨步逃逸的临时容器，否则明确取消 scratch。推进响应、跨步事件驱动、编辑器和 T0c 继续暂停。

### 17.20 T0b.2b3d 画像决策与 P0 关闭记录

P0 从 T0b.1f 到 b3c 已把 `target_20.motion_only` 最小短跑实时因子从 `0.543660` 提高到 `1.689098`，约为原来的 `3.107×`；最差 P95 从 `35.5546 ms` 降至 `10.7528 ms`。当前单步仍只有 20 次载荷求值、60 次 runtime resolve、22 次规范 hash、0 次完整无到期时间线和 20 次完整生命周期派生。剩余场景成本分散，没有形成值得引入新所有权与清理协议的集中临时容器热点，因此 b3d 依停止门取消场景级 scratch，P0 等价优化关闭。

制导档三次 10+60 步实时因子为 `0.527043 / 0.414480 / 0.409571`，最差 P95/最大值为 `44.1455/51.6384 ms`，仍明显不实时。单步画像中弹丸世界约占累计时间 `70.50%`，包含 2696 次几何求交、296 次制导推进和 3922 次对象替换。稳定弹丸容器、配置预绑定、平面批次和单步 scratch 因而继续归入 `T0b.2e1`；但推进响应会改变运动、燃料、命中输入与新黄金，所以仍先完成 `c1 → c2a → c2b → d1 → d1r → d2a → d2b → d3 → d4`，在新权威基线稳定后优先实施 Python 弹丸批次，再评审 Rust。

下一子切片唯一为 `T0b.2c1` 纯推进安全判定器。它只新增不接线的离散车钟/5% 阶段、软硬限制、滞回、有效目标与事件意图纯函数；现有场景调用数必须为 0，旧黄金不得变化。稳态只求值当前阶段和必要的相邻升阶，当前阶段失安时才有界向下搜索；`overg` 只能绕过结构/乘员软限制，不能提高燃料、供电、人员、损毁或跳闸形成的硬上限。资源合同与场景状态/迁移分别留给 c2a、c2b，场景接线留给 d1—d4。

### 17.21 T0b.2c1 纯推进安全判定器完成记录

`T0b.2c1` 已新增完全未接线的推进安全规则内核。六档车钟映射为 `0/2/25/50/75/100%`，实际输出域为 `0、2、5……100%` 共 22 阶；整舰载荷由调用方按需回调，判定器保持原车钟，只产生离散安全上限、相邻许可阶段和稳定事件意图。安全稳态求值 1 次，普通升阶求值 2 次，只有当前阶段失安才有界降序寻找最高安全阶段，避免把连续安全搜索成本重新带回正常路径。

结构和乘员属于带滞回与连续步释放的软限制；`overg` 或关闭乘员锁只影响相应软原因。燃料、供电、人员可用性、执行器/宿主损毁、跳闸和方向互锁作为显式硬上限，始终参与有效目标。专项测试覆盖 484 个有序阶段对、动态降档、无安全阶段、漏步重置、多原因顺序、5 个非法输入和 3 次确定性重放；现有场景调用数为 0，12/12 黄金保持一致。完整回归为 Python 63/63、TypeScript 4/4、Rust 15/15，正式性能运行仍为 0。

原计划 `c2` 经边界复核拆为 `c2a → c2b`。下一子切片唯一为 `T0b.2c2a`：先建立版本化 `PropulsionSafetyProfile` 资源与推进 capability v2 的显式 `startup_time_s`，只允许已知 v1 模块目录通过具名表迁移；旧 v1 场景继续可用且黄金不变。`EngineRuntimeState`、governor 持久状态、场景引用、推进事件和旧场景迁移全部留到 `c2b`，时间推进与力学接线继续留到 d1/d2。

### 17.22 T0b.2c2a 推进资源合同完成记录

`T0b.2c2a` 已新增严格、版本化且不接线的推进安全配置资源，以及独立的模块目录 v2 Schema。推进 capability v2 必须显式声明 `startup_time_s`：主发动机大于 0，首轮姿态推进器等于 0；既有响应时间仍必须为正。旧 v1 Schema、三份 v1 目录、舾装、实例与场景均未改写，新资源尚未被战术场景引用。

三份已知模块目录只能在 id、版本和内容指纹精确匹配时通过具名 v1→v2 迁移；4 个推进模块升为资源版本 2，非推进模块保持逐字段相同。未知目录和被篡改目录均拒绝。推进配置规范往返/指纹、严格负例、三次迁移重放、12/12 旧黄金及 Python 64/64、TypeScript 4/4、Rust 15/15 回归通过；正式性能运行仍为 0。

下一子切片唯一为 `T0b.2c2b`：只定义 `EngineRuntimeState`、`PropulsionGovernorState`、`TacticalPropulsionState`、场景资源引用、推进事件合同和具名旧场景迁移。不得在该切片推进发动机时钟、改变推力/燃料/运动或接入 c1 安全判定器；这些仍按 d1—d4 独立实施。编辑器、T0c、多速率和 Rust 热核继续暂停。

### 17.23 T0b.2c2b 推进场景状态与迁移完成记录

`T0b.2c2b` 已建立执行器、四方向 governor、逐舰聚合推进状态和推进事件的严格合同。旧场景继续使用原 v1 interface/Schema 与原哈希；只有具名迁移后的场景才使用 v2 interface，并强制保存推进安全配置精确引用/指纹和每艘舰的完整推进状态，缺字段、错排序、错引用、非法阶段及部分迁移均拒绝。

12 个旧 T0 初始场景均以场景名和旧内容指纹锁定迁移，三次重放确定一致；当前矩阵共迁移 1224 个执行器状态和 896 个 governor 状态。独立用例冻结 `active → ready + 0%`、`standby/off → off + 0%`，未知、篡改及错误配置迁移拒绝。新 v2 场景在 d1/d2 接线前不能进入旧推进器，实际权威推进为 0 步；12/12 旧黄金及 Python 65/65、TypeScript 4/4、Rust 15/15 回归通过，正式性能运行仍为 0。

下一子切片唯一为 `T0b.2d1` 独立推进时间内核：只实现冷启动、相邻 5% 阶段响应、中途改令、停车和确定性事件，不接入场景力学、油耗、安全 governor、硬故障或方向互锁。编辑器、T0c、多速率和 Rust 热核继续暂停。

### 17.24 T0b.2d1 独立推进时间内核完成记录

`T0b.2d1` 已建立无场景副作用的 60 Hz 推进时间内核与严格边界结果合同。主发动机冷启动、姿态推进器零启动、相邻离散阶段、同边界先提交到期阶段再处理中途改令、逐阶段停车及稳定事件均由单执行器纯函数完成；响应状态显式保存命令边界和已提交输出起点，不使用墙钟或逐步浮点累加。

1 秒启动和 1 秒响应用例分别在第 60、120 步完成，0→100% 与 100→0% 均完整穿过 21 个相邻阶段；混合全速、四分之一与停车命令连续三次重放指纹一致。无法让全部相邻阶段占用独立边界的旧 `0.5/0.2` 秒兼容能力会被显式拒绝，不跳级或更改旧字段语义。统一战术场景仍未导入时间内核，新 v2 场景继续由 `propulsion_unwired` 门禁拒绝；12/12 旧黄金及 Python 66/66、TypeScript 4/4、Rust 15/15 回归通过，正式性能运行仍为 0。

原定下一子切片为 `T0b.2d2` 推进场景接线；d1 后接线前复盘发现能力可排程性、资源绑定、控制输入和合同版本仍有前置缺口，因此该入口已由下一记录正式取代。

### 17.25 T0b.2d1p 接线前复盘与计划重排记录

复盘覆盖 12 个 T0 场景、224 艘舰和 1224 个执行器。280 个 `1.0s` 主发动机可通过 d1 排程门禁；48 个 `0.5s` 无人主发动机和 896 个 `0.2s` 姿态推进器不可排程，共 944 个、占 `77.124183%`。每艘舰都有 4 个不可排程姿态推进器，因此原 d2 对 224/224 艘舰都不能直接接线。现有 3 类场景模块目录仍是 v1，其中 2 类动态组合目录没有 c2a 直接具名迁移；推进原型引用仍是 version 1，也没有落盘的 v2 推进目录或已迁移舾装方案。

控制侧仍使用连续 `TacticalControlInput.move_body/wheel`，基准固定为 `move_body.y=0.1`、`abs(wheel)=0.05`；d1 主机入口则只接受六档车钟。状态侧在原 alpha id 下新增了两个必需排程锚点，同 id 已出现严格形状变化。场景结果侧仍输出旧 scene interface、没有推进事件集合。这些事实说明原 d2 同时承担版本修复、目录/舾装迁移、控制语义、逐执行器绑定、场景接线、力学和新黄金，超出一个可独立审计切片的合理边界。

后续正式重排为：

- `d1r`：只升级 engine/tactical propulsion/scene alpha interface，并提供 c2b→d1 具名状态迁移；
- `d2a`：建立可排程 capability 新版本、三类场景目录与舾装引用迁移、1224 个执行器精确绑定、离散控制合同和新场景结果/推进事件合同；
- `d2b`：解除未接线门禁，按实际阶段接入力、力矩与油耗并建立解释性新黄金；
- `d3`：接入离散软安全 governor；
- `d4`：接入硬跳闸、复位、紧急断推和方向互锁。

本检查点未修改推进生产代码、旧场景运动、燃料或黄金；三套回归继续保持 Python 66/66、TypeScript 4/4、Rust 15/15，正式性能运行仍为 0。证据保存于 `舰艇数据/报告/阶段T0b2d1p接线前复盘与计划重排接口.v1.json`。

下一子切片唯一为 `T0b.2d1r` 推进状态合同修复。资源/控制桥、场景力学、安全 governor、硬故障、弹丸批处理、编辑器和 T0c 均不得提前混入。

### 17.26 T0b.2d1r 推进状态合同修复完成记录

d1 的两个响应排程锚点现已从 c2b 历史 interface 中分离。c2b 的 engine/tactical propulsion/scene 合同恢复原 `v1alpha1/v1alpha1/v2alpha1` 严格形状；d1 使用新的 `v2alpha1/v2alpha1/v3alpha1`。新旧状态均可严格往返，但额外字段、缺字段、父子 interface 混用和未知 interface 会被拒绝；d1 时间内核不再接受未迁移的 c2b 状态。

12 个具名 c2b 推进场景以原目标指纹锁定迁移，三次重放一致，共升级 1224 个执行器状态并保留 896 个 governor 状态。当前初态均可确定迁移；缺少排程锚点的历史 `running/stopping` 状态则明确拒绝，避免猜测响应起点。场景推进门禁、运动、燃料、黄金和正式性能运行均未改变；Python 67/67、TypeScript 4/4、Rust 15/15 回归通过。

下一子切片唯一为 `T0b.2d2a` 推进资源与控制桥。它只处理可排程 capability、3 类目录/舾装引用、1224 个执行器绑定、离散控制和新步结果/推进事件合同；力学、安全 governor 与硬故障继续留在 d2b—d4。

### 17.27 T0b.2d2a 推进资源与控制桥完成记录

模块目录新增 v3 严格合同，以 60 Hz 下全部相邻输出阶段必须占用独立边界为可排程门禁。三类场景目录先复现 c2a v2 指纹再做具名 v2→v3 迁移，技术替身响应统一为 `1.0s`；三份舾装只升级推进原型引用。c2a v2、旧 v1 目录、旧舾装和旧场景均未改写，未知或篡改来源不能迁移。

12 个 T0 场景的 1224 个执行器已全部绑定到精确 v3 原型、目录引用/指纹和配平命令通道。328 个主发动机、896 个姿态推进器均得到 60 步响应排程；主机与姿态推进器的启动步分别为 60 和 0。旧连续控制共 224 份，只能通过两条具名迁移进入新四方向离散合同；自动制动固定选择反向 `quarter / 25%`，姿态推进和车钟量化规则均已版本化。

新场景单步结果接口独立保存带舰艇身份的推进事件，并按固定步、舰艇、执行器、事件类型稳定排序。当前切片只构造该结果合同，没有调用场景推进器；`propulsion_unwired`、旧运动、燃料、力学和黄金保持不变，正式性能运行仍为 0。证据保存于 `舰艇数据/报告/阶段T0b2d2a推进资源与控制桥接口.v1.json`。

下一子切片唯一为 `T0b.2d2b` 推进力学接线。它负责解除未接线门禁、把离散命令送入时间状态并按实际阶段聚合力/力矩/油耗、贯穿推进状态与事件以及建立新版本黄金；d3 软安全 governor 与 d4 硬故障/方向互锁仍须后置。

### 17.28 T0b.2d2b.1 定向控制与边界合同实施记录

d2b 已细化为四个独立验收切片，本轮仅完成合同准备。控制/绑定 v2 将主机平移方向与机动喷口转向分开；governor v2、推进状态 v3、场景 v4 同步消除通道歧义，engine 时间状态仍为 d1 v2。自动线性制动不再请求转向喷口；无反向/侧向主机时返回缺失能力诊断。已知旧控制与初始状态通过具名迁移进入新合同，不自动接纳历史自动制动或运行中歧义状态。

结果 v3 与场景事件 v2 显式记录 n/n+1 和 opening/closing，校验源/结果指纹、执行器身份、事件次序及变化链。281 步独立时间测试三次一致，实际输出在 closing 升阶后只用于下一积分区间；控制桥和场景之间的潜在循环依赖已解除。

224 控制、1224 绑定、12 场景合同往返及旧 12 黄金通过；本轮不改变运动、燃料和力学，不解除 `propulsion_unwired`，不运行正式性能矩阵。详细回归与版本记录见 `舰艇数据/报告/阶段T0b2d2b1定向控制与边界合同接口.v1.json`。下一步 `T0b.2d2b.2` 重建目录→舾装→快照→出航→实例→场景的完整链；d3/d4 及编辑器后续线继续后置。

### 17.29 T0b.2d2b.2—d3.2 汇总与 d3.3 开工边界

d2b.2—d2b.4 已完成完整资源链、逐执行器实际力学和显式 v5 未保护场景，d3.1/d3.2 已完成可否决时间边界与纯整舰向量 governor；最新实现检查点为已推送的 `def414c`。现有两套黄金、v5 场景和 v4 单步结果继续保持未保护语义，正常玩法与编辑器没有切换到新路径。

d3.3 接线前复核确定不能原地改写 v5：新路径须使用场景 v6、单步结果 v5、受保护存档和诊断新版本。`S_n` 仍是已提交状态；开边界只接令和按既有上限排程，闭边界 n+1 在命中/毁伤等结算后唯一求值 governor 并提交下一状态。安全事件单独按舰艇和命令通道发布，不伪造执行器事件；已退出舰冻结，生命周期抑制交付时安全采样使用同一抑制口径。

实施顺序为合同先行、无场景适配器、统一场景与存档、独立新黄金和全回归。d3.3 只使用显式中性硬上限，硬跳闸/复位/方向互锁继续留给 d4；正式性能、弹丸优化、编辑器与 T0c 均不提前。详细冻结决策见[《T0b.2d3 软安全接线实施细则》](./高天荒野T0b2d3软安全接线实施细则.md#六d33-接线前复盘与冻结决策)。

### 17.30 T0b.2d3.3 第一阶段合同与旧版门禁完成记录

受控路径已新增场景 v6、单步结果 v5、存档 v1、诊断 v2、治理标记、开/收边界记录和通道安全事件八个严格 schema。开边界审计保留源/结果控制、六通道 governor 命令和全部逐执行器 d3.1 结果；收边界审计绑定最终 runtime/运动及完整 d3.2 结果，场景安全事件不伪造执行器身份。诊断 v2 明确 soft governor 已接线，同时继续声明硬故障与方向互锁未接线。

统一场景现在能严格识别 v6，但在无场景适配器和完整接线完成前会显式拒绝推进，不能退回 v5 无保护路径。v5 多带治理字段、v6 缺少治理标记、v4/v5 结果混用、非退出舰 governor 时钟不一致及 tripped 存档均被拒绝。旧统一场景仍不隐式导入 d3.2；旧 v5 初态、v4 单步及两套黄金保持原语义。

下一步只构建不依赖统一场景的开边界命令/收边界安全适配器，覆盖自动制动、无绑定通道、到期升阶否决、生命周期抑制采样和逐舰精确重放。适配器独立验收前，v6 场景执行门禁保持关闭，Web/编辑器也不切换到受控路径。

### 17.31 T0b.2d3.3 第二阶段无场景适配器完成记录

已新增不依赖统一场景的受控推进适配器。它在边界 0 用真实资源和载荷建立 governor 时钟；开边界只接令和重排 d3.1 目标，不改变实际输出、限幅或滞回历史；区间积分使用已保护实际向量；收边界在最终 runtime/运动上唯一执行 d3.2。12 个具名场景 224 艘舰、1224 个执行器和 1344 个 governor 全部完成无事件、无限幅初始化。

无绑定通道命令仍可审计，首次 2% 升阶严格由到期收边界提交。高推力夹具的介入和 overg 释放分别只产生一个通道事件，开边界不会提前释放。falling/uncommanded 会同时抑制积分交付与安全采样中的推进贡献，但保留阻力和原候选向量；exited 冻结、tripped、过期时钟、到期未提交转换以及记录指纹篡改均被拒绝。

专项证据见 `舰艇数据/报告/阶段T0b2d3d无场景受控推进适配器接口.v1.json`。下一步将由具名 v6 场景构建器和统一场景消费该适配器，并补受控存档连续/重载重放；独立新黄金、正常玩法、Web/编辑器切换和正式性能矩阵继续后置。

### 17.32 T0b.2d3.3 第三阶段统一场景与受控存档完成记录

十二个指纹锁定 T0 初态现在可以通过独立构建入口建立 v6；224 艘舰、1224 个执行器和 1344 个 governor 均在边界 0 用真实载荷完成初始化。统一场景已接入 opening 命令提交、受保护实际向量积分和 closing 整舰安全判定；closing 位于命中、毁伤、人员与生命周期处理之后，每个活动舰每个边界只求值一次。新 v5 单步结果把开/收记录、执行器事件与舰艇/通道安全事件分开，诊断 v2 保留源受控状态血缘。

专项验证覆盖首次命令、联合 2% 候选否决、乘员锁、overg 释放及其额外排程延迟、falling 零推进交付、exited 冻结和命中后最终 runtime。受控存档在限幅和释放候选状态均证明连续与重载续跑逐步一致，加载时重建完整资源和 runtime/模型缓存。旧 v5/v4 路径与冷导入隔离保持不变。

证据见 `舰艇数据/报告/阶段T0b2d3e统一场景受控推进与存档接口.v1.json`。下一步只生成独立受控黄金并执行十二场景三次重放、长序列/重载矩阵及全回归；Web/编辑器、正常玩法、d4 硬故障/方向互锁和正式性能矩阵继续后置。

### 17.33 T0b.2d3.3 第四阶段与 d3.3 退出门完成记录

独立受控黄金已经保存，十二个具名 v6 场景分别执行 12 步、三次重放；每次覆盖 2688 个舰步和 2688 次 closing 安全记录。逐步场景与结果轨迹完全一致，默认安全配置没有产生虚假安全介入，运动、燃料和原生推进事件保持与 v5 相同；v6 治理状态、受控审计、诊断版本和结果接口造成的序列化差异已独立记录。

331 步连续序列及第 121、200 边界重载续跑逐步一致，限幅释放、离散升降档、改令、停车、燃料和事件均保持确定性。旧权威与实际推进黄金逐字未变，完整证据见 `舰艇数据/报告/阶段T0b2d3f受控黄金与完整矩阵接口.v1.json`。d3.3 已关闭；正式性能运行仍为 0，正常玩法、Web/编辑器和生产默认路径没有切换。

下一阶段只准备 `T0b.2d4`：先复核并冻结硬可用性、跳闸/复位、紧急断推和方向切换互锁的合同与边界，再分片实施。e1 弹丸批处理、T0c 传输和 W2 编辑会话继续等待 d4 新权威基线稳定。

### 17.34 T0b.2d4.1—d4.6 实施与接续记录

d4.1—d4.5 的硬故障、显式复位、紧急断推和方向互锁已组合为完整无场景推进链，检查点为 `5c25564`。本轮 d4.6 以独立场景 v7、单步 v6、存档/治理 v2 和诊断 v3 承载该链的状态与审计证据；逐舰一次性硬命令绑定精确源场景和固定步，事件保留硬故障→时间→软安全的因果次序。

存档只接受已提交边界，合法跳闸状态可完整往返，旧 d3 不会隐式接纳新版本。具名 d3→d4 迁移先验证原始资源及指纹，对待复位、未完成响应和对向切换歧义拒绝迁移。详细验收见 d4f 阶段报告；合同矩阵不代替 d4 场景连续重放。

下一步为 d4.7 的具名构建、资源重建、统一场景接线及存档续跑验证；d4.8 才关闭独立黄金、长序列和全回归退出门。实施边界以[《T0b.2d4 完整安全场景接线实施细则》](./高天荒野T0b2d4完整安全场景接线实施细则.md)为准。

### 17.35 T0b.2d4.7 统一场景与存档续跑完成记录

完整 d4 安全链已通过具名 v7 入口接入统一场景：一次性硬命令、方向互锁、实际推力积分和最终收边界安全判定均形成 v6 单步记录。v2 存档加载重建精确资源及全新缓存，跳闸、复位、紧急断推和反向切换中的重载不会重复消费命令或改变逐步结果。

十二个命名场景共 224 艘舰完成构建、首步物理重算及保存续跑，52 步反向序列覆盖三个重载边界。真实命中后的最终 runtime 与区间末损毁探针确认：已经交付的推力/燃耗保留，最终安全采样按受损生命周期处理，新硬故障在下一 opening 生效。证据见 d4g 阶段报告。

下一步 d4.8 建立独立黄金、较长组合序列和全回归退出证据。三套既有黄金未修改，正常玩法及 Web/编辑器默认入口尚未切换，正式性能运行仍为 0。

### 17.36 T0b.2d4.8 黄金与完整验收记录

完整安全场景新增独立 d4 黄金，冻结十二个具名 v7 场景各 12 步与 401 步故障/复位/紧急断推/反向切换序列；三次重放、两组存档点和 d3 迁移后继续推进共同构成退出证据。三套既有黄金继续保留，默认场景运动、燃料及原生事件须与 d3 基线一致。

完整回归入口包含 86 项，验收结果以 d4h 报告为准；通过该门后 d4.1—d4.8 收口，主线继续 `T0b.2e1` Python 弹丸批处理。正常玩法、Web/编辑器默认入口没有切换，正式性能运行仍为 0。编辑器接线须继续遵循其自身的后续实施门禁。
