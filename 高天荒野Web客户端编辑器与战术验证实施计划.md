# 《高天荒野》Web 客户端、舰艇编辑器与战术验证实施计划

> 建立日期：2026 年 8 月 27 日
> 当前基线：`T0b.2b1` 完成状态（基于 `27b3d9d`，计划、实现与报告同提交）
> 当前回归：舰艇测试总入口 `57/57 PASS`、TypeScript `4/4 PASS`、Rust `15/15 PASS`（2026-08-31）
> 更新日期：2026 年 8 月 31 日
> 当前状态：`T0b.2b1` 安全搜索快速路径已完成；下一子切片为 `T0b.2b2` runtime 稳定核心；其他开发线继续暂停

## 一、目标与顺序修正

本计划把舰艇规则内核转化为可实际使用和复核的产品工具，并冻结以下实施顺序：

`Web 共用平台 → 船壳编辑器 → 舾装编辑器 → 测试战术场景 → 正式教程舰设计 → 联合标定 → 阶段 I 正式化门禁`

正式教程舰不得在可用船壳编辑器、可用舾装编辑器和测试战术场景之前完成数值标定。阶段 F 的最小合法舰、常规有人舰和完全无人旗舰继续作为 `contract_fixture / technical_surrogate` 支持界面、传输、性能和战斗闭环测试，不得改名或复制为正式教程舰。

I0 已建立的教程舰基准包和正式化门禁继续有效，但其作用是阻止技术替身被误升格，而不是要求当前立即填写正式参数。I1 至 I12a 已完成的弹药、后勤、实例设计、武器时间线、弹丸、战损、指挥、制导、持续毁伤、人员和观测/火控合同继续作为编辑器和战术场景的权威规则内核。

## 二、范围与非目标

本计划交付：

- Tauri 桌面宿主、TypeScript Web 界面和 Python 权威规则 sidecar；
- 玩家可用的船壳编辑器；
- 玩家可用的舾装编辑器；
- 可回放、可实时操纵、可压力测试的二维战术场景；
- 从编辑器设计到出航、战斗、战损和保存重载的垂直闭环；
- 在上述工具完成后设计并联合标定正式教程舰。

本计划暂不交付：

- 模块原型制作编辑器；
- 完整战略地图、据点经济、世界事件和战略 AI；
- I12b 舰载航迹持久化、舰队观测共享与正式 EW 来源；
- 自由三维战场、体积云或三维舰艇物理；
- 航空器、航母、主动防御和阶段 J 扩展舰种；
- 未经教程舰联合验证的雷达/EW、伤亡、蔓延、殉爆、搜救和回收概率。

I12b 与战略模式可以在本计划完成共用平台后接续，但不是编辑器和测试战术场景的前置条件。

## 三、冻结的技术原则

第一版技术栈冻结为：

- 桌面宿主：Tauri 2；
- Web 工程：Vite + TypeScript + React；
- 二维渲染：PixiJS WebGL；
- 权威规则：现有 Python 模块；
- 进程形态：由 Tauri 启动并随应用打包的 Python sidecar；
- 开发期通信：逐行 JSON 请求/响应与事件；
- 正式高频战术传输：先以版本化 JSON 快照/差量验证，只有性能报告证明必要时才替换为二进制；
- 第一发布目标：Windows 桌面版；
- 第一版可视化：调试几何和简化占位资源，不等待正式美术。

TypeScript 只负责交互、显示、视觉插值和非权威操作预测。以下规则只能由 Python 权威内核结算：

- 船壳拓扑、对称、甲板支撑和安装空间；
- 模块安装、重叠、净空、嵌入和派生性能；
- 质量、惯量、结构、气动、RCS、升力、推力、功率和人员；
- 武器、火控、弹丸、命中、装甲、战损、火灾、人员和生命周期；
- 规范保存、资源版本、内容指纹和正式化门禁。

## 四、当前已具备的基础

| 领域 | 当前状态 | 本计划用途 |
|---|---|---|
| `gaotian.ship/v1alpha1` 规范资源 | 已完成 | 前后端唯一数据口径 |
| 无界面船壳编译器 | 已完成 | 船壳编辑器最终裁决 |
| 无界面舾装编译器 | 已完成 | 舾装编辑器最终裁决 |
| 编辑器领域层与三舰 L3 往返 | 已完成 | Web 编辑器后端起点 |
| 运行时、出航和实例状态 | 已完成 | 编辑设计进入战术场景 |
| I1 至 I12a 技术闭环 | 已完成 | 测试战术场景规则内核 |
| Web/Tauri 工程 | W1 真实应用、监管器、诊断页和 sidecar 生命周期已完成 | T0/W2 共用宿主 |
| 可视化编辑器 | 尚未建立 | H/O 阶段实施 |
| 战术渲染和实时桥 | 尚未建立 | T 阶段实施 |
| 正式教程舰 | 尚未定义 | X 阶段后进入 I-F |

已测得阶段 F 常规有人舰的现有领域预览成本约为：船壳完整预览 `77ms`，舾装完整预览 `0.9ms`。船壳拖动必须采用草稿显示、后台编译和防抖；舾装可以接近实时调用权威编译器。

## 五、依赖关系

```text
G0 当前 Python 规则基线
        │
        ▼
W0 架构与协议冻结
        │
        ▼
W1 Tauri + Web UI + Python sidecar
        │
        ├──────────────► T0 战术性能风险验证 → T1 战术表现协议
        │
        ▼
W2 编辑会话、资源和通信层
        ├──► W3 共用二维画布与前端测试设施
        ├──► H1 ──(+W3)──► H2 → H3 → H4 船壳编辑器
        └──► O1 → O2 ──(+W3)──► O3 ──(+H4)──► O4 舾装编辑器

T1 + W3 ──► T2 → T3 → T4 测试战术场景

H4 + O4 + T4 ──► X1 编辑—战斗闭环 ──► X2 桌面打包与总验收
                                                │
                                                ▼
                                  I-F1 至 I-F7 正式教程舰
```

T0 必须提前执行。若 20 至 30 艘舰艇和高弹丸密度证明当前 Python 固定步或进程通信存在关键瓶颈，应在大量 UI 开发前冻结优化方案。

## 六、共用 Web 平台切片

### W0：架构与协议冻结

依赖：G0。

状态：2026-08-28 已落实。《高天荒野Web客户端W0架构与协议决策》冻结架构、目录、生命周期与环境边界；`gaotian.web-bridge/v1alpha1` 提供 Schema、18 个正面示例、45 个可移植负面用例、Python 编解码/握手参考合同与确定性报告。W1 的 18 项运行时验收和 T0 的 6/20/30 舰性能计划均已保存，但未标为执行通过。

任务：

- 建立架构决策记录，冻结宿主、前端、渲染器和 sidecar；
- 冻结 `editor.* / tactical.* / resource.* / save.*` 命名空间；
- 定义请求 ID、接口版本、会话 ID、修订号、命令、结果和结构化错误信封；
- 定义 sidecar 启动、握手、超时、崩溃和关闭策略；
- 定义开发日志、玩家日志和性能报告边界；
- 定义 Windows 开发、测试和打包前置依赖；
- 为未来 `strategy.*` 保留命名空间，但不实现战略模式。

验收：

- 所有跨进程消息均有明确版本；
- 前后端职责无重叠；
- 明确第一版不用纯浏览器 Pyodide，Python 后端不使用本地监听端口；Vite 的仅开发期页面/HMR 服务除外；
- 形成 W1 可直接实现的文件结构和接口样例。

### W1：应用骨架与 sidecar 生命周期

依赖：W0。

状态：2026-08-31 已落实。React/Vite/TypeScript、Tauri 2、ASCII Python sidecar、Rust 进程监管、Channel、心跳、有界队列、结构化故障与诊断页均已建立；18/18 W1 运行时用例通过，真实窗口完成自动握手、Ping、显式重启、停止和关闭回收复核。机器报告见《阶段W1Web应用与Sidecar生命周期接口.v1.json》。

任务：

- 创建 Tauri、Vite、React 和 TypeScript 工程；
- 创建 ASCII 名称的 Python 后端入口，内部复用现有中文模块；
- 实现 sidecar 启动、握手、健康检查、关闭和异常恢复；
- 实现逐行 JSON 调用与 Python 契约错误透传；
- 建立前端开发诊断页和 Python 日志查看入口。

验收：

- Tauri 窗口可以启动 Python 后端并取得接口版本；
- 后端异常不会令界面永久等待；
- 一次请求只能对应一次成功或失败结果；
- 不修改现有 Python 规范资源和测试语义。

### T0：战术性能与传输风险验证

依赖：W1。逻辑上属于战术分支，但必须提前完成。

任务：

- 建立 6 舰功能档、20 舰目标档和 30 舰压力档；
- 分档加入普通炮弹、制导弹和连续武器事件；
- 测量单固定步、弹丸世界、运行时重编译、完整状态序列化和跨进程传输；
- 比较完整快照与差量快照；
- 记录参考机器、场景内容和可重复运行命令。

验收：

- 目标档达到实时因子 `>= 1.0`，或形成明确热点和优化切片；
- 压力档即使降帧也不得改变权威结果；
- 冻结 T1 表现协议的初始数据密度；
- 生成机器可读性能报告。

### W2：资源、文档会话与命令历史

依赖：W1。

任务：

- 建立材料、涂料、模块目录、船壳和舾装资源索引；
- 建立后端 `EditorSession`；
- 命令携带 `expected_revision`，拒绝过期修改；
- 保存当前草稿、最近合法编译结果、撤销/重做历史和脏状态；
- 实现新建、打开、保存、另存版本、关闭和恢复草稿；
- 非法草稿不得覆盖磁盘上的合法资源。

验收：

- 撤销/重做后内容指纹确定；
- 过期修订被原子拒绝；
- 未修改资源原样保存后保持规范 JSON 一致；
- sidecar 重启后可以恢复明确标记的草稿。

### W3：共用二维画布与前端测试设施

依赖：W2。

任务：

- 建立 PixiJS WebGL 画布、摄像机、坐标转换和五米网格；
- 实现缩放、平移、吸附、悬停、点选和框选；
- 建立 React 属性面板、诊断面板、状态栏和快捷键；
- 建立模拟 transport，使前端测试不必启动 Python；
- 建立 TypeScript 类型检查、单元测试和端到端测试入口。

验收：

- 世界坐标与屏幕坐标往返无漂移；
- 高 DPI 和窗口缩放正常；
- 相同输入得到相同选择与吸附结果；
- 同一 UI 可以在模拟和真实 transport 上运行。

## 七、船壳编辑器切片

### H1：细粒度船壳编辑命令

依赖：W2。

在现有 `HullEditorDocument` 上增加：

- 新建空白船壳；
- 添加、删除甲板；
- 添加、删除区域；
- 插入、移动、删除端点；
- 拆分或删除边；
- 闭合区域；
- 设置基底层；
- 设置结构材料和逐边装甲；
- 单侧修改后的明确镜像操作；
- 端点数量变化时的边装甲迁移规则。

验收：每条命令独立可撤销，复合命令失败保持原子性，规范形状与旧资源兼容。

### H2：草稿与正式预览

依赖：H1、W3。

- 拖动时显示宽松草稿；
- 停止操作后以约 `100—150ms` 防抖执行正式编译；
- 非法草稿保留最近一次合法派生结果；
- 诊断路径映射到甲板、区域、端点和边；
- 过期编译结果不得覆盖新草稿。

验收：船壳全量编译不阻塞拖动，自交、未闭合、重叠、无支撑和非对称错误均可定位。

### H3：船壳编辑器界面

依赖：H2。

- 甲板列表与层级切换；
- 端点、边和区域选择模式；
- 单侧绘制与镜像显示；
- 结构材料与边装甲属性；
- 内部格、露天格和侧挂槽显示；
- 质量、结构、惯量、气动和 RCS 派生面板；
- 新建、打开、保存和派生版本。

验收：可从空白文档完成最小合法船壳，可修改三条技术替身，非法船壳不可保存。

### H4：船壳 UI L3 门禁

依赖：H3。

- 用真实 UI 打开三条规范测试船壳；
- 未修改保存并重载；
- 比较规范几何、安装空间、派生结果和内容指纹；
- 覆盖镜像、装甲和多甲板交互。

验收：UI 往返与现有阶段 H 领域报告一致，Python 和 Web 回归同时通过并生成独立报告。

## 八、舾装编辑器切片

### O1：模块资源浏览与显示模型

依赖：W2。可与 H1/H2 并行。

- 按类别、安装方式和版本筛选模块；
- 显示质量、耐久、功率、人员、自动化和能力；
- 显示内部、顶挂、侧挂、嵌入和净空几何；
- 明确标识 `contract_fixture / prototype_unbalanced / balance_reference`。

验收：现有十三类模块均可完整显示，精确版本与内容指纹可追溯。

### O2：补齐舾装操作命令

依赖：O1。

在现有 `OutfitEditorDocument` 上增加：

- 侧挂模块移动；
- 嵌入模块更换宿主；
- 替换模块原型；
- 复制、粘贴、批量选择和删除；
- 镜像复制与实例自动命名；
- 复合操作原子提交。

验收：内部、顶挂、侧挂和嵌入均可完整编辑，失败操作不留下半个镜像或半次替换。

### O3：舾装编辑器界面

依赖：O2、W3。最终联调使用 H4 产出的船壳。

- 模块拖放、移动、旋转、删除和可选镜像；
- 内部、顶挂、侧挂、本体与净空五层占用；
- 冲突、嵌入槽和宿主关系显示；
- 质量、惯量、升力、供电、人员、推力和警告面板；
- 武器射界状态显示。

验收：可从空白方案完成最小合法舰，模块预览流畅，非法方案不可保存。

### O4：舾装 UI L3 门禁

依赖：O3、H4。

- 用真实 UI 打开三条规范舰；
- 显示全部模块和五类占用；
- 未修改保存并重载；
- 覆盖镜像、非对称删除、侧挂和嵌入操作。

验收：模块实例、位置、旋转、占用和派生输出逐字段一致，生成舾装 UI 往返报告。

## 九、测试战术模式切片

### T1：战术表现协议

依赖：W1、T0。

建立独立于权威存档的：

- `TacticalRenderSnapshot`；
- `TacticalRenderDelta`；
- `TacticalVisualEvent`；
- `TacticalInputCommand`。

静态船壳/模块/阵营数据与高频位置、朝向、速度和耐久分离。输入带序号和目标固定步，表现快照带权威场景步号。

验收：表现协议不进入权威存档，旧包、重复包和过期包可以识别，相同场景产生相同事件序列。

### T2：战术回放渲染器

依赖：T1、W3。

- 舰艇轮廓、朝向和阵营；
- 三高度层与云层黑影；
- 弹丸、导弹和尾迹；
- 开火、命中、爆炸和火灾占位表现；
- 相机、速度、航向和目标调试覆盖层；
- 从记录快照播放，不接实时控制。

验收：相同记录重复播放顺序一致，回放不修改权威状态，WebGL 资源可正确重建和释放。

### T3：实时仿真桥与控制

依赖：T2。

- Python 以 `60Hz` 固定步运行；
- 前端以 `20—30Hz` 接收快照并在显示帧间插值；
- 接入舵盘、推力、制动和 OverG；
- 接入 RTS 选择、航点和攻击命令；
- 接入暂停、单步和开发回放；
- 渲染帧率变化不得改变仿真时间线。

验收：重复输入不重复执行，暂停后权威场景不推进，直控和舰队命令继续通过 I9 仲裁。

### T4：功能、目标规模与压力场景

依赖：T3。

功能场景包含两个阵营、约六艘舰艇、一直控旗舰、其他 RTS 舰艇、三高度层、普通炮弹、制导武器、观测火控、局部装甲、模块战损、火灾、人员伤亡、二次毁伤、坠落和离场。

目标规模为十五至二十艘舰艇；压力规模为三十艘舰艇和高弹丸密度。未定义概率继续使用明确、可复现的脚本裁决，不冒充正式概率。

验收：功能场景可确定性回放，目标规模实时因子 `>= 1.0`，压力降帧不破坏权威结果，生成战术功能与性能报告。

## 十、跨模式闭环与打包

### X1：编辑设计直接进入战术场景

依赖：H4、O4、T4。

固定流程：

1. 船壳编辑器修改并保存新船壳版本；
2. 舾装编辑器在该船壳上布置模块并保存；
3. 通过现有编译链生成出航配置和舰艇实例；
4. 加入测试战术场景；
5. 执行移动、开火、命中和战损；
6. 保存并重载战术状态。

验收：不手填整舰聚合性能，战术参数可追溯到编辑资源精确版本，实际几何和模块位置参与命中与战损。

### X2：Windows 打包与总验收

依赖：X1。

- Python sidecar 随应用打包；
- 目标机器无需预装 Python；
- 完成本地资源权限、存档目录、日志和崩溃恢复；
- 执行 Python、TypeScript、UI 往返、战术功能和性能总回归。

验收：编辑—编译—出航—战斗—保存闭环通过，构建产物与源码边界明确。

## 十一、正式教程舰与阶段 I 收口

只有 X1 通过后才进入正式教程舰设计和联合标定：

| 切片 | 内容 | 主要依赖 |
|---|---|---|
| I-F1 | 确定教程舰船型、尺度、定位、武备、装甲、船员和教学目标 | X1 |
| I-F2 | 用编辑器建立独立正式船壳、舾装、出航、实例和基准包 | I-F1 |
| I-F3 | 标定推进、升力、供电、操纵、换层和三层环境 | I-F2 |
| I-F4 | 标定装甲、甲弹、RCS、传感器、人员、武器、弹药和火控 | I-F2、I-F3 的相关输出 |
| I-F5 | 标定建造、维修、补给、改装和回收经济 | I-F2、I-F4 |
| I-F6 | 编辑器、战术、后勤和保存重载跨域验证 | I-F3、I-F4、I-F5 |
| I-F7 | 获得 `user_approved`，将全部关键项标为 `frozen_formula / calibrated` 并通过正式化门禁 | I-F6 |

编辑器完成前只允许明确教程舰的教学目标和体验边界，不冻结船体几何、模块布局或正式数值。

## 十二、推荐执行顺序

单一路线按以下顺序实施：

1. W0 架构与协议冻结；
2. W1 应用骨架与 sidecar；
3. T0 战术性能风险验证；
4. W2 编辑会话与资源层；
5. W3 共用二维画布；
6. H1 至 H4 船壳编辑器；
7. O1 至 O4 舾装编辑器；
8. T1 至 T4 测试战术场景；
9. X1 至 X2 闭环与打包；
10. I-F1 至 I-F7 正式教程舰与阶段 I 收口。

最小依赖与推荐串行顺序不同：W2 完成后 H1、O1、W3 可以并行；T1 只要求 W1、T0，T2 才要求 W3。O4 必须使用 H4 的合法船壳，X1 必须等待 H4、O4 和 T4 全部通过。W0 已按此校正依赖图，不把渲染设施误列为纯领域命令的前置条件。

## 十三、逐切片复核规则

每个编号切片作为一个独立、可提交的复核单元，提交前必须：

- 明确新增接口版本和兼容边界；
- 至少具有一个正面测试和一个负面测试；
- JSON 输出保持确定性；
- Python 全量回归不退化；
- Web 类型检查、单元测试和适用的 UI 端到端测试通过；
- `git diff --check` 通过；
- TypeScript 未复制权威规则；
- 更新对应机器报告和实施状态；
- 明确本切片未实现范围及下一依赖节点。

涉及性能的切片必须保存参考机器、测试场景、实体数量、运行时长、平均值、P95 和峰值，不得只报告主观“流畅”。

## 十四、W1 完成记录与 T0 开工边界

W0 的架构与协议合同继续有效。W1 已在该合同上完成以下交付：

1. 不修改系统级 PATH 的 Windows 工具链预检、构建、测试与开发入口；
2. 锁定 npm 与 Cargo 依赖的 React/Vite/TypeScript/Tauri 工程；
3. ASCII 模块入口 `backend.high_wilderness_sidecar`，只开放三个 `system.*` 能力；
4. Rust 子进程所有权、严格 JSON Lines、一次结算、代次隔离、心跳、超时与有界队列；
5. 单窗口 Channel、旧代次过滤、64 KiB 前端日志和 200 项事件上限；
6. 自动握手的开发诊断页，显示接口、sidecar、数据契约、能力和实例 ID；
7. Python 8 项 sidecar 检查、TypeScript 4 项测试、Rust 15 项测试和真实窗口复核；
8. `w1-acceptance.v1.json` 18/18 PASS 的机器可读报告。

协议参考代码与测试仍位于《高天荒野Web桥接协议.py》《高天荒野Web桥接协议测试.py》；W1 实现位于 `apps/desktop/`、`backend/high_wilderness_sidecar/` 和 `tools/Invoke-HighWildernessWeb.ps1`。未改动现有舰艇规则或规范资源。

下一切片 T0 按已冻结的性能计划开工：

1. 先建立可重复场景生成器与无界面基线，冻结输入、资源和最终状态 hash；
2. 实现 6 舰功能档、20 舰目标档和 30 舰压力档；
3. 分级测量固定步、弹丸世界、运行时重编译和全状态序列化；
4. 在 W1 桥接上测量全量 JSON 与实验性精简/差量投影，不将实验投影升格为 T1 合同；
5. 保存每档三次重复的平均值、P95、峰值、实时因子、队列等待和最终权威 hash；
6. 若 20 舰目标档未达到实时因子 1.0，先形成有证据的优化切片，再决定是否进入 W2。

W1 的 PASS 只证明桌面宿主和 sidecar 生命周期可用；不代表 T0 性能、W2 编辑会话、编辑器 UI、战术表现协议或发行打包已通过。

## 十五、T0a 完成记录与 T0b 开工边界

T0a 已建立严格计划解析、正式运行矩阵、最近秩 P95、机器与 Git 元数据、结果 Schema、命令行入口和阶段 F / I 技术资源负载审计。正式矩阵按 `3 档 × 4 级负载 × 3 次重复 × 7 个模式/频率变体` 展开为 252 次运行；无界面基线不虚构快照频率，同一档位、负载级和重复序号的七个传输变体共用输入 seed 与输入流 hash。

负载审计形成以下约束：

1. 三档运动负载均可使用现有三舰编成进入 T0b；
2. 阶段 F 保存出航与实例均未装载弹药，T0b 必须物化独立、合法、可复核的基准装载，不得修改保存夹具冒充现状；
3. 完全无人旗舰武器原型尚无阶段 I 武器时间配置，不能直接进入合法射击时间线；
4. 现有合法时间配置下，若按统一场景实际输出的 `fire + reload` 武器动作事件计数，6/20/30 舰档最大持续事件率仅为 4/20/28 次每秒，60 秒容量仅为 240/1200/1680 次，低于计划的 24/120/300 次每秒；对应最大合法开火率为 2/10/14 发每秒；
5. 因此三档武器事件目标均标记为 `NOT_COVERED`。后续只能补充可追溯技术夹具或依据审计修订目标，不能注入脱离弹药、装填、制导和武器时间线的虚构事件；
6. 当前没有执行正式性能运行，五项 T0 门禁仍为 `NOT_COVERED`，`t0_performance_measured = false`。

T0b 的直接范围是确定性 6/20/30 舰场景生成、合法基准装载、分级活动实体计数、输入/资源/最终权威状态与事件 hash，以及只包含权威固定步总墙钟的无界面基线。T0b 不实现 JSON 投影、差量编码、跨进程/WebView ACK 或正式 T1 字段；这些分别留给 T0c、T0d 和 T0e。

## 十六、T0b 完成记录与 T0c 开工边界

T0b 已建立以下可执行边界：

1. 生成 6/20/30 舰三档与运动、普通弹、制导弹、脚本战损重编译四级负载，共 12 个确定性初始场景；
2. 每艘舰使用阶段 F 精确船壳、舾装和运行时链；战斗舰通过独立 T0 技术出航配置装入 100 发弹药库库存和 1 发待发弹，不修改阶段 F 保存资源；
3. 新增仅供 T0 使用的无人武器时间技术替身，使常规有人舰与无人旗舰都能进入合法 60Hz 武器时间线；
4. 普通弹与制导弹压力初态均通过现有 `TacticalSceneState` 持久化解析；后续新增弹丸必须来自合法弹药、装填、射击事件和同边界发射指令；
5. 脚本战损档为每艘舰建立低风险、可追溯的持续毁伤状态，每步继续走现有毁伤结算与运行时重编译；
6. 三次重复复用完全相同的输入流和输入 hash，重复序号只表示再次测量；当前为 12 条唯一输入流、36 个重复比较组和 252 次模式/频率运行；
7. 无界面运行器记录固定步样本、实时因子、驻留内存、活动实体分布、事件数量、最终权威状态 hash 和滚动事件流 hash；`-Resume` 只复用输入、资源、初态和步数均匹配的完整结果；
8. 12/12 初态、4/4 分级单步、合法装载、负面输入、结果合同和重复权威 hash 均已回归通过。

补入无人武器时间配置后，三档合法持续武器动作事件上限提高到 8/28/40 次每秒，但仍低于 24/120/300 次每秒目标。初始压力状态可以达到计划弹丸数；由于弹丸寿命为 30 秒、正式测量窗口为 60 秒，而合法补充射速不足，不能据此宣称全窗口负载已覆盖。正式 600 步预热、3600 步测量尚未执行，T0 五项门禁继续为 `NOT_COVERED`，`t0_performance_measured = false`。

T0c 只在同一权威场景输出上比较完整权威 JSON、实验性表现全量与实验性表现差量，记录序列化耗时、帧字节、差量基线和恢复规则；不得改变权威推进、补造无效负载或把实验字段升格为 T1 稳定协议。跨进程和 WebView ACK 仍留给 T0d。

## 十七、T0b.1 权威热路径优化计划

### 17.1 当前唯一进行项与暂停边界

当前唯一进行项是 `T0b.1`。在其决策门通过前，暂停以下工作：

1. `T0c` 权威全量、实验投影与实验差量序列化；
2. `T0d` 跨进程与 WebView ACK、`T0e` 正式结果汇总和 T1 建议；
3. `W2` 及后续船壳编辑器、舾装编辑器和测试战术界面；
4. 24/120/300 次每秒武器事件目标的修订、专用饱和夹具和弹丸寿命调整；
5. 600 步预热、3600 步测量和 252 次正式性能矩阵。

暂停不等于取消。T0c 及后续阶段保持原有依赖和范围，待 T0b.1 决策门给出稳定的权威基线后恢复。当前不得通过降低负载、关闭规则或修改权威结果来制造性能通过。

### 17.2 诊断基线

以下数据是 2026 年 8 月 31 日在当前工作树执行的短程诊断，不是正式 T0 成绩：

| 诊断项 | 当前结果 | 结论 |
| --- | ---: | --- |
| `target_20.motion_only` 三步平均 | 2265.848 ms/步 | 远高于 60Hz 的 16.667 ms 预算，弹丸不是首要瓶颈 |
| `target_20.guided_projectiles` 三步平均 | 2384.079 ms/步 | 当前约为实时速度的 0.007 倍 |
| `stress_30.guided_projectiles` 首步 | 3998.165 ms | 30 舰与 1650 枚初始弹丸无法用于正式长跑 |
| 30 舰制导单步完整快照哈希访问 | 约 700 次 | 不变设计资源在固定步内被重复完整序列化和哈希 |
| 30 舰制导单步完整运行时编译 | 约 160 次 | 同舰、同边界和相邻阶段存在大量重复编译与校验 |
| 30 舰制导单步战术舰模型构建 | 约 60 次 | 静态几何、气动与 RCS 结构被重复构建和复核 |
| 仅临时缓存设计快照哈希后的 20/30 舰制导首步 | 约 380/747 ms | 静态哈希是第一热点，但移除后仍不足以达到 60Hz |

剖析累计时间会发生调用包含关系重叠，不能相加为总时间；上述调用次数和短跑墙钟只用于确定优化顺序。

### 17.3 不可破坏的约束

1. 60Hz 固定步、输入顺序、事件顺序、弹药消耗、武器边界、弹丸命中、持续毁伤和生命周期语义保持不变；
2. 相同输入和资源必须产生与优化前相同的最终权威状态 hash 与滚动事件 hash；
3. 外部输入、载入、存档、IPC 和资源替换边界继续执行严格内容指纹校验；只允许可信、已验证的进程内对象使用快速路径；
4. 快速路径不能依赖可变但未版本化的嵌套容器。若对象尚未真正不可变，必须先封装、复制或引入显式 revision；
5. 本轮不修改性能目标、舰艇编成、武器射速、弹舱容量、弹丸寿命或正式 T1 表现字段；
6. 在 Python 热路径完成结构性减负前，不直接启动整体 Rust 迁移，也不修改权威固定步频率。

### 17.4 子切片、依赖与验收

| 顺序 | 子切片 | 实施内容 | 可复核交付物 | 退出条件 |
| --- | --- | --- | --- | --- |
| 1 | `T0b.1a` 基线与观测 | 增加默认关闭的阶段计时和调用计数；区分完整性校验、运行时编译、生命周期、武器时间线、机动、弹丸、毁伤、结果物化和观测器开销；冻结 12 个场景的权威黄金 hash | 短跑协议、热点报告、12 场景黄金结果 | 关闭观测时不改变结果或产生可测退化；当前基线可一键复现 |
| 2 | `T0b.1b` 静态指纹与验证边界 | 在编译/载入时一次性计算设计快照、气动/RCS缓存和配置目录指纹；场景绑定保存已验证 token；严格验证移到资源边界 | 指纹缓存接口、严格/快速双路径、篡改与失效测试 | 固定步内设计快照完整哈希计算降为 0；严格与快速路径权威 hash 一致；短跑至少比当前快 4 倍 |
| 3 | `T0b.1c` 静态模型与动态运行时复用 | 分离静态战术舰模型和动态实例状态；按舰艇 revision/dirty domain 复用 runtime；生命周期、机动和输出共享同一边界结果；燃料、模块、人员、供电、弹药和毁伤分别失效 | 缓存键与失效表、运行时复用实现、逐类失效测试 | 稳态固定步不再重建静态模型；完整 runtime 编译调用降低至少 75%；20 舰纯运动短跑 P95 不高于 50 ms |
| 4 | `T0b.1d` 事件和弹丸热路径 | 使用确定性到期事件索引避免无事件舰艇反复扫描；同边界批量生成弹丸并只合并/排序一次；预编译目标几何；活动实体改为增量计数 | 事件索引、批量生成接口、几何缓存、顺序等价测试 | 事件集合、顺序和 hash 与原实现一致；20 舰制导短跑固定步 P95 达到 16.667 ms 目标，或形成明确的剩余热点证据 |
| 5 | `T0b.1e` 测量器去扰动 | 事件 hash 直接读取事件字段，不先物化整份 resolution；环境元数据每次运行只采集一次；内存低频采样；分别报告权威推进墙钟、观测器开销和包含 drain 的实时因子 | 更新后的无界面结果、测量边界测试 | 固定步样本不包含观测器开销；实时因子仍包含计划规定的 drain；结果 Schema 与续跑校验通过 |
| 6 | `T0b.1f` 决策门 | 在固定机器元数据下执行 10 步预热、60 步测量、三次重复的短程诊断；比较优化前后调用数、P95、最大值、内存和权威 hash | T0b.1 优化报告与下一步决策 | 按 17.5 的分支作出唯一决策；仍不冒充正式 600+3600 步 T0 结果 |

子切片必须按表中依赖顺序实施，每个子切片独立提交、独立回归、可以单独回退。不得把结构优化、负载目标调整和 T0c 序列化混入同一个提交。

### 17.5 T0b.1f 决策分支

1. 若 `target_20` 运动和制导短跑三次重复均达到实时因子不低于 1.0，且权威 hash 全部一致：结束 T0b.1，恢复 T0c；
2. 若运动档通过而弹丸档失败：保持 Python 规则编排，新增一个只覆盖弹丸积分、制导和碰撞的 Rust 热核评审，不迁移整套规则；
3. 若运动档仍失败且热点集中在动态运行时、对象分配或 Python 调度：先形成“舰艇/损管低频、弹丸高频、表现插值”的多速率权威 RFC，与继续迁移热核做数据化比较；
4. 若短跑达到目标但内存、队列或最大步耗时无界：继续留在 T0b.1，不进入 T0c；
5. 只有决策分支 1 可以直接恢复 T0c。分支 2 或 3 必须先完成新切片规划和权威等价门禁。

### 17.6 每个子切片的共同回归门禁

1. 舰艇测试总入口全部通过，且不得减少既有用例；
2. 12 个 T0 场景的初态 hash、输入流 hash 和资源 hash 不变；
3. 严格路径与快速路径在相同输入下的逐步事件、最终状态和事件流 hash 一致；
4. 存档重载、续跑和跨进程输入继续经过严格验证；
5. 至少包含一项缓存失效负例，证明模块损伤、人员、供电、燃料、弹药、毁伤和资源 revision 不会复用过期结果；
6. `git diff --check`、Python 静态编译和对应阶段报告通过；
7. 任一权威结果发生变化时立即停止性能比较，先判断是缺陷、未声明规则变化还是黄金结果错误，不以“性能更快”为理由接受漂移。

### 17.7 风险与复核重点

1. `frozen dataclass` 不等于所有嵌套成员都不可变；缓存指纹前必须审计字典、列表和外部引用；
2. runtime 缓存最危险的是漏失效，必须使用显式 revision/dirty domain，不以对象地址作为长期权威键；
3. 批处理可能改变同一边界事件排序，稳定排序键必须继续进入合同测试；
4. 剖析器和逐步内存采样会显著扰动短步结果，观测器必须默认关闭并单列开销；
5. 缓存命中率、完整编译次数和静态模型重建次数必须作为报告字段，防止只看墙钟掩盖错误复用；
6. 24/120/300 武器事件率与 30 秒弹丸寿命的问题继续保持 `NOT_COVERED`，待权威热路径稳定后另行拆分“代表性场景”和“饱和压力场景”，不在 T0b.1 内绕过。

### 17.8 T0b.1a 完成记录与 T0b.1b 开工边界

T0b.1a 已建立以下可执行基线：

1. 新增严格解析的六场景短程诊断协议，覆盖 6/20/30 舰的运动和制导弹丸场景；协议固定剖析与非剖析运行从同一初态推进相同步数，并明确不得作为正式 T0 成绩；
2. 观测器完全位于 `benchmarks/t0`，只在显式执行诊断命令时启用 `cProfile`；权威场景、运行时编译器、武器时间线、机动、弹丸和毁伤代码没有插入常驻计时钩子；
3. 冻结 3 档 × 4 级负载共 12 个场景的初态、单步最终状态、滚动事件、输入流、资源清单、事件数量和实体数量 hash；重新计算结果为 12/12 一致；
4. 建立完整性校验、运行时编译、生命周期、武器时间线、机动、弹丸和毁伤七类函数探针，并单列结果物化、事件摘要和剖析器估算开销；累计时间允许嵌套重叠，不作为可相加的阶段墙钟；
5. 代表性 6 舰运动步基线为 120 次设计快照内容指纹访问、24 次完整 runtime 编译、12 次战术舰模型构建、12 次武器时间线推进和 6 次机动积分；观测前后的权威状态与事件 hash 完全一致；
6. 六场景本地诊断再次确认调用数随舰数近似线性放大：30 舰制导首步达到 700 次设计快照指纹访问、160 次完整 runtime 编译和 120 次静态模型/时间线相关构建或推进；本地时序结果写入忽略目录，不进入确定性阶段报告；
7. 命令行与 PowerShell 已提供 `verify-golden` / `VerifyGolden` 和 `diagnose-hot-path` / `DiagnoseHotPath`；默认本地输出位于 `artifacts/t0-local/`；
8. 新增回归后舰艇测试总入口为 51/51 PASS。正式性能运行仍为 0，`t0_performance_measured = false`。

T0b.1b 只能调整静态指纹计算和严格验证边界。不得同时引入 runtime dirty-domain、事件批处理、弹丸优化或测量器改写。其首要验收是固定步内设计快照完整哈希计算降为 0、严格/快速路径 12 场景黄金结果一致，并在相同短跑协议下达到至少 4 倍加速。

### 17.9 T0b.1b 完成记录与 T0b.1c 开工边界

T0b.1b 已完成静态指纹和校验边界收敛：

1. 递归审计三种基准舰的派生快照，并将模块能力字段内部残留的 `list/dict` 深度冻结；规范 `to_dict()`、资源 hash 和既有黄金结果不变；
2. 派生设计快照、气动缓存、RCS 缓存、武器时间目录、弹丸目录、制导目录和持续毁伤配置均在编译或载入时一次性生成规范指纹；指纹字段不进入序列化和 dataclass 相等性；
3. 场景初始化始终执行严格快照重算，并在每个绑定中保存已验证 token；公共步进默认保持严格模式，T0 内部构造的不可变场景显式使用 `trusted_prevalidated` 快速模式；
4. 新增未验证绑定、过期 token 和快照内容篡改负例；合法资源 revision 会生成新指纹，篡改但未刷新指纹会在严格边界拒绝；
5. 3 档 × 4 负载的 12 个场景严格/快速单步状态 hash 和事件流 hash 均与 T0b.1a 黄金结果一致；六场景固定步内 `compute_derived_ship_snapshot_sha256` 调用均为 0；
6. 在同一本地机器、同一单步短跑协议下，六场景从 `632.7692–4205.1559 ms` 降为 `12.1109–324.7820 ms`，各案加速为 `12.95×–54.85×`，全部超过 4 倍退出门禁；时序明细仍只保存在忽略目录，不冒充正式 T0 成绩；
7. 新增回归后舰艇测试总入口为 52/52 PASS，TypeScript 为 4/4 PASS，Rust 为 15/15 PASS。正式性能运行仍为 0，`t0_performance_measured = false`。

T0b.1c 只能在本切片建立的不可变静态指纹和已验证绑定上，引入静态战术舰模型与动态 runtime 的显式 revision/dirty-domain 复用。不得使用对象地址作为长期权威缓存键，不得在本子切片并入事件批处理或弹丸优化。

### 17.10 T0b.1c 完成记录与 T0b.1d 开工边界

T0b.1c 已完成静态模型和动态 runtime 的显式复用：

1. 新增值语义 `RuntimeStateRevision`，权威键只由设计/出航来源、船体完整度、模块状态、高度层、燃料可用边界、人员、货物和供电策略组成，不使用对象地址作为长期键；
2. 单舰 runtime 缓存对自动激活事件保留有界变体，主 revision 变化时整组失效；场景初始化为已排队武器预热两种规范唤醒组合，固定步内不再冷启动完整编译；
3. 弹药和武器时钟不改变 runtime 派生值，因此命中时只刷新 `instance_snapshot` 及其指纹；模块毁伤、船体毁伤、人员、供电、燃料可用边界、货物、高度层和资源 revision 会按明示 dirty domain 失效；
4. 公共战术步进的严格模式会在缓存命中时重做来源、舰载、弹药、人员和模块合法性校验；只有 T0 内部预验证路径跳过这些重复检查，无效弹药不能借缓存绕过原合同；
5. 新增 `TacticalShipStaticModel`，船壳结构点、气动/RCS 缓存、环境和动力学调参在绑定期构建一次；固定步只将当前 runtime 绑定到静态模型；
6. 六场景单步诊断中，完整 runtime 编译调用从 `24/32/80/108/120/160` 全部降为 `0`，静态模型构建调用从 `12/12/40/40/60/60` 全部降为 `0`；12 个黄金场景权威结果不变；
7. 同机单步短跑中，`target_20.motion_only` 为 `25.2300 ms`，低于本切片 `50 ms` 退出门禁；六场景范围为 `8.6871–292.4550 ms`，制导弹丸档仍明显超过 60 Hz 步长，留给 T0b.1d 处理；
8. 新增回归后舰艇测试总入口为 53/53 PASS；正式性能运行仍为 0，`t0_performance_measured = false`。

T0b.1d 只能优化到期事件查找、同边界弹丸批量生成/排序、目标几何预编译和活动实体增量计数。不得在本子切片改变事件稳定排序键，不得引入 Rust 热核，不得提前执行 T0b.1e 测量器去扰动。

### 17.11 T0b.1d 完成记录与 T0b.1e 开工边界

T0b.1d 已完成事件和弹丸热路径的确定性收敛：

1. 武器时间线以单次推进内的最小堆维护 `(next_event_time_s, sequence_id)` 到期索引，不再在每个原子事件后重新校验、扫描和排序全部序列；既有稳定排序键、失败语义和事件上限保持不变；
2. 新增同边界 `spawn_projectiles_from_weapon_events`，场景将同一边界的成功开火收集后一次构造、一次合并、一次按弹丸 id 稳定排序并只计算一次源世界 hash；旧单发 API 保留为兼容包装；
3. 舰艇绑定按派生快照 revision 缓存甲板边、局部包围盒和包围半径；弹丸子步复用每个目标的起止位姿，并在精确线段求交前执行保守世界/局部宽相剔除；错误 revision 的几何缓存会显式拒绝；
4. T0 基准观察器新增不进入权威场景合同的增量实体计数器，按生成、命中、过期、开火、殉爆、生命周期和受影响武器序列更新；连续 3 步与全量扫描逐字段一致；
5. 4 枚同边界弹丸的批量结果与逐发兼容路径完全一致，重复 id 负例、跨步几何复用和稳定 id 顺序均通过；六个诊断场景中稳态几何重编译和单发生成调用均为 0，三个制导档都只执行 1 次批量生成；
6. 3 档 × 4 负载的 12 个黄金场景状态、事件、输入和资源 hash 保持一致；本地单步诊断从 T0b.1c 的 `8.6871–292.4550 ms` 收敛到本轮 `9.8077–129.3615 ms`，其中 `target_20.guided_projectiles` 为 `68.5421 ms`；这些数字仍是忽略目录中的诊断结果，不是正式 T0 成绩；
7. 20 舰制导档尚未达到 `16.667 ms`，退出条件按“形成明确剩余热点证据”满足：固定步仍有 2696 次弹丸几何宽相/积分调用，同时存在 runtime 校验 hash 和战术机动积分成本；是否评审 Rust 弹丸热核留到 T0b.1f 决策门，不在本切片越界实施；
8. 新增回归后舰艇测试总入口为 54/54 PASS，TypeScript 为 4/4 PASS，Rust 为 15/15 PASS。正式性能运行仍为 0，`t0_performance_measured = false`。

T0b.1e 只能调整基准测量和结果物化边界：事件摘要直接读取事件字段，环境元数据单次采集，内存低频采样，并分别报告权威推进、观察器开销和包含 drain 的实时因子。不得继续改变时间线、弹丸、runtime 或机动权威算法，不得执行 T0b.1f 的短程决策跑。

### 17.12 T0b.1e 完成记录与 T0b.1f 开工边界

T0b.1e 已完成测量器与结果物化边界收敛：

1. 滚动事件摘要直接遍历 `TacticalSceneStepResolution` 的权威事件元组，并保持原合同中六类基础事件始终输出、其他事件仅在非空时输出的规则；不再先调用整份 `resolution.to_dict()`，因此不会为事件摘要额外物化最终场景、舰艇结果和无关运行时 hash；
2. 3 档 × 4 负载的 12 个单步场景中，直接事件载荷、旧完整物化路径和冻结黄金事件 hash 三方完全一致；事件摘要包装负例会在任何越界调用 `resolution.to_dict()` 时立即失败；
3. 无界面结果接口升级为 `gaotian.t0-headless-baseline/v2`，`fixed_step` 只包围 `advance_scenario_step`；逐步事件摘要、增量实体计数、事件总量和内存采样进入独立 `observer_drain` 指标；
4. 运行结果同时报告 `authoritative_advance_wall_s`、`observer_drain_wall_s` 和总 `measured_wall_s`，三者满足严格分解；实时因子明确使用包含观察 drain、末态 hash 和收尾统计的总墙钟；
5. 环境元数据在 CLI 运行边界采集一次并注入运行器；直接调用运行器时也只采集一次。已提供元数据时禁止重复采集，命令不匹配会显式拒绝；
6. 驻留内存默认从逐步采样降为每 60 个固定步一次，并在测量末步补采；正式 3600 步窗口由 3600 次查询降为 60 次。采样间隔与有效样本数同时进入执行结果和正式 `runMetadata`；
7. 结果 Schema 新增测量策略、实时因子范围、内存间隔/样本数和 `observer_drain`；续跑只接受 `v2` 且输入、资源、初态、步数、策略、采样、指标和墙钟分解全部匹配的完整结果，旧 `v1`、缺失观察指标和错误墙钟分解均被拒绝；
8. 六场景本地诊断中，事件摘要由 T0b.1d 的 `1.1409–17.5870 ms` 降为 `0.0158–0.8719 ms`；这些仍是忽略目录中的观察器诊断，不是正式 T0 成绩。新增回归后舰艇测试总入口为 55/55 PASS，TypeScript 为 4/4 PASS，Rust 为 15/15 PASS；正式性能运行仍为 0，`t0_performance_measured = false`。

T0b.1f 只执行计划规定的 10 步预热、60 步测量、三次重复短程诊断，比较调用数、固定步 P95/最大值、观察 drain、包含 drain 的实时因子、内存和权威 hash，并按 17.5 选择唯一后续分支。不得把短程诊断冒充正式 600+3600 步 T0 结果，也不得在取得决策数据前继续修改权威算法。

### 17.13 T0b.1f 完成记录与 T0b.2 开工边界

T0b.1f 已完成短程诊断决策门：

1. 新增 `gaotian.t0b1f-short-diagnostic-decision/v1` 结果合同、JSON Schema、CLI `decide-short-diagnostic` 与 PowerShell `DecideShortDiagnostic`；协议严格冻结为六场景、10 步预热、60 步测量、三次重复，并始终声明 `official_performance_runs_executed = 0`、`t0_performance_measured = false`；
2. 六场景共 18 次短跑复用同一份机器元数据；结果严格校验六案矩阵、1/2/3 重复顺序、机器元数据 hash、运行总数和未知字段。三次重复的初态、输入流、最终权威状态和事件流 hash 在六案中全部一致；
3. 首次跨 60 步执行暴露了基准输入生成缺口：位于固定步末边界的新导弹尚未进入本步弹丸推进，却被提前加入本步制导输入。现已只为当前边界生成物加入当步输入，末边界生成物从下一步开始接收制导输入，并新增跨第 59→60 步回归；权威场景算法和事件排序未改；
4. 历史单步中每案 `12–60` 次静态战术模型构建和 `24–160` 次完整 runtime 编译，在当前六案剖析中均保持为 0；剩余运动热点位于 Python 权威场景调度、runtime 缓存解析、战术机动积分、载荷计算和 dataclass 对象替换；
5. 本机短跑最差重复中，`target_20.motion_only` 固定步 P95/最大值为 `35.5546/35.8759 ms`，三次实时因子为 `0.556494/0.543660/0.551840`；`target_20.guided_projectiles` 为 `67.5758/74.3283 ms`，三次实时因子为 `0.298540/0.279388/0.280402`。两案均未达到实时因子 1.0；
6. 六案观察 drain P95 为 `0.0365–0.6038 ms`，驻留内存最大值为 `41,807,872–49,016,832 bytes`；三次重复最大内存增量 `1,433,600 bytes`，低于短门 `64 MiB` 容差，固定步最大值均有限，无界面模式不存在传输队列，因此短程资源有界性为 6/6 通过；这不替代正式长跑中的内存和队列门禁；
7. 决策门唯一选择 17.5 分支 3：20 舰纯运动本身仍未实时，不能只把问题归因于弹丸热核，也不能直接恢复 T0c。下一阶段改为 `T0b.2` 推进响应与权威性能优化；先建立 60 Hz 等价基线和推进/安全 RFC，再对“舰艇/损管低频、弹丸高频、表现插值”与继续迁移热核做数据化比较；
8. 本轮实际执行的仍只是 18 次短程诊断，正式 `600+3600` 步 T0 运行数保持为 0。不得引用这些本机数字宣称正式 T0 通过。
9. 新增回归后舰艇测试总入口为 56/56 PASS，TypeScript 为 4/4 PASS，Rust 为 15/15 PASS；Rust 仅保留 F: 文件系统不支持增量缓存硬链接而回退复制的既有警告。

`T0b.2a` 只能形成推进响应、安全保护、多速率权威时序、采样/保持、事件边界、插值、确定性复现和等价门禁的 RFC 与验证计划，并用 T0b.1f 的运动/制导数据同弹丸热核方案比较。不得在 RFC 评审前开始 `T0b.2b`、修改 60 Hz 权威算法、实施多速率调度、引入 Rust 热核或恢复 T0c；评审通过后的实施边界以 17.14 的细分切片为准。

### 17.14 T0b.2 推进响应与其他优化规划入口

T0b.2 的详细 RFC 与切片编排已经保存为[《高天荒野》T0b.2 推进响应与权威性能优化规划](./高天荒野T0b2推进响应与权威性能优化规划.md)，状态为 `implementation_in_progress / T0b.2b1_complete / next_T0b.2b2`。该规划新增以下约束：

1. 既有发动机 `response_time_s` 保留为就绪后 0→100% 的名义响应时间，冷启动另设 `startup_time_s`；
2. 玩家车钟采用停车、微速 2%、四分之一、二分之一、四分之三、全速六个位置，权威实际推力沿 `0、2、5、10……100%` 阶段响应；
3. 推进响应是会改变运动、燃料、事件和 hash 的权威规则扩展，必须与保持现有 hash 的 P0 基线优化分离提交；
4. 在评审多速率或 Rust 前，先建立修正后的 60 Hz Python 基线，处理固定 65 候选推力搜索、runtime 命中全量 hash、边界重复解析、无到期时间线和生命周期重复派生；
5. 后续按推进合同/迁移、推进求解器、Python 弹丸与场景批处理、事件驱动/多速率对比、批量 Rust 热核评审、唯一决策门的依赖顺序推进；
6. `T0b.2-pre` 已确认 `290d044` 是 T0a—T0b.1f 的既有汇总提交且与 `origin/main` 一致；不重写已推送历史，并从此恢复一切片一提交；
7. 安全保护冻结为保留原车钟的离散软限幅/降档：限制解除后逐阶段恢复原命令；燃料、供电、人员、损毁与跳闸属于独立硬可用性边界，`overg` 只能绕过软保护；
8. 实施进一步拆为 P0 等价快速路径、runtime/边界优化、未接线纯安全判定器、版本化推进合同、启动/响应接线、安全保护接线、硬故障接线及后续批处理/架构比较，禁止把语义升级混入等价优化提交。

`T0b.2a` 已进一步冻结模块模式与发动机 phase 映射、首轮能耗/硬故障、独立反向发动机互锁、高频场景状态所有权、显式迁移、事件驱动/多速率边界和 Rust 批次确定性要求。其退出门只允许先实施 `T0b.2b1` 现有连续安全搜索快速路径；该切片现已完成，期间没有提前接入离散车钟或推进状态，也没有把规则变化伪装成权威等价优化。

### 17.15 T0b.2b1 完成记录

`T0b.2b1` 已增加请求比例安全即返回的快速路径，不安全时仍复用原 65 样本与 50 次二分算法。12 个黄金场景与六案三重复 hash 保持一致，`target_20.motion_only` 的 `_load_metrics` 从 1300 次/步降为 20 次/步；短程最差 P95 从 `35.5546 ms` 降到 `27.3490 ms`，最小实时因子从 `0.543660` 升到 `0.799687`。完整回归为 Python 57/57、TypeScript 4/4、Rust 15/15。

该结果仍是 10+60 步短程诊断，正式性能运行保持为 0；目标规模纯运动尚未实时。下一子切片唯一为 `T0b.2b2` runtime 稳定核心，不得提前接入推进响应或恢复 T0c。
